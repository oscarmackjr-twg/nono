---
quick_id: 260405-v0e
description: investigate and fix exec_strategy.rs uncommitted changes
date: 2026-04-06
status: completed
---

# Quick Task 260405-v0e: Fix exec_strategy.rs Unix/Windows Parity Bugs

## Goal

The Unix `exec_strategy.rs` has two classes of bugs introduced by an incomplete refactor:
1. Missing `rollback_status` parameter (Windows version has it, caller passes it)
2. Incomplete startup_timeout removal (functions deleted but call sites and ExecConfig field remain)

## Tasks

### Task 1: Add missing `rollback_status` to Unix `execute_supervised`

**Files:** `crates/nono-cli/src/exec_strategy.rs`

**Action:**
- Add `rollback_status: nono::undo::RollbackStatus` param to `execute_supervised` (after `rollback_state`, matching Windows signature)
- Add `rollback_status` field to `RollbackExitContext` construction

**Verify:** Windows and Unix `execute_supervised` signatures are in sync with `supervised_runtime.rs` caller

### Task 2: Complete startup_timeout removal

**Files:**
- `crates/nono-cli/src/exec_strategy.rs`
- `crates/nono-cli/src/execution_runtime.rs`

**Action:**
- Remove `StartupTimeoutConfig` struct from exec_strategy.rs
- Remove `startup_timeout` param from `wait_for_child_with_pty` and simplify
- Remove `wait_for_child_with_startup_timeout` function
- Remove `startup_timeout` param from both `run_supervisor_loop` variants (Linux + non-Linux) and remove dead deadline/prompt code
- Update all 5 `run_supervisor_loop` call sites to remove the `None` startup_timeout arg
- Remove `PROFILE_HINT_STARTUP_TIMEOUT` constant from execution_runtime.rs
- Remove `should_apply_startup_timeout` function from execution_runtime.rs
- Remove `startup_timeout: ...` from `ExecConfig` construction in execution_runtime.rs
- Remove `startup_timeout_applies_only_to_bare_interactive_profiled_tools` test
- Remove unused `Duration`/`Instant` imports from exec_strategy.rs

**Verify:** No references to `StartupTimeoutConfig`, `startup_timeout`, `should_apply_startup_timeout`, `PROFILE_HINT_STARTUP_TIMEOUT`, `prompt_startup_termination` remain in codebase
