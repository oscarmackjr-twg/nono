---
phase: 14-v1-fix-pass
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - .planning/phases/13-v1-human-verification-uat/13-UAT.md
  - .planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md
  - .planning/phases/07-quick-wins/07-VERIFICATION.md
  - .planning/phases/09-wfp-port-level-proxy-filtering/09-VERIFICATION.md
  - .planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: false
requirements: []
tags:
  - uat
  - verification
  - milestone-closeout
  - windows

must_haves:
  truths:
    - "The Phase 13 runbook (`13-UAT.md`) P05-HV-1 `command:` line uses `--detached` (not the typo `--detach`)."
    - "The Phase 13 runbook P09-HV-1 `command:` line uses the real CLI flags `--network-profile`, `--credential`, `--upstream-proxy` (NOT the nonexistent `--proxy-only`), and the prerequisites block documents the required `nono setup --install-wfp-service` and admin-shell dependency."
    - "After the second UAT pass, every one of the 6 target items (P05-HV-1, P07-HV-2, P07-HV-3, P09-HV-1, P11-HV-1, P11-HV-3) has a terminal verdict of `pass`, `fail`, or `waived` — no item remains `blocked`."
    - "After the second UAT pass, the `## Summary` block in `13-UAT.md` shows `blocked: 0`, `pending: 0`, `skipped: 0`; remaining categories (`passed`, `issues`, `waived`) sum to 10; any items in the `issues` category have filed gaps per the outcome-handling matrix in 13-UAT.md."
    - "Upstream VERIFICATION.md frontmatter `status:` fields reflect verdicts: `05-VERIFICATION.md`, `07-VERIFICATION.md`, `09-VERIFICATION.md`, `11-VERIFICATION.md` are each promoted per the Phase 13 Task 3 rules (pass → passed; fail → human_needed with a documented gap; waived → passed-with-waiver note)."
    - "The 55 stale `.nono-<hex>.json` files observation is recorded either as a non-blocking note in `13-UAT.md` Gaps > Additional observations OR filed as a GSD note — and is explicitly NOT fixed in this plan (out of scope)."
    - "The Phase 14 entry in `.planning/ROADMAP.md` is ticked (`- [x] Phase 14: v1.0 Fix Pass`) once Task 3 lands."
  artifacts:
    - path: ".planning/phases/13-v1-human-verification-uat/13-UAT.md"
      provides: "Corrected runbook + completed 2nd-pass UAT summary with terminal verdicts for all 6 target items"
      contains: "--detached"
    - path: ".planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md"
      provides: "Status reflecting P05-HV-1 verdict"
      contains: "status:"
    - path: ".planning/phases/07-quick-wins/07-VERIFICATION.md"
      provides: "Status reflecting P07-HV-2 and P07-HV-3 verdicts"
      contains: "status:"
    - path: ".planning/phases/09-wfp-port-level-proxy-filtering/09-VERIFICATION.md"
      provides: "Status reflecting P09-HV-1 verdict"
      contains: "status:"
    - path: ".planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md"
      provides: "Status reflecting P11-HV-1 and P11-HV-3 verdicts; P11-HV-2 remains waived"
      contains: "status:"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14 checklist entry ticked after upstream promotion lands"
      contains: "Phase 14"
  key_links:
    - from: ".planning/phases/13-v1-human-verification-uat/13-UAT.md"
      to: ".planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md"
      via: "Phase 13 Task 3 outcome-handling: pass → promote; fail → file gap; waived → note"
      pattern: "status:"
    - from: ".planning/phases/13-v1-human-verification-uat/13-UAT.md"
      to: ".planning/phases/07-quick-wins/07-VERIFICATION.md"
      via: "Same rule per P07-HV-2 and P07-HV-3"
      pattern: "status:"
    - from: ".planning/phases/13-v1-human-verification-uat/13-UAT.md"
      to: ".planning/phases/09-wfp-port-level-proxy-filtering/09-VERIFICATION.md"
      via: "Same rule per P09-HV-1"
      pattern: "status:"
    - from: ".planning/phases/13-v1-human-verification-uat/13-UAT.md"
      to: ".planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md"
      via: "Same rule per P11-HV-1 / P11-HV-3; P11-HV-2 stays waived"
      pattern: "status:"
    - from: ".planning/ROADMAP.md"
      to: "Phase 14 checklist"
      via: "Change `- [ ] Phase 14: v1.0 Fix Pass` to `- [x] Phase 14: v1.0 Fix Pass` after Task 3 commit"
      pattern: "\\[x\\].*Phase 14"
---

<objective>
Finish the v1.0 Windows Parity milestone by (a) correcting the two Phase 13 runbook defects that rendered P05-HV-1 and P09-HV-1 un-runnable, (b) executing a second UAT pass on the 6 target items now that Plans 14-01 and 14-02 have landed, and (c) promoting the upstream VERIFICATION.md files per Phase 13 Task 3 outcome-handling rules.

Purpose: Close Phase 14 roadmap success criteria #3, #4, and #5. This is the final plan of the v1.0 milestone; after it lands, every VERIFICATION.md has an honest terminal status and the milestone can be archived.

Output: A corrected `13-UAT.md` with a second-pass Summary block showing `blocked: 0`; four upstream VERIFICATION.md files with frontmatter `status` fields updated to reflect verdicts; ROADMAP.md Phase 14 entry ticked.

**This plan is non-autonomous.** Task 1 (runbook fix) is automatable. Task 2 (2nd-pass UAT re-run) requires a human tester on an admin Windows 10/11 host with `nono-wfp-service` installed and running; verdicts are human-authored. Task 3 (upstream promotion) is triggered by the Task 2 verdicts — Claude can execute the file edits but only after the human returns verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-v1-human-verification-uat/13-UAT.md
@.planning/phases/13-v1-human-verification-uat/13-01-PLAN.md
@.planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md
@.planning/phases/07-quick-wins/07-VERIFICATION.md
@.planning/phases/09-wfp-port-level-proxy-filtering/09-VERIFICATION.md
@.planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md
@.planning/phases/14-v1-fix-pass/14-01-SUMMARY.md
@.planning/phases/14-v1-fix-pass/14-02-SUMMARY.md

Key reference context (not re-included here — see files above):

- Phase 13 Task 3 outcome-handling matrix (13-UAT.md lines 462-469):
  - `pass` → Update upstream VERIFICATION.md; close item
  - `fail` → File gap in Gaps section; keep upstream at `human_needed`
  - `waived` → Document rationale; update upstream with waived note
  - `blocked` → Document blocker; do NOT update upstream

- Runbook defects to fix:
  - 13-UAT.md line 77: `nono run --detach -- ping -t 127.0.0.1` (typo — should be `--detached`)
  - 13-UAT.md line 169: `nono run --proxy-only -- cmd.exe /c set` (nonexistent flag — real flags are `--network-profile`, `--credential`, `--upstream-proxy`)

- Current upstream VERIFICATION.md status values (pre-promotion):
  - 05-VERIFICATION.md: `status: passed` already; human tests P05-HV-1 / P05-HV-2 are embedded in 13-UAT.md. Any regression on P05-HV-1 would move this to `human_needed`.
  - 07-VERIFICATION.md: `status: passed` at committed HEAD. The v1.0 UAT delta is the `human_verification:` frontmatter items. After 2nd-pass verdicts, update per-item notes; frontmatter stays `passed` unless a verdict is `fail`.
  - 09-VERIFICATION.md: `status: human_needed` → will move to `passed` iff P09-HV-1 passes.
  - 11-VERIFICATION.md: `status: human_needed` → will move to `passed` iff P11-HV-1 AND P11-HV-3 pass; P11-HV-2 stays waived.

- Real CLI flags for P09-HV-1 (confirm against `nono run --help` before committing): `--network-profile`, `--credential`, `--upstream-proxy`.

- Files to preserve unchanged (CLAUDE.md exclusion list): any Cargo source file, any Phase 10/11/12/MILESTONE-AUDIT/INTEGRATION-REPORT file listed as "do not stage".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Phase 13 runbook defects (automatable)</name>
  <files>
    .planning/phases/13-v1-human-verification-uat/13-UAT.md
  </files>
  <read_first>
    - .planning/phases/13-v1-human-verification-uat/13-UAT.md (full file)
    - The `nono run --help` output on the current binary — run it locally BEFORE editing to confirm the three flag names are exactly `--network-profile`, `--credential`, `--upstream-proxy`. If any have been renamed since 2026-04-17, adjust the edit below.
  </read_first>
  <behavior>
    - Before: P05-HV-1 `command:` line reads `nono run --detach -- ping -t 127.0.0.1`; P09-HV-1 reads `nono run --proxy-only -- cmd.exe /c set`.
    - After: P05-HV-1 uses `--detached`; P09-HV-1 uses the real flag set with a concrete worked example (local proxy on `127.0.0.1:8888`, a dummy network profile name, and a test credential service name); prerequisites document the required admin shell and `nono setup --install-wfp-service` step.
  </behavior>
  <action>
1. **Fix P05-HV-1 flag typo.** Locate section `#### 1. P05-HV-1: Detach/Attach lifecycle` (near line 76). Change the `- command:` line from `nono run --detach -- ping -t 127.0.0.1` to `nono run --detached -- ping -t 127.0.0.1`. Leave the existing `notes:` block intact (it's historical evidence of the blocked state) and append a sub-note: "Runbook flag corrected from `--detach` to `--detached` in Phase 14 Plan 03."

2. **Fix P09-HV-1 nonexistent flag.** Locate section `#### 6. P09-HV-1: Proxy env var injection` (near line 163). Replace the `prereq:` and `command:` blocks with this structure (preserve the Markdown indentation style of the surrounding items):

   - prereq items (numbered list):
     1. Admin shell confirmed (pre-flight step 4 = `yes`).
     2. `nono-wfp-service` installed AND in `RUNNING` state. If `sc query nono-wfp-service` shows `STOPPED` or the service is absent, run `nono setup --install-wfp-service` from an admin shell, then `sc start nono-wfp-service`.
     3. Local TCP listener as upstream stand-in. In a separate terminal: `python -m http.server 8888`. Any TCP listener works — it only needs to accept connections.
     4. Dummy network profile. Create `%USERPROFILE%\.config\nono\profiles\p09-uat.json` with at least a profile name and a permissive hostname list (copy from an existing profile under `crates/nono-cli/data/` if one exists, else use the minimum schema).
     5. Credential service key. Pre-seed via `nono credential set p09-uat-service dummy-token` (see `nono credential --help` for the canonical command).
   - command (PowerShell-style; use backticks for line continuation, or a single line):
     ```
     nono run `
       --network-profile p09-uat `
       --credential p09-uat-service `
       --upstream-proxy 127.0.0.1:8888 `
       -- cmd.exe /c set
     ```
   - expected: The child's env output contains both `HTTPS_PROXY=http://127.0.0.1:<nono-proxy-port>` and `NONO_PROXY_TOKEN=<opaque>`. The `<nono-proxy-port>` is the port the nono proxy binds locally (not 8888) — the proxy sits between the sandboxed child and the upstream listener.
   - result: `pending` (will be resolved in Task 2).
   - notes: Runbook corrected in Phase 14 Plan 03 Task 1. The original specified the nonexistent `--proxy-only` flag and lacked admin / WFP prerequisites. See `.planning/phases/14-v1-fix-pass/14-03-SUMMARY.md` for correction rationale.

3. **Add non-blocking observation.** In the Gaps > Additional observations section (near line 443), append a two-sentence note that the 55 stale `.nono-<hex>.json` files remain a UX wart and are deferred to a follow-up `gsd-note` (not fixed in Phase 14).

4. **Update frontmatter timestamp.** Set `updated:` at the top of `13-UAT.md` to the current UTC ISO-8601 timestamp (e.g. `"2026-04-17T23:59:00Z"` — use the actual time when the edit lands).

5. **Do NOT yet modify the Summary block** or the individual `result:` fields for the 6 target items. Those are updated by Task 2 when the human returns verdicts.

6. `git diff .planning/phases/13-v1-human-verification-uat/13-UAT.md` after the edit should show ONLY:
   - The two flag corrections (P05-HV-1 + P09-HV-1).
   - The rewritten prereq block for P09-HV-1.
   - The two short append notes (sub-note under P05-HV-1 and the additional-observations line).
   - The updated frontmatter timestamp.
   No unrelated whitespace churn.

**Note on Task 2 run order (see Task 2 step description):** The ordering chosen below deviates from the `wave:` labels in `13-UAT.md` on purpose — it is a runtime ordering chosen to respect consumption dependencies, not a re-wave of the items. Do NOT change the `wave:` labels on the items themselves; leave them as-is.
  </action>
  <verify>
    <automated>grep -q -E "^\s*- command:.*--detached -- ping" .planning/phases/13-v1-human-verification-uat/13-UAT.md &amp;&amp; ! grep -q -E "^\s*- command:.*--detach( |$)" .planning/phases/13-v1-human-verification-uat/13-UAT.md &amp;&amp; ! grep -q -- "--proxy-only" .planning/phases/13-v1-human-verification-uat/13-UAT.md &amp;&amp; grep -q -- "--network-profile" .planning/phases/13-v1-human-verification-uat/13-UAT.md</automated>
  </verify>
  <done>
    - `grep -q -E "^\s*- command:.*--detached -- ping" .planning/phases/13-v1-human-verification-uat/13-UAT.md` succeeds (fixed P05-HV-1 command line present).
    - `grep -E "^\s*- command:.*--detach( |$)" ...` returns no matches (the typo is gone, the specific command-line form is fixed).
    - `grep "\--proxy-only" ...` returns no matches.
    - `grep -E "\--network-profile|\--credential|\--upstream-proxy" ...` matches at least once (P09-HV-1 block).
    - `updated:` frontmatter timestamp reflects the edit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Second UAT pass on an admin Windows host (human-required)</name>
  <what-built>
    Plan 14-01 landed the Bug #3 fix (detached console-child DLL init).
    Plan 14-02 landed the `setup --check-only` help-text reconciliation.
    Plan 14-03 Task 1 corrected the runbook so P05-HV-1 and P09-HV-1 are runnable as written.
  </what-built>
  <how-to-verify>
Run the six target items from the corrected `13-UAT.md`. Record a terminal verdict (`pass` / `fail` / `waived`) for each in the item's `result:` field. A verdict of `blocked` is NOT acceptable — if the environment blocks an item, either remediate the environment (install WFP service, elevate to admin) or convert to `fail` with the blocker documented.

**Note on item ordering:** Order chosen: independent items (P07-HV-2 setup text, P05-HV-1 detached start) run first; downstream items (P07-HV-3 session commands, P11-HV-1/P11-HV-3 supervised flows) consume the session ID seeded by P05-HV-1. P09-HV-1 runs last because it has the heaviest prerequisite stack (admin + WFP service + local TCP listener + profile + credential). Deviates from 13-UAT.md wave labels intentionally — the wave labels stay untouched.

**Host prerequisites:**
1. Windows 10 build 17763+ or Windows 11 (pre-flight step 1 = yes).
2. Admin-elevated PowerShell (pre-flight step 4 = yes).
3. `nono-wfp-service` installed and RUNNING (pre-flight step 5 = yes). If missing, run `nono setup --install-wfp-service` from the admin shell and then `sc start nono-wfp-service`.
4. Fresh build of nono at the tip of `windows-squash` after Plans 14-01 and 14-02 land: `cargo build -p nono-cli --release`. Record `git rev-parse --short HEAD` into `host.nono_binary_commit` in 13-UAT.md frontmatter.

**Items to run (in the listed order; order matters — P05-HV-1 creates the session that P07-HV-3 / P11-HV-1 / P11-HV-3 consume):**

1. **P07-HV-2** (setup help text — should now be `pass` after Plan 14-02):
   ```
   .\target\release\nono.exe setup --check-only 2>&1 | Select-String "nono wrap"
   .\target\release\nono.exe setup --check-only 2>&1 | Select-String "intentionally unavailable"
   ```
   Expect first to match the canonical sentence; expect second to return nothing. Record verdict `pass` iff both hold, else `fail`.

2. **P05-HV-1** (detached session start — should now be `pass` after Plan 14-01):
   ```
   .\target\release\nono.exe run --detached --allow-cwd -- ping -t 127.0.0.1
   ```
   Expect: banner with session ID within ~2 seconds; `tasklist | findstr ping` shows a live `ping.exe`; no `0xC0000142` in stderr. Record session ID into a local variable `$SID` for subsequent items. Record verdict `pass` iff banner + live process + no DLL-init error; `fail` otherwise.

3. **P07-HV-3** (session commands round-trip — depends on P05-HV-1 creating a session):
   ```
   .\target\release\nono.exe logs $SID
   .\target\release\nono.exe inspect $SID
   .\target\release\nono.exe prune --dry-run
   ```
   Expect each to print real session data; no `UnsupportedPlatform`. Verdict `pass` iff all three return real data.

4. **P11-HV-1** (capability pipe round-trip — depends on P05-HV-1 path working):
   Create `test-cap-client.ps1` from the embedded script in `13-UAT.md` section `#### 7. P11-HV-1`. Run:
   ```
   .\target\release\nono.exe run --supervised -- powershell -ExecutionPolicy Bypass -File test-cap-client.ps1
   ```
   Expect `[nono] Grant access? [y/N]` prompt. Reply `y` → grant response; reply `N` → deny response. Verdict `pass` iff both the prompt appears AND both responses are correctly brokered.

5. **P11-HV-3** (token leak audit — same session setup as P11-HV-1):
   Before running, capture the session token value from the P11-HV-1 run (look for `NONO_SESSION_TOKEN` in the supervisor's audit log or re-spawn the supervised session and note it). Then:
   ```
   $env:RUST_LOG = "trace"
   .\target\release\nono.exe run --supervised -- powershell -ExecutionPolicy Bypass -File test-cap-client.ps1 2> trace-output.txt
   findstr /C:"<full-64-char-hex-token>" trace-output.txt
   ```
   Expect `findstr` to return no matches (exit 1). Then `del trace-output.txt`. Verdict `pass` iff zero log lines contain the full token value.

6. **P09-HV-1** (proxy env injection — uses the corrected runbook block):
   Follow the corrected prereq block (5 steps) from Task 1. Run the multi-flag command:
   ```
   nono run `
     --network-profile p09-uat `
     --credential p09-uat-service `
     --upstream-proxy 127.0.0.1:8888 `
     -- cmd.exe /c set
   ```
   Expect child env includes `HTTPS_PROXY=http://127.0.0.1:<port>` and `NONO_PROXY_TOKEN=<value>`. Verdict `pass` iff both env vars appear.

**After all 6 items have verdicts:**

- Update each item's `result:` field in `13-UAT.md` to the verdict. Write the value in bare form — e.g. `- result: pass` — and strip any pre-existing Markdown bolding (`**pass**`, `**blocked**`) from the line being replaced. The Task 3 machine-verify tolerates both shapes, but writing bare is what Phase 14 adopts going forward.
- Update each item's `notes:` field to record evidence (actual output snippet, exit code, any anomalies).
- Update the `## Summary` block counts:
  - `total: 10` (unchanged)
  - `passed: N` (sum of new passes + existing P07-HV-1, P09-HV-2)
  - `issues: M` (new fails; expect 0 if Bug #3 fix + runbook fix hold)
  - `blocked: 0` (mandatory — no item may remain blocked)
  - `pending: 0` (mandatory — no item may remain pending)
  - `waived: 2` (P05-HV-2, P11-HV-2 — unchanged)
  - `skipped: 0`
- Update the `Disposition` lines under the Summary block to reflect the new verdicts.
- Update the YAML frontmatter `status:` to `complete` (or the value Plan 13-01 specifies as the finished state — grep 13-01-PLAN for the expected terminal status if unsure).
- Set the YAML frontmatter `updated:` to the current UTC timestamp.
  </how-to-verify>
  <resume-signal>
Type "verdicts recorded" when all six items have terminal verdicts written into 13-UAT.md and the Summary block shows `blocked: 0`. Also paste the verdicts inline (e.g. `P05-HV-1: pass, P07-HV-2: pass, P07-HV-3: pass, P09-HV-1: pass, P11-HV-1: pass, P11-HV-3: pass`) so Task 3 can use them to drive the upstream promotion.

If any item is `fail`, describe the failure in the resume message so Task 3 files the gap instead of promoting.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Promote upstream VERIFICATION.md files per verdicts</name>
  <files>
    .planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md
    .planning/phases/07-quick-wins/07-VERIFICATION.md
    .planning/phases/09-wfp-port-level-proxy-filtering/09-VERIFICATION.md
    .planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md
    .planning/phases/13-v1-human-verification-uat/13-UAT.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - The Task 2 resume signal with the six verdicts.
    - Each of the four upstream VERIFICATION.md files (frontmatter + any Gaps / human_verification blocks that need updating).
    - .planning/phases/13-v1-human-verification-uat/13-UAT.md lines 462-469 (outcome-handling matrix).
  </read_first>
  <action>
For each of the four upstream VERIFICATION.md files, apply the promotion rule based on the verdicts from Task 2:

1. **05-VERIFICATION.md (P05-HV-1, P05-HV-2):**
   - If P05-HV-1 = `pass` AND P05-HV-2 = `waived`: ensure frontmatter `status:` is `passed`. Update the file's `human_verification:` block (if present) or append a new section "Phase 13 UAT Verdicts" noting P05-HV-1 → pass with the 13-UAT.md evidence link and P05-HV-2 → waived with the documented rationale. Also bump `re_verification.previous_status` and `re_verification.previous_score` fields if they exist; bump `verified:` to the current date.
   - If P05-HV-1 = `fail`: keep frontmatter `status: passed` only if the structural verification still holds (code paths unchanged), OR drop to `human_needed` if the fail implies a regression. Add a "Gaps" section (or append to existing one) with the failure description, severity, and a link back to 13-UAT.md. File a follow-up note in STATE.md's blockers list.

2. **07-VERIFICATION.md (P07-HV-1, P07-HV-2, P07-HV-3):**
   - P07-HV-1 was already `pass` in the first UAT pass — do not re-verify.
   - If P07-HV-2 = `pass` (after Plan 14-02) AND P07-HV-3 = `pass` (after Plan 14-01): frontmatter `status:` stays `passed`. Update the `human_verification:` block's per-item entries to record Phase 13 UAT verdicts. Bump `verified:` to current date.
   - If either fails: update frontmatter to `human_needed` and add a Gaps section entry.

3. **09-VERIFICATION.md (P09-HV-1, P09-HV-2):**
   - P09-HV-2 was already `pass` in the first UAT pass.
   - If P09-HV-1 = `pass`: promote frontmatter `status:` from `human_needed` to `passed`. Update the file's `human_verification:` block to record the pass verdict with evidence link. Bump `verified:` to current date. Bump `score:` from `6/7` to `7/7`.
   - If P09-HV-1 = `fail`: keep `status: human_needed` and add a Gaps section entry.

4. **11-VERIFICATION.md (P11-HV-1, P11-HV-2, P11-HV-3):**
   - P11-HV-2 stays `waived` (documented rationale in 13-UAT.md).
   - If P11-HV-1 = `pass` AND P11-HV-3 = `pass`: promote frontmatter `status:` from `human_needed` to `passed`. Update `human_verification:` entries. Bump `verified:` to current date. Bump `score:` from `4/4 success criteria structurally verified` to `4/4 success criteria verified (structural + UAT)` or equivalent.
   - If either fails: keep `status: human_needed` and add a Gaps section entry.

5. For any VERIFICATION.md that gets promoted to `passed`, also add a brief "Phase 13 UAT Closeout" paragraph at the bottom of the file (above the final `---`) linking to `.planning/phases/13-v1-human-verification-uat/13-UAT.md` and `.planning/phases/14-v1-fix-pass/14-03-SUMMARY.md` as the authoritative evidence trail.

6. After all four VERIFICATION.md files are updated, commit with:
   ```
   docs(14): promote upstream VERIFICATION.md files per Phase 13 UAT verdicts

   Closes Phase 14 roadmap success criterion #5. After Plans 14-01 and
   14-02 unblocked the six previously blocked/failed UAT items, the
   second UAT pass produced terminal verdicts:

     P05-HV-1: <verdict>
     P07-HV-2: <verdict>
     P07-HV-3: <verdict>
     P09-HV-1: <verdict>
     P11-HV-1: <verdict>
     P11-HV-3: <verdict>

   Upstream VERIFICATION.md promotions:
     - 05-VERIFICATION.md: <status change or "unchanged">
     - 07-VERIFICATION.md: <status change or "unchanged">
     - 09-VERIFICATION.md: <status change or "unchanged">
     - 11-VERIFICATION.md: <status change or "unchanged">

   The v1.0 Windows Parity milestone can now be archived: every
   VERIFICATION.md has a terminal status backed by either structural
   code evidence or a human-executed UAT verdict.

   Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
   ```

7. Stage only the files this plan modifies: `13-UAT.md` (with the Task 2 updates) plus the four VERIFICATION.md files and `ROADMAP.md`. Do NOT stage the pre-existing uncommitted files listed in CLAUDE.md.

8. **ROADMAP.md bookkeeping:** Tick the Phase 14 entry in `.planning/ROADMAP.md`. Find the Phase 14 list item and change `- [ ] Phase 14: v1.0 Fix Pass` to `- [x] Phase 14: v1.0 Fix Pass` (exact casing; keep the trailing description text intact). Also tick the 14-01, 14-02, 14-03 sub-entries if they exist in the same ROADMAP section. Do NOT mark the milestone itself archived in this plan — that's a separate follow-up outside Phase 14's scope.
  </action>
  <verify>
    <automated>
# --- Core invariants (unchanged) ---
grep -q "^blocked: 0" .planning/phases/13-v1-human-verification-uat/13-UAT.md &amp;&amp; \
! grep -q -E "^\s*-?\s*result:\s*\*{0,2}blocked\*{0,2}\s*$" .planning/phases/13-v1-human-verification-uat/13-UAT.md &amp;&amp; \
grep -q "^\s*pending: 0" .planning/phases/13-v1-human-verification-uat/13-UAT.md &amp;&amp; \
grep -q "^\s*skipped: 0" .planning/phases/13-v1-human-verification-uat/13-UAT.md &amp;&amp; \
grep -E "^status: (passed|human_needed)" \
  .planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md \
  .planning/phases/07-quick-wins/07-VERIFICATION.md \
  .planning/phases/09-wfp-port-level-proxy-filtering/09-VERIFICATION.md \
  .planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md &amp;&amp; \
grep -q "\[x\].*Phase 14: v1.0 Fix Pass" .planning/ROADMAP.md &amp;&amp; \
# --- Upstream consistency check (Option A per revision W2) ---
# Helper: extract the `result:` line for a UAT item id and trim to the final token.
verdict_of() {
  awk -v id="$1" '
    $0 ~ "#### .*"id {in_item=1; next}
    in_item && /^####/ {exit}
    in_item &amp;&amp; /^\s*-?\s*result:/ {
      val = $NF; gsub(/[[:space:]*]/, "", val); print val; exit
    }
  ' .planning/phases/13-v1-human-verification-uat/13-UAT.md
}
# Rule: if every owned UAT item for a file is pass OR waived, the upstream must be `status: passed`.
check_status_passed() {
  local file="$1"; shift
  for id in "$@"; do
    v=$(verdict_of "$id")
    if [ "$v" != "pass" ] &amp;&amp; [ "$v" != "waived" ]; then return 0; fi
  done
  grep -q "^status: passed" "$file" || { echo "FAIL: $file must be status: passed when all owned UAT verdicts are pass/waived (ids: $*)"; return 1; }
}
check_status_passed .planning/phases/09-wfp-port-level-proxy-filtering/09-VERIFICATION.md P09-HV-1 &amp;&amp; \
check_status_passed .planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md P11-HV-1 P11-HV-3 &amp;&amp; \
check_status_passed .planning/phases/07-quick-wins/07-VERIFICATION.md P07-HV-2 P07-HV-3 &amp;&amp; \
check_status_passed .planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md P05-HV-1
    </automated>
  </verify>
  <done>
    - `grep -q "^blocked: 0" 13-UAT.md` succeeds (Task 2 Summary block is written).
    - `! grep -q -E "^\s*result: blocked" 13-UAT.md` succeeds (no item retains a blocked verdict inline).
    - `grep -q "^\s*pending: 0" 13-UAT.md` and `grep -q "^\s*skipped: 0" 13-UAT.md` both succeed.
    - For each of the four upstream VERIFICATION files: if every owned UAT item in `13-UAT.md` is `pass` or `waived`, the upstream `status:` is `passed`; otherwise `status:` is NOT `human_needed` in silent denial — either `passed` with waivers/notes documented or a gap is filed in the file's Gaps section.
    - **Automated upstream/UAT consistency:** The `<verify>` block's `check_status_passed` helper asserts, for each of the four upstream VERIFICATION.md files, that whenever every owned `result:` in `13-UAT.md` is `pass` or `waived` the upstream carries `status: passed`. This closes the runner-silence gap flagged in revision iteration 1 item W2.
    - If P09-HV-1 reached `pass`, `09-VERIFICATION.md` contains `^score: 7/7` (bumped from 6/7).
    - `grep -q "\[x\].*Phase 14: v1.0 Fix Pass" .planning/ROADMAP.md` succeeds (roadmap checkbox ticked — step 8).
    - Commit landed with DCO sign-off.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation ↔ reality | VERIFICATION.md files are the source of truth for milestone archival. Incorrect status claims would mislead future phases and downstream consumers. |
| Runbook ↔ tester | The runbook is executable by humans. Defects make items un-runnable and bias verdicts toward `blocked`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Repudiation | Promoting a VERIFICATION.md to `passed` based on a `blocked` or missing verdict | mitigate | Task 2 explicitly rejects `blocked` as a terminal verdict; Task 3's promotion rules require a concrete `pass` verdict before frontmatter status moves. Fail verdicts route to Gaps instead of silent promotion. Task 3 `<verify>` block machine-checks the upstream `status:` consistency per UAT verdicts so a runner cannot silently leave an upstream file at `human_needed` when its owned items are `pass`/`waived`. |
| T-14-03-02 | Tampering | Runbook re-introducing the `--proxy-only` or `--detach` defects in a future edit | mitigate | Task 1's grep-based automated verify clause will fail on any PR that re-introduces the stale flags. |
| T-14-03-03 | Information Disclosure | `trace-output.txt` from P11-HV-3 contains tracing data that may include other sensitive fields | accept / mitigate | Task 2 step 5 explicitly requires `del trace-output.txt` after grep. Document this in the P11-HV-3 `notes:` block. |
</threat_model>

<verification>
1. `13-UAT.md` contains `--detached` and does NOT contain `--detach ` (standalone flag) or `--proxy-only`.
2. `13-UAT.md` `## Summary` block has `blocked: 0` and `pending: 0` after Task 2.
3. No item line in `13-UAT.md` still carries `result: blocked`.
4. Each of `05-VERIFICATION.md`, `07-VERIFICATION.md`, `09-VERIFICATION.md`, `11-VERIFICATION.md` has an updated `status:` field consistent with the Task 2 verdicts.
5. If P09-HV-1 reached `pass`, `09-VERIFICATION.md` carries `score: 7/7`.
6. `ROADMAP.md` Phase 14 entry is ticked (`[x] Phase 14: v1.0 Fix Pass`).
7. Single commit (or small series of commits) landed with DCO sign-off on every commit.
8. No pre-existing uncommitted files (per CLAUDE.md exclusion list) have been accidentally staged.
9. Task 3 automated `<verify>` `check_status_passed` helper passes: for each of the four upstream VERIFICATION.md files, whenever every owned UAT item in 13-UAT.md is `pass` or `waived`, the upstream carries `status: passed` (machine-verified, not narrative-only).
</verification>

<success_criteria>
- Closes roadmap success criteria #3 (runbook corrected), #4 (no blocked items), and #5 (upstream VERIFICATION.md promotions).
- Every v1.0 VERIFICATION.md has an honest terminal status backed by structural evidence or a human UAT verdict.
- The milestone can be archived after this plan lands (archival itself is a follow-up; this plan's scope ends at the promotion).
- Out-of-scope follow-ups (55 stale state files, UNC path warning) are documented in 13-UAT.md Gaps > Additional observations.
</success_criteria>

<output>
After completion, create `.planning/phases/14-v1-fix-pass/14-03-SUMMARY.md` summarizing:
- The six verdicts from Task 2 (inline list).
- Each upstream VERIFICATION.md promotion (before → after).
- Runbook corrections applied (the two flag fixes + prereq block rewrite).
- Any gaps that remain (should be zero if Plans 14-01 and 14-02 held).
- Pointer to the next milestone's planning entry point (so the reader knows where the project resumes).
</output>
</content>
</invoke>