# Phase 36.5: Profile drafts feature absorption (optional) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-13
**Phase:** 36.5 — Profile drafts feature absorption (optional)
**Areas discussed:** CLI surface & semantics, Storage path & error variant, Plan slicing & cherry-pick approach, Shadowing safeguards & promote review UX

---

## CLI surface & semantics

### Q1: How should drafts get created on disk?

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror upstream: `init --draft` flag | Add `--draft` flag to existing `nono profile init <name>`. Drops the skeleton into `profile-drafts/<name>.json`. Reuses 100% of init's name validation, `--extends`/`--groups`/`--description`/`--full`/`--force` logic. Diverges from REQ literal wording but matches upstream byte-for-byte. | ✓ |
| Literal REQ: new `nono profile draft <name>` subcommand | Add a brand-new `ProfileCommands::Draft` variant + `ProfileDraftArgs` struct. Stricter literal-REQ adherence. Costs a fresh subcommand surface + duplicate name validation + new docs row. Diverges from upstream. | |
| Both: `--draft` flag on init AND a `draft` subcommand alias | Belt-and-suspenders. Maximum surface area; doubles the test matrix. | |

**User's choice:** Mirror upstream `init --draft` flag.
**Notes:** REQ acceptance #1 literal phrasing reinterpreted to preserve upstream byte-for-byte parity. Captured as D-36.5-A1.

### Q2: How should `nono profile validate --draft` compose with `--strict` (Plan 36-01a)?

| Option | Description | Selected |
|--------|-------------|----------|
| Independent flags, both composable | `--draft` switches input source; `--strict` switches deprecation-key handling. Both compose: `validate --draft --strict <name>` validates a draft with strict legacy-key mode. Mirrors upstream. | ✓ |
| `--draft` implies `--strict` | Apply strict deprecation-key handling automatically for drafts. Surprises power users; not upstream. | |
| `--draft <name>` only; no flag composition with `--strict` | Reject combo at clap parse time. Forces two-step validation. Breaks user expectations. | |

**User's choice:** Independent, composable.
**Notes:** Captured as D-36.5-A2.

### Q3: What's the `nono profile promote` UX shape?

| Option | Description | Selected |
|--------|-------------|----------|
| Upstream-faithful interactive diff + `--yes` flag | Default: unified diff + y/N prompt. `--yes` for CI. Reuses fork's existing `ProfileDiffArgs`/`cmd_diff` rendering. | ✓ |
| Atomic apply only; no interactive prompt | `promote` always lands the draft atomically. Loses "review before apply" safety. Diverges from upstream. | |
| Interactive prompt only; no `--yes` flag | Always prompts y/N. Blocks scripted/CI use entirely. | |

**User's choice:** Interactive diff + `--yes`.
**Notes:** Captured as D-36.5-A3.

### Q4: Should `promote` accept piped/stdin input or strictly file-on-disk?

| Option | Description | Selected |
|--------|-------------|----------|
| Strict file-on-disk: `promote <name>` only | Draft must exist at `profile-drafts/<name>.json`. Matches upstream. Atomicity + base-hash well-defined. | ✓ |
| Allow `--from-stdin` flag | Stdin path for CI flows. Extra surface; security-relevant; base-hash semantics fuzzier. | |

**User's choice:** Strict file-on-disk.
**Notes:** `--from-stdin` deferred to v2.5-FU. Captured as D-36.5-A4.

---

## Storage path & error variant

### Q5: Where should `profile-drafts/` live on Windows?

| Option | Description | Selected |
|--------|-------------|----------|
| `%APPDATA%\nono\profile-drafts\` (fork actual) | Reuse existing `nono_config_dir()` helper. Sibling of `profiles/` + `packages/`. Single helper, single mental model. Flag REQ-04 / Plan 22-03 PKG-02 docs as stale (separate cleanup). | ✓ |
| `%LOCALAPPDATA%\nono\profile-drafts\` (REQ literal) | Honor REQ acceptance #4 literal text. New path-resolution helper. Creates third nono data root on Windows. Drift risk. | |
| Add `nono_drafts_dir()` helper + update REQ to APPDATA | Same final layout as option 1 but explicitly fix REQ wording in this phase. | |

**User's choice:** `%APPDATA%` fork actual.
**Notes:** REQ acceptance #4 literal `%LOCALAPPDATA%` reinterpreted as `%APPDATA%`. REQ-04 / Plan 22-03 PKG-02 docs flagged stale (separate cleanup deferred). Captured as D-36.5-B1.

### Q6: What shape should `NonoError::ActionRequired` take?

| Option | Description | Selected |
|--------|-------------|----------|
| Typed struct: `expected/actual/resolve_via` | Matches REQ acceptance #3 wording literally. Machine-readable fields. Display impl renders REQ-prescribed message. | ✓ |
| Generic single-string body (upstream-faithful) | `ActionRequired(String)`. Mirrors upstream. Loses structured fields. | |
| Enum-of-causes variant | `ActionRequired { cause: ActionRequiredCause }`. Maximum forward-compat. Premature for 1-week phase. | |

**User's choice:** Typed struct `{ expected, actual, resolve_via }`.
**Notes:** Reused for shadow-refusal errors with a different `resolve_via`. Captured as D-36.5-B2.

### Q7: C FFI: how should `ActionRequired` map?

| Option | Description | Selected |
|--------|-------------|----------|
| Map to `ErrConfigParse` (-9) (upstream-faithful) | Mirror upstream FFI map. No new error code value. Matches deferred-items § P34-DEFER-04b-2 line 74. | ✓ |
| Add new `ErrActionRequired` code | Lets C-side callers distinguish base-hash mismatches structurally. Extends FFI ABI. Diverges from upstream. | |

**User's choice:** `ErrConfigParse` (-9), upstream-faithful.
**Notes:** Captured as D-36.5-B3.

### Q8: Base-hash algorithm for draft → canonical staleness check?

| Option | Description | Selected |
|--------|-------------|----------|
| SHA-256 | Already in workspace dep tree (`sha2`). Matches Phase 22-03 PKG-04, Phase 32 sigstore, Phase 04 undo ObjectStore. One algorithm across codebase. | ✓ |
| Blake3 | ~3-5x faster. Adds dep surface without benefit for sub-MB profile JSON. | |
| Match upstream verbatim | Defer to research phase. Likely SHA-256. | |

**User's choice:** SHA-256.
**Notes:** Captured as D-36.5-B4.

---

## Plan slicing & cherry-pick approach

### Q9: Cherry-pick `829c341a` first, or default to D-20 manual-replay?

| Option | Description | Selected |
|--------|-------------|----------|
| Research-phase probe + decide | `git cherry-pick --no-commit 829c341a` measures conflict surface. ≤500 LOC mechanical → D-19. >500 LOC structural → D-20. Decision in PLAN.md. | ✓ |
| Default D-20 manual-replay; no cherry-pick attempt | Safer (no half-applied cherry-pick state). Costs Upstream-commit trailer audit signal. | |
| Default D-19 cherry-pick; escalate if blocked | Optimistic; risks repeat of 3619-line conflict. | |

**User's choice:** Research-phase probe with conflict-size rubric.
**Notes:** Captured as D-36.5-C2.

### Q10: How many plans?

| Option | Description | Selected |
|--------|-------------|----------|
| 1 plan, multi-commit sequencing | Single PLAN.md, 3 commits, 1 PR. Mirrors Plan 36-03 D-36-D2 shape. Commit boundaries map cleanly to acceptance criteria. | ✓ |
| 2 plans split by surface | 36.5-01 read/write + 36.5-02 state-change. Two PRs. Costs inter-plan dependency. | |
| 3 plans split by acceptance criteria | Three PRs. Finest reviewer attention. 3x close-gate runs. Overkill for 1-week phase. | |

**User's choice:** 1 plan + 3 sequenced commits.
**Notes:** Captured as D-36.5-C1. Commit boundaries: (1) drafts dir + `init --draft` + atomic write, (2) SHA-256 + `ActionRequired` + FFI + `promote` + shadowing, (3) `validate --draft` + `package_status.rs` + tests + docs.

### Q11: `package_status.rs` (218 LOC) — port in 36.5 or split off?

| Option | Description | Selected |
|--------|-------------|----------|
| Port in 36.5 per REQ scope | Additive port (no fork analog → zero conflict). Closes full REQ. ~1 day in Commit 3. | ✓ |
| Defer to v2.5-FU; close acceptance #1–#5 only | Scope drift on REQ wording. Forces new v2.5-FU registration. | |
| Port skeleton only in 36.5 | Half-measure; carries dead code. Avoid. | |

**User's choice:** Port in 36.5 per REQ scope.
**Notes:** Captured as D-36.5-C3.

### Q12: PR strategy and close-gate inheritance?

| Option | Description | Selected |
|--------|-------------|----------|
| Direct-on-main, one PR per plan, D-36-A5 8-step close gate | Inherit Phase 35/34/36 verbatim. macOS cross-target clippy load-bearing for `package_status.rs`. | ✓ |
| Direct-on-main, one PR per plan, D-36-A5 close gate MINUS smoke-gate rows 6-8 | Skip Phase 15 detached-console smoke + WFP + learn tests. Sets ad-hoc gate-skipping precedent. | |

**User's choice:** Full 8-step gate verbatim.
**Notes:** Gates 6-8 expected documented-skipped (no exec/WFP/learn surface). Captured as D-36.5-C4.

---

## Shadowing safeguards & promote review UX

### Q13: How should promote detect shadow of built-in / package-managed?

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse `profile/mod.rs` existing helpers + add built-in lookup | Use `is_package_managed_profile` + embedded built-in name table from `config/embedded.rs`. Fail-closed on either. | ✓ |
| Add a new `is_shadowed_profile()` helper | Wrap both checks. Cleaner call site. New helper + test surface. | |
| Inline the checks in `cmd_promote` | Simplest. Loses readability. | |

**User's choice:** Reuse existing helpers + built-in lookup.
**Notes:** Captured as D-36.5-D1. Planner discretion on wrapping the two checks behind a single helper.

### Q14: Should there be a `--force` override on `promote`?

| Option | Description | Selected |
|--------|-------------|----------|
| No `--force` override; refuse-always with `ActionRequired` | Built-in and package-managed are immutable from `promote`. Users override via `init --force` (user-layer shadow). Cleaner threat model. Deliberate deviation from upstream. | ✓ |
| Allow `--force` on package-managed only (not built-in) | Compromise. More complex error message. | |
| Match upstream: `--force` on both | Maximum upstream parity. Supply-chain risk. | |

**User's choice:** No `--force` override; refuse-always.
**Notes:** Deliberate security-posture deviation from upstream. Captured as D-36.5-D2. Document in `docs/cli/features/profiles-groups.mdx`.

### Q15: What should the refuse-error message say for shadow refusals?

| Option | Description | Selected |
|--------|-------------|----------|
| Cite type + resolution path (reuse `ActionRequired`) | Reuse `NonoError::ActionRequired` with shadow-specific `resolve_via`. Operator gets clear action. | ✓ |
| Use a dedicated `NonoError::ProfileShadowed` variant | Cleaner taxonomy. Costs another NonoError + C FFI map arm. Overkill. | |
| Generic `ProfileParse` error with embedded shadow context | Cheapest. Loses structured fields. Avoid. | |

**User's choice:** Reuse `ActionRequired` with shadow-specific `resolve_via`.
**Notes:** Captured as D-36.5-D3.

### Q16: What does the interactive diff render in `promote` review prompt?

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse `cmd_diff` JSON-aware diff | Call into existing `cmd_diff` rendering helper. Shows structural deltas. Consistent with `nono profile diff` UX. | ✓ |
| Plain text-diff (`diff -u`-style) | Byte-level unified diff. Simpler. Less informative for nested structure. Diverges from existing semantics. | |
| Show only summary of added/removed/changed keys | Faster for big profiles. Loses signal for security-relevant value changes. Avoid. | |

**User's choice:** Reuse `cmd_diff` JSON-aware diff.
**Notes:** Captured as D-36.5-D4. Sample rendering captured in CONTEXT.md.

---

## Claude's Discretion

- Whether to add a single `is_shadowed_profile(name)` wrapper helper or inline the two existing checks directly in `cmd_promote`.
- Whether to include `--refresh` mode on `init --draft` in Commit 1 (recommended yes, because `ActionRequired` Display references it) or defer to v2.5-FU.
- Whether `package_status.rs` raises `ActionRequired` on critical advisories (recommended yes, second callsite reinforces typed-variant rationale).
- Test fixture organization (single `profile_drafts_tests.rs` vs split per surface).
- PR title conventions, draft vs ready-for-review at open, reviewer assignment — inherited from Phase 34/35/36.
- Phase 36.5 SUMMARY closure section in `.planning/phases/34-…/deferred-items.md` flipping P34-DEFER-04b-2 to closed.
- ROADMAP.md line 112 `(completed 2026-05-13)` annotation cleanup — handled by `/gsd-progress` or plan-close artifact.

## Deferred Ideas

- `--from-stdin` on `nono profile promote` — defer to v2.5-FU if a CI use case surfaces.
- REQ-PORT-CLOSURE-03 acceptance #4 wording cleanup in REQUIREMENTS.md (APPDATA vs LOCALAPPDATA) — follow-up docs commit; opportunistically in Commit 3 alongside other docs work.
- Plan 22-03 PKG-02 doc claim of `%LOCALAPPDATA%` vs actual `%APPDATA%` — separate docs cleanup audit.
- `--force` override on `promote` — deliberate deviation from upstream; reconsider only if a concrete use case can't be solved via `init --force` user-layer shadowing.
- Dedicated `NonoError::ProfileShadowed` variant — promote only if shadow-refusal callsites grow beyond 1.
- Blake3 base-hash — reconsider only if profile JSON grows > 10MB.
- Enum-of-causes `ActionRequired` variant shape — reconsider only if a third callsite needs different structured fields.
- Audit-event retrofit for `promote` success / `validate --draft` outcomes — out of scope per D-34-B2; reconsider in Phase 39 UPST4 audit.
