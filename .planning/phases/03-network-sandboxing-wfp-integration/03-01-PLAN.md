---
phase: 03-network-sandboxing-wfp-integration
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified: [crates/nono-cli/Cargo.toml, crates/nono-cli/src/windows_wfp_contract.rs, crates/nono-cli/src/bin/nono-wfp-service.rs, crates/nono-cli/src/cli.rs, crates/nono-cli/src/setup.rs]
autonomous: true
requirements: [NETW-01, NETW-02, DEPL-02]

must_haves:
  truths:
    - "Service can register with Windows Service Control Manager (SCM)"
    - "Service starts and listens on Named Pipe \\\\.\\pipe\\nono-wfp-control"
    - "Service initializes WFP session with FWPM_SESSION_FLAG_DYNAMIC"
    - "CLI provides command to install and register the WFP service"
  artifacts:
    - path: "crates/nono-cli/src/windows_wfp_contract.rs"
      provides: "Updated IPC contract with session_sid"
    - path: "crates/nono-cli/src/bin/nono-wfp-service.rs"
      provides: "Persistent Named Pipe server and SCM registration logic"
    - path: "crates/nono-cli/src/setup.rs"
      provides: "Service installation logic"
  key_links:
    - from: "crates/nono-cli/src/bin/nono-wfp-service.rs"
      to: "Windows SCM"
      via: "windows-service crate"
    - from: "crates/nono-cli/src/bin/nono-wfp-service.rs"
      to: "\\\\.\\pipe\\nono-wfp-control"
      via: "tokio::net::windows::named_pipe::ServerOptions"
    - from: "nono setup"
      to: "Windows SCM"
      via: "Service installation API"
---

<objective>
Implement the background WFP service foundation with SCM registration, persistent Named Pipe IPC, dynamic WFP engine initialization, and CLI installation support.

Purpose: Establish the secure control channel and lifecycle management for kernel-level network enforcement, ensuring the service can run as a standard Windows service and be easily installed by the user.
Output: Refactored `nono-wfp-service` capable of handling persistent requests and interacting with the Windows Service Control Manager, and CLI integration for setup.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-RESEARCH.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-CONTEXT.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Windows Service Dependencies</name>
  <files>crates/nono-cli/Cargo.toml</files>
  <action>
    Add the `windows-service` crate to `crates/nono-cli/Cargo.toml`. This will provide the necessary abstractions for communicating with the Windows Service Control Manager.
  </action>
  <verify>
    <automated>cargo build -p nono-cli</automated>
  </verify>
  <done>windows-service crate is added to dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Update WFP IPC Contract</name>
  <files>crates/nono-cli/src/windows_wfp_contract.rs</files>
  <action>
    Update `WfpRuntimeActivationRequest` to include a `session_sid: Option<String>` field. This field will carry the unique SID generated by the CLI to the service.
  </action>
  <verify>
    <automated>cargo check -p nono-cli</automated>
  </verify>
  <done>WfpRuntimeActivationRequest has session_sid field.</done>
</task>

<task type="auto">
  <name>Task 3: Implement SCM Registration and Main Loop</name>
  <files>crates/nono-cli/src/bin/nono-wfp-service.rs</files>
  <action>
    Refactor `nono-wfp-service.rs` to:
    - Use `windows_service::service_dispatcher` to handle SCM commands (Start, Stop).
    - Implement a service main function that sets up the `tokio` runtime.
    - Create a Named Pipe server at `\\.\pipe\nono-wfp-control`.
    - Secure the pipe using the tighter SDDL `D:(A;;GA;;;SY)(A;;GA;;;BA)(A;;GRGW;;;OW)` to restrict to System, Admins, and Owner Rights.
    - Loop and accept connections, reading `WfpRuntimeActivationRequest` JSON and responding with `WfpRuntimeActivationResponse`.
  </action>
  <verify>
    <automated>cargo build -p nono-cli --bin nono-wfp-service</automated>
  </verify>
  <done>Service handles SCM commands and listens on Named Pipe with restricted access.</done>
</task>

<task type="auto">
  <name>Task 4: Initialize WFP Engine with Dynamic Session</name>
  <files>crates/nono-cli/src/bin/nono-wfp-service.rs</files>
  <action>
    Update `open_wfp_engine` to:
    - Use `FWPM_SESSION_FLAG_DYNAMIC` in `FWPM_SESSION0`.
    - Create a dedicated `nono` sublayer (`FWPM_SUBLAYER0`) with a high weight (e.g., `0x1000`) to ensure it precedes standard Windows Firewall rules.
    - Ensure the engine handle is held for the duration of the service process to keep the dynamic session alive.
  </action>
  <verify>
    <automated>cargo check -p nono-cli --bin nono-wfp-service</automated>
  </verify>
  <done>WFP engine initialized with dynamic session and custom sublayer.</done>
</task>

<task type="auto">
  <name>Task 5: CLI Integration for Service Installation</name>
  <files>crates/nono-cli/src/cli.rs, crates/nono-cli/src/setup.rs</files>
  <action>
    Update the CLI to support service installation:
    - Add `--register-wfp-service` flag to `nono setup` command in `cli.rs`.
    - In `setup.rs`, implement the logic to:
        1. Check for administrative privileges.
        2. Use `windows-service` or direct SCM calls to register `nono-wfp-service` as a Windows service.
        3. Configure the service to start automatically.
        4. Provide clear feedback to the user on success/failure.
  </action>
  <verify>
    <automated>cargo build -p nono-cli</automated>
  </verify>
  <done>CLI can register the nono-wfp-service via `nono setup --register-wfp-service`.</done>
</task>

</tasks>

<verification>
Check service compilation, SCM dispatcher initialization, and CLI setup command integration.
Reference: @.planning/phases/03-network-sandboxing-wfp-integration/03-VALIDATION.md
</verification>

<success_criteria>
- `nono-wfp-service` compiles and correctly uses `windows-service`.
- Named Pipe server logic is present and uses tightened SDDL security.
- WFP session initialization uses `FWPM_SESSION_FLAG_DYNAMIC`.
- `nono setup --register-wfp-service` is available in the CLI.
</success_criteria>

<output>
After completion, create `.planning/phases/03-network-sandboxing-wfp-integration/03-01-SUMMARY.md`
</output>
