---
phase: 03-network-sandboxing-wfp-integration
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [crates/nono-cli/Cargo.toml, crates/nono-cli/src/exec_strategy_windows/restricted_token.rs, crates/nono-cli/src/exec_strategy_windows/launch.rs, crates/nono-cli/src/exec_strategy_windows/network.rs, tests/integration/test_network_wfp.sh]
autonomous: true
requirements: [NETW-01, NETW-02, NETW-03]

must_haves:
  truths:
    - "Spawned process has a restricted token containing the session SID"
    - "CLI communicates with WFP service via Named Pipe to activate filtering before process start"
    - "Network access is blocked/allowed as configured for the child process"
  artifacts:
    - path: "crates/nono-cli/src/exec_strategy_windows/restricted_token.rs"
      provides: "SID generation and restricted token creation"
    - path: "crates/nono-cli/src/exec_strategy_windows/launch.rs"
      provides: "Restricted process spawning logic"
    - path: "tests/integration/test_network_wfp.sh"
      provides: "End-to-end WFP enforcement verification"
  key_links:
    - from: "nono-cli (executor)"
      to: "\\\\.\\pipe\\nono-wfp-control"
      via: "Named Pipe client"
---

<objective>
Update `nono-cli` to use the SID-based filtering strategy: generate unique SIDs, create restricted tokens, and activate enforcement via the WFP service Named Pipe.

Purpose: Complete the network sandboxing integration for the CLI executor.
Output: `nono-cli` with kernel-level network enforcement on Windows.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-network-sandboxing-wfp-integration/03-02-SUMMARY.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-RESEARCH.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-CONTEXT.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-VALIDATION.md
@crates/nono-cli/src/exec_strategy_windows/launch.rs
@crates/nono-cli/src/exec_strategy_windows/network.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token Foundation (SID generation & Restricted Token creation)</name>
  <files>crates/nono-cli/Cargo.toml, crates/nono-cli/src/exec_strategy_windows/restricted_token.rs</files>
  <action>
    Add `uuid` to `crates/nono-cli/Cargo.toml`.
    Create `crates/nono-cli/src/exec_strategy_windows/restricted_token.rs` to implement:
    - Session-unique SID generation: Generate a random GUID and create a SID string (e.g., `S-1-5-117-{GUID_PARTS}`).
    - Restricted token creation: Use `OpenProcessToken` and `CreateRestrictedToken`. Use `SidsToRestrict` to add the session SID to the new token.
  </action>
  <verify>
    <automated>cargo build -p nono-cli</automated>
  </verify>
  <done>CLI can create restricted tokens with session-unique SIDs.</done>
</task>

<task type="auto">
  <name>Task 2: IPC Client Implementation</name>
  <files>crates/nono-cli/src/exec_strategy_windows/network.rs</files>
  <action>
    Implement a Named Pipe client for `\\.\pipe\nono-wfp-control` within `WfpNetworkBackend`.
    - Use `tokio::net::windows::named_pipe::ClientOptions` to connect.
    - Implement serialization/deserialization of `WfpRuntimeActivationRequest/Response`.
    - Ensure it handles connection failures gracefully with appropriate nono-diagnostics.
  </action>
  <verify>
    <automated>cargo check -p nono-cli</automated>
  </verify>
  <done>Named Pipe client is implemented and ready for communication with the WFP service.</done>
</task>

<task type="auto">
  <name>Task 3: Spawn Context Integration</name>
  <files>crates/nono-cli/src/exec_strategy_windows/launch.rs</files>
  <action>
    Update the spawn flow in `launch.rs` (e.g., `spawn_windows_child`) to:
    - Integrate the generation of the session-unique SID.
    - Integrate the creation of the restricted token using the foundation from Task 1.
    - Ensure the restricted token is correctly passed down the call stack to the process creation step.
  </action>
  <verify>
    <automated>cargo check -p nono-cli</automated>
  </verify>
  <done>Spawn context now includes session SID and restricted token assignment.</done>
</task>

<task type="auto">
  <name>Task 4: Process Execution Bridge</name>
  <files>crates/nono-cli/src/exec_strategy_windows/launch.rs</files>
  <action>
    Implement the final execution bridge in `launch.rs`:
    - Call WFP service via the IPC client (Task 2) to activate policy for the session SID *before* launching the process.
    - Use `CreateProcessAsUserW` with the restricted token to launch the child process in a suspended state.
    - Attach the suspended process to the Job Object.
    - Resume the process thread to start execution under sandboxed conditions.
  </action>
  <verify>
    <automated>cargo build -p nono-cli</automated>
  </verify>
  <done>CLI executor successfully bridges IPC activation and restricted process execution.</done>
</task>

<task type="auto">
  <name>Task 5: Network Integration Test</name>
  <files>tests/integration/test_network_wfp.sh</files>
  <action>
    Create an integration test script that:
    1. Ensures `nono-wfp-service` is registered and running.
    2. Runs `nono run --net-block` with the `test-connector` probe that attempts to connect to a remote IP.
    3. Verifies that the connection fails (probe exits with 0 because it expected failure).
    4. Runs `nono run --net-block --net-allow 80` with the `test-connector` probe that connects to port 80.
    5. Verifies that the connection succeeds.
  </action>
  <verify>
    <automated>bash ./tests/integration/test_network_wfp.sh</automated>
  </verify>
  <done>End-to-end network enforcement is verified on Windows.</done>
</task>

</tasks>

<verification>
Check end-to-end integration test passes on a Windows machine with WFP service running.
Reference: @.planning/phases/03-network-sandboxing-wfp-integration/03-VALIDATION.md
</verification>

<success_criteria>
- `nono-cli` correctly uses restricted tokens for network sandboxing.
- SID-based WFP rules are applied before process start.
- WFP service correctly handles requests over Named Pipe.
</success_criteria>

<output>
After completion, create `.planning/phases/03-network-sandboxing-wfp-integration/03-03-SUMMARY.md`
</output>
