---
phase: 03-network-sandboxing-wfp-integration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [crates/nono-cli/src/bin/nono-wfp-service.rs]
autonomous: true
requirements: [NETW-01, NETW-03]

must_haves:
  truths:
    - "Service converts SID string to Security Descriptor correctly"
    - "Service adds BLOCK filter with ALE_USER_ID condition for given SID"
    - "Service adds PERMIT filters with ALE_USER_ID and PORT conditions"
  artifacts:
    - path: "crates/nono-cli/src/bin/nono-wfp-service.rs"
      provides: "SID-based WFP filtering implementation"
  key_links:
    - from: "crates/nono-cli/src/bin/nono-wfp-service.rs"
      to: "Windows Filtering Platform ALE layers"
      via: "FWPM_CONDITION_ALE_USER_ID"
---

<objective>
Implement the core SID-based WFP filtering logic (BLOCK and PERMIT) within the `nono-wfp-service`.

Purpose: Enable process-specific network enforcement based on session SIDs.
Output: WFP service with functional filtering logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-network-sandboxing-wfp-integration/03-01-SUMMARY.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-RESEARCH.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-CONTEXT.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SID to Security Descriptor Conversion</name>
  <files>crates/nono-cli/src/bin/nono-wfp-service.rs</files>
  <action>
    Implement a helper function to convert a session SID string to a binary Security Descriptor (SD) using `ConvertStringSidToSidW` and `ConvertStringSecurityDescriptorToSecurityDescriptorW`.
    The SD should use the SDDL format `D:(A;;CC;;;{SID})` to allow "Create Child" access to the unique SID, which ALE uses for matching restricted tokens.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --bin nono-wfp-service</automated>
  </verify>
  <done>Service can convert SID strings to Security Descriptors for WFP.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SID-based BLOCK and PERMIT Filters</name>
  <files>crates/nono-cli/src/bin/nono-wfp-service.rs</files>
  <action>
    Update `add_policy_filter` to use `FWPM_CONDITION_ALE_USER_ID` with the Security Descriptor derived from the session SID.
    - Block filters (for `network_mode: "blocked"` or fallback) should have weight 0.
    - Permit filters (for specific ports) should have weight 100 within the same `nono` sublayer.
    - Ensure both V4 and V6 ALE layers (`FWPM_LAYER_ALE_AUTH_CONNECT_V4/V6`) are targeted.
  </action>
  <verify>
    <automated>cargo check -p nono-cli --bin nono-wfp-service</automated>
  </verify>
  <done>Service implements SID-based WFP filters.</done>
</task>

<task type="auto">
  <name>Task 3: Update Request Handling for SID filtering</name>
  <files>crates/nono-cli/src/bin/nono-wfp-service.rs</files>
  <action>
    Update the IPC request handler to extract the `session_sid` from the request.
    If `session_sid` is present, use the new SID-based filtering logic instead of the previous `ALE_APP_ID` (path-based) logic.
    Ensure `deactivate` requests correctly remove filters associated with that SID if necessary (though dynamic sessions will handle cleanup on exit).
  </action>
  <verify>
    <automated>cargo check -p nono-cli --bin nono-wfp-service</automated>
  </verify>
  <done>Service handles SID-based activation requests.</done>
</task>

</tasks>

<verification>
Check compilation of SID-based filter logic and unit tests for SID conversion.
Reference: @.planning/phases/03-network-sandboxing-wfp-integration/03-VALIDATION.md
</verification>

<success_criteria>
- `nono-wfp-service` implements `ALE_USER_ID` based filtering.
- Security Descriptor generation from SID strings works.
- Sublayer weighting follows the 0/100 block/permit pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/03-network-sandboxing-wfp-integration/03-02-SUMMARY.md`
</output>
