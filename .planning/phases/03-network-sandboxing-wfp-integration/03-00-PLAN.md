---
phase: 03-network-sandboxing-wfp-integration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [crates/nono-cli/src/bin/test-connector.rs]
autonomous: true
requirements: [NETW-01, NETW-03]

must_haves:
  truths:
    - "Probe utility can attempt TCP connections to arbitrary IP:port"
    - "Probe utility returns 0 on success and non-zero on failure"
  artifacts:
    - path: "crates/nono-cli/src/bin/test-connector.rs"
      provides: "Universal network probe utility for integration testing"
  key_links:
    - from: "tests/integration/test_network_wfp.sh"
      to: "crates/nono-cli/src/bin/test-connector.rs"
      via: "command-line execution"
---

<objective>
Implement the `test-connector` network probe utility to facilitate automated testing of WFP filtering rules.

Purpose: Provide a reliable, scriptable way to verify network blocking and port-level permissions in integration tests.
Output: A new Rust binary `test-connector` in the `nono-cli` crate.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-RESEARCH.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement test-connector utility</name>
  <files>crates/nono-cli/src/bin/test-connector.rs</files>
  <action>
    Create a new Rust binary `crates/nono-cli/src/bin/test-connector.rs` that:
    - Accepts `--host <IP>` and `--port <PORT>` arguments.
    - Accepts an optional `--should-fail` flag.
    - Attempts a TCP connection to the specified host:port with a 2-second timeout.
    - Exits with 0 if connection matches expectation (success without `--should-fail`, or failure with `--should-fail`).
    - Exits with 1 if connection does not match expectation.
    - Prints clear error messages to stderr on failure.
  </action>
  <verify>
    <automated>cargo build -p nono-cli --bin test-connector</automated>
  </verify>
  <done>Binary compiles and provides the expected CLI interface for probing.</done>
</task>

</tasks>

<verification>
Verify binary compilation and basic CLI argument parsing.
Reference: @.planning/phases/03-network-sandboxing-wfp-integration/03-VALIDATION.md
</verification>

<success_criteria>
- `test-connector` binary exists in `crates/nono-cli/src/bin/`.
- `cargo build --bin test-connector` succeeds.
- Binary returns 0 when connecting to a listening local port.
</success_criteria>

<output>
After completion, create `.planning/phases/03-network-sandboxing-wfp-integration/03-00-SUMMARY.md`
</output>
