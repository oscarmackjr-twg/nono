---
phase: 27.2-audit-attestation-test-re-enablement
plan: 03
subsystem: documentation
tags: [adr, audit-bundle-target, deferred-items, v2.5-followups, non-repudiation, cmd-verify]

# Dependency graph
requires:
  - phase: 27.1-nono-test-home-seam
    provides: NONO_TEST_HOME seam, audit/rollback root layout, deferred-items.md style invariants, Phase 27.1 D-27.1-14 large-fix branch surfaced gaps
  - phase: 25-cross-platform-resl-and-aipc-unix-design
    provides: docs/architecture ADR convention (aipc-unix-futures.md heading structure)
  - phase: 22-05a-audit-attestation
    provides: dual-location bundle layout this ADR supersedes (Decision 5 "for backward compatibility")
provides:
  - docs/architecture/audit-bundle-target.md ADR (Option A bundle-target choice; Plan 22-05a Decision 5 superseded)
  - v2.5-FU-1 (cmd_verify shim removal) entry in deferred-items.md
  - v2.5-FU-2 (cmd_verify v2 JSON schema) entry in deferred-items.md
  - Backward-compat shim contract (warning text + removal milestone) locked for Phase 27.2 Plan 02 implementation half
affects: [27.2-01, 27.2-02, 27.2-04, v2.5 planning]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "ADR-per-architectural-decision convention preserved (Phase 25-02 precedent): top-level docs/architecture/<topic>.md with Status frontmatter + Context + Decision Table + Decision + Consequences + References"
    - "Status frontmatter with Supersedes field (5-key shape) for ADRs that retire a prior decision"
    - "Backward-compat shim contract subsection pattern (Trigger / Action / Warning text / Removal milestone) for future deprecation ADRs"

key-files:
  created:
    - docs/architecture/audit-bundle-target.md
  modified:
    - .planning/phases/27.1-nono-test-home-seam/deferred-items.md

key-decisions:
  - "ADR file location locked at docs/architecture/audit-bundle-target.md (mirrors Phase 25-02 convention per D-27.2-03)"
  - "ADR explicitly supersedes Plan 22-05a Decision 5's dual-location rationale (recorded in Status frontmatter Supersedes field)"
  - "Backward-compat shim warning text locked verbatim in ADR Backward-compat shim contract subsection"
  - "v2.5-FU-1 + v2.5-FU-2 cross-linked from ADR (forcing function for v2.5 planning to find them)"

patterns-established:
  - "ADR Supersedes field: when an ADR retires a prior decision, add a 5th frontmatter key Supersedes: <ref> alongside Status/Date/Phase/Requirement"
  - "Cross-linking ADRs to deferred-items.md: ADR Backward-compat shim contract subsection references the v2.5-FU-N entry in deferred-items.md and vice versa, so the removal milestone is discoverable from either side"

requirements-completed: [REQ-AAHX-02]

# Metrics
duration: ~10 min
completed: 2026-05-09
---

# Phase 27.2 Plan 03: Audit-Bundle-Target ADR + v2.5 Follow-Ups Summary

**Authored docs/architecture/audit-bundle-target.md ADR recording Option A bundle-target choice (supersedes Plan 22-05a Decision 5) and appended v2.5-FU-1 (shim removal) + v2.5-FU-2 (cmd_verify v2 JSON schema) entries to deferred-items.md.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-09 (worktree spawn)
- **Completed:** 2026-05-09
- **Tasks:** 3
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments

- ADR `docs/architecture/audit-bundle-target.md` (93 lines) authored with full heading hierarchy mirroring `docs/architecture/aipc-unix-futures.md`: Status frontmatter (5 fields including Supersedes), Context (with Goals + Non-goals subsections), Decision Table (option matrix A/B/C with verdicts), Decision, Consequences (Positive + Negative + Backward-compat shim contract), References (Internal + Source code + Related ADRs).
- ADR explicitly supersedes Plan 22-05a Decision 5's "for backward compatibility" dual-location rationale (Supersedes field + Context paragraph 1 + Decision Table footer).
- Backward-compat shim contract locked: trigger condition, action, exact `tracing::warn!` text, and v2.5-FU-1 removal milestone all named in the ADR.
- `.planning/phases/27.1-nono-test-home-seam/deferred-items.md` extended with new H2 § "Phase 27.2 v2.5 production follow-ups (audit-verify surface coordinated bump)" containing v2.5-FU-1 (shim removal) + v2.5-FU-2 (cmd_verify v2 JSON schema) entries. Existing § "Phase 27.1 Plan 03 v2.4 production follow-ups" untouched.
- D-27.2-11 invariant trivially satisfied: `crates/nono/` byte-identical (no source code touched).

## Task Commits

Each task was completed; the plan committed atomically per Task 3's instruction (single coherent docs commit):

1. **Task 1: Author the audit-bundle-target.md ADR** — content authored (verified via heading-hierarchy grep + acceptance-criteria greps).
2. **Task 2: Append v2.5-FU-1 + v2.5-FU-2 entries to deferred-items.md** — content appended (verified via H2/H3 count greps + xref grep).
3. **Task 3: Verify D-27.2-11 invariant + commit ADR + deferred-items append** — `d52b8a0d` (docs(27.2-03): add audit-bundle-target ADR + v2.5 follow-up entries).

**Single commit hash:** `d52b8a0d` (docs).

_Note: Plan 27.2-03's Task 3 is explicitly a "verification + commit only" task. Tasks 1 and 2 produce file-system state that Task 3 commits in one coherent docs commit per the plan's instruction "since they're both part of the FU-2 documentation half"._

## Files Created/Modified

- `docs/architecture/audit-bundle-target.md` (created, 93 lines) — new ADR; Option A bundle-target choice; supersedes Plan 22-05a Decision 5; documents v2.5 shim-removal milestone; mirrors Phase 25-02 ADR convention.
- `.planning/phases/27.1-nono-test-home-seam/deferred-items.md` (modified, +21 lines appended) — new H2 § "Phase 27.2 v2.5 production follow-ups" with v2.5-FU-1 + v2.5-FU-2 H3 entries.

## Decisions Made

None beyond what the plan specified. The ADR content is planner-authored verbatim (per the plan's "Use the EXACT content below" instruction in Task 1's action step) and the deferred-items append is also planner-authored verbatim. Executor's role is to faithfully render the locked text and verify the structural acceptance criteria.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed in-body reference that triggered double-counting on the `### Backward-compat shim contract` acceptance grep**
- **Found during:** Task 1 verification (acceptance criterion `grep -c '### Backward-compat shim contract' docs/architecture/audit-bundle-target.md` returned exactly 1).
- **Issue:** The planner-authored ADR body in Task 1's action step contained a backref `(see \`### Backward-compat shim contract\` below; ...)` in the `## Decision` section. Combined with the actual `### Backward-compat shim contract` heading later in the file, the unanchored grep returned 2 instead of the required exactly 1.
- **Fix:** Replaced the backref text in the `## Decision` section from the literal heading reference to the prose `"see the shim contract subsection below"` so the grep counts only the H3 heading itself. The link from `## Decision` to the contract subsection is preserved (just rephrased).
- **Files modified:** `docs/architecture/audit-bundle-target.md`
- **Verification:** `grep -c '### Backward-compat shim contract' docs/architecture/audit-bundle-target.md` returns 1 (was 2 before edit).
- **Committed in:** `d52b8a0d` (single docs commit per Task 3).

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug — verbatim-text discrepancy with plan's acceptance grep).
**Impact on plan:** No scope change. The ADR's prose intent is preserved; only the wording of one cross-reference was adjusted to satisfy the unanchored acceptance grep `grep -c '### Backward-compat shim contract'`. Heading hierarchy, structural shape, and all substantive content (option matrix, supersession note, v2.5 milestone, shim contract) are unchanged.

## Issues Encountered

None during planned work.

## User Setup Required

None — pure documentation plan; no external service configuration required.

## Next Phase Readiness

- REQ-AAHX-02's documentation half is closed by this plan (Plan 27.2-03). The implementation half is owned by Plan 27.2-02 (Wave 1, parallel — production code change in `crates/nono-cli/src/rollback_runtime.rs` to redirect bundle target to `<audit_root>/<id>/`).
- The ADR's Backward-compat shim contract subsection is the locked specification for Plan 27.2-01 and 27.2-02 to implement against (warning text, removal milestone v2.5-FU-1, audit-first lookup with rollback-root fallback).
- v2.5 milestone scope-lock has v2.5-FU-1 + v2.5-FU-2 surfaced in deferred-items.md and cross-linked from the ADR — discoverable from either side when v2.5 planning starts.
- No blockers. Wave 1 plans 27.2-01, 27.2-02, 27.2-03 are file-disjoint by design (this plan touches docs/architecture/ + .planning/ only; the others touch crates/nono-cli/src/).

## Self-Check: PASSED

Verified after writing SUMMARY.md:

- `docs/architecture/audit-bundle-target.md` exists (`test -f` returned 0; 93 lines; all 14 expected H1/H2/H3 headings present in correct order).
- `.planning/phases/27.1-nono-test-home-seam/deferred-items.md` modified (new H2 + 2 new H3 entries; existing v2.4 section untouched).
- Commit `d52b8a0d` exists in `git log` with conventional-commit subject `docs(27.2-03):`, DCO sign-off present, exactly 2 files changed, `crates/nono/` diff empty (D-27.2-11 invariant intact).

---
*Phase: 27.2-audit-attestation-test-re-enablement*
*Plan: 03*
*Completed: 2026-05-09*
