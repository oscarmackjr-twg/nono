---
phase: 15-detached-console-conpty-investigation
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - .planning/phases/13-v1-human-verification-uat/13-UAT.md
  - .planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md
  - .planning/phases/07-quick-wins/07-VERIFICATION.md
  - .planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md
  - .planning/phases/14-v1-fix-pass/14-01-SUMMARY.md
  - .planning/debug/windows-supervised-exec-cascade.md    # move to resolved/
  - .planning/debug/resolved/windows-supervised-exec-cascade.md   # destination
  - CHANGELOG.md
autonomous: true
requirements: []    # No new requirements — closes v2.0-known-issue carry-forward
tags: [windows, bookkeeping, uat-promotion, changelog]

must_haves:
  truths:
    - "P05-HV-1, P07-HV-3, P11-HV-1, P11-HV-3 in 13-UAT.md are promoted from waived to pass (or documented with a fresh gap if they fail under the new fix)"
    - "14-01-SUMMARY.md status field is updated from escalated-out-of-scope to resolved-by-phase-15-plan-02"
    - "CHANGELOG.md [Unreleased] section has a v2.1 bug-fix entry for the 0xC0000142 fix"
    - "Debug doc is moved from .planning/debug/ to .planning/debug/resolved/"
    - "make ci stays green after all doc edits (no build impact from doc-only changes, but verify)"
  artifacts:
    - path: ".planning/debug/resolved/windows-supervised-exec-cascade.md"
      provides: "Resolved debug doc in the correct directory"
      contains: "status: resolved"
    - path: ".planning/phases/13-v1-human-verification-uat/13-UAT.md"
      provides: "UAT runbook with promoted verdicts"
      contains: "pass"
    - path: "CHANGELOG.md"
      provides: "v2.1 bug-fix entry"
      contains: "0xC0000142"
  key_links:
    - from: "13-UAT.md P05-HV-1"
      to: "15-02-SUMMARY.md smoke-gate row 1"
      via: "promotion of waived verdict based on passing smoke row 1"
      pattern: "result: \\*\\*pass\\*\\*"
    - from: "14-01-SUMMARY.md frontmatter"
      to: "15-02-PLAN.md"
      via: "status field update"
      pattern: "resolved-by-phase-15-plan-02"
---

<objective>
Promote the four Phase 13 UAT items from `waived (v2.0-known-issue)` to `pass` (or file fresh gaps if they fail), update the Phase 14 post-mortem, move the debug doc to `resolved/`, and append a CHANGELOG entry. This closes Phase 15 cleanly.

Purpose: The fix shipped in Plan 15-02 is the evidence that unblocks these bookkeeping steps. Without them, the audit trail is incomplete: UAT items remain `waived` indefinitely, the debug doc sits in the active queue, and the CHANGELOG does not reflect the fix.

Output: All four UAT items have terminal `pass` verdicts, the Phase 14 post-mortem is updated, the debug doc is in `resolved/`, and CHANGELOG is updated. Phase 15 is closeable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-detached-console-conpty-investigation/15-02-SUMMARY.md
@.planning/phases/13-v1-human-verification-uat/13-UAT.md
@.planning/phases/14-v1-fix-pass/14-01-SUMMARY.md
@.planning/debug/windows-supervised-exec-cascade.md

<!-- Key content locations for edits: -->
<interfaces>
<!-- 13-UAT.md — items to promote (current verdict: waived (v1.0-known-issue)): -->
<!-- P05-HV-1 (line ~83): `result: **waived (v1.0-known-issue)**` → `result: **pass**` -->
<!-- P07-HV-3 (line ~136): `result: **waived (v1.0-known-issue)**` → `result: **pass**` -->
<!-- P11-HV-1 (line ~201): `result: **waived (v2.0-known-issue)**` → `result: **pass**` -->
<!-- P11-HV-3 (line ~XX):  `result: **waived (v2.0-known-issue)**` → `result: **pass**` -->
<!-- NOTE: P07-HV-3 is a wave-2 item that depends on P05-HV-1. Promote both together. -->

<!-- 14-01-SUMMARY.md frontmatter: -->
<!-- Line 6: `status: escalated-out-of-scope` → `status: resolved-by-phase-15-plan-02` -->

<!-- 05-VERIFICATION.md, 07-VERIFICATION.md, 11-VERIFICATION.md: -->
<!-- Each has a carry-forward note referencing Phase 15. Append a resolution addendum. -->

<!-- Debug doc move: -->
<!-- git mv .planning/debug/windows-supervised-exec-cascade.md .planning/debug/resolved/ -->
<!-- Must update any cross-references in other docs that point to the old path. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote UAT verdicts in 13-UAT.md and update VERIFICATION.md addendums</name>
  <files>
    .planning/phases/13-v1-human-verification-uat/13-UAT.md
    .planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md
    .planning/phases/07-quick-wins/07-VERIFICATION.md
    .planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md
  </files>
  <read_first>
    - `.planning/phases/13-v1-human-verification-uat/13-UAT.md` (full — to locate each waived item's exact text)
    - `.planning/phases/15-detached-console-conpty-investigation/15-02-SUMMARY.md` (smoke-gate results)
    - `.planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md` (locate carry-forward note)
    - `.planning/phases/07-quick-wins/07-VERIFICATION.md` (locate carry-forward note)
    - `.planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md` (locate carry-forward note)
  </read_first>
  <behavior>
    Before: P05-HV-1, P07-HV-3, P11-HV-1, P11-HV-3 all carry `waived (v1.0-known-issue)` or `waived (v2.0-known-issue)` verdicts. VERIFICATION.md files have "pending Phase 15" carry-forward notes.
    After: Each of the four items has `result: **pass**` with a note referencing the Phase 15-02 fix commit. VERIFICATION.md files have resolution addendums.
  </behavior>
  <action>
    1. Read `15-02-SUMMARY.md` to extract:
       - The fix direction (a or b) and the commit SHA for the production fix.
       - The smoke-gate result for row 1 (P05-HV-1 evidence) and row 2 (covers P07-HV-3, P11-HV-1, P11-HV-3 indirectly as same detached path).

    2. For each of the four UAT items in `13-UAT.md`:
       - Change `result: **waived (v1.0-known-issue)**` / `result: **waived (v2.0-known-issue)**` to `result: **pass**`.
       - Append to the `notes:` field (on a new line, indented to match existing notes):
         ```
         Promoted to pass: Phase 15-02 fixed STATUS_DLL_INIT_FAILED (0xC0000142) in
         detached console grandchildren. Fix commit: [SHA from 15-02-SUMMARY]. Smoke-gate
         row [N] confirmed on [date]. See .planning/debug/resolved/windows-supervised-exec-cascade.md.
         ```

       **Special case for P07-HV-3** (session commands round-trip): This item was waived because P05-HV-1 (its prereq) was waived. Once P05-HV-1 passes (a session can be created), P07-HV-3's commands (`nono logs`, `nono inspect`, `nono prune`) function on the session. If the user has not manually re-run P07-HV-3 commands, note this:
       ```
       Promoted to pass: Prereq P05-HV-1 now passes (Phase 15-02 fix). Session commands
       (nono logs, nono inspect, nono prune) exercised by unit + integration tests in
       session_commands*.rs. Live end-to-end re-run not separately gated — promoted based
       on P05-HV-1 smoke confirmation + test coverage. Fix commit: [SHA].
       ```

       **Special case for P11-HV-1 and P11-HV-3** (capability pipe end-to-end): These are Phase 11 items that require the supervised path (which now works in detached mode). Verify that the smoke-gate confirmed the supervised path works (row 3 non-detached passes; rows 1–2 detached passes). Promote based on that evidence. If the interactive test script from the UAT runbook was not re-run, note:
       ```
       Promoted to pass: Supervised path now functional in detached mode (Phase 15-02 fix).
       Capability pipe protocol tested by Phase 11 unit + integration tests. Interactive
       PowerShell script end-to-end re-run not required for verdict — P11 tests cover the
       protocol invariants. Fix commit: [SHA].
       ```

    3. Also update `13-UAT.md` frontmatter:
       - `updated:` → today's date (2026-04-17 per system date, but use actual current date at execution time)

    4. For each upstream VERIFICATION.md (`05-VERIFICATION.md`, `07-VERIFICATION.md`, `11-VERIFICATION.md`):
       - Locate the carry-forward note referencing Phase 15 or the v2.0-known-issue.
       - Append a resolution addendum immediately after it:
         ```markdown
         **Phase 15 resolution (2026-04-XX):** STATUS_DLL_INIT_FAILED (0xC0000142) in
         detached console grandchildren resolved by Phase 15-02 (fix commit [SHA]).
         UAT item [PXX-HV-Y] promoted to `pass` in `13-UAT.md`. Debug session archived
         at `.planning/debug/resolved/windows-supervised-exec-cascade.md`.
         ```

    5. Commit all four files together:
       ```
       git add .planning/phases/13-v1-human-verification-uat/13-UAT.md \
               .planning/phases/05-windows-detach-readiness-fix/05-VERIFICATION.md \
               .planning/phases/07-quick-wins/07-VERIFICATION.md \
               .planning/phases/11-runtime-capability-expansion/11-VERIFICATION.md
       git commit -m "docs(13/15): promote P05-HV-1, P07-HV-3, P11-HV-1, P11-HV-3 to pass

       Phase 15-02 fixed STATUS_DLL_INIT_FAILED (0xC0000142) for console grandchildren
       in detached-supervisor mode. The four v2.0-known-issue carries from 13-UAT.md are
       promoted to pass based on the Phase 15 smoke-gate confirmation.

       Fix commit: [SHA from 15-02-SUMMARY].

       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>"
       ```
  </action>
  <verify>
    <automated>grep -n "waived.*known-issue" .planning/phases/13-v1-human-verification-uat/13-UAT.md</automated>
    Expected: zero matches for P05-HV-1, P07-HV-3, P11-HV-1, P11-HV-3 (they are now `pass`). Other waived items (P09-HV-1 `no-test-fixture`, pre-existing waivers) are unaffected.
  </verify>
  <done>
    - All four UAT items show `result: **pass**` with fix-reference notes.
    - Three VERIFICATION.md files have resolution addendums.
    - Commit is on `windows-squash` with DCO sign-off, staging only the four listed files.
    - `grep -n "waived.*known-issue" 13-UAT.md` returns zero lines for the four items.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update 14-01-SUMMARY.md and move debug doc to resolved/</name>
  <files>
    .planning/phases/14-v1-fix-pass/14-01-SUMMARY.md
    .planning/debug/windows-supervised-exec-cascade.md          (moved FROM here)
    .planning/debug/resolved/windows-supervised-exec-cascade.md (moved TO here)
    CHANGELOG.md
  </files>
  <read_first>
    - `.planning/phases/14-v1-fix-pass/14-01-SUMMARY.md` (frontmatter and § Follow-up section)
    - `CHANGELOG.md` (locate `[Unreleased]` section and any v2.0 Known Issues block)
    - `.planning/phases/15-detached-console-conpty-investigation/15-02-SUMMARY.md` (fix details for CHANGELOG)
  </read_first>
  <behavior>
    Before: `14-01-SUMMARY.md` has `status: escalated-out-of-scope`. Debug doc is in `.planning/debug/`. CHANGELOG has a Known Issues entry for `0xC0000142`.
    After: `14-01-SUMMARY.md` has `status: resolved-by-phase-15-plan-02`. Debug doc is in `.planning/debug/resolved/`. CHANGELOG `[Unreleased]` has a Bug Fixes entry.
  </behavior>
  <action>
    1. **Update `14-01-SUMMARY.md` frontmatter:**
       Change line 5: `status: escalated-out-of-scope` → `status: resolved-by-phase-15-plan-02`

       Also append to the `## Follow-up` section (or replace it with a `## Resolution` section):
       ```markdown
       ## Resolution

       Resolved by Phase 15 plan 15-02 (commit [SHA from 15-02-SUMMARY]).
       Direction [a/b] was implemented. See `.planning/debug/resolved/windows-supervised-exec-cascade.md`
       for the full investigation trail and root-cause pin.

       Phase 14 roadmap success criterion #1 (detached console-child initializes) is now MET.
       Phase 13 UAT items P05-HV-1, P07-HV-3, P11-HV-1, P11-HV-3 promoted to `pass`
       in `13-UAT.md` (Phase 15-03 plan commit [commit SHA from Task 1]).
       ```

    2. **Move the debug doc:**
       ```bash
       mkdir -p .planning/debug/resolved
       git mv .planning/debug/windows-supervised-exec-cascade.md \
              .planning/debug/resolved/windows-supervised-exec-cascade.md
       ```
       After the move, update the `status:` field in the doc's YAML frontmatter from `partially-resolved` to `resolved` and update `updated:` to today's date.

       Search for cross-references to the old path in other planning docs and update them:
       ```bash
       grep -r "debug/windows-supervised-exec-cascade" .planning/
       ```
       For each file found, update the path from `.planning/debug/windows-supervised-exec-cascade.md` to `.planning/debug/resolved/windows-supervised-exec-cascade.md`.

    3. **Update CHANGELOG.md:**
       Locate the `[Unreleased]` section. If a `## Known Issues` or `## v2.0 Known Issues` block lists `STATUS_DLL_INIT_FAILED` or `0xC0000142`, move that entry from Known Issues to a `### Bug Fixes` subsection under `[Unreleased]`:

       ```markdown
       ### Bug Fixes

       - **Windows:** Fixed `STATUS_DLL_INIT_FAILED (0xC0000142)` for console-application
         grandchildren spawned in detached-supervisor mode (`nono run --detached`). Root cause:
         `PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE` combined with `DETACHED_PROCESS` prevents DLL
         loader initialization in console children. Fix: [direction-a: PTY allocation is now
         skipped for Windows detached supervisors, preserving the WRITE_RESTRICTED session-SID
         token and WFP identity / direction-b: PTY and restricted token are both suppressed on
         the detached Windows path; AppID-based WFP filtering is the kernel-level network
         boundary]. Affected UAT items P05-HV-1, P07-HV-3, P11-HV-1, P11-HV-3 promoted to pass.
         (Phase 15-02, commit [SHA])
       ```

       If no Known Issues block exists referencing this bug, add the `### Bug Fixes` entry to `[Unreleased]` without removing anything.

    4. Commit all files together:
       ```
       git add .planning/phases/14-v1-fix-pass/14-01-SUMMARY.md \
               .planning/debug/resolved/windows-supervised-exec-cascade.md \
               CHANGELOG.md
       git rm --cached .planning/debug/windows-supervised-exec-cascade.md 2>/dev/null || true
       git commit -m "docs(15-03): close Phase 15 — update 14-01 status, resolve debug doc, update CHANGELOG

       - 14-01-SUMMARY status: escalated-out-of-scope → resolved-by-phase-15-plan-02
       - Debug doc moved to .planning/debug/resolved/ (status: resolved)
       - CHANGELOG [Unreleased]: 0xC0000142 moved from Known Issues to Bug Fixes

       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>"
       ```

       Note: because `git mv` is used, git tracks this as a rename. The `git rm --cached` guard is a no-op safety for the old path if the rename was done outside git.
  </action>
  <verify>
    <automated>
      git log --oneline -2
      test -f .planning/debug/resolved/windows-supervised-exec-cascade.md && echo "MOVED OK"
      test ! -f .planning/debug/windows-supervised-exec-cascade.md && echo "OLD GONE"
      grep -n "resolved-by-phase-15-plan-02" .planning/phases/14-v1-fix-pass/14-01-SUMMARY.md
    </automated>
    Expected: MOVED OK, OLD GONE, status line found.
  </verify>
  <done>
    - `14-01-SUMMARY.md` frontmatter `status:` is `resolved-by-phase-15-plan-02`.
    - Debug doc is at `.planning/debug/resolved/windows-supervised-exec-cascade.md` with `status: resolved`.
    - No file at `.planning/debug/windows-supervised-exec-cascade.md`.
    - CHANGELOG has a Bug Fixes entry for `0xC0000142` under `[Unreleased]`.
    - All cross-references to the old debug doc path updated.
    - Commit has DCO sign-off; ONLY the listed files are staged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final ci gate and phase SUMMARY</name>
  <files>
    .planning/phases/15-detached-console-conpty-investigation/15-03-SUMMARY.md
  </files>
  <read_first>
    - `.planning/phases/15-detached-console-conpty-investigation/15-02-SUMMARY.md`
    - Current git log (last 5 commits)
  </read_first>
  <behavior>
    Before: Phase 15 plan 15-03 is not summarized.
    After: `cargo test --workspace` passes, `cargo clippy -D warnings` passes, and the SUMMARY is written confirming Phase 15 closure.
  </behavior>
  <action>
    1. Run the ci gate:
       ```bash
       cargo test --workspace --all-features
       cargo clippy --workspace --all-targets --all-features -- -D warnings -D clippy::unwrap_used
       cargo fmt --all -- --check
       ```

       If any check fails: diagnose and fix before writing the SUMMARY. The planning doc edits in Tasks 1 and 2 are doc-only and should not affect compilation; if they do, that indicates a stale reference in Rust source (unlikely but possible if a doc comment references a file path).

    2. Write `15-03-SUMMARY.md` with:
       - Overall Phase 15 outcome: direction chosen, fix commit SHA, smoke-gate results table.
       - UAT promotion: which four items were promoted to pass.
       - Phase 14 carry-forward closure: `14-01-SUMMARY.md` updated.
       - Debug doc resolution: moved to `resolved/`.
       - CHANGELOG entry: confirmed present.
       - CI gate: all green (or any pre-existing failures from STATE.md noted).
       - Known remaining gaps: none expected; if any row 4 (WFP) was SKIP, note the user setup requirement.

    3. Commit the SUMMARY:
       ```
       git add .planning/phases/15-detached-console-conpty-investigation/15-03-SUMMARY.md
       git commit -m "docs(15-03): write phase 15 closure summary

       Phase 15 complete. 0xC0000142 detached-console-grandchild bug resolved.
       UAT items P05-HV-1, P07-HV-3, P11-HV-1, P11-HV-3 promoted to pass.
       Phase 14 v2.0 known-issue carry-forward closed.

       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>"
       ```
  </action>
  <verify>
    <automated>
      cargo test --workspace --all-features 2>&1 | tail -5
      cargo clippy --workspace --all-targets --all-features -- -D warnings -D clippy::unwrap_used 2>&1 | tail -5
      cargo fmt --all -- --check 2>&1
      test -f .planning/phases/15-detached-console-conpty-investigation/15-03-SUMMARY.md && echo "SUMMARY OK"
    </automated>
  </verify>
  <done>
    - `cargo test --workspace` passes (or pre-existing failures match STATE.md baseline).
    - `cargo clippy -D warnings -D clippy::unwrap_used` passes with zero new warnings.
    - `cargo fmt --check` passes.
    - `15-03-SUMMARY.md` exists and contains the full closure record.
    - All Phase 15 commits are on `windows-squash` with DCO sign-off.
    - No pre-existing WIP files (staging constraint) were swept into any commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → codebase | Doc edits in this plan are pure documentation; no code boundary changes |

## STRIDE Threat Register

Documentation-only plan — no production code changes.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | UAT verdict promotion without re-running the tests | accept | Promotion is evidence-based: Phase 15-02 smoke-gate rows 1–2 directly exercise the same scenarios as P05-HV-1 and P07-HV-3. P11-HV-1/HV-3 rely on the supervised path which is validated by the non-detached row 3 + existing Phase 11 unit tests. Explicit rationale recorded in each item's `notes:` field. |
| T-15-03-02 | Repudiation | Debug doc moved; old path links break | mitigate | Task 2 action includes a `grep -r` scan to update all cross-references before commit. |
</threat_model>

<verification>
```bash
# CI gate (Task 3):
cargo test --workspace --all-features
cargo clippy --workspace --all-targets --all-features -- -D warnings -D clippy::unwrap_used
cargo fmt --all -- --check

# UAT promotion check (Task 1):
grep -n "waived.*known-issue" .planning/phases/13-v1-human-verification-uat/13-UAT.md
# Expected: zero matches for P05-HV-1, P07-HV-3, P11-HV-1, P11-HV-3

# Debug doc move check (Task 2):
test -f .planning/debug/resolved/windows-supervised-exec-cascade.md && echo "MOVED OK"
test ! -f .planning/debug/windows-supervised-exec-cascade.md && echo "OLD GONE"

# 14-01 status check (Task 2):
grep "resolved-by-phase-15-plan-02" .planning/phases/14-v1-fix-pass/14-01-SUMMARY.md

# CHANGELOG check (Task 2):
grep "0xC0000142" CHANGELOG.md
# Expected: in Bug Fixes section, not in Known Issues

# Commit log:
git log --oneline -5
```
</verification>

<success_criteria>
Maps to ROADMAP.md Phase 15 success criteria:

1. **(SC-3 continued)** All four Phase 13 UAT items (P05-HV-1, P07-HV-3, P11-HV-1, P11-HV-3) are promoted to `pass` in `13-UAT.md`.
2. **(SC-4)** `make ci` stays green — `cargo clippy -D warnings`, `cargo fmt --check`, `cargo test --workspace` all pass.
3. **(SC-5)** Debug doc moved to `.planning/debug/resolved/` with `status: resolved`. `14-01-SUMMARY.md` status updated. CHANGELOG updated.
4. Phase 15 SUMMARY (`15-03-SUMMARY.md`) exists with complete closure record.
5. All commits on `windows-squash` have DCO sign-off. No pre-existing WIP files swept into any commit.
</success_criteria>

<output>
After completion, create `.planning/phases/15-detached-console-conpty-investigation/15-03-SUMMARY.md` containing:
- Phase 15 overall outcome (direction, fix commit, smoke-gate table)
- UAT promotion: four items, their new verdicts, and the evidence basis for each
- CI gate results
- Planning artifacts updated: 14-01 SUMMARY, debug doc (resolved/), 13-UAT.md, VERIFICATION.md addendums, CHANGELOG
- Status: complete (Phase 15 closed)
</output>
