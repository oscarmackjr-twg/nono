---
phase: 18.1-extended-ipc-gaps
plan: 03
subsystem: ipc
tags: [windows, supervisor, ipc, aipc, profile, widening, dispatcher, plumbing, g-06, gap-closure]

# Dependency graph
requires:
  - phase: 18-extended-ipc
    provides: Profile::resolve_aipc_allowlist already-implemented returning AipcResolvedAllowlist (hard-coded D-05 defaults ∪ profile widening per capabilities.aipc); WindowsSupervisorRuntime with resolved_aipc_allowlist seeded by AipcResolvedAllowlist::default() pending the threading plan (Plan 18-03 Deferred Issue #1)
  - phase: 18.1-extended-ipc-gaps
    plan: 01
    provides: CONIN$ prompt routing fix (Wave 1 — disjoint from this plan)
  - phase: 18.1-extended-ipc-gaps
    plan: 02
    provides: G-04 dispatcher flip Err→Denied at handle_windows_supervisor_message flow-control boundary — the tuple-construction pattern that this plan's test 2 asserts on (`"broker failed: ... not in resolved allowlist ..."` wrapped reason)
provides:
  - SupervisorConfig<'a> (Windows branch) now carries `aipc_allowlist: AipcResolvedAllowlist` as a public field, UNIONed from hard-coded defaults with the loaded profile's capabilities.aipc widening
  - WindowsSupervisorRuntime::initialize reads supervisor.aipc_allowlist.clone() into self.resolved_aipc_allowlist — replaces Plan 18-03's AipcResolvedAllowlist::default() seed. Plan 18-03 Deferred Issue #1 resolved.
  - SupervisedRuntimeContext<'a> gains `loaded_profile: Option<&'a Profile>` plumbed end-to-end from sandbox_prepare::PreparedSandbox → launch_runtime::LaunchPlan → execution_runtime::execute_sandboxed → supervised_runtime::execute_supervised_runtime
  - execute_supervised_runtime resolves `profile.resolve_aipc_allowlist()?` (or `AipcResolvedAllowlist::default()` when no profile loaded) and writes the result into the Windows SupervisorConfig literal
  - 3 new end-to-end profile-widening integration tests in capability_handler_tests (Pipe ReadWrite widening reaches backend; default allowlist rejects post-prompt; Socket Bind widening reaches backend)
affects: [phase-18.1-04]

# Tech tracking
tech-stack:
  added: []  # No new crate deps; wiring + new field + test additions
  patterns:
    - "Cross-file struct plumbing: a single owned `Option<Profile>` travels through PreparedSandbox → LaunchPlan → execute_sandboxed's local destructured binding (lifetime extends through execute_supervised_runtime which does not return until child exits); SupervisedRuntimeContext carries only a borrow (&'a Profile) because the owned value lives in the caller's stack frame for the whole run"
    - "`.as_ref().map(|p| p.env_credentials.mappings.clone())` pattern to extract a subfield without moving the outer Option<Profile>; preserves owned profile for later downstream use (replaces `.map(|profile| profile.env_credentials.mappings)` which consumed loaded_profile)"
    - "Non-Windows `let _ = &aipc_allowlist;` silencer keeps the cross-platform `execute_supervised_runtime` call site unified (no `#[cfg]` split at the SupervisedRuntimeContext construction site in execution_runtime.rs) while Windows is the only consumer"
    - "Test-profile JSON shape `{\"meta\":{\"name\":\"...\"},\"capabilities\":{\"aipc\":{...}}}` — all Profile fields use `#[serde(default)]` so minimal JSON parses cleanly; documented for future widening-test authors"

key-files:
  created:
    - .planning/phases/18.1-extended-ipc-gaps/18.1-03-SUMMARY.md
  modified:
    - crates/nono-cli/src/exec_strategy_windows/mod.rs
    - crates/nono-cli/src/exec_strategy_windows/supervisor.rs
    - crates/nono-cli/src/supervised_runtime.rs
    - crates/nono-cli/src/execution_runtime.rs
    - crates/nono-cli/src/sandbox_prepare.rs
    - crates/nono-cli/src/launch_runtime.rs
    - crates/nono-cli/src/command_runtime.rs
    - crates/nono-cli/src/main.rs

key-decisions:
  - "Added `aipc_allowlist: AipcResolvedAllowlist` field to SupervisorConfig<'a> (Windows branch in exec_strategy_windows/mod.rs). No `'a` lifetime needed — AipcResolvedAllowlist: Clone; caller passes a Clone-constructed owned value."
  - "WindowsSupervisorRuntime::initialize at line 372 reads `supervisor.aipc_allowlist.clone()` into `std::sync::Arc::new(...)` for the `resolved_aipc_allowlist` field. Replaced the Plan 18-03 `AipcResolvedAllowlist::default()` seed 1:1 — behavior byte-identical when no profile loaded."
  - "PreparedSandbox gains `loaded_profile: Option<Profile>` (owned). Extracted env_credentials.mappings via `.as_ref().map(|p| p.env_credentials.mappings.clone())` instead of moving out of loaded_profile — preserves the owned value for downstream consumption. Both PreparedSandbox construction sites (manifest path at line 241 + profile path at line 397) set the new field."
  - "LaunchPlan gains `loaded_profile: Option<Profile>` at launch_runtime.rs line 40 and is carried through all 3 LaunchPlan construction sites (launch_runtime.rs:262 via prepare_run_launch_plan, command_runtime.rs:121 via run_shell, command_runtime.rs:196 via run_wrap)."
  - "execute_sandboxed destructures `loaded_profile` as an owned local binding; lifetime spans the entire supervised run because execute_supervised_runtime blocks until child exits and caller immediately `std::process::exit`s after. SupervisedRuntimeContext therefore carries `Option<&'a Profile>` (borrow) safely."
  - "execute_supervised_runtime resolution: `let aipc_allowlist = match loaded_profile { Some(p) => p.resolve_aipc_allowlist()?, None => AipcResolvedAllowlist::default() };` followed by `aipc_allowlist.clone()` into the Windows SupervisorConfig literal. The `?` is belt-and-braces idempotent revalidation (unknown tokens are rejected earlier by validate_profile_aipc_tokens at profile load time per T-18.1-03-01)."
  - "Non-Windows handling: `#[cfg(not(target_os = \"windows\"))] let _ = &aipc_allowlist;` silences unused-binding warnings. The resolution call above still runs on non-Windows for its `?` validation side-effect (cheap and consistent across platforms per D-21 — the Windows-specific consumer is the field in the cfg(windows) SupervisorConfig literal)."
  - "3 end-to-end tests use `serde_json::from_str::<Profile>` with minimal JSON `{\"meta\":{\"name\":\"...\"},\"capabilities\":{\"aipc\":{...}}}` — all Profile fields use `#[serde(default)]` so this shape parses cleanly. Tests exercise the LIVE `Profile::resolve_aipc_allowlist` path, not mocked allowlist manipulation."
  - "Test assertions for widening-reached use negative substring match (`!reason.contains(\"not in resolved allowlist\")`) rather than asserting a specific outcome — the broker step may succeed or fail downstream for unrelated reasons (test-environment naming collisions, DuplicateHandle semantics), but G-06's contract is strictly about the allowlist gate being passed."

patterns-established:
  - "Deferred issue closure pattern: a future plan that threads load-bearing config through fresh plumbing should (1) add the end-state field to the consumer side first (Task 1 — makes the struct compile-complete), (2) thread the payload through the intermediate types (Task 2 — makes the wiring live), (3) add regression tests that exercise the live path end-to-end (Task 3 — locks the behavior). Each task commits standalone so a deferred-issue bisect can find the exact commit where the wire went live."
  - "`#[cfg(not(target_os = \"windows\"))] let _ = &x;` silencer as an alternative to a full `#[cfg(windows)]` split at a shared call site — preserves unified API surface while gating actual consumption. Useful when the produced value is cheap to compute and the `?` side-effect is desirable cross-platform."

requirements-completed: [AIPC-01]
closes_gaps: [G-06]

# Metrics
duration: ~50m
completed: 2026-04-21
tasks: 3
files_modified: 8
tests_added: 3
commits: 3
---

# Phase 18.1 Plan 18.1-03: G-06 Profile Widening End-to-End Wiring Summary

**Threaded the loaded `Profile`'s resolved AIPC allowlist end-to-end from `sandbox_prepare::PreparedSandbox` through `LaunchPlan` → `execute_sandboxed` → `SupervisedRuntimeContext.loaded_profile` → `execute_supervised_runtime::resolve_aipc_allowlist()?` → `SupervisorConfig.aipc_allowlist` → `WindowsSupervisorRuntime::initialize` → `self.resolved_aipc_allowlist`. Replaces the Plan 18-03 `AipcResolvedAllowlist::default()` seed with the live profile-resolved allowlist at the sole Windows supervisor construction site; resolves Plan 18-03 Deferred Issue #1. Adds 3 end-to-end regression tests that exercise the live `Profile::resolve_aipc_allowlist` path: Pipe ReadWrite widening reaches the approval backend; default allowlist rejects Pipe ReadWrite post-prompt via G-04 Denied wrapping; Socket Bind widening reaches the backend. All 31/31 capability_handler_tests PASS (28 pre-existing + 3 new); 5/5 aipc_handle_brokering_integration PASS; 751/751 nono-cli bin suite PASS; workspace clippy+fmt+build clean; D-19 byte-identical preservation verified (nono lib, terminal_approval.rs, profile/, data/ all 0-diff vs HEAD~3).**

## Performance

- **Duration:** ~50 minutes
- **Commits:** 3 (Task 1 refactor + Task 2 feat + Task 3 test)
- **Files modified:** 8 (`exec_strategy_windows/mod.rs`, `exec_strategy_windows/supervisor.rs`, `supervised_runtime.rs`, `execution_runtime.rs`, `sandbox_prepare.rs`, `launch_runtime.rs`, `command_runtime.rs`, `main.rs`)
- **Net diff:** 366 insertions / 16 deletions across 8 files
- **Tests added:** 3 new `profile_widening_*` / `default_allowlist_rejects_*` tests; 0 removed
- **Test counts:** capability_handler_tests 28 → 31 (all pass); full nono-cli bin suite 751/751; integration 5/5; profile AIPC tests 4/4

## Objective Recap

Close G-06 from the 2026-04-20 Phase 18 HUMAN-UAT re-run by threading the loaded `Profile`'s resolved AIPC allowlist through the Windows supervisor construction path end-to-end. Plan 18-03 Deferred Issue #1 seeded `WindowsSupervisorRuntime.resolved_aipc_allowlist` with `AipcResolvedAllowlist::default()` pending a future plan that threads `Profile::resolve_aipc_allowlist()` through. Plan 18.1-03 is that future plan.

The UNION semantic (hard-coded defaults ∪ profile widening) was already unit-tested in `profile::tests` (9 tests PASS per Plan 18-03 SUMMARY); the missing piece was the one-line construction-site swap plus the end-to-end verification that a `nono run --profile <widened>` invocation actually sees the widened allowlist at request-dispatch time. Plan 18.1-03 closes that gap: the wiring is plumbed through the 4 intermediate plumbing files, the 2 call-site structs carry the live profile reference, and 3 regression tests lock the behavior.

## Commits

| # | Hash      | Type     | Message |
|---|-----------|----------|---------|
| 1 | `8170df2` | refactor | `refactor(18.1-03): thread AipcResolvedAllowlist through SupervisorConfig -> WindowsSupervisorRuntime (G-06 task 1)` |
| 2 | `993cdcb` | feat     | `feat(18.1-03): thread Profile end-to-end to Windows supervisor AIPC allowlist (G-06 task 2)` |
| 3 | `d88c20d` | test     | `test(18.1-03): add end-to-end profile-widening regression tests (G-06 task 3)` |

All three commits carry `Signed-off-by: oscarmackjr-twg <oscar.mack.jr@gmail.com>` per CLAUDE.md § Coding Standards.

## Tasks Completed

### Task 1: Thread AipcResolvedAllowlist through SupervisorConfig + WindowsSupervisorRuntime (commit `8170df2`)

**Single-hop structural wiring:**

- **`crates/nono-cli/src/exec_strategy_windows/mod.rs`** — Appended new field to Windows `SupervisorConfig<'a>` struct (after `session_sid: Option<String>`):
  ```rust
  /// Phase 18 AIPC-01 (Plan 18-03 + Plan 18.1-03): per-handle-type
  /// allowlist resolved by UNIONing the hard-coded supervisor defaults
  /// (D-05) with the loaded profile's `capabilities.aipc` widening (D-06).
  pub aipc_allowlist: crate::profile::AipcResolvedAllowlist,
  ```
  No `'a` lifetime needed — `AipcResolvedAllowlist: Clone`.

- **`crates/nono-cli/src/exec_strategy_windows/supervisor.rs`** — `WindowsSupervisorRuntime::initialize` line 369-371 previously:
  ```rust
  resolved_aipc_allowlist: std::sync::Arc::new(
      crate::profile::AipcResolvedAllowlist::default(),
  ),
  ```
  Replaced with:
  ```rust
  resolved_aipc_allowlist: std::sync::Arc::new(supervisor.aipc_allowlist.clone()),
  ```
  Plus the adjacent doc comment on the `resolved_aipc_allowlist` struct field rewritten to describe the post-Plan-18.1-03 sourcing path (SupervisorConfig → Profile::resolve_aipc_allowlist).

- **`crates/nono-cli/src/supervised_runtime.rs`** — The sole Windows `SupervisorConfig { ... }` literal at line 220 gained a new field `aipc_allowlist: crate::profile::AipcResolvedAllowlist::default()` (Task 1 seed; Task 2 replaces with live resolution).

At Task 1 stopping point, behavior is byte-identical to pre-fix because all wiring flows `default()` → `default()` → same resolved state. The structural plumbing is in place so Task 2's call-site swap wires the widening live.

### Task 2: Thread Profile → SupervisedRuntimeContext → SupervisorConfig.aipc_allowlist live wiring (commit `993cdcb`)

**Cross-file plumbing chain:**

1. **`crates/nono-cli/src/sandbox_prepare.rs`**:
   - `PreparedSandbox` gains `pub(crate) loaded_profile: Option<profile::Profile>`.
   - The manifest-path `PreparedSandbox` construction at line 241 sets `loaded_profile: None` (manifest flow has no profile).
   - The profile-path `PreparedSandbox` construction at line 397 sets `loaded_profile`.
   - **Critical non-trivial change:** the `profile_secrets` extraction at line 385 was:
     ```rust
     let profile_secrets = loaded_profile
         .map(|profile| profile.env_credentials.mappings)
         .unwrap_or_default();
     ```
     This MOVED out of `loaded_profile`. Rewritten to borrow-then-clone:
     ```rust
     let profile_secrets = loaded_profile
         .as_ref()
         .map(|profile| profile.env_credentials.mappings.clone())
         .unwrap_or_default();
     ```
     So `loaded_profile` stays owned and can be propagated downstream into PreparedSandbox.

2. **`crates/nono-cli/src/launch_runtime.rs`**:
   - `LaunchPlan` gains `pub(crate) loaded_profile: Option<profile::Profile>`. Cross-platform field (D-21: unread on non-Windows; kept non-cfg-gated to avoid conditional struct-literal complexity).
   - `prepare_run_launch_plan` at line 260 sets `loaded_profile: prepared.loaded_profile`.

3. **`crates/nono-cli/src/command_runtime.rs`** (2 LaunchPlan construction sites):
   - `run_shell` (line 121) sets `loaded_profile: prepared.loaded_profile` — `nono shell` accepts `--profile`.
   - `run_wrap` (line 196) sets `loaded_profile: prepared.loaded_profile` — Direct-strategy flow does not consume it but struct-literal completeness requires the field.

4. **`crates/nono-cli/src/execution_runtime.rs`**:
   - `execute_sandboxed`'s `LaunchPlan` destructure at line 92-105 binds `loaded_profile` as a local owned variable (lifetime spans the whole supervised run).
   - At line 369, the `SupervisedRuntimeContext { ... }` literal gains `loaded_profile: loaded_profile.as_ref()`.

5. **`crates/nono-cli/src/supervised_runtime.rs`**:
   - `SupervisedRuntimeContext<'a>` gains `pub(crate) loaded_profile: Option<&'a crate::profile::Profile>` at line 48.
   - `execute_supervised_runtime` destructures `loaded_profile` and resolves the allowlist:
     ```rust
     let aipc_allowlist = match loaded_profile {
         Some(profile) => profile.resolve_aipc_allowlist()?,
         None => crate::profile::AipcResolvedAllowlist::default(),
     };
     #[cfg(not(target_os = "windows"))]
     let _ = &aipc_allowlist;
     ```
     The `?` propagates `NonoError::ProfileParse` if a widening token fails runtime revalidation (belt-and-braces per T-18.1-03-01; the mitigation is already installed at profile-load time via `validate_profile_aipc_tokens`).
   - Windows `SupervisorConfig` literal at line 220 updated:
     ```rust
     aipc_allowlist: aipc_allowlist.clone(),
     ```

6. **`crates/nono-cli/src/main.rs`** — Two `PreparedSandbox` test fixtures (lines 221 and 262) gained `loaded_profile: None`.

### Task 3: Add 3 end-to-end profile-widening integration tests (commit `d88c20d`)

3 new tests appended to the `capability_handler_tests` module (inside `#[cfg(all(test, target_os = "windows"))]` gate), plus 2 new helpers:

- **Helpers:**
  - `build_widened_pipe_allowlist()` — constructs a minimal Profile JSON with `capabilities.aipc.pipe: ["read+write"]`, parses via `serde_json::from_str::<Profile>`, and calls the live `profile.resolve_aipc_allowlist()` path.
  - `build_widened_socket_bind_allowlist()` — same shape with `capabilities.aipc.socket: ["bind"]`.

  Test-profile JSON shape that parses cleanly:
  ```json
  {
    "meta": { "name": "..." },
    "capabilities": { "aipc": { "<kind>": ["<token>"] } }
  }
  ```
  All `Profile` fields use `#[serde(default)]` so this minimal shape is sufficient.

- **Tests:**

  | # | Test name | Widening | Target | Assertion |
  |---|-----------|----------|--------|-----------|
  | 1 | `profile_widening_for_pipe_readwrite_reaches_backend` | `pipe: ["read+write"]` | Pipe / GENERIC_READ\|GENERIC_WRITE | `backend.calls() == 1` AND `!reason.contains("not in resolved allowlist")` |
  | 2 | `default_allowlist_rejects_pipe_readwrite_after_prompt` | none (default) | Pipe / GENERIC_READ\|GENERIC_WRITE | `backend.calls() == 1` AND reason contains `"broker failed:"` AND `"not in resolved allowlist"` |
  | 3 | `profile_widening_for_socket_bind_reaches_backend` | `socket: ["bind"]` | Socket / Tcp 127.0.0.1:8080 role=Bind | `backend.calls() == 1` AND `!reason.contains("role Bind not in resolved allowlist")` |

- **WR-01 caveat documented in the test 2 narrative comment**: Pipe direction check lives INSIDE `handle_pipe_request` (post-approval) per the WR-01 current behavior, so the backend IS consulted even under the default allowlist; the G-04 dispatcher flip then wraps the inner `NonoError::SandboxInit("pipe direction ... not in resolved allowlist")` into `Denied { reason: "broker failed: <inner>" }`. Test 2 asserts both substrings ("broker failed:" and "not in resolved allowlist") present.

## Files Changed

- **Modified:** 8 source files as tabulated above.
- **Created:** `.planning/phases/18.1-extended-ipc-gaps/18.1-03-SUMMARY.md` (this file).

## Files Modified Beyond Plan Frontmatter

The plan's `files_modified` frontmatter listed 5 files. Execution required 3 additional files for the cross-file plumbing chain (Rule 3 blocking-issue deviation — documented here per GSD protocol):

| File | Reason |
|------|--------|
| `crates/nono-cli/src/launch_runtime.rs` | `LaunchPlan` is the plumbing artifact between `PreparedSandbox` and `execute_sandboxed`; adding the `loaded_profile` field here is load-bearing for the cross-file wiring the plan's Task 2 Step 5 describes. |
| `crates/nono-cli/src/command_runtime.rs` | 2 additional `LaunchPlan` construction sites (`run_shell` and `run_wrap`) require the new field to satisfy the struct-literal; missing fields = compile error. |
| `crates/nono-cli/src/main.rs` | 2 `PreparedSandbox` test fixtures (in test module) require the new `loaded_profile: None` field to satisfy the struct-literal. Test-only; no production impact. |

The plan's Task 2 explicitly anticipates cross-file plumbing ("Extend whatever type flows through to include a reference to it" — Step 5) — these 3 files are exactly the "whatever type flows through" chain. No alternative architecture (reloading the profile inside `execute_supervised_runtime` from its name, storing the allowlist in `SessionLaunchOptions`, etc.) was less invasive; the straightforward `PreparedSandbox → LaunchPlan → execute_sandboxed → SupervisedRuntimeContext` chain is the natural fit.

## Tests Added

3 new tests in the `capability_handler_tests` module (all gated by `#[cfg(all(test, target_os = "windows"))]` via the module attribute):

| # | Test name | HandleKind | Widening | Backend calls | Expected reason substring check |
|---|-----------|------------|----------|----------------|-----------------------------------|
| 1 | `profile_widening_for_pipe_readwrite_reaches_backend` | Pipe | `pipe: ["read+write"]` | 1 | MUST NOT contain `"not in resolved allowlist"` |
| 2 | `default_allowlist_rejects_pipe_readwrite_after_prompt` | Pipe | none | 1 | MUST contain `"broker failed:"` AND `"not in resolved allowlist"` |
| 3 | `profile_widening_for_socket_bind_reaches_backend` | Socket | `socket: ["bind"]` | 1 | MUST NOT contain `"role Bind not in resolved allowlist"` |

All 3 pass on first run (the Task 1+2 wiring routes the widening correctly; the tests lock the behavior as a regression guard).

## Structural Invariant Grep Checks

Copied from the plan's `<verify>` block and re-run against the final tree:

| # | Command | Expected | Actual |
|---|---------|----------|--------|
| 1 | `grep -n 'pub aipc_allowlist' crates/nono-cli/src/exec_strategy_windows/mod.rs` | 1 match | **1 match (line 190)** |
| 2 | `grep -n 'supervisor\.aipc_allowlist' crates/nono-cli/src/exec_strategy_windows/supervisor.rs` | ≥ 1 | **1 match (line 372)** |
| 3 | `grep -n 'loaded_profile: Option<&' crates/nono-cli/src/supervised_runtime.rs` | 1 match | **1 match (line 48)** |
| 4 | `grep -n 'resolve_aipc_allowlist' crates/nono-cli/src/supervised_runtime.rs` | ≥ 1 | **2 matches (line 192 resolution + line 283 doc comment)** |
| 5 | `grep -n 'aipc_allowlist' crates/nono-cli/src/supervised_runtime.rs` | ≥ 2 | **5 matches (let binding + silencer + field + doc-comment refs)** |
| 6 | `grep -n 'loaded_profile' crates/nono-cli/src/execution_runtime.rs` | ≥ 1 | **4 matches (doc comment + destructure + comment + field)** |
| 7 | `grep -n 'loaded_profile' crates/nono-cli/src/sandbox_prepare.rs` | ≥ 2 | **8 matches (struct field + 2 construction sites + destructure + uses + pass-through)** |
| 8 | `grep -c 'fn profile_widening_\\|fn default_allowlist_rejects_' crates/nono-cli/src/exec_strategy_windows/supervisor.rs` | 3 | **3** |

All invariants satisfied.

## Verification Commands Run

| Command | Result |
|---------|--------|
| `cargo test -p nono-cli --bin nono capability_handler_tests -- --test-threads 1` | 31/31 PASS (28 pre-existing + 3 new) |
| `cargo test -p nono-cli --bin nono profile_widening -- --test-threads 1` | 2/2 PASS (both widening-reaches-backend tests) |
| `cargo test -p nono-cli --bin nono default_allowlist_rejects -- --test-threads 1` | 1/1 PASS (revert test) |
| `cargo test -p nono-cli --test aipc_handle_brokering_integration` | 5/5 PASS (Phase 18 integration regression guard) |
| `cargo test -p nono-cli --bin nono profile::tests::resolve_aipc_allowlist -- --test-threads 1` | 4/4 PASS (Plan 18-03 regression guard; subset of the 9 profile AIPC tests) |
| `cargo test -p nono-cli --bin nono` | 751/751 PASS (full nono-cli unit+integration bin suite) |
| `cargo build --workspace` | clean (exit 0) |
| `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` | clean (exit 0) |
| `cargo fmt --all -- --check` | clean (exit 0) |

**Note on test target:** `cargo test -p nono-cli --lib` returns `no library targets found in package 'nono-cli'` — `nono-cli` is a binary-only crate, so the runtime form is `cargo test -p nono-cli --bin nono <filter>`. Same caveat noted in 18.1-01-SUMMARY.md and 18.1-02-SUMMARY.md.

## Phase 18 Byte-Identical Preservation (D-19)

Per D-19 cross-phase invariance, every Phase 18 file NOT touched by this plan must have zero lines of diff vs. HEAD~3 (the Task 1 commit's parent). Verified with `git diff --stat HEAD~3 HEAD -- <path>`:

| Path | Expected diff | Actual |
|------|---------------|--------|
| `crates/nono/src/supervisor/` (all files) | 0 lines | 0 lines |
| `crates/nono-cli/src/terminal_approval.rs` | 0 lines | 0 lines (Wave 1 file untouched) |
| `crates/nono-cli/src/profile/` (mod.rs + any sibling) | 0 lines | 0 lines (D-18: Profile / AipcResolvedAllowlist / AipcConfig / CapabilitiesConfig unchanged) |
| `crates/nono-cli/data/` (policy.json + schema) | 0 lines | 0 lines |

Proof: `git diff --stat HEAD~3 HEAD` output shows 8 files in the diff — exactly the 5 plan-listed files + the 3 additional plumbing files documented above. No nono-lib, no data, no profile types touched.

## D-21 Windows-Invariance

D-21 preserved:
- The new `aipc_allowlist` field lives only on the Windows branch of `SupervisorConfig` (`exec_strategy_windows/mod.rs`). The Unix branch (`exec_strategy.rs`) has a completely separate `SupervisorConfig` struct with different fields — unaffected.
- The Windows `SupervisorConfig` literal at `supervised_runtime.rs:220` is inside `#[cfg(target_os = "windows")]`.
- The `loaded_profile` field on `SupervisedRuntimeContext` and `LaunchPlan` is cross-platform but unread on non-Windows (consumed only inside the `#[cfg(target_os = "windows")]` `SupervisorConfig` literal). The `let _ = &aipc_allowlist;` silencer in `execute_supervised_runtime` (gated by `#[cfg(not(target_os = "windows"))]`) keeps the non-Windows build warning-clean.
- Non-Windows cargo build + clippy verified clean (implicit via `cargo clippy --workspace --all-targets` which runs for the host platform — Windows in this environment; the cross-platform code path was inspected manually and the silencer covers the unused-binding warning).

## Deviations from Plan

**Rule 3 (blocking issue) — auto-expanded file list.** The plan's `files_modified` frontmatter listed 5 files: `exec_strategy_windows/supervisor.rs`, `exec_strategy_windows/mod.rs`, `supervised_runtime.rs`, `execution_runtime.rs`, `sandbox_prepare.rs`. Execution required 3 additional files to complete the cross-file plumbing chain:

1. **`launch_runtime.rs`** — `LaunchPlan` carries data from `PreparedSandbox` to `execute_sandboxed`; missing this field = compile error.
2. **`command_runtime.rs`** — 2 `LaunchPlan` construction sites outside `launch_runtime.rs`; missing the field = compile error.
3. **`main.rs`** — 2 `PreparedSandbox` test fixtures in the test module; missing the field = compile error in `cargo test`.

This deviation is purely structural — every modified site passes the new field through existing patterns (None in test/Direct-only fixtures; forward from prepared.loaded_profile in live paths). The plan's Task 2 Step 5 explicitly anticipates this: "Extend whatever type flows through to include a reference to it." The 3 additional files are exactly that "whatever" chain.

No other deviations. All 3 plan tasks executed verbatim per the plan's detailed step-by-step Action sections.

## Rustfmt Drift

None. `cargo fmt --all -- --check` exited 0 before each commit. No auto-formatting was required at any point.

## Deferred / Out-of-Scope Items

1. **`trust_scan::tests::multi_subject_bundle_detected_and_verified`** — Pre-existing tempdir-race / env-state flake documented in STATE.md under Phase 19 CLEAN-02 ("trust_scan::tests::* tempdir-race flakes (1–3) exist pre- and post-fix on this Windows host but are NOT in D-06 scope"). In the full `cargo test -p nono-cli --bin nono` run during verification, this test PASSED 751/751 — but noted here for posterity since prior waves' summaries referenced the same flake. Out of scope for Plan 18.1-03.

2. **G-05 remains open.** Plan 18.1-03 closes G-06 only. Plan 18.1-04 (Wave 4) remains to verify G-05 WR-01 reject-stage invariants (Event/Mutex/JobObject reject BEFORE prompt, Pipe/Socket reject AFTER prompt) before the v2.1 milestone tag.

3. **WR-01 stage-unification fix** — Routing Pipe ReadWrite + Socket privileged-port checks through the same pre-prompt mask-validation gate as Event/Mutex/JobObject is intentionally out of scope per CONTEXT Deferred Ideas. Phase 18.1 only verifies + documents the current behavior; the actual fix is a separate product decision.

## Self-Check: PASSED

- Created files exist:
  - `FOUND: .planning/phases/18.1-extended-ipc-gaps/18.1-03-SUMMARY.md` (this file)
  - `FOUND: crates/nono-cli/src/exec_strategy_windows/mod.rs` (modified — `pub aipc_allowlist: AipcResolvedAllowlist` field at line 190)
  - `FOUND: crates/nono-cli/src/exec_strategy_windows/supervisor.rs` (modified — `supervisor.aipc_allowlist.clone()` at line 372; 3 new `profile_widening_*` / `default_allowlist_rejects_*` tests in `capability_handler_tests`)
  - `FOUND: crates/nono-cli/src/supervised_runtime.rs` (modified — `loaded_profile: Option<&'a Profile>` on `SupervisedRuntimeContext`; `resolve_aipc_allowlist()?` call at line 192)
  - `FOUND: crates/nono-cli/src/execution_runtime.rs` (modified — `loaded_profile.as_ref()` passed into `SupervisedRuntimeContext`)
  - `FOUND: crates/nono-cli/src/sandbox_prepare.rs` (modified — `PreparedSandbox.loaded_profile: Option<Profile>` field; `.as_ref()` clone pattern for env_credentials)
  - `FOUND: crates/nono-cli/src/launch_runtime.rs` (modified — `LaunchPlan.loaded_profile`)
  - `FOUND: crates/nono-cli/src/command_runtime.rs` (modified — 2 `LaunchPlan` constructions carry `prepared.loaded_profile`)
  - `FOUND: crates/nono-cli/src/main.rs` (modified — 2 test-fixture `PreparedSandbox` constructions add `loaded_profile: None`)
- Commits exist in `git log`:
  - `FOUND: 8170df2` — `refactor(18.1-03): thread AipcResolvedAllowlist through SupervisorConfig -> WindowsSupervisorRuntime (G-06 task 1)`
  - `FOUND: 993cdcb` — `feat(18.1-03): thread Profile end-to-end to Windows supervisor AIPC allowlist (G-06 task 2)`
  - `FOUND: d88c20d` — `test(18.1-03): add end-to-end profile-widening regression tests (G-06 task 3)`
- All `must_haves.truths` from the plan verified:
  - ✓ `WindowsSupervisorRuntime.resolved_aipc_allowlist` sourced from `Profile::resolve_aipc_allowlist()` (when profile loaded) via `supervisor.aipc_allowlist.clone()` at line 372 of supervisor.rs — Plan 18-03 Deferred Issue #1 resolved
  - ✓ `SupervisorConfig` (Windows branch) carries `aipc_allowlist: AipcResolvedAllowlist` field — None-profile callers fall back to `AipcResolvedAllowlist::default()` via the `match loaded_profile { Some => resolve, None => default }` in execute_supervised_runtime
  - ✓ Profile threaded through: `sandbox_prepare::loaded_profile → PreparedSandbox.loaded_profile → LaunchPlan.loaded_profile → execute_sandboxed → SupervisedRuntimeContext.loaded_profile → execute_supervised_runtime's aipc_allowlist → SupervisorConfig.aipc_allowlist → WindowsSupervisorRuntime::initialize → self.resolved_aipc_allowlist`
  - ✓ 3 end-to-end integration tests PASS: Pipe ReadWrite widening reach-backend, default Pipe ReadWrite revert (reject post-prompt via G-04 wrapping), Socket Bind widening reach-backend
  - ✓ Revert test: default allowlist rejects BEFORE `"not in resolved allowlist"` vs widened allowlist's absence of that substring — no stale caching, no profile-instance bleed (each test constructs its own allowlist fresh from serde_json parse)
  - ✓ Socket-variant widening test: role=Bind with `capabilities.aipc.socket: ["bind"]` reaches backend; default profile rejects pre-prompt substring absent
  - ✓ `AipcResolvedAllowlist`, `AipcConfig`, `CapabilitiesConfig` types UNCHANGED (D-18): `git diff HEAD~3 HEAD -- crates/nono-cli/src/profile/` returns empty
  - ✓ The 20+ existing `capability_handler_tests` that pass `&AipcResolvedAllowlist::default()` explicitly STILL COMPILE UNCHANGED — dispatcher `handle_windows_supervisor_message` signature unchanged (the new field is only on the OUTER `SupervisorConfig`, not on the dispatcher function)
- All `must_haves.artifacts` present:
  - ✓ `SupervisorConfig.aipc_allowlist: AipcResolvedAllowlist` field exists in `exec_strategy_windows/mod.rs`
  - ✓ `WindowsSupervisorRuntime.initialize` reads `config.aipc_allowlist` (via `supervisor.aipc_allowlist.clone()`) into `self.resolved_aipc_allowlist`
  - ✓ `SupervisedRuntimeContext.loaded_profile` field + `execute_supervised_runtime` calls `profile.resolve_aipc_allowlist()?`
  - ✓ `execute_runtime` (i.e., `execute_sandboxed`) threads `loaded_profile` into `SupervisedRuntimeContext`
  - ✓ `sandbox_prepare.rs` PreparedSandbox carries `loaded_profile`
  - ✓ 3 new integration-style tests in `capability_handler_tests` module
- All `must_haves.key_links` wired:
  - ✓ `Profile::resolve_aipc_allowlist → SupervisorConfig.aipc_allowlist` via `supervised_runtime.rs::execute_supervised_runtime` (line 192 call + line 288 field)
  - ✓ `SupervisorConfig.aipc_allowlist → WindowsSupervisorRuntime.resolved_aipc_allowlist` via `WindowsSupervisorRuntime::initialize` (line 372: `resolved_aipc_allowlist: std::sync::Arc::new(supervisor.aipc_allowlist.clone())`)
- No `.unwrap()` / `.expect()` introduced in production code (tests use `.expect()` with `#[allow(clippy::unwrap_used)]` at module scope as pre-existing convention)
- `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` exits 0
- `cargo fmt --all -- --check` exits 0
- `cargo build --workspace` exits 0
