---
phase: 18.1-extended-ipc-gaps
plan: 04
subsystem: ipc
tags: [windows, supervisor, ipc, aipc, test, regression, wr-01, wr01, deny-stage, g-05, gap-closure]

# Dependency graph
requires:
  - phase: 18-extended-ipc
    provides: handle_windows_supervisor_message dispatcher with pre-broker mask-validation gate at lines 1846-1878 (Event/Mutex/JobObject branch); handle_pipe_request direction-allowlist check at line 1411; handle_socket_request privileged-port + role-allowlist checks at line 1479; CountingGrantBackend + make_request_aipc + new_pair test helpers in capability_handler_tests; policy::EVENT_DEFAULT_MASK / MUTEX_DEFAULT_MASK / JOB_OBJECT_TERMINATE / PRIVILEGED_PORT_MAX constants
  - phase: 18.1-extended-ipc-gaps
    plan: 02
    provides: G-04 dispatcher flip (Err->Denied { reason: "broker failed: {e}" }) at handle_windows_supervisor_message ~line 1948 — the post-broker rejection shape this plan's Pipe/Socket tests lock empirically
  - phase: 18.1-extended-ipc-gaps
    plan: 03
    provides: end-to-end Profile::resolve_aipc_allowlist threading used by the default-allowlist-rejection assertions (every new wr01_ test passes `AipcResolvedAllowlist::default()` as the resolved allowlist)
provides:
  - 5 new wr01_* regression tests in capability_handler_tests (supervisor.rs) empirically locking the WR-01 reject-stage verdict matrix for all 5 AIPC HandleKinds
  - Module-level docstring at capability_handler_tests (lines 2006-2049) documenting the WR-01 verdict matrix + D-14 deferral note
  - Narrative comment block above the pre-existing handle_denies_job_object_with_terminate_mask_no_profile_widening assertion explicitly tying its backend.calls() == 0 to the WR-01 G-05 stage claim
  - Closure signal for G-05 — Phase 18.1 implementation work complete (awaiting gsd-verifier for phase-level sign-off)
affects: []  # Phase 18.1 is the final 18-extended-ipc follow-up; no downstream plans consume this

# Tech tracking
tech-stack:
  added: []  # Test-only + docs-only — no new crates, no new production code, no behavioral changes
  patterns:
    - "Stage-encoded regression-test naming: the test name `wr01_<kind>_rejects_{before,after}_prompt_<condition>` encodes the expected `backend.calls()` value in the identifier — a future maintainer who copies the Event shape to the Pipe row MUST rename the test to preserve compile-time legibility. Paired with named assertions (\"WR-01 invariant: ...\" / \"WR-01 deviation: ...\") so the test name, the assertion message, and the module docstring agree."
    - "Module-docstring-as-verdict-matrix: the tabular `//!` docstring at the top of `capability_handler_tests` encodes the expected stage per HandleKind in a single auditable place. Any future refactor that accidentally moves a mask/direction/role/port check pre/post-broker will flip a `backend.calls()` assertion; the failing test name points directly to the row of the matrix that's now wrong, making the fix-or-update decision explicit. Cheaper than a compile-time enum and more expressive than prose."
    - "Empirical-verification footnote: the Socket privileged-port test docstring explicitly enumerates the observed shape (backend.calls() == 1, reason contains 'broker failed:' + 'privileged port') AND the fallback protocol ('if this test fails with `backend.calls() == 0`, the privileged-port check has been moved to the pre-broker gate — update BOTH the assertion AND the module docstring'). Turns a possible semantic-drift surprise into an explicit update checklist."

key-files:
  created:
    - .planning/phases/18.1-extended-ipc-gaps/18.1-04-SUMMARY.md
  modified:
    - crates/nono-cli/src/exec_strategy_windows/supervisor.rs

key-decisions:
  - "Used `0x1000_0000` as the out-of-allowlist probe mask for Event and Mutex tests. The bit is unambiguously outside EVENT_DEFAULT_MASK (0x0010_0002) and MUTEX_DEFAULT_MASK (0x0010_0001), so `mask_is_allowed` returns false. Coincidentally equal to `policy::GENERIC_ALL`, but the semantic does not matter — only that the bit fails the subset check. Documented in an inline comment above the first `wr01_` test."
  - "Socket privileged-port test named with the `_empirical` suffix (`wr01_socket_privileged_port_rejects_after_prompt_empirical`) per plan Step 2 / CONTEXT.md D-14: the stage was empirically determined at execution time, not assumed from the read. Observed shape: `backend.calls() == 1`, reason contains both `broker failed:` (G-04 wrapper) and `privileged port` (inner rejection from handle_socket_request:1479). Matches the pre-read hypothesis in the plan. Test docstring enumerates the observation + the fallback protocol if the stage ever changes."
  - "Pipe test overlaps the Wave 3 `default_allowlist_rejects_pipe_readwrite_after_prompt` (line 3943), but per plan must-haves the `wr01_pipe_` test is mandatory: the named test carries the WR-01 deviation claim in its identifier, while the Wave 3 test approaches the same shape from the G-06 profile-widening angle. Both coexist — the redundancy is intentional regression-lock (future refactors must preserve BOTH observables to satisfy both naming intents)."
  - "Extended the pre-existing `handle_denies_job_object_with_terminate_mask_no_profile_widening` test (line 3062) with a narrative comment block above the `backend.calls() == 0` assertion tying it to the WR-01 G-05 stage claim. The assertion value is unchanged (was already `== 0`); only a ~10-line comment added. The redundant `wr01_job_object_rejects_before_prompt_on_terminate_mask` test was added anyway per plan must_have #3, because naming it with the `wr01_` prefix is what makes the regression-guard semantic visible in the test output + matrix. Both tests fire on the same dispatcher path; neither is deletable without losing one of the two documentation surfaces."
  - "Module docstring lives INSIDE `mod capability_handler_tests { ... }` as a `//!` comment block after the `#[cfg]`/`#[allow]` attributes on the `mod` declaration. Placing `//!` BEFORE the `mod` declaration is invalid Rust (the inner-doc-comment has no enclosing scope); this was caught during implementation and the docstring was moved. Documenting here for future maintainers."
  - "No new test helpers. The plan's test-seam requirement is satisfied by the pre-existing `CountingGrantBackend` + `make_request_aipc` + `new_pair` + `AipcResolvedAllowlist::default()` helpers. No `ProbeApprovalBackend` added (the plan mentions it as a potential name — `CountingGrantBackend` already satisfies the call-counting contract)."

patterns-established:
  - "Test names as contracts: when a regression-test suite locks a semantic invariant (like WR-01 reject-stage), encoding the expected observation in the test identifier (`wr01_event_rejects_before_prompt_on_out_of_allowlist_mask`) prevents the copy-paste bug where a future maintainer adapts an Event-shaped test for Pipe without re-reading the assertions. The name forces the reader to ask \"is this BEFORE or AFTER prompt?\" at copy time, making the answer a rename decision. Pair with a module-level docstring verdict matrix so the pattern scales across multiple invariants (stage-rules, token-leak protection, etc.)."

requirements-completed: [AIPC-01]
closes_gaps: [G-05]

# Metrics
duration: ~35m
completed: 2026-04-21
tasks: 1
files_modified: 1
tests_added: 5
commits: 1
---

# Phase 18.1 Plan 18.1-04: G-05 WR-01 Reject-Stage Invariant Verification Summary

**Closed G-05 from the 2026-04-20 Phase 18 HUMAN-UAT re-run by landing 5 `wr01_*` regression tests in `capability_handler_tests` that empirically verify the WR-01 reject-stage invariant across all 5 AIPC HandleKinds. Event/Mutex/JobObject reject BEFORE the approval backend is consulted (`backend.calls() == 0`) via the pre-broker mask-validation gate at `handle_windows_supervisor_message` lines 1846-1878; Pipe ReadWrite + Socket privileged-port reject AFTER the backend (`backend.calls() == 1`) via `handle_pipe_request:1411` and `handle_socket_request:1479` respectively, with the G-04 flip (Wave 2) wrapping the inner `Err` into `Denied { reason: "broker failed: <inner>" }`. A module-level docstring at the top of `capability_handler_tests` documents the verdict matrix + Phase 18.1 D-14 deferral note (WR-01 stage-unification fix explicitly deferred to v2.2 as a product decision). The pre-existing `handle_denies_job_object_with_terminate_mask_no_profile_widening` test gained a narrative comment tying its `backend.calls() == 0` assertion to the WR-01 G-05 stage claim. 36/36 capability_handler_tests PASS (31 pre-existing + 5 new); 5/5 integration regression guard PASS; 31/31 terminal_approval Wave 1 regression PASS; 60/60 nono lib supervisor D-19 byte-identical proof PASS; clippy/fmt/build clean. D-19 verified — the only file modified by this plan is `crates/nono-cli/src/exec_strategy_windows/supervisor.rs` (test module + docstring only; no production code touched).**

## Performance

- **Duration:** ~35 minutes
- **Commits:** 1 (test-only, no behavioral changes — regression lock only)
- **Files modified:** 1 (`crates/nono-cli/src/exec_strategy_windows/supervisor.rs`)
- **Net diff:** 447 lines added / 0 deleted (pure addition: 1 module docstring + 1 narrative comment in existing JobObject test + 5 new `wr01_*` tests at end of module)
- **Tests added:** 5 new `wr01_*` tests; 0 removed
- **Test counts:** `capability_handler_tests` 31 → 36 (all pass); `aipc_handle_brokering_integration` 5/5 (regression guard); `terminal_approval` 31/31 (Wave 1 regression guard); nono lib supervisor 60/60 (D-19 proof)

## Objective Recap

Close G-05 from `.planning/phases/18-extended-ipc/18-HUMAN-UAT.md § Gaps`. The 2026-04-20 HUMAN-UAT re-run only exercised approval paths (no scripted deny scenarios), so the WR-01 reject-stage invariant — Event/Mutex/JobObject reject BEFORE the approval prompt via the server-side mask-validation gate (Phase 18 D-07); Pipe/Socket reject AFTER the approval prompt pending a future fix (WR-01 deviation) — remained empirically unverified. Phase 18.1 D-14 locks the scope: Phase 18.1 VERIFIES + DOCUMENTS the current behavior; the actual WR-01 stage-unification fix (routing Pipe/Socket checks through the same pre-broker gate as Event/Mutex/JobObject) is a separate product decision explicitly **deferred to v2.2**. Phase 18.1 Plan 04 does not touch production code; it only lands regression tests + a module-docstring verdict matrix.

Phase 18.1 Plan 18.1-04 is the **final wave** of Phase 18.1 — after this plan completes, phase-level verification runs next.

## Commits

| # | Hash      | Type | Message |
|---|-----------|------|---------|
| 1 | `1699339` | test | `test(18.1-04): verify WR-01 reject-stage invariant across all 5 AIPC kinds (G-05)` |

Commit carries `Signed-off-by: oscarmackjr-twg <oscar.mack.jr@gmail.com>` per CLAUDE.md § Coding Standards.

## Tasks Completed

### Task 1: Add 5 WR-01 reject-stage verification tests + module docstring + narrative comment (commit `1699339`)

Three additions to `crates/nono-cli/src/exec_strategy_windows/supervisor.rs`, all inside the `#[cfg(all(test, target_os = "windows"))]`-gated `mod capability_handler_tests { ... }` block:

1. **Module-level docstring** (`//!` comment block immediately after the `use super::*;` / `use std::sync::atomic::...;` imports, lines ~2006-2049 post-patch). Documents the WR-01 verdict matrix for all 6 HandleKinds — Event, Mutex, JobObject, Pipe, Socket, File (the Phase 11 legacy path is referenced implicitly; File goes through `handle_windows_supervisor_message`'s File branch at ~line 1906 post-approval with Phase 11 semantics). Includes the CONTEXT.md D-14 deferral note: **WR-01 stage-unification fix deferred to v2.2 as a product decision**. The docstring mentions the 1846-1878 enforcement site for the pre-broker gate, and the 1411 (Pipe) / 1479 (Socket) enforcement sites for the post-broker helpers. A future refactor that moves a check from the pre-broker gate to a post-broker helper (or vice versa) will flip a `backend.calls()` assertion in one of the 5 tests below and break CI.

2. **Narrative comment block above the `backend.calls() == 0` assertion in the pre-existing `handle_denies_job_object_with_terminate_mask_no_profile_widening` test** (line ~3062 post-Wave-3, now ~3073 post-patch). The assertion value is unchanged (was already `== 0`); only a ~10-line comment added explicitly tying the assertion to the WR-01 G-05 stage claim and cross-referencing the new `wr01_job_object_rejects_before_prompt_on_terminate_mask` test appended at the end of the module.

3. **5 new `wr01_*` tests** appended to the end of the `capability_handler_tests` module (after `profile_widening_for_socket_bind_reaches_backend` at line ~4061 post-Wave-3), separated by a comment banner + rationale block that enumerates the `0x1000_0000` probe-mask choice (unambiguously outside both EVENT_DEFAULT_MASK and MUTEX_DEFAULT_MASK):

| # | Test name | HandleKind | Mask / Target | Expected observables |
|---|-----------|------------|---------------|---------------------|
| 1 | `wr01_event_rejects_before_prompt_on_out_of_allowlist_mask` | Event | `access_mask=0x1000_0000`, `EventName { name: "wr01-event" }` | `backend.calls() == 0`; reason contains "access mask" + "allowlist"; reason does NOT contain "broker failed:" |
| 2 | `wr01_mutex_rejects_before_prompt_on_out_of_allowlist_mask` | Mutex | `access_mask=0x1000_0000`, `MutexName { name: "wr01-mutex" }` | Same as Event |
| 3 | `wr01_job_object_rejects_before_prompt_on_terminate_mask` | JobObject | `access_mask=policy::JOB_OBJECT_TERMINATE (0x0008)`, `JobObjectName { name: "wr01-jobobj" }` | Same as Event; pairs with the pre-existing test at line ~3073 |
| 4 | `wr01_pipe_rejects_after_prompt_on_readwrite_default_profile` | Pipe | `access_mask=GENERIC_READ \| GENERIC_WRITE`, `PipeName { name: "wr01-pipe-rw" }` | `backend.calls() == 1`; reason contains "broker failed:" AND "not in resolved allowlist" |
| 5 | `wr01_socket_privileged_port_rejects_after_prompt_empirical` | Socket | `port=80`, `role=Connect`, `host="127.0.0.1"` | `backend.calls() == 1`; reason contains "broker failed:" AND "privileged port" |

All 5 tests use the pre-existing `CountingGrantBackend` test seam + `make_request_aipc` + `new_pair` helpers + `&AipcResolvedAllowlist::default()` as the resolved allowlist. Each test constructs a request, dispatches through `handle_windows_supervisor_message`, asserts the `backend.calls()` value + Denied reason shape, and drains the child side socket. No new test infrastructure added.

## Socket Privileged-Port Empirical Observation

Per plan must-have #5 + CONTEXT.md D-14, the Socket test's stage was determined empirically at execution time rather than assumed from the code read. Observed shape on Windows 11 26200, HEAD = 424eac6 + this patch:

- `backend.calls() == 1` — the approval backend IS consulted before the privileged-port check fires.
- `audit_log[0].decision == Denied { reason }` with `reason.contains("broker failed:")` AND `reason.contains("privileged port")`.

Matches the pre-read hypothesis in the plan's Step 1: the privileged-port check at `handle_socket_request:1479` lives inside the per-kind helper, which the dispatcher invokes AFTER the backend returns `Granted`; the G-04 flip (Wave 2) wraps the inner `NonoError::SandboxInit("privileged port 80 not allowed (port must be > 1023)")` into `Denied { reason: "broker failed: privileged port 80 not allowed ..." }`. The test docstring + assertion agree with the module-docstring verdict matrix. No discrepancy found; no update to the docstring required post-empirical-check.

## Module-Docstring Line Range

Post-patch `supervisor.rs`:

- **Module docstring:** lines ~2008-2049 (inside `mod capability_handler_tests { ... }`, immediately before the `use super::*;` import statement). Tabular verdict matrix + CONTEXT.md D-14 deferral note.
- **WR-01 G-05 narrative comment in existing JobObject test:** lines ~3077-3089 (~10 lines above the `assert_eq!(backend.calls(), 0, ...)` assertion inside `handle_denies_job_object_with_terminate_mask_no_profile_widening`).
- **5 new `wr01_*` tests:** lines ~4063-4398 (after `profile_widening_for_socket_bind_reaches_backend`'s closing brace, before the module's closing brace). Comment banner + rationale block at ~4063-4082 explains the `0x1000_0000` probe-mask choice.

Line numbers given with `~` because rustfmt may have normalized subtle whitespace; use `grep -n 'fn wr01_'` etc. as the canonical locator.

## Files Changed

- **Modified:** `crates/nono-cli/src/exec_strategy_windows/supervisor.rs` — test module additions only.
- **Created:** `.planning/phases/18.1-extended-ipc-gaps/18.1-04-SUMMARY.md` (this file).

## Structural Invariant Grep Checks

Copied from the plan's `<verify>` block and re-run against the final tree:

| # | Command | Expected | Actual |
|---|---------|----------|--------|
| 1 | `grep -c 'fn wr01_' crates/nono-cli/src/exec_strategy_windows/supervisor.rs` | 5 | **5** |
| 2 | `grep -c 'WR-01 reject-stage verdict matrix' crates/nono-cli/src/exec_strategy_windows/supervisor.rs` | 1 | **1** |
| 3 | `grep -c 'WR-01 G-05 stage assertion' crates/nono-cli/src/exec_strategy_windows/supervisor.rs` | 1 | **1** (inside the narrative comment block prepended to the pre-existing JobObject-terminate test) |

All three invariants satisfied.

## Verification Commands Run

| Command | Result |
|---------|--------|
| `cargo test -p nono-cli --bin nono wr01_ -- --test-threads 1` | 5/5 PASS (all new G-05 tests) |
| `cargo test -p nono-cli --bin nono capability_handler_tests -- --test-threads 1` | 36/36 PASS (31 pre-existing + 5 new) |
| `cargo test -p nono-cli --test aipc_handle_brokering_integration` | 5/5 PASS (Phase 18 integration regression guard) |
| `cargo test -p nono-cli --bin nono terminal_approval` | 31/31 PASS (Wave 1 regression guard) |
| `cargo test -p nono --lib supervisor` | 60/60 PASS (nono lib supervisor — D-19 byte-identical proof) |
| `cargo build --workspace` | clean (exit 0) |
| `cargo clippy -p nono-cli --all-targets -- -D warnings -D clippy::unwrap_used` | clean (exit 0) |
| `cargo fmt --all -- --check` | clean (exit 0) |

**Note on test target:** `cargo test -p nono-cli --lib` returns `no library targets found in package 'nono-cli'` — `nono-cli` is a binary-only crate, so the runtime form is `cargo test -p nono-cli --bin nono <filter>`. Same caveat noted in 18.1-01/02/03 SUMMARY.md.

## Phase 18 / Phase 18.1 Prior Waves Byte-Identical Preservation (D-19)

Per D-19 cross-phase invariance, every file NOT listed in this plan's `files_modified` must have zero lines of diff vs HEAD~1 (the Plan 18.1-03 close-out commit's head). Verified with `git diff --stat HEAD~1 HEAD`:

| Path | Expected diff | Actual |
|------|---------------|--------|
| `crates/nono-cli/src/exec_strategy_windows/supervisor.rs` | >0 (this plan's target) | **447 insertions** |
| ALL other paths | 0 lines | **0 lines** (only supervisor.rs in the diff) |

`git diff --stat HEAD~1 HEAD` output shows ONLY `crates/nono-cli/src/exec_strategy_windows/supervisor.rs | 447 +++++++++++++++++++++` — the full cross-phase D-19 contract honored. Phase 18 library files (nono/src/supervisor/), Phase 18.1-01 target (terminal_approval.rs), Phase 18.1-02 G-03/G-04 (supervisor.rs lines 1675 and 1899 kept untouched — edits were test-module only), Phase 18.1-03 plumbing files (mod.rs, supervised_runtime.rs, execution_runtime.rs, sandbox_prepare.rs, launch_runtime.rs, command_runtime.rs, main.rs) — all 0-diff.

## D-21 Windows-Invariance

D-21 preserved: all 447 lines added are inside the `#[cfg(all(test, target_os = "windows"))]`-gated `capability_handler_tests` module (via the module attribute). No cross-platform surface touched; no non-Windows build paths affected. Clippy + fmt + build verified clean for the host platform (Windows). The module-level docstring is also inside the cfg-gated module so it only renders on Windows builds.

## Phase 18.1 Rollup

Phase 18.1 opened 5 gaps from the 2026-04-20 HUMAN-UAT re-run (G-02..G-06); all 5 are now closed across 4 waves:

| Gap | Plan | Wave | Status | Commits |
|-----|------|------|--------|---------|
| G-02 (CONIN$ approval prompts generic template) | 18.1-01 | 1 | CLOSED 2026-04-21 | `1960239` + `70984ac` |
| G-03 (JobObject broker OpenJobObjectW vs CreateJobObjectW) | 18.1-02 | 2 | CLOSED 2026-04-21 | `e6ac4bb` |
| G-04 ((Approved, grant=None) protocol violation) | 18.1-02 | 2 | CLOSED 2026-04-21 | `9f81a39` + `3493dd8` |
| G-06 (profile widening end-to-end wiring) | 18.1-03 | 3 | CLOSED 2026-04-21 | `8170df2` + `993cdcb` + `d88c20d` |
| G-05 (WR-01 reject-stage invariant verification) | 18.1-04 | 4 | CLOSED 2026-04-21 | `1699339` ← THIS PLAN |

The 18-HUMAN-UAT.md can now transition the 4 items previously carrying `blocked` / `partial` / `issue` verdicts to `pass` (with WR-01 deviation documented via the module-docstring verdict matrix + this SUMMARY's Deferred section). That transition is a phase-verification bookkeeping step, not a plan-level action; gsd-verifier will drive the final UAT-file roll-up when phase verification runs.

## Deferred Items

1. **WR-01 fix — stage-unification for Pipe/Socket** — Explicitly deferred to v2.2 per CONTEXT.md D-14 + 18-HUMAN-UAT Test 2 result. Phase 18.1 only VERIFIES + DOCUMENTS the current behavior. The deferred fix would route the Pipe direction-allowlist check (supervisor.rs:1411) and the Socket privileged-port + role-allowlist checks (supervisor.rs:1479) through the same pre-broker mask-validation gate at handle_windows_supervisor_message:1846-1878, eliminating the UX inconsistency where Pipe/Socket denials show the approval prompt BEFORE ultimately denying while Event/Mutex/JobObject deny silently. The 5 `wr01_*` tests in this plan lock the current behavior as a regression guard — a v2.2 fix would FLIP the Pipe and Socket assertions from `backend.calls() == 1` to `backend.calls() == 0` and correspondingly update the module-docstring verdict matrix. Tracked in 18.1-CONTEXT.md Deferred Ideas.

2. **WR-02 CompareObjectHandles EDR empirical test** — Deferred to v3.0 (unchanged from Phase 18 18-HUMAN-UAT Test 4 result). Requires an EDR-instrumented test runner (CrowdStrike / Defender ATP / EDR with Job Object telemetry hooks); out of scope for v2.1.

3. **Optional: add a WR-01-matrix-to-prose-sync test** — A proptest-style test that parses the module docstring's ASCII verdict-matrix table and compares each row to the corresponding `wr01_*` test's `backend.calls()` assertion would structurally prevent docstring drift from the test assertions. Out of scope for Phase 18.1 (test of a test), but a plausible Phase 18.2 / v2.2 improvement if the matrix grows past 5 rows.

## Known Stubs

None. All 5 new `wr01_*` tests exercise live dispatcher paths end-to-end; no mocks or placeholders beyond the pre-existing `CountingGrantBackend`.

## Threat Flags

None. The patch is test-only + docs-only; no new network endpoints, auth paths, file access patterns, or schema changes. The threat register entries T-18.1-04-01 (docstring drift vs dispatcher behavior) and T-18.1-04-02 (future maintainer copy-paste) are both mitigated/accepted per the plan — no new threats surfaced during execution.

## Deviations from Plan

**None.** The plan's Task 1 with 6 steps was followed verbatim:

1. **Step 1 — Confirm the WR-01 verdict matrix empirically.** Code read of supervisor.rs:1846-1878 (pre-broker mask gate) + 1411 (pipe direction check) + 1479 (socket privileged-port check) + 1900+ (post-approval dispatch arm with G-04 flip) confirmed the predicted matrix before writing tests. All 5 test-run observations matched the pre-read hypothesis.

2. **Step 2 — Choose a concrete out-of-allowlist mask.** Selected `0x1000_0000`. Value rationale documented in an inline comment above the first `wr01_` test: unambiguously outside EVENT_DEFAULT_MASK (0x0010_0002) and MUTEX_DEFAULT_MASK (0x0010_0001); coincidentally equal to `policy::GENERIC_ALL`, but the specific semantic does not matter. `mask_is_allowed(HandleKind::Event, 0x1000_0000, 0x0010_0002)` returns false (verified via reading `policy::mask_is_allowed` which is `requested & !resolved == 0` — `0x1000_0000 & !0x0010_0002 = 0x1000_0000` which is non-zero → false).

3. **Step 3 — Add the module-level docstring.** Added as a `//!` block INSIDE the `mod capability_handler_tests { ... }` braces (immediately after the `#[cfg]`/`#[allow]` attributes on the `mod` declaration and before the first `use` statement). Initial attempt placed the docstring BEFORE the `mod` declaration, which is invalid Rust (no enclosing scope for inner-doc); corrected during implementation.

4. **Step 4 — Write the 5 new `wr01_*` tests.** All 5 written per the plan's templates. Assertion messages carry `WR-01 invariant:` (for pre-broker cases) vs `WR-01 deviation:` (for post-broker cases) so test failures self-describe the violated/documented semantic.

5. **Step 5 — Extend the existing JobObject-terminate test.** Added a ~10-line narrative comment block immediately above the existing `assert_eq!(backend.calls(), 0, "backend must not be consulted on out-of-allowlist mask")` assertion. The comment ties the assertion to the WR-01 G-05 stage claim and cross-references the new `wr01_job_object_rejects_before_prompt_on_terminate_mask` test. The assertion value and message are byte-identical to pre-patch.

6. **Step 6 — Run + diagnose.** 5/5 wr01 tests PASS; 36/36 capability_handler_tests PASS; 5/5 integration PASS; clippy + fmt + build clean. No diagnosis required — all tests passed on first run because the behavior the tests lock was already in place post-Wave-3.

## Rustfmt Drift

None. `cargo fmt --all -- --check` exited 0 immediately before commit. No auto-formatting was required.

## Self-Check: PASSED

- Created files exist:
  - FOUND: `.planning/phases/18.1-extended-ipc-gaps/18.1-04-SUMMARY.md` (this file)
  - FOUND: `crates/nono-cli/src/exec_strategy_windows/supervisor.rs` (modified — 5 new `wr01_*` tests + module docstring + narrative comment on existing JobObject test; 447 insertions; 0 deletions)
- Commits exist in `git log`:
  - FOUND: `1699339` — `test(18.1-04): verify WR-01 reject-stage invariant across all 5 AIPC kinds (G-05)`
- All `must_haves.truths` from the plan verified:
  - 5 new scripted deny-stage integration tests empirically verify the WR-01 reject-stage invariant end-to-end through `handle_windows_supervisor_message` — all present + PASS
  - Event with mask outside DEFAULT_MASK rejects BEFORE backend (`backend.calls() == 0`) — `wr01_event_rejects_before_prompt_on_out_of_allowlist_mask` PASS
  - Mutex with mask outside DEFAULT_MASK rejects BEFORE backend (`backend.calls() == 0`) — `wr01_mutex_rejects_before_prompt_on_out_of_allowlist_mask` PASS
  - JobObject with JOB_OBJECT_TERMINATE rejects BEFORE backend (`backend.calls() == 0`) — `wr01_job_object_rejects_before_prompt_on_terminate_mask` PASS + narrative comment added to pre-existing `handle_denies_job_object_with_terminate_mask_no_profile_widening` test
  - Pipe with direction=ReadWrite under default profile rejects AFTER backend (`backend.calls() == 1`), G-04-wrapped reason — `wr01_pipe_rejects_after_prompt_on_readwrite_default_profile` PASS
  - Socket with privileged port empirically verified as POST-prompt (`backend.calls() == 1`), reason contains "broker failed:" + "privileged port" — `wr01_socket_privileged_port_rejects_after_prompt_empirical` PASS
  - Module-level docstring at `capability_handler_tests` documents the WR-01 verdict matrix for all 6 HandleKinds with per-kind enforcement sites + the D-14 deferral note — present
- All `must_haves.artifacts` present:
  - 5 new deny-stage tests in `capability_handler_tests` module, all named with the `wr01_` prefix — 5/5 PASS
  - Module-level docstring recording the WR-01 verdict (Phase 18.1 D-14) — present at lines ~2008-2049
- All `must_haves.key_links` wired:
  - `wr01_event_rejects_before_prompt` → `handle_windows_supervisor_message` mask-validation gate via `backend.calls() == 0` assertion — wired; the assertion fails if the gate is moved post-broker
- No `.unwrap()` / `.expect()` introduced in production code. The test module has module-level `#[allow(clippy::unwrap_used)]` (pre-existing convention). `expect(...)` used in test control flow (`.expect("dispatch")`, `.expect("drain")`) follows the pre-existing pattern — the `#[allow(clippy::unwrap_used)]` module attribute covers it.
- `cargo clippy -p nono-cli --all-targets -- -D warnings -D clippy::unwrap_used` exits 0
- `cargo fmt --all -- --check` exits 0
- `cargo build --workspace` exits 0
