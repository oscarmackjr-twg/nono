---
status: resolved
phase: 18.1-extended-ipc-gaps
source: [18.1-VERIFICATION.md]
started: 2026-04-21T00:00:00Z
updated: 2026-04-21T16:10:00Z
---

## Current Test

[complete — all 3 items resolved]

## Tests

### 1. Live end-to-end re-run of `nono run --profile claude-code -- aipc-demo.exe` on a Windows 11 host
expected: All 5 AIPC HandleKinds (Event, Mutex, Pipe, Socket, JobObject) successfully broker handles end-to-end. CONIN$ approval prompts render the D-04 per-kind templates (not the pre-18.1 generic Path/Access/Reason block). G-03's `os error 2 (ERROR_FILE_NOT_FOUND)` on the JobObject broker no longer occurs. Child SDK does NOT surface "supervisor granted but returned no ResourceGrant" (G-04). This is the closing empirical evidence for the whole phase.
result: pass — 2026-04-21 15:51 UTC re-run against rebuilt `nono.exe` (windows-squash HEAD `44cfebf`). All 5 kinds rendered D-04 per-kind prompts (`Grant event access? name=... access=signal`, `Grant mutex access? ... access=release`, `Grant pipe access? ... direction=read`, `Grant socket access? proto=tcp host=127.0.0.1 port=8080 role=connect`, `Grant Job Object access? ... access=query`); every broker returned `-> OK, handle granted`; demo exited with `[demo] all 5 request_* methods succeeded`. No `os error 2` on JobObject (G-03 empirically closed). No "supervisor granted but returned no ResourceGrant" (G-04 empirically closed).

### 2. Widened-profile live exercise of G-06 via a custom profile with `capabilities.aipc.pipe: ["read+write"]`
expected: A child that requests a pipe with direction=ReadWrite under the widened profile is prompted (reaches the approval backend) and, upon approval, receives a valid pipe handle. Removing the widening and re-running shows the request rejected before any CONIN$ prompt.
result: pass — 2026-04-21 16:09 UTC dual-run A/B test. Demo modified to request `PipeDirection::ReadWrite` (was `Read`); rebuilt. Profile `C:\Users\omack\aipc-widen.json` created (`extends: claude-code`, `capabilities.aipc.pipe: ["read+write"]`). Widened run: Pipe prompt rendered `direction=read+write`, broker granted handle, demo exited `all 5 request_* methods succeeded`. Default (`claude-code`) run: Pipe prompt rendered `direction=read+write` (WR-01 deviation — direction check is post-approval per Phase 18.1 D-14 module docstring), approval accepted, broker rejected with `Error: SandboxInit("supervisor denied capability: broker failed: ... pipe direction ReadWrite not in resolved allowlist (profile widening required)")`. The delta — same binary, same request, different profile, opposite outcomes — empirically proves `Profile::resolve_aipc_allowlist()` is wired end-to-end through `SupervisedRuntimeContext.loaded_profile → SupervisorConfig.aipc_allowlist → WindowsSupervisorRuntime.resolved_aipc_allowlist` and replaces the Plan 18-03 `AipcResolvedAllowlist::default()` seed. Revert semantics confirmed: removing widening returns the request to the default-deny path, no stale caching, no profile-instance bleed. Note on reject-stage wording: Plan 18.1-03 must_have truth #5 described the default-profile reject as "BEFORE prompt" but the observed stage is AFTER prompt per the documented WR-01 deviation; the widening semantic (the actual subject of the test) works correctly — only the plan's stage-phrasing was imprecise.

### 3. WR-02 CompareObjectHandles empirical test on an EDR-instrumented Windows host (CrowdStrike, Defender ATP, or similar)
expected: `CompareObjectHandles` returns non-zero for same-object and zero for distinct-object under EDR Job-Object-telemetry hooks; no fail-open observed.
result: skipped — unchanged from Phase 18 (carried forward). Deferred to v3.0 per `.planning/phases/18.1-extended-ipc-gaps/18.1-CONTEXT.md § Deferred`. No EDR-instrumented runner available in the v2.1 development environment; flagged here only for Phase-18 regression-guard traceability.

## Summary

total: 3
passed: 2
issues: 0
pending: 0
skipped: 1
blocked: 0

## Gaps
