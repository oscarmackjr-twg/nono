---
phase: 01-windows-control-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [crates/nono-cli/src/session_commands_windows.rs, crates/nono-cli/src/exec_strategy_windows/supervisor.rs]
autonomous: true
requirements: [SUPV-03, SUPV-04]
must_haves:
  truths:
    - "nono ps correctly identifies exited sessions on Windows if the Job Object is missing"
    - "nono stop terminates an agent and its entire process tree using the Hybrid strategy"
  artifacts:
    - path: "crates/nono-cli/src/session_commands_windows.rs"
      provides: "Implementation of ps and stop commands"
    - path: "crates/nono-cli/src/exec_strategy_windows/supervisor.rs"
      provides: "Named Pipe IPC server for session control"
  key_links:
    - from: "nono ps"
      to: "OpenJobObjectW"
      via: "Liveness verification"
    - from: "nono stop"
      to: "TerminateJobObject"
      via: "Forced termination fallback"
---

<objective>
Implement the primary control commands `nono ps` and `nono stop` for Windows using Job Object liveness and a hybrid termination strategy.

Purpose: Provide the user with visibility and control over agent processes.
Output: Working `ps` and `stop` commands on Windows.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-windows-control-foundation/01-RESEARCH.md
@crates/nono-cli/src/session_commands_windows.rs
@crates/nono-cli/src/exec_strategy_windows/supervisor.rs
@crates/nono-cli/src/session.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Object-Verified nono ps</name>
  <files>crates/nono-cli/src/session_commands_windows.rs</files>
  <behavior>
    - `run_ps()`: Loads sessions from disk.
    - If `job_object_name` is present, use `OpenJobObjectW(JOB_OBJECT_QUERY, FALSE, name)`.
    - If `ERROR_FILE_NOT_FOUND`, mark session as `Exited` (-1) in memory and on disk.
    - If successful, call `QueryInformationJobObject` to get the list of active PIDs.
  </behavior>
  <action>
    Implement the "File-First, Object-Verified" discovery logic in `run_ps`.
    Ensure it handles the variable-length `JOBOBJECT_BASIC_PROCESS_ID_LIST` correctly as described in research.
  </action>
  <verify>
    <automated>cargo run -p nono-cli -- ps</automated>
  </verify>
  <done>`nono ps` accurately reflects the liveness of Windows agents.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Named Pipe IPC for Supervisor</name>
  <files>crates/nono-cli/src/exec_strategy_windows/supervisor.rs</files>
  <action>
    Implement a simple Named Pipe server using `tokio::net::windows::named_pipe`.
    Pipe name: `\\.\pipe\nono-session-{id}`.
    Apply the SDDL string `D:P(A;;GA;;;OW)(D;;GA;;;WD)` for security.
    The server should listen for a `Terminate` message and trigger a clean shutdown of the agent.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --lib exec_strategy_windows::supervisor::tests</automated>
  </verify>
  <done>Secure Named Pipe IPC server implemented for session control.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Hybrid nono stop</name>
  <files>crates/nono-cli/src/session_commands_windows.rs</files>
  <action>
    Implement the `run_stop` command logic.
    1. Send `Terminate` request via the session's Named Pipe.
    2. Wait up to 5 seconds for the process to exit.
    3. Fallback: Call `TerminateJobObject(h_job, 1)` to force stop the entire tree.
  </action>
  <verify>
    <automated>cargo run -p nono-cli -- stop {session_id}</automated>
  </verify>
  <done>`nono stop` reliably terminates agents using the Hybrid strategy.</done>
</task>

</tasks>

<verification>
Start an agent. Verify it appears in `nono ps`. Run `nono stop` and verify it disappears from `nono ps` and no processes from the tree are left running.
</verification>

<success_criteria>
- `nono ps` shows real-time status based on Job Object presence.
- `nono stop` works even if the agent is unresponsive (via `TerminateJobObject`).
- Security: Named Pipe only allows the owner process to connect.
</success_criteria>

<output>
After completion, create `.planning/phases/01-windows-control-foundation/01-03-SUMMARY.md`
</output>
