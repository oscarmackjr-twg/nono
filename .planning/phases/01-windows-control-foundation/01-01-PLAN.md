---
phase: 01-windows-control-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [crates/nono-cli/Cargo.toml, crates/nono-cli/src/exec_strategy_windows/mod.rs, crates/nono-cli/src/setup.rs]
autonomous: true
requirements: [DEPL-02]
must_haves:
  truths:
    - "nono setup --check-only shows Job Object creation permissions status"
    - "nono setup --check-only shows Token Integrity level support status"
    - "nono setup --check-only shows BFE (Windows Filtering Platform) service status"
  artifacts:
    - path: "crates/nono-cli/src/exec_strategy_windows/mod.rs"
      provides: "Windows diagnostic probe logic"
    - path: "crates/nono-cli/src/setup.rs"
      provides: "Diagnostic report integration"
  key_links:
    - from: "crates/nono-cli/src/setup.rs"
      to: "crates/nono-cli/src/exec_strategy_windows/mod.rs"
      via: "Function calls to probes"
---

<objective>
Establish the foundational Win32 features and implement the required diagnostic checks for the Windows environment.

Purpose: Ensure the system meets the security requirements before attempting to run agents.
Output: Updated dependencies and a functional `nono setup --check-only` report for Windows.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-windows-control-foundation/01-RESEARCH.md
@crates/nono-cli/Cargo.toml
@crates/nono-cli/src/setup.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Cargo.toml with Win32 features</name>
  <files>crates/nono-cli/Cargo.toml</files>
  <action>
    Add `Win32_System_Services` to the `windows-sys` features for the Windows target.
    This is required for querying the BFE service status.
    Ensure `Win32_System_Threading`, `Win32_Security`, and `Win32_System_JobObjects` are also present per research.
  </action>
  <verify>
    <automated>cargo check -p nono-cli</automated>
  </verify>
  <done>Cargo.toml updated and project compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Windows Diagnostic Probes</name>
  <files>crates/nono-cli/src/exec_strategy_windows/mod.rs</files>
  <behavior>
    - `probe_job_object_permissions()`: Returns Ok(()) if a named Job Object can be created in `Local\` namespace.
    - `probe_integrity_level_support()`: Returns Ok(()) if the process can adjust its token to a lower integrity level.
    - `probe_bfe_service_status()`: Returns Ok(()) if the 'BFE' (Base Filtering Engine) service is RUNNING.
  </behavior>
  <action>
    Implement the probes using `windows-sys` 0.59.
    Use `CreateJobObjectW` with a name like `Local\nono-setup-probe-{random}` for the permissions check.
    Use `OpenSCManagerW` and `QueryServiceStatusEx` for the BFE check.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --lib exec_strategy_windows::mod::tests</automated>
  </verify>
  <done>Diagnostic probes implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate probes into setup report</name>
  <files>crates/nono-cli/src/setup.rs</files>
  <action>
    Update `test_windows_support` and `print_check_only_summary` to include the results of the 3 new probes.
    Ensure the output is clear and follows the existing diagnostic report style.
  </action>
  <verify>
    <automated>cargo run -p nono-cli -- setup --check-only</automated>
  </verify>
  <done>`nono setup --check-only` displays the new Windows-specific checks.</done>
</task>

</tasks>

<verification>
Run `nono setup --check-only` on a Windows machine and verify all three new checks are present and reporting correctly.
</verification>

<success_criteria>
- `nono setup --check-only` includes Job Object, Integrity, and BFE status.
- No regression in existing setup checks.
- Code follows `windows-sys` 0.59 patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/01-windows-control-foundation/01-01-SUMMARY.md`
</output>
