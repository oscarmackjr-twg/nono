---
phase: 01-windows-control-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [crates/nono-cli/src/exec_strategy_windows/launch.rs, crates/nono-cli/src/session.rs, crates/nono-cli/src/exec_strategy_windows/supervisor.rs]
autonomous: true
requirements: [SUPV-05]
must_haves:
  truths:
    - "Windows Job Objects for nono sessions are named using the session ID"
    - "Session JSON files on Windows contain the Job Object name"
  artifacts:
    - path: "crates/nono-cli/src/exec_strategy_windows/launch.rs"
      provides: "Named Job Object creation logic"
    - path: "crates/nono-cli/src/session.rs"
      provides: "Updated SessionRecord with job_object_name"
  key_links:
    - from: "crates/nono-cli/src/exec_strategy_windows/launch.rs"
      to: "Local\\nono-session-{id}"
      via: "CreateJobObjectW"
---

<objective>
Implement persistent process lifecycle management using named Job Objects. This ensures that agents can be discovered and managed even after the initiating CLI process terminates.

Purpose: Foundation for session discovery and atomic process tree control.
Output: Named Job Object implementation and updated session metadata.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-windows-control-foundation/01-RESEARCH.md
@crates/nono-cli/src/exec_strategy_windows/launch.rs
@crates/nono-cli/src/session.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SessionRecord for Windows</name>
  <files>crates/nono-cli/src/session.rs</files>
  <action>
    Add `job_object_name: Option<String>` to the `SessionRecord` struct.
    Ensure it is serialized to JSON.
    Update `SessionGuard` or its creation logic to allow setting the job object name on Windows.
  </action>
  <verify>
    <automated>cargo check -p nono-cli</automated>
  </verify>
  <done>`SessionRecord` supports storing the Windows Job Object name.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Named Job Object creation</name>
  <files>crates/nono-cli/src/exec_strategy_windows/launch.rs</files>
  <action>
    Modify `create_process_containment` to accept an optional `session_id: &str`.
    If provided, use it to name the Job Object: `Local\nono-session-{session_id}`.
    Use a helper to convert the string to null-terminated UTF-16 for `CreateJobObjectW`.
    Maintain existing `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` configuration.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --lib exec_strategy_windows::launch::tests</automated>
  </verify>
  <done>Job Objects are correctly named using the session ID on Windows.</done>
</task>

<task type="auto">
  <name>Task 3: Update Windows launch flow to use Named Jobs</name>
  <files>crates/nono-cli/src/execution_runtime.rs, crates/nono-cli/src/launch_runtime.rs</files>
  <action>
    Ensure that when starting a session on Windows, the `session_id` is passed to the containment creation.
    Update the `SessionRecord` creation to include the resulting `Local\nono-session-{id}` name.
  </action>
  <verify>
    <automated>cargo run -p nono-cli -- run -- cmd /c echo test</automated>
  </verify>
  <done>Launched processes are assigned to named Job Objects and recorded in session metadata.</done>
</task>

</tasks>

<verification>
Start an agent on Windows. Check the session JSON in `~/.nono/sessions/` and verify `job_object_name` is present. Use a tool like Process Hacker or WinObj to verify the Job Object `Local\nono-session-{id}` exists.
</verification>

<success_criteria>
- All Windows nono sessions have a corresponding named Job Object.
- Session metadata includes the Job Object name.
- Processes are correctly assigned to these Jobs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-windows-control-foundation/01-02-SUMMARY.md`
</output>
