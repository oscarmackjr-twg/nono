---
phase: 02-persistent-sessions-detach-attach
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/tests/test_windows_detach.ps1
  - scripts/tests/test_windows_attach.ps1
autonomous: true
requirements: [SUPV-01, SUPV-02]
must_haves:
  truths:
    - "Integration test scripts for Windows detach/attach exist"
  artifacts:
    - path: "scripts/tests/test_windows_detach.ps1"
      provides: "Automated verification of supervisor daemonization"
    - path: "scripts/tests/test_windows_attach.ps1"
      provides: "Automated verification of session re-attachment"
---

<objective>
Create the integration test harness for Phase 2. These scripts will serve as the primary automated verification for the detachment and attachment functionality on Windows.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-sessions-detach-attach/02-RESEARCH.md
@.planning/phases/02-persistent-sessions-detach-attach/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Detach Integration Test</name>
  <files>scripts/tests/test_windows_detach.ps1</files>
  <action>
    Create a PowerShell script that:
    1. Starts a `nono` agent with `--detach`.
    2. Verifies the CLI exits immediately.
    3. Verifies a background process (supervisor) is still running.
    4. Verifies the session log file is created and contains initial output.
    5. Cleans up by stopping the agent.
  </action>
  <verify>
    <automated>Test-Path scripts/tests/test_windows_detach.ps1</automated>
  </verify>
  <done>scripts/tests/test_windows_detach.ps1 created.</done>
</task>

<task type="auto">
  <name>Task 2: Create Attach Integration Test</name>
  <files>scripts/tests/test_windows_attach.ps1</files>
  <action>
    Create a PowerShell script that:
    1. Starts a detached `nono` agent.
    2. Waits for some output to be generated.
    3. Runs `nono attach` and captures the output.
    4. Verifies the captured output contains both the historical log data and live updates.
    5. Verifies that sending input to the attached session reaches the agent.
    6. Cleans up.
  </action>
  <verify>
    <automated>Test-Path scripts/tests/test_windows_attach.ps1</automated>
  </verify>
  <done>scripts/tests/test_windows_attach.ps1 created.</done>
</task>

</tasks>

<verification>
Ensure both test scripts exist in the `scripts/tests/` directory.
</verification>

<success_criteria>
- `scripts/tests/test_windows_detach.ps1` exists.
- `scripts/tests/test_windows_attach.ps1` exists.
</success_criteria>

<output>
After completion, create `.planning/phases/02-persistent-sessions-detach-attach/02-00-SUMMARY.md`
</output>
