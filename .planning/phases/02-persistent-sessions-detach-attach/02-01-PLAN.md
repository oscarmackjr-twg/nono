---
phase: 02-persistent-sessions-detach-attach
plan: 01
type: execute
wave: 1
depends_on: [02-persistent-sessions-detach-attach-00]
files_modified:
  - crates/nono-cli/Cargo.toml
  - crates/nono-cli/src/pty_proxy_windows.rs
  - crates/nono-cli/src/exec_strategy_windows/supervisor.rs
autonomous: true
requirements: [SUPV-01, SUPV-02]
must_haves:
  truths:
    - "ConPTY can be initialized via Win32 API using windows-sys"
    - "Named Pipes are created with owner-only SDDL security"
  artifacts:
    - path: "crates/nono-cli/src/pty_proxy_windows.rs"
      provides: "PseudoConsole (ConPTY) lifecycle management"
    - path: "crates/nono-cli/src/exec_strategy_windows/supervisor.rs"
      provides: "Secured IPC pipes (Control and Data)"
  key_links:
    - from: "crates/nono-cli/src/exec_strategy_windows/supervisor.rs"
      to: "crates/nono-cli/src/pty_proxy_windows.rs"
      via: "ConPTY output streaming to Data Pipe"
---

<objective>
Implement the core terminal emulation and secure communication channels for persistent Windows sessions. This foundation is required for both detaching from and re-attaching to agents.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-sessions-detach-attach/02-RESEARCH.md
@.planning/phases/02-persistent-sessions-detach-attach/02-CONTEXT.md
@.planning/phases/02-persistent-sessions-detach-attach/02-VALIDATION.md

<interfaces>
From crates/nono-cli/src/pty_proxy_windows.rs (stubs):
```rust
pub struct PtyPair;
pub struct PtyProxy;
pub fn open_pty() -> Result<PtyPair>;
```

From crates/nono-cli/src/exec_strategy_windows/supervisor.rs:
```rust
pub(super) struct WindowsSupervisorRuntime { ... }
impl WindowsSupervisorRuntime {
    pub(super) fn initialize(supervisor: &SupervisorConfig<'_>) -> Result<Self>;
    fn start_control_pipe_server(&self) -> Result<()>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Win32_System_Console feature</name>
  <files>crates/nono-cli/Cargo.toml</files>
  <action>
    Add "Win32_System_Console" to the `windows-sys` features list for the `target.'cfg(target_os = "windows")'.dependencies` section. This is required for ConPTY APIs like `CreatePseudoConsole`.
  </action>
  <verify>
    <automated>cargo check -p nono-cli</automated>
  </verify>
  <done>Cargo.toml updated and crate compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ConPTY Wrapper</name>
  <files>crates/nono-cli/src/pty_proxy_windows.rs</files>
  <action>
    Replace stubs with a manual implementation of ConPTY using `windows-sys`.
    - Implement `PtyPair` to hold the PseudoConsole handle and I/O pipe handles.
    - Implement `open_pty` to create input/output pipes and call `CreatePseudoConsole`.
    - Ensure handles are NOT inherited by default (use `SECURITY_ATTRIBUTES` with `bInheritHandle = 0`).  
    - Implement `close_pseudo_console` to safely tear down the PTY.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --src pty_proxy_windows.rs</automated>
  </verify>
  <done>ConPTY wrapper implemented and basic creation/closure verified.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Secure Pipe Helper and Dual-Pipe Setup</name>
  <files>crates/nono-cli/src/exec_strategy_windows/supervisor.rs</files>
  <action>
    - Implement `create_secure_pipe` helper using `ConvertStringSecurityDescriptorToSecurityDescriptorW` with SDDL: `D:P(A;;GA;;;SY)(A;;GA;;;BA)(A;;GA;;;OW)`.
    - Update `WindowsSupervisorRuntime` to initialize both Control Pipe (`nono-session-{id}`) and Data Pipe (`nono-data-{id}`).
    - Integrate the Data Pipe with the ConPTY output from Task 2.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --src exec_strategy_windows/supervisor.rs</automated>
  </verify>
  <done>Supervisor creates both Control and Data pipes with owner-only access.</done>
</task>

</tasks>

<verification>
Check that `cargo build` succeeds and that the foundation is ready for daemonization and attachment.
Note: Integration tests from 02-00 may still fail at this stage as daemonization is not yet implemented.
</verification>

<success_criteria>
- `windows-sys` has `Win32_System_Console` enabled.
- `pty_proxy_windows.rs` provides functional `open_pty`.
- Supervisor initializes with secured Named Pipes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-persistent-sessions-detach-attach/02-01-SUMMARY.md`
</output>
