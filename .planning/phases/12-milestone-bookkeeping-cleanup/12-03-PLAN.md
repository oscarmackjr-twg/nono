---
phase: 12-milestone-bookkeeping-cleanup
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "`make ci` passes on the host after the planning-trail edits and code fixes from plans 12-01 and 12-02 are applied"
    - "No new clippy warnings, format diffs, or test failures introduced by Phase 12 edits"
  artifacts: []
  key_links:
    - from: "make ci"
      to: "clippy + fmt-check + tests"
      via: "Makefile target"
      pattern: "make ci"
---

<objective>
Verify that the Phase 12 bookkeeping edits (12-01 planning-trail updates + 12-02 code fixes) do not regress the CI gate. This is a pure verification plan — no file edits.

Purpose: Success criterion 6 from ROADMAP.md Phase 12 requires `make ci` to still pass after the code changes. Because 12-02 only touches one module doc comment and one test file, the probability of regression is near zero, but we verify it explicitly so the milestone can be archived cleanly.
Output: Confirmation that `make ci` exits 0 (or a precise failure report if it does not).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/12-milestone-bookkeeping-cleanup/12-01-PLAN.md
@.planning/phases/12-milestone-bookkeeping-cleanup/12-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `make ci` and confirm it passes</name>
  <files></files>
  <read_first>
    - Makefile (to confirm `make ci` composition — clippy + fmt-check + tests)
    - .planning/phases/12-milestone-bookkeeping-cleanup/12-02-PLAN.md (to confirm scope of code changes under verification)
    - crates/nono/src/sandbox/windows.rs (confirm 12-02 Task 1 was applied cleanly)
    - crates/nono-cli/tests/wfp_port_integration.rs (confirm 12-02 Task 2 was applied cleanly)
  </read_first>
  <action>
    Run `make ci` from the repository root. This target runs clippy (strict `-D warnings -D clippy::unwrap_used`), `cargo fmt --check`, and the full test suite per CLAUDE.md.

    Expected outcome: exit code 0.

    If the host is not Windows (the wfp_port_integration.rs file is gated behind `#![cfg(target_os = "windows")]`), the integration test file will be skipped during cross-platform compilation — this is expected and does not count as a regression.

    If `make ci` fails, capture the exact failing step (clippy / fmt / test name) and the error output, then STOP and surface the failure in the task SUMMARY. Do not attempt to auto-fix — that belongs in a follow-up plan. The executor must fail the task rather than silently skip, per the CLAUDE.md "fail secure" principle applied to verification.

    If `make ci` is not available on the host (e.g. Windows bash environment without `make`), fall back to the individual commands from CLAUDE.md:

    ```
    cargo clippy --all-targets --all-features -- -D warnings -D clippy::unwrap_used
    cargo fmt --all -- --check
    cargo test --workspace
    ```

    All three must pass.
  </action>
  <verify>
    <automated>make ci 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `make ci` exits with status 0, OR the fallback sequence (clippy + fmt-check + test) all exit 0
    - No new clippy warnings or errors attributable to `crates/nono/src/sandbox/windows.rs` or `crates/nono-cli/tests/wfp_port_integration.rs`
    - No `cargo fmt` diff in either file after formatting
    - Task SUMMARY records the exact command used (make ci vs fallback) and the final exit status
  </acceptance_criteria>
  <done>`make ci` (or the fallback clippy+fmt+test sequence) passes cleanly; Phase 12 success criterion 6 is satisfied.</done>
</task>

</tasks>

<verification>
- `make ci` exit code is 0
- No regression introduced by Phase 12 code changes
</verification>

<success_criteria>
- CI gate passes after all Phase 12 edits are applied
- Milestone can proceed to Phase 13 (UAT) archive without a broken CI trail
</success_criteria>

<output>
After completion, create `.planning/phases/12-milestone-bookkeeping-cleanup/12-03-SUMMARY.md`
</output>
