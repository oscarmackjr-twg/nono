---
phase: 21-windows-single-file-grants
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [WSFG-01, WSFG-02, WSFG-03]
must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` contains a `## WSFG — Windows Single-File Grants` section with three requirement IDs (WSFG-01, WSFG-02, WSFG-03)."
    - "Each WSFG-XX entry has a `**Maps to:** Phase 21` trailer, matching the existing RESL/AIPC/ATCH/CLEAN/UPST entry style."
    - "`.planning/ROADMAP.md` Phase 21 entry carries a `**Requirements:**` line listing WSFG-01..03."
    - "Downstream plans 21-02..21-05 can reference WSFG-XX IDs in their frontmatter without dangling references."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "WSFG requirement IDs (WSFG-01 enforcement primitive, WSFG-02 lifecycle + error surface, WSFG-03 UAT close-out)"
      contains: "## WSFG"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 21 requirements traceability line"
      contains: "WSFG-01"
  key_links:
    - from: ".planning/phases/21-windows-single-file-grants/*-PLAN.md"
      to: ".planning/REQUIREMENTS.md § WSFG"
      via: "requirements: [WSFG-XX] frontmatter"
      pattern: "WSFG-0[123]"
---

<objective>
Add the three Windows Single-File Filesystem Grants requirement IDs (WSFG-01, WSFG-02, WSFG-03) to `.planning/REQUIREMENTS.md` and update `.planning/ROADMAP.md` Phase 21 entry to reference them.

Purpose: Phase 21 currently has **no REQ-ID in REQUIREMENTS.md** — the roadmap entry (line 102) references `SingleFileGrant` and `WriteOnlyDirectoryGrant` informally but there is no formal acceptance-criteria section. Downstream plans 21-02..21-05 cannot populate their `requirements:` frontmatter until WSFG IDs exist. This plan mirrors the Phase 20 Plan 20-01 precedent (UPST-01..04 were added to REQUIREMENTS.md as the first plan of that phase).

Output: A `## WSFG — Windows Single-File Grants` section in REQUIREMENTS.md with three structured acceptance-criteria entries, and a `**Requirements:**` line in the ROADMAP.md Phase 21 entry. No code touched, no tests run — this plan is pure planning bookkeeping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-windows-single-file-grants/21-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Existing requirement-entry shape in REQUIREMENTS.md (the template to mirror). -->
<!-- Extracted from .planning/REQUIREMENTS.md § UPST-01 (lines 305-315): -->

### UPST-01: Security upgrade + workspace version realignment

**What:** `rustls-webpki` transitive upgrade to `0.103.12` ...

**Acceptance:**
1. `cargo tree -i rustls-webpki` shows ...
2. Every workspace crate ... reports `0.37.1` via `cargo pkgid`.
3. `cargo build --workspace` and `cargo test --workspace --all-features` exit 0 on the Windows host.
4. Phase 15 5-row detached-console smoke (...) passes unchanged.

**Maps to:** Phase 20 Plan 20-01.

<!-- Existing roadmap-entry `**Requirements:**` shape (the template to mirror). -->
<!-- Extracted from .planning/ROADMAP.md § v2.1 block (line 53): -->

**Requirements (10):** RESL-01..04, AIPC-01, ATCH-01, CLEAN-01..04. See `.planning/REQUIREMENTS.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WSFG section to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full read — section style, numbered acceptance lists, "Maps to:" trailer)
    - .planning/phases/21-windows-single-file-grants/21-CONTEXT.md (D-01..D-07, I-01..I-03)
  </read_first>
  <action>
Append a new `## WSFG — Windows Single-File Grants` section to `.planning/REQUIREMENTS.md`. Place it immediately after the existing `## UPST — Upstream Parity Sync` section (end of file — it becomes the final top-level section).

Structure the section exactly like UPST/RESL (Context paragraph + three REQ-XX sub-sections with `**What:**`, `**CLI:** / **Enforcement:** / **Security:** / **Acceptance:**` / `**Maps to:**` blocks). Use these verbatim contents:

---

## WSFG — Windows Single-File Grants

Context: The Windows filesystem sandbox (`crates/nono/src/sandbox/windows.rs`) currently rejects `FsCapability { is_file: true, .. }` with `WindowsUnsupportedIssueKind::SingleFileGrant` and directory-scope `AccessMode::Write` with `WindowsUnsupportedIssueKind::WriteOnlyDirectoryGrant`. This blocks `nono run --profile claude-code` on Windows because the `claude-code` profile pulls in the `git_config` policy group whose five members (`.gitconfig`, `.gitignore_global`, `.config/git/config`, `.config/git/ignore`, `.config/git/attributes`) are all single-file grants. Phase 21 makes both enforceable via per-file / per-directory Low IL mandatory-label ACEs, preserving fail-closed semantics.

### WSFG-01: Per-file and write-only-directory label enforcement

**What:** `compile_filesystem_policy` emits `WindowsFilesystemRule` entries (no longer pushes `unsupported`) for single-file grants in all three access modes (`Read`, `Write`, `ReadWrite`) AND for write-only directory grants (`AccessMode::Write` on a directory). The enforcement primitive is a `SYSTEM_MANDATORY_LABEL_ACE` at `SECURITY_MANDATORY_LOW_RID` applied via `SetNamedSecurityInfoW(path, SE_FILE_OBJECT, LABEL_SECURITY_INFORMATION, ..)`. The ACE `Mask` field encodes the access mode per the CONTEXT.md D-01 mask-encoding table: `Read` → `NO_WRITE_UP | NO_EXECUTE_UP`, `Write` → `NO_READ_UP | NO_EXECUTE_UP`, `ReadWrite` → `NO_EXECUTE_UP`.

**Enforcement:**
- Windows: `SetNamedSecurityInfoW` with `LABEL_SECURITY_INFORMATION`, mode-derived mask, fail-closed on any Win32 error.
- Linux / macOS: no change (Landlock / Seatbelt already support file-scope grants natively).

**Security:**
- Fail-closed on any label-apply error (I-01). Never silently degrade to labeling a parent directory or dropping the grant.
- Path-component comparison throughout (I-03). No `str::starts_with` on paths.
- D-21 Windows-invariance held (I-02): cross-platform files stay byte-identical modulo the additive `NonoError::LabelApplyFailed` variant.

**Acceptance:**
1. `compile_filesystem_policy` on a `CapabilitySet` containing one `FsCapability::new_file(p, Read)` emits exactly one `WindowsFilesystemRule { is_file: true, access: Read, path: p }` and zero `WindowsUnsupportedIssue` entries.
2. The same assertion for `Write` and `ReadWrite` access modes on single files.
3. `compile_filesystem_policy` on a `CapabilitySet` containing `FsCapability::new_dir(p, Write)` emits exactly one rule (no unsupported entry).
4. A Windows integration test creates `tempdir()/only-this.txt`, applies `apply(&caps)` with `FsCapability::new_file(&file, Read)`, reads the file's mandatory-label ACE via `GetNamedSecurityInfoW`, and asserts RID == `SECURITY_MANDATORY_LOW_RID` and mask == `NO_WRITE_UP | NO_EXECUTE_UP`.
5. Silent-degradation regression test: grant single file X; assert `low_integrity_label_rid(X.parent())` equals its pre-grant value (parent directory's integrity label was NOT mutated).

**Maps to:** Phase 21 Plans 21-02, 21-03, 21-05.

---

### WSFG-02: Label lifecycle + error surface

**What:** The supervisor applies labels at launch and reverts them at session exit / detach-and-stop. Apply is idempotent (if the file is already at Low IL for reasons unrelated to this session, we no-op the apply AND skip the revert — no state to restore). Write-mode grants to non-existent files are rejected at `CapabilitySet` construction time (`FsCapability::new_file` returns `PathNotFound`). On `SetNamedSecurityInfoW` failure, `apply()` returns `NonoError::LabelApplyFailed { path, hresult, hint }` (new variant) carrying the exact path, Win32 HRESULT, and a human-actionable hint.

**Security:**
- Revert on session end restores user's files to pre-grant integrity state (D-02).
- Concurrent sessions: "last session out restores" — accepted trade-off.
- `ERROR_ACCESS_DENIED` hint: "Ensure the target file is writable by the current user and is on NTFS (not ReFS or a network share)."
- Unknown HRESULT: include raw hex value for support triage.

**Acceptance:**
1. `NonoError::LabelApplyFailed { path, hresult, hint }` exists in `crates/nono/src/error.rs` with `#[error(..)]` attribute via `thiserror`.
2. `apply()` returns `LabelApplyFailed` (NOT `UnsupportedPlatform`) when `SetNamedSecurityInfoW` returns non-zero.
3. An RAII guard type (`AppliedLabelsGuard` or equivalent, in `exec_strategy_windows/`) records the per-file pre-grant label state and reverts on `Drop`. Test: label applied, guard dropped, file integrity restored.
4. `FsCapability::new_file(path, Write)` on a non-existent path returns `NonoError::PathNotFound` (no change — existing behavior is the contract).
5. The `LabelApplyFailed` error's `Display` output includes both the path and the HRESULT (hex-formatted).

**Maps to:** Phase 21 Plans 21-02, 21-04.

---

### WSFG-03: Phase 18 UAT close-out

**What:** The Phase 18 AIPC UAT cookbook (`docs/cli/internals/aipc-uat-cookbook.mdx`) Path B + Path C are re-run end-to-end on the Windows host after Plans 21-02..21-04 land. All 4 live-CONIN$ tests in `.planning/phases/18-extended-ipc/18-HUMAN-UAT.md` transition from `result: [blocked — see Blocker above; retry after Phase 21]` to concrete `pass` or `issue` verdicts. If the UAT surfaces new issues they are routed to follow-up quick-tasks (not reopened in Phase 21).

**Acceptance:**
1. `.planning/phases/18-extended-ipc/18-HUMAN-UAT.md` frontmatter `status:` is no longer `blocked`; `blocked_on: phase-21-windows-single-file-grants` is removed.
2. All 4 test entries in `18-HUMAN-UAT.md` have `result: [pass]` or `result: [issue — <short description>]`.
3. `nono run --profile claude-code -- <aipc-demo>` launches successfully on the Windows host (the `git_config` group's 5 single-file grants no longer trip a launch-time error).
4. The Summary block at the bottom of `18-HUMAN-UAT.md` reflects the new totals (passed + issues + blocked where blocked = 0).

**Maps to:** Phase 21 Plan 21-05.

---

Do NOT modify the existing `## Out of Scope (v2.1)` section — WSFG closes an in-scope Windows gap, not a v2.1 scope expansion.
  </action>
  <verify>
    <automated>grep -c "^### WSFG-0[123]:" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains literal string `## WSFG — Windows Single-File Grants` (count >= 1 via grep)
    - `.planning/REQUIREMENTS.md` contains `### WSFG-01:` (count == 1)
    - `.planning/REQUIREMENTS.md` contains `### WSFG-02:` (count == 1)
    - `.planning/REQUIREMENTS.md` contains `### WSFG-03:` (count == 1)
    - `.planning/REQUIREMENTS.md` contains `**Maps to:** Phase 21 Plans 21-02, 21-03, 21-05.` (count == 1)
    - `.planning/REQUIREMENTS.md` contains `**Maps to:** Phase 21 Plans 21-02, 21-04.` (count == 1)
    - `.planning/REQUIREMENTS.md` contains `**Maps to:** Phase 21 Plan 21-05.` (count == 1)
    - `.planning/REQUIREMENTS.md` contains the string `SECURITY_MANDATORY_LOW_RID` inside the WSFG-01 acceptance list (proves D-01 primitive is encoded)
    - `.planning/REQUIREMENTS.md` contains the string `LabelApplyFailed` inside the WSFG-02 acceptance list (proves D-04 error shape is encoded)
    - `.planning/REQUIREMENTS.md` contains the phrase "last session out restores" (proves D-02 concurrent-session disposition is encoded)
    - `.planning/REQUIREMENTS.md` contains the string `18-HUMAN-UAT.md` inside the WSFG-03 acceptance list (proves Phase 18 UAT link is encoded)
  </acceptance_criteria>
  <done>Three WSFG-XX entries landed in REQUIREMENTS.md with the exact structural shape of UPST/RESL/AIPC entries; no other section modified.</done>
</task>

<task type="auto">
  <name>Task 2: Add Requirements line to ROADMAP.md Phase 21 entry</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read the Phase 21 block at ~line 102, the v2.1 milestone header at ~line 49-53 for the exact `**Requirements (N):** ...` style, and other phase entries like Phase 16 that carry `**Depends on:**` trailers so the new `**Requirements:**` line slots in cleanly)
  </read_first>
  <action>
Locate the Phase 21 entry in `.planning/ROADMAP.md` (begins with `- [ ] **Phase 21: Windows Single-File Filesystem Grants**` at approximately line 102).

Insertion point: BETWEEN the existing `**Unblocks:**` and `**Depends on:**` lines, add a new line:

```
  **Requirements:** WSFG-01, WSFG-02, WSFG-03. See `.planning/REQUIREMENTS.md`.
```

Do NOT alter:
- The Phase 21 entry's goal paragraph or scope text
- The `**Unblocks:**` paragraph (Phase 18 HUMAN-UAT reference)
- The `**Depends on:**` paragraph
- The `**Open questions**` block
- The `**Plans:**` line (currently says "TBD during `/gsd-plan-phase 21`." — leave it alone; Plan 21-02..21-05 orchestration will update it downstream)

Also update the v2.1 milestone header (`### 🚧 v2.1` at ~line 49): the `**Requirements (10):**` line should be bumped to `**Requirements (13):**` and extended to mention WSFG-01..03. Exact replacement:

BEFORE: `**Requirements (10):** RESL-01..04, AIPC-01, ATCH-01, CLEAN-01..04. See \`.planning/REQUIREMENTS.md\`.`

AFTER: `**Requirements (13):** RESL-01..04, AIPC-01, ATCH-01, CLEAN-01..04, WSFG-01..03. See \`.planning/REQUIREMENTS.md\`.`

Do NOT update the Phase 21 progress-table row at ~line 139 (`21. Windows Single-File Filesystem Grants | v2.1 | 0/? | Not planned | —`) in this plan — Plan 21-05 will update plan counts on close-out; downstream orchestration (`/gsd-plan-phase`) may update the `0/?` to `0/5` after this plan set commits.
  </action>
  <verify>
    <automated>grep -c "WSFG-01, WSFG-02, WSFG-03" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains literal string `**Requirements:** WSFG-01, WSFG-02, WSFG-03.` (count == 1) in the Phase 21 entry
    - `.planning/ROADMAP.md` contains literal string `**Requirements (13):**` (count == 1) in the v2.1 milestone header
    - `.planning/ROADMAP.md` NO LONGER contains literal string `**Requirements (10):**` (count == 0)
    - `.planning/ROADMAP.md` contains literal string `WSFG-01..03` (count >= 1) in the v2.1 milestone header
    - `.planning/ROADMAP.md` Phase 21 goal paragraph untouched — `grep -c "WindowsUnsupportedIssueKind::SingleFileGrant"` still returns >= 1
    - `.planning/ROADMAP.md` Phase 21 `**Plans:**` line untouched — `grep -c "TBD during \`/gsd-plan-phase 21\`"` still returns >= 1
  </acceptance_criteria>
  <done>Phase 21 entry carries a `**Requirements:** WSFG-01..03` line and the v2.1 milestone header reflects the new requirement count (13 instead of 10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies planning artifacts only; no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Information disclosure | `.planning/REQUIREMENTS.md` | accept | Planning files are intentionally public in-repo; no secret content added. |
| T-21-01-02 | Tampering | Requirement IDs (WSFG-01..03) | accept | Requirement IDs are an audit trail, not an enforcement surface. Tampering is detected by git diff review at merge time. |
</threat_model>

<verification>
- `grep -c "^### WSFG-0[123]:" .planning/REQUIREMENTS.md` returns `3`.
- `grep -c "WSFG-01, WSFG-02, WSFG-03" .planning/ROADMAP.md` returns `1`.
- `grep -c "Requirements (13):" .planning/ROADMAP.md` returns `1`.
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` has a new `## WSFG — Windows Single-File Grants` section with three REQ-XX entries (WSFG-01, WSFG-02, WSFG-03), each carrying `**What:**`, `**Acceptance:**`, and `**Maps to:** Phase 21 Plan(s) ...` trailers matching the established RESL/AIPC/UPST style.
- `.planning/ROADMAP.md` Phase 21 entry (~line 102) carries a `**Requirements:** WSFG-01, WSFG-02, WSFG-03.` line immediately after the existing `**Unblocks:**` line.
- `.planning/ROADMAP.md` v2.1 milestone header shows `**Requirements (13):**` (incremented from 10) with WSFG-01..03 appended to the list.
- Downstream plans 21-02..21-05 can populate their `requirements:` frontmatter with valid IDs (WSFG-01..03) that resolve to existing entries in REQUIREMENTS.md.
- No code touched, no tests run.
</success_criteria>

<output>
After completion, create `.planning/phases/21-windows-single-file-grants/21-01-SUMMARY.md` documenting which requirement IDs were added and the exact phrasing used for each Acceptance block, so downstream plan SUMMARYs can reference WSFG-XX IDs consistently.
</output>
