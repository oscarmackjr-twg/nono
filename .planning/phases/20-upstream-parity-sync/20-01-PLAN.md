---
phase: 20-upstream-parity-sync
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - Cargo.toml
  - crates/nono/Cargo.toml
  - crates/nono-cli/Cargo.toml
  - crates/nono-proxy/Cargo.toml
  - bindings/c/Cargo.toml
  - Cargo.lock
autonomous: true
requirements: [UPST-01]

must_haves:
  truths:
    - "cargo tree -i rustls-webpki shows version 0.103.12 (or later in the 0.103.x line) and NO version <0.103.12 in the transitive closure"
    - "Every workspace package (nono, nono-cli, nono-proxy, nono-ffi/bindings-c) reports version 0.37.1 via `cargo pkgid` or `grep -E '^version = ' Cargo.toml`"
    - "Path-dep version pins in crates/nono-cli/Cargo.toml (nono, nono-proxy) and bindings/c/Cargo.toml (nono) are updated to 0.37.1 so cargo does not fall back to resolving against old registry versions"
    - "cargo build --workspace exits 0 after the lock is regenerated"
    - "cargo test --workspace --all-features exits 0 on the Windows host (no new regressions vs Phase 19 baseline)"
    - "Phase 15 5-row detached-console smoke gate (nono run -> nono ps -> nono stop) exits 0"
    - "REQUIREMENTS.md contains a new section for UPST-01..04 anchoring Phase 20 plans"
    - "Each workspace-bump commit and the REQUIREMENTS.md commit carry a DCO `Signed-off-by:` trailer"
  artifacts:
    - path: "Cargo.lock"
      provides: "Regenerated dependency lock with rustls-webpki 0.103.12 in the transitive closure"
    - path: "Cargo.toml"
      provides: "Workspace manifest (no version field change expected; version lives on each crate) — listed because regeneration may touch workspace.package if upstream cherry-pick applies cleanly"
    - path: "crates/nono/Cargo.toml"
      provides: "nono crate pinned at 0.37.1"
    - path: "crates/nono-cli/Cargo.toml"
      provides: "nono-cli crate pinned at 0.37.1; path-deps on nono and nono-proxy also at 0.37.1"
    - path: "crates/nono-proxy/Cargo.toml"
      provides: "nono-proxy crate pinned at 0.37.1; path-dep on nono also at 0.37.1"
    - path: "bindings/c/Cargo.toml"
      provides: "nono-ffi crate pinned at 0.37.1; path-dep on nono also at 0.37.1 (reconciling from the current 0.1.0 value)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "New `## UPST — Upstream Parity Sync` section with UPST-01..04 requirement IDs mapped to Phase 20 plans"
  key_links:
    - from: "cargo build --workspace"
      to: "Cargo.lock rustls-webpki entry"
      via: "transitive resolution via hyper-rustls → rustls → rustls-webpki"
      pattern: "^name = \"rustls-webpki\"\\nversion = \"0\\.103\\."
    - from: "`cargo pkgid` output per crate"
      to: "0.37.1 version pin in each Cargo.toml"
      via: "package.version field"
      pattern: "^version = \"0\\.37\\.1\""
    - from: ".planning/ROADMAP.md Phase 20 entry"
      to: ".planning/REQUIREMENTS.md § UPST"
      via: "requirement-ID anchor"
      pattern: "^### UPST-0"
---

<objective>
Land the `rustls-webpki 0.103.12` transitive upgrade (RUSTSEC-2026-0098, RUSTSEC-2026-0099) and realign every workspace crate version from `0.30.1` to `0.37.1` so the fork's published version surface matches upstream `v0.37.1`. Also add UPST-01..04 requirement IDs to `.planning/REQUIREMENTS.md` so the verifier has a concrete per-plan anchor.

Purpose: Ship the RUSTSEC CVE fix inside v2.1 even if downstream Phase 20 plans drag, and signal upstream-parity via the version bump. This plan is the Wave 0 prerequisite for Plans 20-02 / 20-03 / 20-04 — they rebase on the regenerated `Cargo.lock` before beginning work.
Output: 3 DCO-signed atomic commits on `windows-squash` (REQUIREMENTS, workspace-version-bump, rustls-webpki upgrade) + a regenerated `Cargo.lock` that clears `cargo audit` for both RUSTSEC IDs (or a documented-skip if `cargo audit` is not installed on this host).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-upstream-parity-sync/20-CONTEXT.md
@.planning/quick/260419-cmp-upstream-036-windows-parity/COMPARISON.md
@Cargo.toml
@crates/nono/Cargo.toml
@crates/nono-cli/Cargo.toml
@crates/nono-proxy/Cargo.toml
@bindings/c/Cargo.toml
@Makefile

<interfaces>
Current workspace version surface (grep-verified 2026-04-19):

```
crates/nono/Cargo.toml:3:        version = "0.30.1"
crates/nono-cli/Cargo.toml:3:    version = "0.30.1"
crates/nono-cli/Cargo.toml:39:   nono       = { version = "0.30.1", path = "../nono" }
crates/nono-cli/Cargo.toml:40:   nono-proxy = { version = "0.30.1", path = "../nono-proxy" }
crates/nono-proxy/Cargo.toml:3:  version = "0.30.1"
crates/nono-proxy/Cargo.toml:15: nono       = { version = "0.30.1", path = "../nono" }
bindings/c/Cargo.toml:3:         version = "0.1.0"            # NOTE: fork divergence from CONTEXT D-19 text
bindings/c/Cargo.toml:18:        nono = { version = "0.1.0", path = "../../crates/nono" }  # wrong — should match nono crate
```

rustls-webpki is a **transitive** dependency (via `hyper-rustls` → `rustls`):

```
crates/nono-proxy/Cargo.toml:35: hyper-rustls = { version = "0.27", features = ["http1", "ring", "webpki-tokio"] }
crates/nono-proxy/Cargo.toml:37: rustls = { version = "0.23", features = ["ring"] }
```

No direct dep line to bump. `cargo update -p rustls-webpki --precise 0.103.12` (or simply `cargo update -p rustls-webpki`) is the surgical tool; the upgrade propagates through `Cargo.lock` only.

CONTEXT D-19 says "bump all workspace crates to 0.37.1" — the current `bindings/c/Cargo.toml` value is `0.1.0`, not `0.30.1`. This plan reconciles both forks to `0.37.1` in a single atomic commit; noted here to avoid executor confusion.

Upstream commit `8876d89` is the cherry-pick candidate:
- `git show v0.37.0:Cargo.lock | grep -A2 'name = "rustls-webpki"'` — confirms upstream's lock has `0.103.12`.
- Upstream `8876d89` is a pure `Cargo.lock` diff. Cherry-pick is structurally safe on Windows (D-21 invariant holds: no `*_windows.rs` touched), but may conflict with the fork's lock (fork has Windows deps upstream doesn't). Plan fallback = run `cargo update -p rustls-webpki` locally if cherry-pick hits merge conflicts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UPST-01..04 requirement IDs to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (existing RESL/AIPC/ATCH/CLEAN section structure)
    - .planning/phases/20-upstream-parity-sync/20-CONTEXT.md § Deferred Ideas "Retroactive update of REQUIREMENTS.md to add UPST-01..04 requirement IDs" (the open_requirement_question)
    - .planning/phases/20-upstream-parity-sync/20-CONTEXT.md § Feature Scope IN (D-05..D-13 — the content that UPST-01..04 must cover)
    - .planning/ROADMAP.md § Phase 20 entry (requirement-anchor target)
  </read_first>
  <action>
    DECISION — Planner option (a) from the open_requirement_question: add UPST-01..04 to REQUIREMENTS.md as the first commit of Plan 20-01. Rationale: (i) gives `/gsd-verify-phase 20` a concrete per-plan target, matching Phase 19's verifier pass pattern; (ii) keeps the ROADMAP + REQUIREMENTS + plan frontmatter `requirements:` field consistent; (iii) the edit is trivial and atomic so it doesn't bloat the security commit.

    Append a new section at the end of `.planning/REQUIREMENTS.md` (after the existing CLEAN section) with this exact content (adjust heading depth to match the file's existing `##` pattern for top-level requirement groups):

    ```
    ---

    ## UPST — Upstream Parity Sync

    Context: The fork is pinned at crate version 0.30.1 while upstream `always-further/nono` has shipped 0.31–0.37.1. Phase 20 back-ports selected upstream features and the `rustls-webpki` RUSTSEC-2026-0098/0099 security upgrade while preserving all Windows-specific work from Phases 1–19. Decomposition and scope locked in `.planning/phases/20-upstream-parity-sync/20-CONTEXT.md`.

    ### UPST-01: Security upgrade + workspace version realignment

    **What:** `rustls-webpki` transitive upgrade to `0.103.12` (clears RUSTSEC-2026-0098 and RUSTSEC-2026-0099) and workspace-wide crate version bump from `0.30.1` to `0.37.1` so the fork's published surface matches upstream `v0.37.1`.

    **Acceptance:**
    1. `cargo tree -i rustls-webpki` shows only versions `>= 0.103.12` in the transitive closure.
    2. Every workspace crate (`nono`, `nono-cli`, `nono-proxy`, `nono-ffi`/`bindings-c`) reports `0.37.1` via `cargo pkgid`.
    3. `cargo build --workspace` and `cargo test --workspace --all-features` exit 0 on the Windows host.
    4. Phase 15 5-row detached-console smoke (`nono run <profile> → nono ps → nono stop`) passes unchanged.

    **Maps to:** Phase 20 Plan 20-01.

    ### UPST-02: Profile & claude-code fixes

    **What:** Port upstream profile `extends` infinite-recursion fix (commit `c1bc439`) and claude-code `.claude.json` symlink for token refresh (commit `97f7294`) into the fork's profile/hooks surface.

    **Acceptance:**
    1. A profile with `extends` referencing itself (direct or cyclic indirect) returns a clear `NonoError` within bounded time instead of stack-overflowing.
    2. `.claude.json` symlink path resolution behaves as upstream v0.37.1 when refreshing claude-code tokens.
    3. `make ci` passes on the Windows host.

    **Maps to:** Phase 20 Plan 20-02.

    ### UPST-03: Credentials & environment parity

    **What:** Port upstream `keyring://service/account` credential URI + `?decode=go-keyring` query-param handling (upstream 0.36), environment-variable filtering (upstream 0.37.0 #688, commit `1b412a7`), and `command_blocking_deprecation.rs` backport (upstream 0.33, ~190 lines).

    **Acceptance:**
    1. `keyring://service/account` parses into a typed URI variant; hostile inputs (path traversal, junk decode param) are rejected with `NonoError::InvalidConfig` (fail-closed).
    2. An env-var filter profile rejects a malformed pattern up-front; well-formed filters allow/deny at the sandboxed process-env boundary as specified upstream.
    3. `command_blocking_deprecation.rs` surfaces the deprecation warning for the documented command list and does not break existing `run`/`wrap`/`shell` paths.
    4. `make ci` passes on the Windows host.

    **Maps to:** Phase 20 Plan 20-03. If the keyring manual port exceeds ~400 lines the plan splits into 20-03a (keyring) and 20-03b (env-filter + deprecation) per CONTEXT § Specifics.

    ### UPST-04: GPU + trust parity

    **What:** Port upstream `--allow-gpu` flag (upstream 0.31–0.33) with NVIDIA procfs + `nvidia-uvm-tools` Linux device allowlist (upstream 0.34), and GitLab ID tokens for trust signing (upstream 0.35) alongside the existing GitHub ID token path.

    **Acceptance:**
    1. `nono run --allow-gpu …` parses cleanly; on Linux the sandbox grants `/dev/nvidia*` + NVIDIA procfs + `nvidia-uvm-tools`; on macOS it grants Metal/GPU framework paths via Seatbelt; on Windows it accepts the flag with a `not-enforced-on-this-platform` warning (Phase 16 pattern).
    2. A `nono trust` signing path that consumes a GitLab ID token completes end-to-end, mirroring the existing GitHub ID token test coverage.
    3. `make ci` passes on the Windows host.

    **Maps to:** Phase 20 Plan 20-04.
    ```

    Then stage and commit ONLY this file.

    Preflight: Replace every `<capture from ...>` placeholder in the heredoc body with the literal value produced by running the bracketed git log command NOW (before git commit). The `'EOF'` heredoc deliberately suppresses shell substitution — the substitution happens as a manual pre-commit step. If the commit message contains the literal string `<capture from`, abort and re-substitute.

    ```
    git add .planning/REQUIREMENTS.md
    git commit -s -m "$(cat <<'EOF'
    docs(20-01): add UPST-01..04 requirement IDs for Phase 20 upstream parity sync

    Anchors Phase 20's four plans (security/version realignment, profile fixes,
    credentials & env, GPU + trust) to concrete requirement IDs so the verifier
    agent has a per-plan target. Mirrors the RESL/AIPC/ATCH/CLEAN pattern used
    in v2.1 to date.

    Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
    EOF
    )"
    ```

    (The DCO hook will reject the commit if the `-s` sign-off is missing; include it explicitly regardless.)
  </action>
  <verify>
    <automated>grep -E "^### UPST-0[1-4]:" .planning/REQUIREMENTS.md | wc -l &amp;&amp; git log -1 --format='%s' &amp;&amp; git log -1 --format='%b' | grep -c '^Signed-off-by: '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^### UPST-0[1-4]:" .planning/REQUIREMENTS.md | wc -l` returns `4`.
    - `grep -E "^## UPST " .planning/REQUIREMENTS.md` returns one line containing "Upstream Parity Sync".
    - `git log -1 --format='%s'` starts with `docs(20-01): add UPST-01..04`.
    - `git log -1 --format='%b' | grep -c '^Signed-off-by: '` returns exactly `1`.
    - `git log -1 --format=%B | grep -c '<capture from'` returns `0` (no unsubstituted placeholders in the commit body).
    - Commit diff touches only `.planning/REQUIREMENTS.md` (verify with `git show --stat HEAD`).
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md contains the UPST section with four requirement IDs mapped to Phase 20's four plans; commit landed on `windows-squash` with DCO sign-off.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump workspace crate versions 0.30.1 → 0.37.1 and reconcile bindings/c</name>
  <files>
    crates/nono/Cargo.toml
    crates/nono-cli/Cargo.toml
    crates/nono-proxy/Cargo.toml
    bindings/c/Cargo.toml
  </files>
  <read_first>
    - crates/nono/Cargo.toml (lines 1-15 — package version field)
    - crates/nono-cli/Cargo.toml (lines 1-45 — package version + path-dep pins on `nono` and `nono-proxy`)
    - crates/nono-proxy/Cargo.toml (lines 1-20 — package version + path-dep pin on `nono`)
    - bindings/c/Cargo.toml (lines 1-25 — package version + path-dep pin on `nono`; CURRENT VALUE IS 0.1.0, not 0.30.1)
    - `git show v0.37.1:crates/nono/Cargo.toml | head -20` (confirm upstream's package.version is 0.37.1 — sanity check before bumping)
    - `git show v0.37.1:bindings/c/Cargo.toml | head -20` (verify upstream's bindings-c version pin — may or may not match the crate version)
    - .planning/phases/20-upstream-parity-sync/20-CONTEXT.md § D-19
  </read_first>
  <action>
    1. Edit each of the 4 crate `Cargo.toml` files to set `version = "0.37.1"` on the `[package]` section.

    2. Edit the internal path-dep version pins so they match:
       - `crates/nono-cli/Cargo.toml` line ~39: `nono       = { version = "0.37.1", path = "../nono" }`
       - `crates/nono-cli/Cargo.toml` line ~40: `nono-proxy = { version = "0.37.1", path = "../nono-proxy" }`
       - `crates/nono-proxy/Cargo.toml` line ~15: `nono       = { version = "0.37.1", path = "../nono" }`
       - `bindings/c/Cargo.toml` line ~18: `nono = { version = "0.37.1", path = "../../crates/nono" }`

    3. For `bindings/c/Cargo.toml` specifically: the package was on `0.1.0`, not `0.30.1`. Still bump to `0.37.1` per D-19 (the instruction is "all workspace crates to 0.37.1", not "all crates currently at 0.30.1"). Note in the commit body that this reconciles a pre-existing fork divergence.

    4. Regenerate `Cargo.lock` by running `cargo check --workspace` (or `cargo build --workspace` if preferred; `check` is faster). This rewrites the `version = "0.30.1"` / `"0.1.0"` entries for the workspace members in `Cargo.lock` to `0.37.1`.

    5. Confirm `cargo build --workspace` exits 0 after the version bumps. If any transitive crate pins an exact `0.30.1` for a workspace member (unlikely), reconcile before proceeding.

    6. Stage exactly these 5 files and commit.

       Preflight: Replace every `<capture from ...>` placeholder in the heredoc body with the literal value produced by running the bracketed git log command NOW (before git commit). The `'EOF'` heredoc deliberately suppresses shell substitution — the substitution happens as a manual pre-commit step. If the commit message contains the literal string `<capture from`, abort and re-substitute.

    ```
       git add crates/nono/Cargo.toml crates/nono-cli/Cargo.toml crates/nono-proxy/Cargo.toml bindings/c/Cargo.toml Cargo.lock
       git commit -s -m "$(cat <<'EOF'
       chore(20-01): bump workspace crate versions 0.30.1 → 0.37.1 for upstream parity

       Realigns nono, nono-cli, nono-proxy, and nono-ffi (bindings/c) to upstream
       v0.37.1's published version surface per CONTEXT D-19. Reconciles the
       pre-existing 0.1.0 pin on bindings/c/Cargo.toml to match. Internal
       path-dep version pins updated in lockstep so cargo does not fall back to
       the registry.

       Upstream-tag: v0.37.1
       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
       EOF
       )"
       ```

    7. If `Cargo.lock` also absorbed unrelated updates from a cache refresh, revert those specific lines and keep only the version bumps for the 4 workspace crates (plus any transitive fallout from the workspace version change, which is rare — path-deps don't trigger transitive changes).
  </action>
  <verify>
    <automated>grep -E '^version = "0\.37\.1"' crates/nono/Cargo.toml crates/nono-cli/Cargo.toml crates/nono-proxy/Cargo.toml bindings/c/Cargo.toml | wc -l &amp;&amp; grep -E 'version = "0\.37\.1", path' crates/nono-cli/Cargo.toml crates/nono-proxy/Cargo.toml bindings/c/Cargo.toml | wc -l &amp;&amp; cargo build --workspace &amp;&amp; git log -1 --format='%s' &amp;&amp; git log -1 --format='%b' | grep -c '^Signed-off-by: '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^version = "0\.37\.1"' crates/nono/Cargo.toml crates/nono-cli/Cargo.toml crates/nono-proxy/Cargo.toml bindings/c/Cargo.toml | wc -l` returns `4`.
    - Combined path-dep grep returns `4` lines (nono in nono-cli, nono-proxy in nono-cli, nono in nono-proxy, nono in bindings/c — all pinned to 0.37.1).
    - `cargo build --workspace` exits 0.
    - `cargo pkgid -p nono` output ends with `#0.37.1` (and same for nono-cli, nono-proxy, nono-ffi — the crate name for bindings/c).
    - `git log -1 --format='%s'` starts with `chore(20-01): bump workspace crate versions`.
    - `git log -1 --format='%b' | grep -c '^Signed-off-by: '` returns exactly `1`.
    - `git show --stat HEAD` lists exactly 5 files: the 4 Cargo.toml files + Cargo.lock.
    - `git log -1 --format=%B | grep -c '<capture from'` returns `0` (no unsubstituted placeholders in the commit body).
  </acceptance_criteria>
  <done>
    All 4 workspace crates report 0.37.1 via `cargo pkgid`; path-deps match; `cargo build --workspace` green; commit landed with DCO sign-off.
  </done>
</task>

<task type="auto">
  <name>Task 3: Upgrade rustls-webpki to 0.103.12 (RUSTSEC-2026-0098, RUSTSEC-2026-0099)</name>
  <files>Cargo.lock</files>
  <read_first>
    - Cargo.lock (current state — grep `name = "rustls-webpki"` to confirm pre-upgrade version)
    - `git show v0.37.0:Cargo.lock | grep -A2 'name = "rustls-webpki"'` (upstream's target lock entry)
    - Upstream commit `8876d89` via `git log -1 v0.37.0 --grep 'rustls-webpki' --format='%H %s'` (confirm hash + message)
    - .planning/phases/20-upstream-parity-sync/20-CONTEXT.md § D-05
    - CLAUDE.md § Security Considerations (fail-secure + validate-all-inputs for rustls cert validation)
  </read_first>
  <action>
    1. Capture the pre-upgrade version:
       ```
       grep -A1 'name = "rustls-webpki"' Cargo.lock | head -5
       ```
       Record the baseline version(s) — there may be multiple entries.

    2. Attempt cherry-pick of upstream `8876d89` first (clean path if it applies):
       ```
       git cherry-pick 8876d89 --strategy-option theirs --no-commit
       ```
       - If the cherry-pick touches ONLY `Cargo.lock`, proceed: run `cargo build --workspace` to validate; then amend the commit message via `git commit` (see step 5 for template).
       - If the cherry-pick tries to touch any file other than `Cargo.lock` (e.g., `Cargo.toml` — unlikely given upstream's commit is lock-only, but possible if upstream also bumped a direct dep), STOP and fall back to the manual `cargo update` path in step 3.
       - If the cherry-pick hits a conflict on `Cargo.lock` (likely because fork has Windows-only deps not in upstream's lock), `git cherry-pick --abort` and fall to step 3.

    3. Manual fallback — run the surgical upgrade:
       ```
       cargo update -p rustls-webpki --precise 0.103.12
       ```
       If the registry does not yet offer `0.103.12` exactly (unlikely — upstream shipped 2026-03 per CHANGELOG; at 2026-04-19 the registry will have it), use the broader `cargo update -p rustls-webpki` and confirm the resolved version is `>= 0.103.12`.

    4. Verify the transitive closure no longer contains a vulnerable version:
       ```
       cargo tree -i rustls-webpki 2>&1 | head -20
       grep -A1 'name = "rustls-webpki"' Cargo.lock | grep 'version ='
       ```
       ALL `version =` lines for `rustls-webpki` must be `>= 0.103.12`. Multiple entries are fine so long as every one is at or above the fix.

    5. `cargo build --workspace` and `cargo test --workspace --all-features` — the rustls-webpki API surface changed very little in the 0.103.x line; workspace tests should remain green. If they go red, STOP and investigate (per CONTEXT STOP trigger #2).

    6. If `cargo audit` is installed (`cargo audit --version` succeeds), run `cargo audit --ignore` and confirm NEITHER RUSTSEC-2026-0098 NOR RUSTSEC-2026-0099 appears in the output. If `cargo audit` is absent, document-skip in the SUMMARY with rationale: "`cargo audit` not installed on Windows host; transitive closure verified via `cargo tree -i rustls-webpki`; RUSTSEC IDs cleared by version inspection."

    7. Commit (if not already committed via cherry-pick in step 2).

       Preflight: Replace every `<capture from ...>` placeholder in the heredoc body with the literal value produced by running the bracketed git log command NOW (before git commit). The `'EOF'` heredoc deliberately suppresses shell substitution — the substitution happens as a manual pre-commit step. If the commit message contains the literal string `<capture from`, abort and re-substitute.

    ```
       git add Cargo.lock
       git commit -s -m "$(cat <<'EOF'
       chore(20-01): upgrade rustls-webpki to 0.103.12 (RUSTSEC-2026-0098, RUSTSEC-2026-0099)

       Transitive dependency upgrade via `cargo update -p rustls-webpki` (or
       cherry-pick of upstream 8876d89 when lock-compatible). Clears both
       RUSTSEC advisories for the nono-proxy hyper-rustls → rustls stack.
       No direct-dependency bumps needed; no API-surface changes observed in
       the 0.103.x line.

       Upstream-commit: 8876d89
       Upstream-tag: v0.37.0
       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
       EOF
       )"
       ```

    8. If the cherry-pick path in step 2 succeeded, amend the cherry-picked commit's message to match this template (preserving the upstream author as `Co-Authored-By:` and adding the DCO sign-off as the final trailer).

       Preflight: Replace every `<capture from ...>` placeholder in the heredoc body with the literal value produced by running the bracketed git log command NOW (before git commit). The `'EOF'` heredoc deliberately suppresses shell substitution — the substitution happens as a manual pre-commit step. If the commit message contains the literal string `<capture from`, abort and re-substitute.

    ```
       git commit --amend -s -m "$(cat <<'EOF'
       chore(20-01): upgrade rustls-webpki to 0.103.12 (RUSTSEC-2026-0098, RUSTSEC-2026-0099)

       Cherry-picked from upstream 8876d89 onto windows-squash. Transitive
       upgrade only — no fork-side API impact.

       Upstream-commit: 8876d89
       Upstream-tag: v0.37.0
       Upstream-author: <capture from `git log -1 8876d89 --format='%an <%ae>'`>
       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
       EOF
       )"
       ```
  </action>
  <verify>
    <automated>grep -E '^version = "0\.103\.' Cargo.lock | sort -u &amp;&amp; cargo build --workspace &amp;&amp; git log -1 --format='%s' &amp;&amp; git log -1 --format='%b' | grep -c '^Signed-off-by: '</automated>
  </verify>
  <acceptance_criteria>
    - Every `rustls-webpki` entry in `Cargo.lock` is at version `0.103.12` or higher (grep-verifiable: `grep -A1 'name = "rustls-webpki"' Cargo.lock | grep 'version' | awk '{print $3}' | tr -d '"'` — all values must be `>= 0.103.12`).
    - `cargo tree -i rustls-webpki` shows no version `< 0.103.12`.
    - `cargo build --workspace` exits 0.
    - `cargo test --workspace --all-features` exits 0 on the Windows host (Phase 19 deferred flakes in `tests/env_vars.rs` and `trust_scan::tests::*` may still fail — document-skip per CONTEXT § Known risks, but no NEW failures attributable to this upgrade).
    - `git log -1 --format='%s'` starts with `chore(20-01): upgrade rustls-webpki`.
    - `git log -1 --format='%b'` contains both `Upstream-commit: 8876d89` and `Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>`.
    - If `cargo audit` is available: neither RUSTSEC-2026-0098 nor RUSTSEC-2026-0099 appears in its output. If absent: a documented-skip is recorded in the SUMMARY.
    - `git log -1 --format=%B | grep -c '<capture from'` returns `0` (no unsubstituted placeholders in the commit body).
  </acceptance_criteria>
  <done>
    `rustls-webpki` transitive closure is at `>= 0.103.12`; workspace builds and tests green; commit landed with DCO sign-off and upstream-provenance trailers.
  </done>
</task>

<task type="auto">
  <name>Task 4: Plan-level `make ci` gate</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - Makefile (the `ci` target chain: `ci: check test audit` → `check: clippy fmt-check` → workspace tests)
    - .planning/phases/20-upstream-parity-sync/20-CONTEXT.md § D-16 (verification gate)
    - .planning/phases/19-cleanup/19-01-PLAN.md Task 3 (precedent for treating `make ci` as a plan-level gate)
  </read_first>
  <action>
    1. Run `make ci` from the repo root. Capture stdout+stderr to a log buffer (the SUMMARY will reference it).

    2. Expected outcome: exit 0. `make ci` runs `cargo build` + `cargo clippy --workspace -- -D warnings -D clippy::unwrap_used` + `cargo fmt --all -- --check` + `cargo test --workspace --all-features` + optionally `cargo audit`.

    3. Allowed tolerances (documented pre-existing flakes carried forward from Phase 19 CLEAN-02 — NOT regressions introduced by this plan):
       - `tests/env_vars.rs` may have up to 19 failures (deferred list in `.planning/phases/19-cleanup/19-02-SUMMARY.md`).
       - `trust_scan::tests::*` may have 1–3 tempdir-race failures (also deferred).

       If `make ci` fails with MORE failures than this pre-existing list, or with failures in files outside `env_vars.rs` / `trust_scan`, STOP per CONTEXT STOP trigger #2 ("`make ci` goes red on a plan and root cause isn't obvious within ~30 minutes").

    4. If `make ci` red for a new reason, revert the most recent commit (`git reset --hard HEAD~1`) and investigate before re-attempting. Do NOT chain red into red.

    5. Record in the SUMMARY: (a) `make ci` exit code, (b) any pre-existing flake count observed, (c) confirmation that no new failures were introduced.
  </action>
  <verify>
    <automated>make ci</automated>
  </verify>
  <acceptance_criteria>
    - `make ci` exits 0, OR
    - `make ci` fails ONLY in `tests/env_vars.rs` and/or `trust_scan::tests::*` within the documented pre-existing deferred window, in which case the SUMMARY explicitly lists each failure and confirms its match against the Phase 19 CLEAN-02 deferred list.
    - `cargo fmt --all -- --check` exits 0 (no new fmt drift from this plan).
    - `cargo clippy --workspace -- -D warnings -D clippy::unwrap_used` exits 0.
  </acceptance_criteria>
  <done>
    `make ci` gate passes (or matches the documented pre-existing deferred-flake window) and the SUMMARY has the exit code, duration, and per-subtarget results recorded.
  </done>
</task>

<task type="auto">
  <name>Task 5 (D-20 Windows regression safety net — BLOCKING, LAST TASK): Windows-host regression verification</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - .planning/phases/20-upstream-parity-sync/20-CONTEXT.md § D-20
    - .planning/milestones/v2.0-ROADMAP.md § Phase 15 smoke gate (5-row detached-console verification pattern — archived; capture the essentials)
    - crates/nono-cli/tests/wfp_port_integration.rs (test names, `#[ignore]` pattern)
    - crates/nono-cli/tests/learn_windows_integration.rs (test names)
  </read_first>
  <action>
    1. Run the full workspace test suite explicitly: `cargo test --workspace --all-features`. Expected: exit 0 within the pre-existing deferred-flake tolerance listed in Task 4 step 3.

    2. Run the Phase 15 5-row detached-console smoke gate. From a Windows PowerShell (not WSL) session with the freshly-built `nono.exe` on PATH:
       ```
       nono run --profile default -- powershell -Command "Write-Host 'row1'; Write-Host 'row2'; Write-Host 'row3'; Write-Host 'row4'; Write-Host 'row5'; Start-Sleep 30"
       nono ps
       nono stop <session-id-from-ps>
       ```
       Expected: 5 rows visible; `nono ps` lists the session with state `running`; `nono stop` returns exit 0; subsequent `nono ps` no longer lists the session (or lists it as `exited`).

    3. If admin + `nono-wfp-service` are available on the host, run the ignored WFP integration tests:
       ```
       cargo test -p nono-cli --test wfp_port_integration -- --ignored
       ```
       Expected: all tests pass. If admin/service NOT available, document-skip in SUMMARY with rationale: "WFP ignored-test suite requires admin + nono-wfp-service running; not run on this host. Documented as skip per CONTEXT D-20."

    4. Run the ETW learn integration tests:
       ```
       cargo test -p nono-cli --test learn_windows_integration
       ```
       Expected: exit 0.

    5. If ANY of steps 1–4 produce a regression (new failure not covered by the Phase 19 CLEAN-02 deferred window), STOP per CONTEXT STOP trigger #4 ("Phase 15 5-row smoke gate fails on any plan → the port regressed Windows behavior; revert and re-scope"). Revert to `HEAD~3` (pre-Task-1 state) and surface the regression before any further Plan 20-01 work.

    6. Record in SUMMARY: (a) workspace test pass/fail summary, (b) 5-row smoke gate result, (c) WFP port integration result or documented-skip, (d) learn_windows_integration result.
  </action>
  <verify>
    <automated>cargo test --workspace --all-features &amp;&amp; cargo test -p nono-cli --test learn_windows_integration</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace --all-features` exits 0 within the documented deferred-flake window (no NEW failures attributable to Tasks 1–3).
    - Phase 15 5-row detached-console smoke gate passes as described.
    - `cargo test -p nono-cli --test wfp_port_integration -- --ignored` passes on admin + service hosts, or is documented-skipped otherwise.
    - `cargo test -p nono-cli --test learn_windows_integration` exits 0.
    - SUMMARY records all four steps' outcomes.
  </acceptance_criteria>
  <done>
    D-20 Windows regression safety net is fully cleared for Plan 20-01. Phase 1–19 Windows behavior is preserved. Wave 1 plans (20-02, 20-03, 20-04) may now rebase and begin.
  </done>
</task>

</tasks>

<non_goals>
**Windows-invariance (D-21):** This plan touches NO Windows-only files. Specifically, any diff touching `crates/nono/src/sandbox/windows.rs`, `crates/nono-cli/src/bin/nono-wfp-service.rs`, `crates/nono-cli/src/exec_strategy_windows/`, `crates/nono/src/supervisor/socket_windows.rs`, `crates/nono-cli/src/pty_proxy_windows.rs`, `crates/nono-cli/src/learn_windows.rs`, `crates/nono-cli/src/session_commands_windows.rs`, `crates/nono-cli/src/trust_intercept_windows.rs`, `crates/nono-cli/src/open_url_runtime_windows.rs`, or any `#[cfg(target_os = "windows")]` block inside an upstream port is by definition a cherry-pick bug — abort and investigate (D-21).

**Unix native RESL backends:** Unix-side `--cpu-percent`/`--memory`/`--timeout`/`--max-processes` native enforcement is out of scope (cross-platform follow-up milestone).

**New requirement IDs outside UPST:** Only UPST-01..04 are added to REQUIREMENTS.md in this plan. No changes to RESL/AIPC/ATCH/CLEAN requirement IDs.

**Pre-existing Phase 19 deferred flakes:** `tests/env_vars.rs` 19 failures and `trust_scan::tests::*` 1–3 failures are documented-deferred per Phase 19 CLEAN-02. Plan 20-01 must NOT attempt to fix them, but also must NOT let them mask new regressions (Task 4 step 3 handles the distinction).

**Ancillary rustls-webpki transitive upgrades** (e.g., bumping `rustls` itself to 0.23.x, or `hyper-rustls` to 0.28.x) are out of scope — those are direct-dep bumps that trigger broader surface-area review. This plan stays surgical.
</non_goals>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry → local workspace | Updated `rustls-webpki` crate pulled from crates.io via `cargo update`; crate authenticity guarded by `Cargo.lock` checksums which `cargo` verifies on fetch. |
| nono-proxy ↔ remote TLS peer | rustls-webpki validates cert chains crossing this boundary. A regression here is the primary threat this plan exists to mitigate (RUSTSEC-2026-0098/0099). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Tampering | `rustls-webpki` cert validation | mitigate | Upgrade to 0.103.12 (RUSTSEC-2026-0098, RUSTSEC-2026-0099). Acceptance criterion: `cargo audit` clears both IDs (or `cargo tree -i rustls-webpki` shows `>= 0.103.12` throughout the transitive closure if audit unavailable). |
| T-20-01-02 | Tampering | Upgraded `rustls-webpki` accepts a cert the old version rejected | mitigate | `cargo test --workspace --all-features` must remain green post-upgrade; `nono-proxy` existing TLS integration tests (if any; otherwise documented-skip) must pass. Fail-closed: if the workspace test suite surfaces new rustls failures, STOP per CONTEXT STOP trigger #2. |
| T-20-01-03 | Repudiation | Commit provenance for security-sensitive upgrade | mitigate | Every commit in this plan (3 total) carries a DCO `Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>` trailer. Commit messages explicitly cite RUSTSEC IDs and upstream commit hash so a downstream audit can trace the provenance chain. |
| T-20-01-04 | Denial of Service | Version bump breaks path-dep resolution (cargo falls back to registry, resolves an older crate, workspace breaks) | mitigate | Internal path-dep version pins updated in lockstep with crate versions (Task 2 step 2). Acceptance grep verifies all 4 path-dep entries match `0.37.1`. |
| T-20-01-05 | Information Disclosure | Regenerated `Cargo.lock` accidentally downgrades a security-relevant crate other than `rustls-webpki` | accept | Cargo only modifies the crate named in `cargo update -p rustls-webpki`; unrelated crates stay at their prior lock entries. Task 3 step 4 spot-checks the lock diff. If the cherry-pick path is used, Task 3 step 2's conflict-detection step surfaces unexpected file changes. |
| T-20-01-06 | Elevation of Privilege | `make ci` masking a clippy/fmt regression under the guise of "green CI" | mitigate | Task 4 runs `make ci` explicitly; acceptance criterion requires `cargo fmt --all -- --check` exit 0 AND `cargo clippy --workspace -- -D warnings -D clippy::unwrap_used` exit 0. Both are hard gates. |
</threat_model>

<verification>
Per-plan verification gate (D-16 + D-20):

- `cargo build --workspace` exits 0.
- `cargo test --workspace --all-features` exits 0 (within Phase 19 deferred-flake tolerance).
- `cargo fmt --all -- --check` exits 0.
- `cargo clippy --workspace -- -D warnings -D clippy::unwrap_used` exits 0.
- `cargo tree -i rustls-webpki` shows all entries at `>= 0.103.12`.
- `cargo audit` clears RUSTSEC-2026-0098 and RUSTSEC-2026-0099 (or documented-skip if audit not installed).
- `cargo pkgid -p nono`, `-p nono-cli`, `-p nono-proxy`, `-p nono-ffi` all report `#0.37.1`.
- Phase 15 5-row detached-console smoke gate passes on the Windows host.
- `cargo test -p nono-cli --test wfp_port_integration -- --ignored` passes (admin + service) or documented-skip.
- `cargo test -p nono-cli --test learn_windows_integration` exits 0.
- All 3 atomic commits carry a DCO `Signed-off-by:` trailer AND appropriate upstream-provenance trailers (Tasks 1/2/3).
- `git log --oneline -3` shows the 3 commits in order: REQUIREMENTS → version-bump → rustls-webpki.
- Commit diffs are confined to the files in `files_modified`.
</verification>

<success_criteria>
- 3 DCO-signed commits on `windows-squash`:
  1. `docs(20-01): add UPST-01..04 requirement IDs for Phase 20 upstream parity sync`
  2. `chore(20-01): bump workspace crate versions 0.30.1 → 0.37.1 for upstream parity`
  3. `chore(20-01): upgrade rustls-webpki to 0.103.12 (RUSTSEC-2026-0098, RUSTSEC-2026-0099)`
- `rustls-webpki` transitive closure at `>= 0.103.12`; both RUSTSEC IDs cleared.
- All 4 workspace crates at version `0.37.1`; path-deps pinned in lockstep.
- `make ci` green or within the Phase 19 deferred-flake tolerance window (no new failures).
- Phase 15 5-row smoke gate passes; `wfp_port_integration` and `learn_windows_integration` clear or documented-skipped.
- Plan SUMMARY records all 5 tasks' outcomes, the 3 commit hashes, and an explicit forward-pointer confirming Wave 1 (Plans 20-02/03/04) may now rebase on post-20-01 HEAD.
</success_criteria>

<output>
After completion, create `.planning/phases/20-upstream-parity-sync/20-01-SUMMARY.md` using the standard summary template. Required sections: Outcome, What was done (one bullet per task), Verification table (copy from `<verification>` with actual results), Files changed (7-row table: REQUIREMENTS.md + 4 Cargo.tomls + Cargo.lock), Commits (3-row table with hashes), Status, Deferred (explicit call-out that Phase 19 CLEAN-02 deferred flakes remain unfixed and that `cargo audit`-skipped scenarios are recorded with rationale).
</output>
