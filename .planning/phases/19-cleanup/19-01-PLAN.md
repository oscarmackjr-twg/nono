---
phase: 19-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/config/mod.rs
  - crates/nono-cli/src/exec_strategy_windows/restricted_token.rs
  - crates/nono-cli/src/profile/mod.rs
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "cargo fmt --all -- --check exits 0 on the entire workspace"
    - "The diff is confined to the 3 known-drifted files (no collateral reformatting)"
    - "make ci keeps fmt-check as a hard gate (no change needed; verified)"
  artifacts:
    - path: "crates/nono-cli/src/config/mod.rs"
      provides: "Re-formatted to rustfmt canon"
    - path: "crates/nono-cli/src/exec_strategy_windows/restricted_token.rs"
      provides: "Re-formatted to rustfmt canon"
    - path: "crates/nono-cli/src/profile/mod.rs"
      provides: "Re-formatted to rustfmt canon"
  key_links:
    - from: "Makefile target `ci`"
      to: "Makefile target `check` → `fmt-check`"
      via: "`.PHONY` dependency chain"
      pattern: "ci: check test audit"
---

<objective>
Eliminate the pre-existing `cargo fmt --check` drift in 3 files carried forward from the commit `6749494` (EnvVarGuard migration) that Plans 16-01 and 16-02 explicitly deferred. End state: `cargo fmt --all -- --check` is green on the whole workspace, with no functional/logic changes in this plan.

Purpose: Restore `make ci`'s format gate to a trustworthy signal so downstream phases don't silently accumulate more drift.
Output: Commit `style(19-CLEAN-01): cargo fmt drift follow-up from commit 6749494` touching exactly the 3 files named above.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-cleanup/19-CONTEXT.md
@.planning/phases/16-resource-limits/16-02-SUMMARY.md
@Makefile

<interfaces>
Relevant existing surface — no new interfaces created. `cargo fmt` is a purely textual rewrap. The 3 drifted files ALL survived Phase 16 untouched per 16-02 SUMMARY § "Deferred for follow-up" bullet 5.

Makefile chain that proves acceptance criterion #3 already holds:

```
check: clippy fmt-check
fmt-check:
	cargo fmt --all -- --check
ci: check test audit
```

So `make ci` -> `check` -> `fmt-check` -> `cargo fmt --all -- --check`. CLEAN-01 acceptance #3 ("`make ci` re-includes `fmt --check` as a hard gate") is a no-op verification, not a Makefile edit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pre-state and confirm the 3 drifted files are the only offenders</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - .planning/phases/16-resource-limits/16-02-SUMMARY.md § "Deferred for follow-up" bullet 5
    - .planning/phases/19-cleanup/19-CONTEXT.md § D-03
    - Makefile (lines 70-83 for check / fmt-check / ci targets)
  </read_first>
  <action>
    Run `cargo fmt --all -- --check` from repo root. Capture the list of files it wants to reformat. Assert that the set EXACTLY equals these three paths (normalized, forward slashes in the log):
      - crates/nono-cli/src/config/mod.rs
      - crates/nono-cli/src/exec_strategy_windows/restricted_token.rs
      - crates/nono-cli/src/profile/mod.rs

    If the set differs (more files, fewer files, or different files), STOP and record the actual set in the SUMMARY. Do NOT proceed to Task 2 until the delta is reconciled (either by updating D-03 with the planner on the hot-seat, or by confirming the extra files are drift introduced between CONTEXT.md gathering and plan execution — in which case those extras get folded into the same fmt commit with a one-line note).
  </action>
  <verify>
    <automated>cargo fmt --all -- --check 2>&amp;1 | grep -E "^Diff in" | sed -E 's/^Diff in //; s/ at line.*//' | sort -u</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits non-zero (this is the pre-fix state; non-zero is EXPECTED here).
    - The unique list of "Diff in &lt;path&gt;" lines exactly matches the three expected files (sorted). If it does not, Task 2 is blocked pending reconciliation.
  </acceptance_criteria>
  <done>
    The drift set is confirmed to be exactly the 3 files named in D-03. Any deviation is recorded verbatim in the plan SUMMARY § Deviations before Task 2 starts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply `cargo fmt --all` and stage only the 3 expected files</name>
  <files>
    crates/nono-cli/src/config/mod.rs
    crates/nono-cli/src/exec_strategy_windows/restricted_token.rs
    crates/nono-cli/src/profile/mod.rs
  </files>
  <read_first>
    - Output of Task 1 (confirmed file set)
    - .planning/phases/19-cleanup/19-CONTEXT.md § D-04 (acceptance)
    - CLAUDE.md § Coding Standards — DCO sign-off required on every commit
  </read_first>
  <action>
    1. Run `cargo fmt --all` from repo root. This rewrites only the files rustfmt disagrees with — per Task 1, that is exactly the 3 files listed above.
    2. Run `git status --porcelain` and confirm that the modified set is exactly those 3 files. If any other file appears as modified, STOP and investigate before staging (likely indicates another drift not caught in Task 1; handle per that task's fallback).
    3. Stage explicitly by path (no `git add -A`, no `git add .`):
       ```
       git add crates/nono-cli/src/config/mod.rs
       git add crates/nono-cli/src/exec_strategy_windows/restricted_token.rs
       git add crates/nono-cli/src/profile/mod.rs
       ```
    4. Commit with message (heredoc, includes DCO sign-off):
       ```
       style(19-CLEAN-01): cargo fmt drift follow-up from commit 6749494

       Runs `cargo fmt --all` on the three files left drifted after the EnvVarGuard
       migration (commit 6749494). No logic changes; `cargo fmt --all -- --check`
       is now clean on the entire workspace.

       Files: crates/nono-cli/src/config/mod.rs,
              crates/nono-cli/src/exec_strategy_windows/restricted_token.rs,
              crates/nono-cli/src/profile/mod.rs

       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
       ```
  </action>
  <verify>
    <automated>cargo fmt --all -- --check &amp;&amp; git log -1 --format='%H %s' &amp;&amp; git log -1 --format='%b' | grep -c '^Signed-off-by: '</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0 (clean whole workspace).
    - `git log -1 --format='%s'` begins with `style(19-CLEAN-01):`.
    - `git log -1 --format='%b' | grep -c '^Signed-off-by: '` returns exactly `1`.
    - `git log -1 --name-only --format=''` lists exactly the 3 files named in `files_modified`, no more, no less.
  </acceptance_criteria>
  <done>
    Commit landed on `windows-squash` with DCO sign-off; `cargo fmt --all -- --check` is green; only the 3 expected files appear in the diff.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify `make ci` gates fmt and run the plan smoke</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - Makefile (lines 70-83, 121-123)
    - .planning/phases/19-cleanup/19-CONTEXT.md § CLEAN-01 acceptance
  </read_first>
  <action>
    1. Confirm Makefile still has the chain `ci: check test audit` -> `check: clippy fmt-check` -> `fmt-check: cargo fmt --all -- --check`. This is a structural assertion on the Makefile; no edits expected.

    2. Run the fmt-check gate in isolation:
       ```
       make fmt-check
       ```
       Must exit 0.

    3. Do NOT run `make ci` end-to-end as the Task 3 smoke — that would also require `make test` and `make audit` to be green, which includes the 5 CLEAN-02 flakes still unresolved in Wave 1. Running `make fmt-check` alone is the correct CLEAN-01 smoke; the full `make ci` gate comes back online once CLEAN-02 lands.

    4. Record in the plan SUMMARY: (a) Task 1 pre-state confirmation, (b) Task 2 commit hash, (c) Task 3 `make fmt-check` exit code, (d) explicit note that `make ci` is gated by CLEAN-02 test flake fixes and is expected to pass only after Plan 19-02 completes.
  </action>
  <verify>
    <automated>make fmt-check &amp;&amp; grep -E "^check:.*fmt-check" Makefile &amp;&amp; grep -E "^ci:.*check" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `make fmt-check` exits 0.
    - `grep -E "^check:.*fmt-check" Makefile` returns 1 line containing `fmt-check`.
    - `grep -E "^ci:.*check" Makefile` returns 1 line containing `check` as a dependency of `ci`.
    - Plan SUMMARY.md contains: commit hash from Task 2, `make fmt-check` exit code, and the note about `make ci` dependency on CLEAN-02.
  </acceptance_criteria>
  <done>
    Structural `make ci` -> `fmt-check` chain is confirmed intact. `make fmt-check` passes. SUMMARY documents the full flow and explicitly flags the CLEAN-02 dependency for `make ci`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. `cargo fmt` is a local, deterministic, byte-level text transform over files already committed to the repo. No input data crosses a trust boundary during this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering | The 3 drifted files | accept | `cargo fmt` is idempotent; the rewrite is bit-for-bit reproducible from any Rust toolchain at the MSRV pinned in `rust-toolchain.toml` / Cargo.toml. Risk of "hidden tampering in a fmt commit" is mitigated by the Task 2 acceptance criterion that the diff touches exactly the 3 expected files — any unrelated byte change would show up as an additional modified file. |
| T-19-01-02 | Repudiation | Commit provenance | mitigate | DCO sign-off required in the commit message footer (enforced by the `windows-squash` commit hook); Task 2 acceptance criterion greps for `^Signed-off-by: ` in the commit body. |
</threat_model>

<verification>
- `cargo fmt --all -- --check` passes clean on the entire workspace.
- `make fmt-check` passes.
- The Makefile `ci -> check -> fmt-check` chain is intact (grep-verifiable).
- The commit message is `style(19-CLEAN-01): ...` and carries a DCO sign-off.
- The commit diff is confined to the 3 files named in D-03.
</verification>

<success_criteria>
- `cargo fmt --all -- --check` exits 0.
- One new commit on `windows-squash` with kind `style(19-CLEAN-01):`, DCO-signed.
- Exactly 3 files modified: `config/mod.rs`, `restricted_token.rs`, `profile/mod.rs`.
- Plan SUMMARY records Task 1 pre-state, Task 2 commit hash, Task 3 Makefile verification, and a forward-pointer noting that `make ci` fully passes only after Plan 19-02 closes CLEAN-02.
</success_criteria>

<output>
After completion, create `.planning/phases/19-cleanup/19-01-SUMMARY.md` using the standard summary template. Required sections: Outcome, What was done (one bullet per task), Verification table (copy from `<verification>` with actual results), Files changed (the 3-row table), Commits (the 1-row table with the actual hash), Status.
</output>
</content>
</invoke>