---
phase: 19-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/phases/10-etw-based-learn-command/10-RESEARCH.md
  - .planning/phases/10-etw-based-learn-command/10-UAT.md
  - .planning/phases/11-runtime-capability-expansion/11-01-PLAN.md
  - .planning/phases/11-runtime-capability-expansion/11-02-PLAN.md
  - .planning/phases/12-milestone-bookkeeping-cleanup/12-02-PLAN.md
  - .planning/quick/260410-nlt-fix-three-uat-gaps-in-phase-10-etw-learn/260410-nlt-PLAN.md
  - .planning/quick/260412-ajy-safe-layer-roadmap-input/
  - .planning/v1.0-INTEGRATION-REPORT.md
  - host.nono_binary.commit
  - query
autonomous: false
requirements: [CLEAN-03]

must_haves:
  truths:
    - "Every one of the 10 WIP items has a recorded disposition: commit alive, delete dead, or archive uncertain"
    - "The working tree is clean at end of plan except for files actively in flight for Phase 19 itself (this plan's and 19-01/19-02/19-04's)"
    - "`host.nono_binary.commit` and `query` are deleted AND ignored by `.gitignore` so they cannot silently re-accumulate"
    - "The plan SUMMARY contains a per-file disposition table covering all 10 items"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore patterns for the two known stray debug crumbs"
      contains: "host.nono_binary.commit\\n/query"
    - path: ".planning/phases/19-cleanup/19-03-SUMMARY.md"
      provides: "Per-file disposition table (required by D-10 + REQUIREMENTS CLEAN-03 #3)"
  key_links:
    - from: "D-11 (pre-decided deletes)"
      to: "`.gitignore` additions"
      via: "atomic delete-and-ignore commit"
      pattern: "chore\\(19-CLEAN-03\\): remove debug crumbs"
    - from: "`git status` at plan start"
      to: "`git status` clean at plan end"
      via: "per-file triage → commit/delete/archive"
      pattern: "working tree clean (modulo active 19-* work)"
---

<objective>
Triage the 10 uncommitted/untracked files identified in CONTEXT.md D-12 and leave the working tree clean except for files actively in flight for other Phase 19 plans. Each of the 10 items gets a per-file judgment — commit alive / delete dead / archive uncertain — and the decision is recorded in the plan SUMMARY as a required artifact (REQUIREMENTS CLEAN-03 acceptance #3).

Purpose: v2.0 accumulated a pile of disk-resident WIP that masks the real `git status` signal; paying this debt restores `git status` as a trustworthy "am I clean?" indicator for every subsequent phase.

Output:
- `.gitignore` updated with patterns for the two pre-decided stray files (D-11).
- 2 of 10 items deleted outright (the stray root-level debug crumbs).
- 8 of 10 items triaged with per-file judgment; each "commit alive" becomes its own `docs(19-CLEAN-03):` commit; each "delete dead" / "archive uncertain" group becomes one commit per category.
- Plan SUMMARY contains a per-file disposition table, publicly recording the triage log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/19-cleanup/19-CONTEXT.md
@.planning/phases/16-resource-limits/16-02-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
<!-- No code interfaces — this plan operates on planning artifacts and repo hygiene. -->

The 10 items to triage, from CONTEXT.md D-12 (git status at phase start):

Pre-decided deletes (D-11 — no inspection needed):
1.  `??  host.nono_binary.commit` — root-level debug crumb
2.  `??  query` — root-level debug crumb (looks like stdout capture)

Requires per-file judgment (D-10):
3.  `M   .planning/phases/11-runtime-capability-expansion/11-01-PLAN.md`
4.  `M   .planning/phases/11-runtime-capability-expansion/11-02-PLAN.md`
5.  `??  .planning/phases/10-etw-based-learn-command/10-RESEARCH.md`
6.  `??  .planning/phases/10-etw-based-learn-command/10-UAT.md`
7.  `??  .planning/phases/12-milestone-bookkeeping-cleanup/12-02-PLAN.md`
8.  `??  .planning/quick/260410-nlt-fix-three-uat-gaps-in-phase-10-etw-learn/260410-nlt-PLAN.md`
9.  `??  .planning/quick/260412-ajy-safe-layer-roadmap-input/` (directory)
10. `??  .planning/v1.0-INTEGRATION-REPORT.md`

Canonical reference for each item's origin (from CONTEXT.md § canonical_refs § "Existing artifacts CLEAN-03 will inspect"):
- Items 5, 6: likely unfinished research/UAT from v2.0 LEARN-01 (Phase 10).
- Items 3, 4: modified plan files; mods may be real corrections worth preserving.
- Item 7: draft of an unmerged plan.
- Item 8: quick task from 2026-04-10, never completed (no SUMMARY.md).
- Item 9: quick task from 2026-04-12, never completed.
- Item 10: historical artifact from v1.0 milestone close.

Current `.gitignore` at repo root (for Task 2 context):
```
/target
.vscode/
.idea/
.DS_Store
.claude/
proj/
build_notes/
docs/presentations/
docs/cli/development/
bindings/python/.venv*
bindings/node/node_modules*
```
No existing pattern covers `host.nono_binary.commit` or `query`. These need explicit additions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (pre-decided deletes): Remove stray root-level debug crumbs + update .gitignore</name>
  <files>
    .gitignore
    host.nono_binary.commit  (deleted)
    query  (deleted)
  </files>
  <read_first>
    - .planning/phases/19-cleanup/19-CONTEXT.md § D-11
    - .gitignore (current contents)
    - Actual file contents: `cat host.nono_binary.commit` and `cat query` (to confirm they are indeed debug crumbs before deleting — a sanity check, not a judgment call)
  </read_first>
  <action>
    1. Sanity-check the two stray files with `file` / `cat` to confirm they are indeed debug crumbs (per CONTEXT.md specifics: `host.nono_binary.commit` looks like a commit-hash log, `query` looks like captured stdout). Briefly capture their first line + size in the plan SUMMARY for forensic completeness.

    2. Delete both files from disk:
       ```
       rm -f host.nono_binary.commit
       rm -f query
       ```

    3. Append ignore patterns to `.gitignore` immediately after the `# misc` section (line ~12). Add a new section:
       ```
       # Debug crumbs and accidental stdout captures (19-CLEAN-03)
       host.nono_binary.commit
       /query
       ```
       Notes:
       - `/query` (leading slash) restricts the ignore to the repo root so a legitimate `query` file/dir inside a crate's tests doesn't get silently swallowed.
       - `host.nono_binary.commit` is globbed (no leading slash) because the name is specific enough that a false positive is extremely unlikely.

    4. Stage the two deletions and the .gitignore edit as a single atomic commit:
       ```
       git add .gitignore
       git rm host.nono_binary.commit
       git rm query
       ```
       Use `git rm` (not bare `rm` + stage) so the delete is recorded explicitly in the commit.

    5. Commit with message (heredoc, DCO-signed):
       ```
       chore(19-CLEAN-03): remove stray debug crumbs and add .gitignore patterns

       `host.nono_binary.commit` was a commit-hash log left by a build helper.
       `query` was accidentally captured stdout from a CLI run. Neither is
       referenced by any code or doc. Delete both and add .gitignore patterns
       (`host.nono_binary.commit`, `/query` — rooted) to prevent recurrence.

       Per CONTEXT.md D-11.

       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
       ```
  </action>
  <verify>
    <automated>test ! -f host.nono_binary.commit &amp;&amp; test ! -f query &amp;&amp; grep -c "^host\.nono_binary\.commit$\|^/query$" .gitignore &amp;&amp; git log -1 --format='%s' | grep -q "^chore(19-CLEAN-03)"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f host.nono_binary.commit` succeeds (file gone from working tree).
    - `test ! -f query` succeeds.
    - `grep -cE "^host\.nono_binary\.commit$" .gitignore` returns `1`.
    - `grep -cE "^/query$" .gitignore` returns `1`.
    - `git log -1 --format='%s'` starts with `chore(19-CLEAN-03):`.
    - `git log -1 --format='%b' | grep -c '^Signed-off-by: '` returns exactly `1`.
    - A freshly-created placeholder `echo x > host.nono_binary.commit && git status --porcelain host.nono_binary.commit` returns EMPTY (ignore rule works). (Clean up the placeholder after checking.)
  </acceptance_criteria>
  <done>
    Both stray files deleted, `.gitignore` blocks recurrence, one DCO-signed atomic commit on `windows-squash`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (inspection + disposition): Triage the 8 remaining WIP items</name>
  <what-built>
    The following 8 items still on disk as modified or untracked:
      3.  M  .planning/phases/11-runtime-capability-expansion/11-01-PLAN.md
      4.  M  .planning/phases/11-runtime-capability-expansion/11-02-PLAN.md
      5.  ?? .planning/phases/10-etw-based-learn-command/10-RESEARCH.md
      6.  ?? .planning/phases/10-etw-based-learn-command/10-UAT.md
      7.  ?? .planning/phases/12-milestone-bookkeeping-cleanup/12-02-PLAN.md
      8.  ?? .planning/quick/260410-nlt-fix-three-uat-gaps-in-phase-10-etw-learn/260410-nlt-PLAN.md
      9.  ?? .planning/quick/260412-ajy-safe-layer-roadmap-input/  (directory)
      10. ?? .planning/v1.0-INTEGRATION-REPORT.md
  </what-built>
  <how-to-verify>
    For each of the 8 items, read the file (or directory listing) and classify into exactly one of three buckets per D-10:

    - **Commit alive** — the file represents real work-in-progress with a clear destination. Commit it in-place under the appropriate phase directory. Evidence required: file has non-trivial content that references current/future planning artifacts AND is not duplicated elsewhere.

    - **Delete dead** — the file is obsolete, superseded, or purely a debug/experiment artifact. Remove from disk. Evidence required: file is unreferenced, its topic has been resolved elsewhere, or it is clearly transient.

    - **Archive uncertain** — the file has documentation value (historical context, unimplemented ideas worth recovering later) but no active home. Move to `.planning/archive/YYYY-MM/` (create the directory if needed; use the plan-run date `2026-04`). Commit the move, don't just delete.

    For each, produce a disposition record (to be transcribed into the plan SUMMARY):

    | # | Path | Bucket | One-line reason | Resulting action |
    |---|------|--------|------------------|-------------------|
    | 3 | 11-01-PLAN.md (M) | commit / revert / split | | `git add` + commit, OR `git checkout --` to discard, OR `git diff > .planning/archive/...` + checkout |
    | 4 | 11-02-PLAN.md (M) | | | |
    | 5 | 10-RESEARCH.md | | | |
    | 6 | 10-UAT.md | | | |
    | 7 | 12-02-PLAN.md | | | |
    | 8 | 260410-nlt-PLAN.md | | | |
    | 9 | 260412-ajy-*/ | | | |
    | 10 | v1.0-INTEGRATION-REPORT.md | | | |

    Specific inspection guidance (based on CONTEXT.md § canonical_refs):
    - Items 3, 4: check `git diff` against HEAD. If the mods are corrections (typo fixes, doc sharpening) → commit alive. If they're notes/scratch that don't belong in a committed plan → revert (`git checkout --`). If partial mid-edit → archive the diff.
    - Items 5, 6: are there complementary committed files in `.planning/phases/10-etw-based-learn-command/`? If the phase has a SUMMARY already, these may be superseded → delete. If not, they may be the record → commit.
    - Item 7: does `.planning/phases/12-milestone-bookkeeping-cleanup/` already have a SUMMARY? If yes, this draft is stale → delete. If no, it's the only record of Phase 12 plan 2 → commit.
    - Items 8, 9: check for a SUMMARY.md inside each quick-task directory. If absent (per CONTEXT.md note), the task never completed → archive the dir to preserve context OR delete if content is trivial.
    - Item 10: v1.0 is closed (see STATE.md: "v1.0 Windows Alpha — shipped 2026-03-31"). The integration report is historical → either commit (if it's the canonical record) or archive (if it's a draft superseded by a committed report elsewhere).

    Commit grouping per D-13:
    - Each "commit alive" item → its own `docs(19-CLEAN-03): commit &lt;file&gt; — &lt;reason&gt;` commit.
    - All "delete dead" items → ONE grouped commit `chore(19-CLEAN-03): remove &lt;N&gt; superseded WIP items` (list files in body).
    - All "archive uncertain" items → ONE grouped commit `chore(19-CLEAN-03): archive &lt;N&gt; uncertain WIP items to .planning/archive/2026-04/` (list files + one-line reason each in body).

    Every commit MUST:
    - Use `git add`/`git rm` with explicit paths (no `git add -A`).
    - Carry a DCO sign-off.

    Submit the disposition table for human review BEFORE executing any of the commits. The human confirms each bucket assignment (especially "commit alive" for modified items, because reverting vs committing a modification is the most consequential call).
  </how-to-verify>
  <resume-signal>
    Type "approved" with the approved disposition table (one line per item), or "revise: &lt;comments&gt;" to send specific items back for re-inspection. Only after approval does the executor run `git add` / `git rm` / `git mv` and create the grouped commits.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3 (execution): Apply the approved dispositions</name>
  <files>
    (All 8 items from Task 2 — exact set determined by approved disposition table; all paths are listed in `files_modified` frontmatter)
  </files>
  <read_first>
    - Approved disposition table from Task 2
    - CLAUDE.md § Coding Standards (DCO sign-off, staging hygiene)
  </read_first>
  <action>
    For each approved row in the Task 2 disposition table, perform the action column:

    1. **commit alive** (per-file atomic commit):
       ```
       git add &lt;path&gt;
       git commit -m "docs(19-CLEAN-03): commit &lt;file&gt; — &lt;one-line reason it's alive&gt;

       &lt;optional longer body&gt;

       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>"
       ```
       Repeat per file in this bucket.

    2. **delete dead** (one grouped commit):
       Collect all "delete dead" paths. Issue:
       ```
       git rm &lt;path1&gt;
       git rm &lt;path2&gt;
       ...
       # For modified-file-to-revert (items 3, 4 if classified here):
       git checkout -- &lt;path&gt;   # then also git add &lt;path&gt; with no changes — no, simpler: `git restore --staged --worktree &lt;path&gt;`
       ```
       Actually for items 3/4 classified as "revert": use `git restore --source=HEAD --staged --worktree &lt;path&gt;` to drop working-tree mods. That doesn't need a commit (HEAD already has the canonical version). Note this in the SUMMARY as "reverted, no commit".

       For actually-untracked deletes, just `rm -rf &lt;path&gt;` is sufficient (untracked → not in git → no commit needed for the deletion). Log the removal in SUMMARY.

       If any "delete dead" items were TRACKED (none in the current set are — all 6 of items 5-10 are `??`, and items 3-4 would use `git restore`), they'd get `git rm` + one grouped commit:
       ```
       chore(19-CLEAN-03): remove &lt;N&gt; superseded WIP items

       &lt;bulleted list of paths + one-line reason each&gt;

       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
       ```

    3. **archive uncertain** (one grouped commit):
       ```
       mkdir -p .planning/archive/2026-04
       git mv &lt;path&gt; .planning/archive/2026-04/&lt;renamed-if-needed&gt;
       # or, for untracked files: mv &lt;path&gt; .planning/archive/2026-04/ + git add
       ```
       One grouped commit:
       ```
       chore(19-CLEAN-03): archive &lt;N&gt; uncertain WIP items to .planning/archive/2026-04/

       - &lt;path&gt; → &lt;new-path&gt;  (reason)
       - ...

       Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
       ```

    4. At the end, run `git status` — expected output: "working tree clean", OR "modified/untracked files" but ONLY from other Plan-19 work (e.g. 19-01 / 19-02 / 19-04 artifacts actively being worked by sibling plans in the same wave). In the latter case, record in SUMMARY which untracked items are accepted leftovers (and point to the sibling plan that owns them).

    5. Every commit gets DCO sign-off. No `git add -A`. No `git add .`.
  </action>
  <verify>
    <automated>git status --porcelain | grep -vE "^\s*\.planning/phases/19-cleanup/19-0[1-4]-" | grep -vE "^\s*\.planning/phases/19-cleanup/19-03-SUMMARY\.md" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` shows no entries EXCEPT files actively owned by sibling Plan 19 work (Plans 19-01, 19-02, 19-04). All 10 original items from D-12 are either committed, deleted, archived, or reverted — none remain as modified/untracked.
    - Every Task 3 commit starts with one of `docs(19-CLEAN-03):` / `chore(19-CLEAN-03):` and carries a DCO sign-off: `git log windows-squash --grep='19-CLEAN-03' --format='%H' | xargs -I{} git log -1 {} --format='%b' | grep -c '^Signed-off-by: '` matches commit count.
    - If the `archive uncertain` bucket was used: `test -d .planning/archive/2026-04/` succeeds and the directory is non-empty.
    - Plan SUMMARY § Per-file disposition has all 10 rows filled (items 1-2 from Task 1, items 3-10 from Task 2).
  </acceptance_criteria>
  <done>
    All 10 items resolved per approved disposition. Working tree clean except for sibling Plan 19 work. SUMMARY contains the complete per-file disposition table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. All operations affect planning documents (`.planning/**/*.md`), `.gitignore`, and two stray non-code files at repo root. No code in `crates/` or `bindings/` is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Information Disclosure | Stray `host.nono_binary.commit` and `query` files at repo root | mitigate | Inspected before delete (Task 1 action step 1) to confirm they contain no secrets. `.gitignore` patterns added to prevent recurrence. Neither file is known to contain credentials; this is a precaution. |
| T-19-03-02 | Tampering | Planning artifacts under `.planning/` | accept | Planning documents are not security-critical; they are human-authored text. Git history provides integrity via commit hashes + DCO sign-off. Each archive/delete commit explicitly lists the affected paths in the body. |
| T-19-03-03 | Repudiation | Disposition of 10 files | mitigate | Per-file disposition recorded in plan SUMMARY (REQUIREMENTS CLEAN-03 acceptance #3). Commit messages reference CLEAN-03 + D-11/D-12 IDs. Reader can replay the triage decisions from the SUMMARY alone. |
</threat_model>

<verification>
- `host.nono_binary.commit` and `query` deleted + `.gitignore` updated (Task 1).
- Working tree clean at end of plan modulo active sibling Plan 19 work (Task 3).
- Per-file disposition table in plan SUMMARY covers all 10 items from D-12 (Task 1 + Task 3).
- Every new commit carries `(19-CLEAN-03)` scope + DCO sign-off.
- Human checkpoint approved the disposition before any of items 3-10 was touched (Task 2).
</verification>

<success_criteria>
- `git status --porcelain` (filtered to exclude sibling Plan 19 work) returns empty.
- `.gitignore` contains `host.nono_binary.commit` and `/query` patterns.
- `.planning/phases/19-cleanup/19-03-SUMMARY.md` contains a 10-row disposition table + bucket counts (how many alive / dead / archived / reverted).
- All commits atomic, DCO-signed, explicitly-staged, scope `(19-CLEAN-03)`.
</success_criteria>

<output>
After completion, create `.planning/phases/19-cleanup/19-03-SUMMARY.md` using the standard summary template. REQUIRED sections:

1. Outcome.
2. What was done (one bullet per task).
3. **Per-file disposition** — mandatory table with 10 rows (items 1-10 from CONTEXT.md D-12), columns: `#`, `Path`, `Bucket` (alive/dead/archive/revert), `One-line reason`, `Action taken` (commit hash, `rm`, `git rm + commit`, `git mv + commit`, or `git restore`), `New location` (if archived).
4. Bucket summary — how many went into each bucket.
5. Verification table.
6. Files changed.
7. Commits (one row per commit).
8. Status.

SUMMARY must stand alone as the audit trail for CLEAN-03; future sessions should not need to reconstruct the decision process from git log alone.
</output>
</content>
</invoke>