---
phase: 02-pr-b-cli-messaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/setup.rs
  - crates/nono-cli/src/execution_runtime.rs
  - crates/nono-cli/src/command_runtime.rs
  - crates/nono-cli/src/output.rs
autonomous: true
requirements: [CLIMSG-01, CLIMSG-02, CLIMSG-03]

must_haves:
  truths:
    - "setup --check-only emits a single 'Support status: supported' line, no CLI/library split"
    - "Live run on Windows follows the same code path as Linux/macOS (print_applying_sandbox -> apply -> print_sandbox_active)"
    - "nono shell and nono wrap are unconditionally rejected on Windows regardless of is_supported value"
    - "No dead code remains in execution_runtime.rs, output.rs, or setup.rs from the old !is_supported branches"
  artifacts:
    - path: "crates/nono-cli/src/setup.rs"
      provides: "Unified support status line, no windows_cli_support_status_label function"
      contains: "Support status:"
    - path: "crates/nono-cli/src/execution_runtime.rs"
      provides: "No validate_windows_preview_direct_execution, no dead !is_supported branch"
    - path: "crates/nono-cli/src/command_runtime.rs"
      provides: "Unconditional shell/wrap rejection on Windows"
    - path: "crates/nono-cli/src/output.rs"
      provides: "No dead !is_supported branches in print_banner, print_supervised_info, dry_run_summary"
  key_links:
    - from: "crates/nono-cli/src/setup.rs"
      to: "nono::Sandbox::support_info().status_label()"
      via: "unified status line"
      pattern: "info\\.status_label\\(\\)"
    - from: "crates/nono-cli/src/execution_runtime.rs"
      to: "output::print_applying_sandbox / print_sandbox_active"
      via: "cross-platform sandbox path"
      pattern: "print_applying_sandbox|print_sandbox_active"
---

<objective>
Remove all dead `!support.is_supported` branches and the CLI/library support split from production CLI code. After PR-A, `is_supported` is `true` on Windows, making these branches unreachable dead code.

Purpose: Align CLI messaging to the library's promoted contract; eliminate the two-line CLI/library status split; make shell/wrap rejection unconditional (security fix).
Output: Clean production code in setup.rs, execution_runtime.rs, command_runtime.rs, output.rs with no dead Windows branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pr-b-cli-messaging/02-CONTEXT.md

<interfaces>
<!-- Library API (read-only, do not modify) -->

From crates/nono/src/sandbox/mod.rs:
- `Sandbox::support_info()` returns `SupportInfo`
- `SupportInfo::status_label()` returns `self.status.as_str()` = `"supported"` on Windows
- `SupportInfo::is_supported` = `true` on Windows

From crates/nono/src/sandbox/windows.rs:
- `WINDOWS_PREVIEW_SUPPORTED = true`
- `support_info()` returns `SupportStatus::Supported`
- `validate_preview_entry_point(Shell, ...)` returns error with "intentionally unavailable on Windows"
- `validate_preview_entry_point(Wrap, ...)` returns error with "intentionally unavailable on Windows"

From crates/nono-cli/src/output.rs:
- `print_applying_sandbox(silent)` prints `"Applying sandbox..."` to stderr (no newline)
- `print_sandbox_active(silent)` prints `" active"` or `"  active"` (green bold) to stderr
- Combined output on stderr: `"Applying sandbox... active"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code from setup.rs and collapse unified status line (D-01, D-02, D-03, D-04)</name>
  <files>crates/nono-cli/src/setup.rs</files>
  <read_first>
    - crates/nono-cli/src/setup.rs (full file — understand current test_windows_support, print_check_only_summary, windows_cli_support_status_label)
    - crates/nono/src/sandbox/mod.rs (SupportInfo::status_label — read-only reference)
  </read_first>
  <action>
Per D-01: Delete the entire `windows_cli_support_status_label()` function (line ~762, returns `"supported restricted command surface"`).

Per D-02: In `test_windows_support()` (line ~184), replace these two lines:
```rust
println!("  * CLI support status: {}", windows_cli_support_status_label());
println!("  * Library support status: {}", info.status_label());
```
with a single line:
```rust
println!("  * Support status: {}", info.status_label());
```

Per D-03: In `print_check_only_summary()` (line ~741), replace these two lines:
```rust
println!("CLI support status: {}", windows_cli_support_status_label());
println!("Library support status: {}", info.status_label());
```
with a single line:
```rust
println!("Support status: {}", info.status_label());
```

Per D-04: After these changes, `info.status_label()` is the single source of truth for Windows support status in all setup output. No other function generates a separate CLI support label.

Verify that no other call sites reference `windows_cli_support_status_label` anywhere in the crate.
  </action>
  <verify>
    <automated>cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `setup.rs` does NOT contain the function `fn windows_cli_support_status_label`
    - `setup.rs` does NOT contain the string `"CLI support status:"`
    - `setup.rs` does NOT contain the string `"Library support status:"`
    - `setup.rs` contains `"  * Support status: {}"` in `test_windows_support`
    - `setup.rs` contains `"Support status: {}"` in `print_check_only_summary`
    - `cargo clippy -p nono-cli` passes clean
  </acceptance_criteria>
  <done>windows_cli_support_status_label deleted; both test_windows_support and print_check_only_summary emit a single unified "Support status:" line using info.status_label()</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead branches from execution_runtime.rs, command_runtime.rs, and output.rs (D-05 through D-14)</name>
  <files>crates/nono-cli/src/execution_runtime.rs, crates/nono-cli/src/command_runtime.rs, crates/nono-cli/src/output.rs</files>
  <read_first>
    - crates/nono-cli/src/execution_runtime.rs (full file — understand apply_pre_fork_sandbox dead branch lines 22-43, validate_windows_preview_direct_execution lines 64-82, call site lines 224-228)
    - crates/nono-cli/src/command_runtime.rs (run_shell lines 77-87, run_wrap lines 148-158)
    - crates/nono-cli/src/output.rs (print_banner lines 48-61, print_supervised_info lines 317-331, dry_run_summary lines 564-573)
  </read_first>
  <action>
**execution_runtime.rs changes:**

Per D-05/D-14: In `apply_pre_fork_sandbox()`, remove the entire `#[cfg(target_os = "windows")]` block at lines 22-42 that checks `if !support.is_supported { ... return Ok(()); }`. This is dead code since `is_supported` is now `true`. After removal, the function flows directly to `output::print_applying_sandbox(silent)` for all platforms.

Per D-05 (Pitfall 5): After removing the dead branch, the `#[cfg(target_os = "windows")] current_dir: &Path` parameter to `apply_pre_fork_sandbox` is unused — the only usage was inside the dead branch (`Sandbox::preview_runtime_status(caps, current_dir, ...)`). Remove this parameter from the function signature. Also update the call site in `execute_sandboxed()` (line ~229-235) to stop passing `#[cfg(target_os = "windows")] &current_dir`.

Per D-12: Delete the entire `validate_windows_preview_direct_execution()` function (lines 64-82). It always returned `Ok(())` after PR-A because `support.is_supported` is `true`, so `validate_windows_preview_entry_point(RunDirect, ...)` never fired.

Per D-13: Remove the call site block at lines 224-227:
```rust
#[cfg(target_os = "windows")]
if matches!(strategy, exec_strategy::ExecStrategy::Direct) {
    validate_windows_preview_direct_execution(&flags, &caps)?;
}
```

After removing dead code from execution_runtime.rs, check whether any `use` imports at the top of the file are now unused (e.g., `use std::path::Path` may become unused if `current_dir` was the only Path usage on Windows). Remove unused imports.

**command_runtime.rs changes:**

Per D-09: In `run_shell()` (line ~77-87), remove the `if !Sandbox::support_info().is_supported {` guard wrapper. The `#[cfg(target_os = "windows")]` block stays, but the call to `validate_windows_preview_entry_point(Shell, ...)` fires unconditionally:
```rust
#[cfg(target_os = "windows")]
Sandbox::validate_windows_preview_entry_point(
    nono::WindowsPreviewEntryPoint::Shell,
    &prepared.caps,
    &resolve_requested_workdir(args.sandbox.workdir.as_ref()),
    nono::WindowsPreviewContext {
        has_deny_override_policy: !prepared.override_deny_paths.is_empty(),
    },
)?;
```

Per D-10: Same change in `run_wrap()` (line ~148-158): remove the `if !Sandbox::support_info().is_supported {` guard. The `#[cfg(target_os = "windows")]` block calling `validate_windows_preview_entry_point(Wrap, ...)` fires unconditionally.

**output.rs changes (Research Pitfall 1):**

In `print_banner()` (lines 48-61): Remove the `#[cfg(target_os = "windows")]` block that checks `if !support.is_supported { ... }` and prints the "Windows restricted execution" subtitle. After PR-A this branch is dead.

In `print_supervised_info()` (lines 317-331): Remove the `#[cfg(target_os = "windows")]` block that checks `if !support.is_supported { ... }` and prints `"supervised subset (...)"`; after removal, the function always prints `"supervised (...)"`  for all platforms.

In `dry_run_summary()` (lines 564-573, Research Pitfall 3): Remove the `#[cfg(target_os = "windows")]` block that checks `if !_support.is_supported { return "dry-run validates the current Windows command surface without claiming full parity"; }`. After PR-A, this branch is dead. The function now always returns `"sandbox would be applied with above capabilities"` for all platforms. After removing the `#[cfg]` block, the `_support` parameter can be renamed back to `support` (or kept as `_support` if still unused on some cfg — check).

Leave the file-level `#[cfg_attr(target_os = "windows", allow(dead_code))]` attribute at line 0 in place — it covers genuinely Linux/macOS-only functions like `print_abi_info`.
  </action>
  <verify>
    <automated>cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `execution_runtime.rs` does NOT contain `fn validate_windows_preview_direct_execution`
    - `execution_runtime.rs` does NOT contain `preview_runtime_status`
    - `execution_runtime.rs` does NOT contain `if !support.is_supported`
    - `execution_runtime.rs` `apply_pre_fork_sandbox` signature does NOT have `current_dir: &Path`
    - `command_runtime.rs` does NOT contain `if !Sandbox::support_info().is_supported`
    - `command_runtime.rs` DOES contain `validate_windows_preview_entry_point` (called unconditionally)
    - `output.rs` `print_banner` does NOT contain `if !support.is_supported`
    - `output.rs` `print_supervised_info` does NOT contain `if !support.is_supported`
    - `output.rs` `dry_run_summary` does NOT contain `if !_support.is_supported`
    - `cargo clippy -p nono-cli` passes clean (no dead_code warnings, no unused imports)
  </acceptance_criteria>
  <done>All dead !is_supported branches removed from execution_runtime.rs, command_runtime.rs, and output.rs; validate_windows_preview_direct_execution deleted; apply_pre_fork_sandbox parameter cleaned up; shell/wrap rejection fires unconditionally on Windows</done>
</task>

</tasks>

<verification>
cargo build -p nono-cli 2>&1 | tail -5
cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used 2>&1 | tail -10
</verification>

<success_criteria>
- `cargo build -p nono-cli` succeeds with no errors
- `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` passes clean
- No `#[allow(dead_code)]` added anywhere
- No remaining `!support.is_supported` or `!_support.is_supported` dead branches in any modified file
- `windows_cli_support_status_label` function no longer exists
- `validate_windows_preview_direct_execution` function no longer exists
</success_criteria>

<output>
After completion, create `.planning/phases/02-pr-b-cli-messaging/02-01-SUMMARY.md`
</output>
