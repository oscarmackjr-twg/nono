---
phase: 04-pr-d-docs-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cli/development/windows-promotion-criteria.mdx
autonomous: true
requirements:
  - DOCSREL-02
  - DOCSREL-03
must_haves:
  truths:
    - "windows-promotion-criteria.mdx intro paragraph no longer describes the CLI/library split as the adopted contract"
    - "Gate table row 'CI lanes treat Windows identically' shows 'Met' not 'In progress'"
    - "Gate table row 'Formal promotion criteria recorded' shows 'Met' not 'In progress'"
    - "Current Audit Result section no longer describes the CLI/library split as deliberate — describes the aligned contract"
    - "A '## Milestone Closed: WIN-1706' section exists at the bottom of the file with a CI badge, a pinned run URL, and a summary paragraph"
  artifacts:
    - path: "docs/cli/development/windows-promotion-criteria.mdx"
      provides: "Updated promotion criteria doc: split language removed, gates marked Met, WIN-1706 closeout section appended"
      contains: "Milestone Closed: WIN-1706"
  key_links:
    - from: "windows-promotion-criteria.mdx ## Milestone Closed section"
      to: "GitHub Actions CI run"
      via: "pinned run URL fetched via gh CLI at execution time"
      pattern: "github.com/always-further/nono/actions/runs/[0-9]+"
---

<objective>
Update docs/cli/development/windows-promotion-criteria.mdx to reflect the aligned Windows support contract delivered by WIN-1706 (PR-A through PR-C). Remove stale split-language, mark remaining "In progress" gates as "Met", update the Current Audit Result section to describe the full aligned contract, and append a `## Milestone Closed: WIN-1706` section with a CI badge, pinned run URL, and closeout paragraph.

Purpose: Satisfy DOCSREL-02 (no CLI/library split language remaining in docs) and DOCSREL-03 (WIN-1706 closeout documented with green CI evidence). This file becomes the single canonical pointer maintainers use to verify Windows support is real.
Output: windows-promotion-criteria.mdx updated in-place; no new files created.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pr-d-docs-release/04-CONTEXT.md
@.planning/phases/01-pr-a-library-contract/01-VERIFICATION.md
@.planning/phases/03-pr-c-ci-verification/03-VERIFICATION.md

<interfaces>
<!-- Exact current content of windows-promotion-criteria.mdx that the executor must work from. -->

CURRENT FILE (all 94 lines as of plan creation):

Line 8 (intro paragraph body) — currently reads:
  "This document records the gates for the adopted Windows support contract in nono releases. The current branch treats the Windows CLI/release surface as supported while keeping the embedded library `Sandbox::apply()` contract explicitly partial."

  Must become:
  "This document records the gates for the Windows support contract in nono releases. Windows is supported as a first-class platform under the full aligned contract: `Sandbox::apply()` is real on Windows, the CLI/library split is closed, and `nono shell` / `nono wrap` remain intentionally unavailable as documented product decisions."

Line 14–15 (Gate Summary table) — currently two "In progress" rows:
  | CI lanes treat Windows identically to macOS and Linux | REL-01 | In progress |
  | Formal promotion criteria recorded and reviewed | REL-04 | In progress |

  Both must become:
  | CI lanes treat Windows identically to macOS and Linux | REL-01 | Met |
  | Formal promotion criteria recorded and reviewed | REL-04 | Met |

Line 28–29 (REL-01 gate detail) — currently:
  "- [x] Windows CLI/release support is described independently from the embedded library's partial `Sandbox::apply()` contract"

  Must become (the old split framing is now false; replace with the aligned contract fact):
  "- [x] Windows CLI/release support and the embedded library `Sandbox::apply()` contract are aligned under the same first-class supported status"

Lines 63–64 (Current Audit Result "Met" bullet) — currently:
  "- Setup output now distinguishes the supported Windows CLI/release surface from the embedded library's partial support boundary."
  "- README and product docs now describe Windows as a supported CLI platform without claiming library parity that does not exist yet."

  Must become:
  "- Setup output describes Windows as first-class supported under the aligned contract."
  "- README and product docs describe Windows as a supported platform across both the CLI surface and the embedded library."

Lines 68–71 (Current Audit Result "Deliberate boundary" block) — currently:
  "Deliberate boundary:"
  ""
  "- `Sandbox::support_info()` still reports Windows as `partial`, and the library-wide `Sandbox::apply()` path is still not the Windows backend used by the CLI."
  "- That is now the documented support split rather than an accidental contradiction: Windows is supported at the CLI/release layer, while the embedded library contract remains narrower on Windows."
  ""
  "That means `WIN-1706` is complete under the adopted CLI-surface support contract. Further backend/library alignment remains valid follow-up work, but it is no longer a blocker for honest Windows CLI support messaging."

  Must become:
  "WIN-1706 is complete under the full aligned contract. `Sandbox::apply()` is real on Windows as of PR-A; the CLI and library no longer describe different support states. `nono shell` and `nono wrap` remain intentionally unavailable as documented product decisions, not gaps in the contract."

  (Remove the "Deliberate boundary:" sub-header entirely; replace the three-bullet block and closing sentence with the single paragraph above.)

CLOSEOUT SECTION — append at bottom of file (after line 94, which ends "- **Packaging path:** installer, upgrade, uninstall, and any service packaging claims must match what the release workflow actually builds"):

```markdown

## Milestone Closed: WIN-1706

[![CI](https://github.com/always-further/nono/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/always-further/nono/actions/workflows/ci.yml)

Pinned green CI run (first successful run on main after Phase 3 landed): **[View run]({PINNED_RUN_URL})**

WIN-1706 is closed under the full aligned contract — not just the CLI-surface contract. `Sandbox::apply()` validates capability sets and returns `Ok(())` for the supported Windows subset; `is_supported()`, `support_info()`, and `apply()` describe the same contract. The CLI/library support split is gone. `nono shell` and `nono wrap` remain intentionally unavailable on Windows as documented product decisions. The regression harness and CI Windows lanes assert the aligned contract and pass without "partial" language. All four phases (PR-A through PR-D) are complete.
```

Replace `{PINNED_RUN_URL}` with the actual URL fetched at execution time (see action below).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch pinned CI run URL via gh CLI</name>
  <files>(no file written — output used in Task 2)</files>
  <read_first>
    - No file to read. Run the gh CLI command and capture the output.
  </read_first>
  <action>
    Run the following command to find the most recent successful CI run on main:

      gh run list --workflow=ci.yml --branch=main --status=success --limit=5 --json databaseId,headBranch,conclusion

    From the JSON output, select the first entry (most recent). Extract its `databaseId` value.

    Construct the pinned run URL:
      https://github.com/always-further/nono/actions/runs/{databaseId}

    Store this URL for use in Task 2. If the gh CLI returns no results (network unavailable or no successful runs found), use the placeholder text "PENDING — fetch manually via: gh run list --workflow=ci.yml --branch=main --status=success --limit=1" as the URL value so Task 2 can still complete.
  </action>
  <verify>
    <automated>gh run list --workflow=ci.yml --branch=main --status=success --limit=1 --json databaseId | grep -E '"databaseId":[0-9]+' && echo "PASS: run ID found" || echo "WARN: no successful run found — use placeholder"</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --workflow=ci.yml --branch=main --status=success --limit=1 --json databaseId` produces JSON with a `databaseId` field, OR the placeholder string is recorded for use in Task 2.
  </acceptance_criteria>
  <done>Pinned run URL (or placeholder) is ready for embedding in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Update windows-promotion-criteria.mdx — remove split language, mark gates Met, append closeout section</name>
  <files>docs/cli/development/windows-promotion-criteria.mdx</files>
  <read_first>
    - docs/cli/development/windows-promotion-criteria.mdx (full file — confirm exact line content before editing; use the interfaces block above as the reference for what each line currently says)
  </read_first>
  <action>
    Make the following targeted edits to docs/cli/development/windows-promotion-criteria.mdx. Read the file first, confirm each target string is present, then apply:

    EDIT 1 — Intro paragraph (line 8 region):
    Find:
      "This document records the gates for the adopted Windows support contract in nono releases. The current branch treats the Windows CLI/release surface as supported while keeping the embedded library `Sandbox::apply()` contract explicitly partial."
    Replace with:
      "This document records the gates for the Windows support contract in nono releases. Windows is supported as a first-class platform under the full aligned contract: `Sandbox::apply()` is real on Windows, the CLI/library split is closed, and `nono shell` / `nono wrap` remain intentionally unavailable as documented product decisions."

    EDIT 2 — Gate Summary table row (REL-01, line 15 region):
    Find:
      "| CI lanes treat Windows identically to macOS and Linux | REL-01 | In progress |"
    Replace with:
      "| CI lanes treat Windows identically to macOS and Linux | REL-01 | Met |"

    EDIT 3 — Gate Summary table row (REL-04, line 19 region):
    Find:
      "| Formal promotion criteria recorded and reviewed | REL-04 | In progress |"
    Replace with:
      "| Formal promotion criteria recorded and reviewed | REL-04 | Met |"

    EDIT 4 — REL-01 gate detail bullet (line 28 region):
    Find:
      "- [x] Windows CLI/release support is described independently from the embedded library's partial `Sandbox::apply()` contract"
    Replace with:
      "- [x] Windows CLI/release support and the embedded library `Sandbox::apply()` contract are aligned under the same first-class supported status"

    EDIT 5 — Current Audit Result "Met" bullets (lines 63–64 region):
    Find:
      "- Setup output now distinguishes the supported Windows CLI/release surface from the embedded library's partial support boundary."
    Replace with:
      "- Setup output describes Windows as first-class supported under the aligned contract."

    Find:
      "- README and product docs now describe Windows as a supported CLI platform without claiming library parity that does not exist yet."
    Replace with:
      "- README and product docs describe Windows as a supported platform across both the CLI surface and the embedded library."

    EDIT 6 — Remove "Deliberate boundary" block and replace closing sentence (lines 68–71 region):
    Find and remove the following block entirely (including surrounding blank lines that are part of this block only):
      "Deliberate boundary:"
      ""
      "- `Sandbox::support_info()` still reports Windows as `partial`, and the library-wide `Sandbox::apply()` path is still not the Windows backend used by the CLI."
      "- That is now the documented support split rather than an accidental contradiction: Windows is supported at the CLI/release layer, while the embedded library contract remains narrower on Windows."
      ""
      "That means `WIN-1706` is complete under the adopted CLI-surface support contract. Further backend/library alignment remains valid follow-up work, but it is no longer a blocker for honest Windows CLI support messaging."

    Replace the removed block with the single paragraph:
      "WIN-1706 is complete under the full aligned contract. `Sandbox::apply()` is real on Windows as of PR-A; the CLI and library no longer describe different support states. `nono shell` and `nono wrap` remain intentionally unavailable as documented product decisions, not gaps in the contract."

    APPEND — Milestone closeout section (at end of file, after "Prior Phase Prerequisites" section):
    Append a blank line then the following content, substituting the actual pinned run URL from Task 1 for `{PINNED_RUN_URL}`:

    ```
    ## Milestone Closed: WIN-1706

    [![CI](https://github.com/always-further/nono/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/always-further/nono/actions/workflows/ci.yml)

    Pinned green CI run (first successful run on main after Phase 3 landed): **[View run]({PINNED_RUN_URL})**

    WIN-1706 is closed under the full aligned contract — not just the CLI-surface contract. `Sandbox::apply()` validates capability sets and returns `Ok(())` for the supported Windows subset; `is_supported()`, `support_info()`, and `apply()` describe the same contract. The CLI/library support split is gone. `nono shell` and `nono wrap` remain intentionally unavailable on Windows as documented product decisions. The regression harness and CI Windows lanes assert the aligned contract and pass without "partial" language. All four phases (PR-A through PR-D) are complete.
    ```
  </action>
  <verify>
    <automated>
      grep -n "CLI/library split is closed" docs/cli/development/windows-promotion-criteria.mdx || echo "FAIL: intro paragraph not updated"
      grep -n "In progress" docs/cli/development/windows-promotion-criteria.mdx && echo "FAIL: 'In progress' rows remain" || echo "PASS: all gates Met"
      grep -n "Deliberate boundary" docs/cli/development/windows-promotion-criteria.mdx && echo "FAIL: old deliberate boundary block remains" || echo "PASS: block removed"
      grep -n "support contract still reports Windows as partial" docs/cli/development/windows-promotion-criteria.mdx && echo "FAIL: old bullet remains" || echo "PASS: removed"
      grep -n "adopted CLI-surface support contract" docs/cli/development/windows-promotion-criteria.mdx && echo "FAIL: old closing sentence remains" || echo "PASS: removed"
      grep -n "Milestone Closed: WIN-1706" docs/cli/development/windows-promotion-criteria.mdx || echo "FAIL: closeout section missing"
      grep -n "actions/runs/" docs/cli/development/windows-promotion-criteria.mdx || echo "FAIL: pinned run URL missing"
      grep -n "ci.yml/badge.svg" docs/cli/development/windows-promotion-criteria.mdx || echo "FAIL: CI badge missing"
      grep -n "prior-phase-prerequisites\|Prior Phase Prerequisites" docs/cli/development/windows-promotion-criteria.mdx || echo "FAIL: Prior Phase Prerequisites section missing"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "CLI/library split is closed" docs/cli/development/windows-promotion-criteria.mdx` returns a match (intro updated)
    - `grep "In progress" docs/cli/development/windows-promotion-criteria.mdx` returns no matches (all gates now Met)
    - `grep "Deliberate boundary" docs/cli/development/windows-promotion-criteria.mdx` returns no matches
    - `grep "adopted CLI-surface support contract" docs/cli/development/windows-promotion-criteria.mdx` returns no matches
    - `grep "support contract still reports Windows as partial" docs/cli/development/windows-promotion-criteria.mdx` returns no matches
    - `grep "Milestone Closed: WIN-1706" docs/cli/development/windows-promotion-criteria.mdx` returns a match
    - `grep "ci.yml/badge.svg" docs/cli/development/windows-promotion-criteria.mdx` returns a match (badge present)
    - `grep "actions/runs/" docs/cli/development/windows-promotion-criteria.mdx` returns a match (run URL present, not just placeholder text)
    - `grep "Prior Phase Prerequisites" docs/cli/development/windows-promotion-criteria.mdx` returns a match (section preserved)
    - `grep "aligned under the same first-class supported status" docs/cli/development/windows-promotion-criteria.mdx` returns a match (REL-01 detail bullet updated)
  </acceptance_criteria>
  <done>windows-promotion-criteria.mdx describes the aligned contract throughout; all gates are Met; the WIN-1706 closeout section with CI badge and pinned run URL is appended at the bottom.</done>
</task>

</tasks>

<verification>
All acceptance_criteria grep commands above return the expected results (matches for PASS conditions, no matches for removed content).

Additionally:
- `git diff --stat` shows only `docs/cli/development/windows-promotion-criteria.mdx`
- `git diff docs/cli/development/windows-promotion-criteria.mdx` shows no remaining "In progress", no "Deliberate boundary", no "partial" in the audit section, and the new closeout section at the bottom
- The file is valid Markdown (no unclosed code fences, no broken table syntax — count pipe characters in each table row)
</verification>

<success_criteria>
1. windows-promotion-criteria.mdx intro paragraph describes Windows as first-class supported under the aligned contract — no "current branch treats" split language.
2. Both "In progress" gate rows in the Gate Summary table read "Met".
3. The "Deliberate boundary" block and all three lines describing the old split are removed from Current Audit Result.
4. A `## Milestone Closed: WIN-1706` section exists at the bottom with: a CI badge linking to the ci.yml workflow, a pinned run URL pointing to a real GitHub Actions run ID, and a summary paragraph stating the aligned contract is complete.
5. The "What Windows Does Not Support" section and "Prior Phase Prerequisites" section are untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pr-d-docs-release/04-02-SUMMARY.md` following the summary template.
</output>
