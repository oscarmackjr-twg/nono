---
phase: 04-pr-d-docs-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/cli/internals/security-model.mdx
autonomous: true
requirements:
  - DOCSREL-01
  - DOCSREL-02
must_haves:
  truths:
    - "README contains no sentence 'The embedded library Sandbox::apply() contract remains partial on Windows for now'"
    - "README contains no paragraph beginning 'On Windows, the CLI support contract is broader than'"
    - "security-model.mdx contains no paragraph beginning 'There is also a current support-contract split to keep in mind'"
    - "security-model.mdx 'Windows Model' section and structural limitations paragraphs remain intact"
  artifacts:
    - path: "README.md"
      provides: "Platform support paragraph (line 46 region) with stale sentence removed; Library section (line 88 region) with stale paragraph removed"
    - path: "docs/cli/internals/security-model.mdx"
      provides: "Windows Model section with support-contract split paragraph (former lines 373–378) removed"
  key_links:
    - from: "README.md line 46 region"
      to: "surrounding platform support text"
      via: "inline removal — surrounding sentences unchanged"
      pattern: "nono shell.*nono wrap.*intentionally unavailable.*Installation Guide"
    - from: "docs/cli/internals/security-model.mdx"
      to: "## Summary section"
      via: "paragraph removed, section header and structural limitations above it preserved"
      pattern: "Windows currently uses a narrower restricted-execution"
---

<objective>
Remove two stale passages from README.md and one stale paragraph from docs/cli/internals/security-model.mdx. These passages described the now-gone CLI/library support split. With PR-A/B/C complete, the split no longer exists and these sentences are false.

Purpose: Eliminate false claims from public-facing docs before the PR-D branch is published.
Output: README.md and security-model.mdx with split-describing text removed; surrounding content unchanged.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pr-d-docs-release/04-CONTEXT.md

<interfaces>
<!-- Key facts the executor needs. No code interfaces — this is a docs-only plan. -->

README.md line 46 (full line, to be surgically edited):
  **Platform support:** macOS, Linux, [WSL2](https://nono.sh/docs/cli/internals/wsl2), and the Windows native CLI are supported today. Windows native builds support a real restricted-execution command surface for setup, dry-run, direct execution, blocked-network, and selected supervised flows. Live `nono shell` and `nono wrap` remain intentionally unavailable on Windows; use their `--dry-run` forms to inspect policy. The embedded library `Sandbox::apply()` contract remains partial on Windows for now. See the [Installation Guide](https://docs.nono.sh/cli/getting_started/installation) for the current Windows boundary.

  The sentence to remove (exact text):
    "The embedded library `Sandbox::apply()` contract remains partial on Windows for now."

  After removal, line 46 reads:
    **Platform support:** macOS, Linux, [WSL2](https://nono.sh/docs/cli/internals/wsl2), and the Windows native CLI are supported today. Windows native builds support a real restricted-execution command surface for setup, dry-run, direct execution, blocked-network, and selected supervised flows. Live `nono shell` and `nono wrap` remain intentionally unavailable on Windows; use their `--dry-run` forms to inspect policy. See the [Installation Guide](https://docs.nono.sh/cli/getting_started/installation) for the current Windows boundary.

README.md line 88 (full line, to be removed entirely):
  On Windows, the CLI support contract is broader than the current embedded library contract: the native CLI surface above is supported, while the library-wide `Sandbox::apply()` path remains partial until backend/library parity work lands.

  This is a standalone paragraph. Remove the entire line (and any blank line separating it from the next element). The line immediately before (line 87) is the paragraph text "The core is a Rust library..." and the line after (line 90) begins the Rust language subsection header.

security-model.mdx lines 373–378 (exact block to remove):
  Line 373: There is also a current support-contract split to keep in mind:
  Line 374: (blank)
  Line 375: - the CLI now exposes a real Windows restricted-execution command surface for setup, dry-run, direct execution, blocked-network, and selected supervised flows
  Line 376: - the library-wide `Sandbox::apply()` support contract still reports Windows as partial rather than first-class supported
  Line 377: (blank)
  Line 378: That split is the adopted Windows support contract for now. It lets release and support messaging treat the Windows CLI surface as supported without overstating the narrower embedded library contract.

  Lines 379–380 begin "## Summary" — that section header must remain.
  Lines 355–372 (Windows Model header through structural limitations) must remain untouched.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale sentence from README.md line 46 and stale paragraph from README.md line 88</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — confirm exact line content before editing; line numbers may have drifted)
  </read_first>
  <action>
    Make two targeted removals in README.md:

    REMOVAL 1 — Line 46 sentence:
    Find the platform support paragraph (starts with "**Platform support:**"). Within that paragraph, locate and delete the single sentence:
      "The embedded library `Sandbox::apply()` contract remains partial on Windows for now."
    Delete only that sentence (including the space before it). The surrounding text — "Live `nono shell` and `nono wrap` remain intentionally unavailable on Windows; use their `--dry-run` forms to inspect policy." and "See the [Installation Guide](...) for the current Windows boundary." — must remain in place.

    REMOVAL 2 — Line 88 paragraph:
    Find the paragraph in the ## Library section that begins:
      "On Windows, the CLI support contract is broader than the current embedded library contract:"
    Delete this entire paragraph (the single-line standalone paragraph). Do not delete any surrounding blank lines that are needed to preserve Markdown structure (the blank line between the intro paragraph ending in "...applies only what clients explicitly request." and the Rust language subsection header "#### <img ...> Rust" must be preserved so the heading renders correctly).

    After both removals, the file must have no occurrence of the strings:
    - "contract remains partial on Windows for now"
    - "CLI support contract is broader than"
  </action>
  <verify>
    <automated>
      grep -n "contract remains partial on Windows for now" README.md && echo "FAIL: sentence still present" || echo "PASS: sentence removed"
      grep -n "CLI support contract is broader than" README.md && echo "FAIL: paragraph still present" || echo "PASS: paragraph removed"
      grep -n "intentionally unavailable on Windows" README.md || echo "FAIL: surrounding text was deleted"
      grep -n "Installation Guide" README.md | grep -q "current Windows boundary" && echo "PASS: link preserved" || echo "FAIL: link missing"
      grep -n "Sandbox::apply.*Irreversible" README.md || echo "FAIL: Rust example block missing"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "contract remains partial on Windows for now" README.md` returns no matches
    - `grep "CLI support contract is broader than" README.md` returns no matches
    - `grep "intentionally unavailable on Windows" README.md` returns a match on the line 46 region
    - `grep "current Windows boundary" README.md` returns a match (the Installation Guide link survives)
    - `grep "Sandbox::apply.*Irreversible" README.md` returns a match (Rust code example below the Library section is intact)
    - `grep "#### .*Rust.*crates.io" README.md` returns a match (Rust subsection header intact)
  </acceptance_criteria>
  <done>README.md contains no split-describing language; the platform support paragraph and Library section read cleanly without stale qualifications.</done>
</task>

<task type="auto">
  <name>Task 2: Remove support-contract split paragraph from security-model.mdx</name>
  <files>docs/cli/internals/security-model.mdx</files>
  <read_first>
    - docs/cli/internals/security-model.mdx lines 355–385 (Windows Model section through Summary header) — confirm exact block boundaries before editing
  </read_first>
  <action>
    In docs/cli/internals/security-model.mdx, locate and remove the support-contract split block. The block to remove is the paragraph that begins "There is also a current support-contract split to keep in mind:" and ends with "...without overstating the narrower embedded library contract."

    Exact text to remove (6 lines including surrounding blank lines that are part of the block):
    ```
    There is also a current support-contract split to keep in mind:

    - the CLI now exposes a real Windows restricted-execution command surface for setup, dry-run, direct execution, blocked-network, and selected supervised flows
    - the library-wide `Sandbox::apply()` support contract still reports Windows as partial rather than first-class supported

    That split is the adopted Windows support contract for now. It lets release and support messaging treat the Windows CLI surface as supported without overstating the narrower embedded library contract.
    ```

    After removal:
    - The paragraph ending "...while Windows is an intentionally explicit restricted command surface with fail-closed diagnostics where parity is not yet credible." must remain as the last paragraph before ## Summary.
    - The ## Summary section header must follow directly (with one blank line separation).
    - The Windows Model section header ("## Windows Model") and all content through line 372 must be untouched.

    Do not modify any other section of the file.
  </action>
  <verify>
    <automated>
      grep -n "support-contract split" docs/cli/internals/security-model.mdx && echo "FAIL: split paragraph still present" || echo "PASS: split paragraph removed"
      grep -n "library-wide.*Sandbox::apply.*support contract still reports Windows as partial" docs/cli/internals/security-model.mdx && echo "FAIL: old bullet still present" || echo "PASS: removed"
      grep -n "Windows currently uses a narrower restricted-execution" docs/cli/internals/security-model.mdx || echo "FAIL: Windows Model header content missing"
      grep -n "## Summary" docs/cli/internals/security-model.mdx || echo "FAIL: Summary section missing"
      grep -n "deny-within-allow" docs/cli/internals/security-model.mdx || echo "FAIL: structural limitations paragraph missing"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "support-contract split" docs/cli/internals/security-model.mdx` returns no matches
    - `grep "library-wide.*Sandbox::apply.*support contract still reports Windows as partial" docs/cli/internals/security-model.mdx` returns no matches
    - `grep "That split is the adopted Windows support contract" docs/cli/internals/security-model.mdx` returns no matches
    - `grep "Windows currently uses a narrower restricted-execution" docs/cli/internals/security-model.mdx` returns a match (section intro line preserved)
    - `grep "deny-within-allow" docs/cli/internals/security-model.mdx` returns a match (structural limitations preserved)
    - `grep "## Summary" docs/cli/internals/security-model.mdx` returns a match (Summary header preserved)
  </acceptance_criteria>
  <done>security-model.mdx Windows Model section ends at the structural limitations paragraph; the false support-contract split text is gone; the Summary section follows directly.</done>
</task>

</tasks>

<verification>
Run both verify blocks above. All PASS lines must appear and no FAIL lines.

Additionally confirm no unintended edits:
- `git diff --stat` shows only `README.md` and `docs/cli/internals/security-model.mdx`
- `git diff README.md` shows exactly two deletion hunks (one in the platform-support paragraph, one in the Library section) and zero insertions
- `git diff docs/cli/internals/security-model.mdx` shows exactly one deletion hunk (the support-contract split block) and zero insertions
</verification>

<success_criteria>
1. README.md: "contract remains partial on Windows for now" and "CLI support contract is broader than" do not appear anywhere in the file.
2. security-model.mdx: "support-contract split" and "That split is the adopted Windows support contract" do not appear anywhere in the file.
3. All surrounding content (platform support paragraph, Rust/Python/TypeScript code blocks, Library intro, Windows Model header, structural limitations, Summary section) is intact and unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pr-d-docs-release/04-01-SUMMARY.md` following the summary template.
</output>
