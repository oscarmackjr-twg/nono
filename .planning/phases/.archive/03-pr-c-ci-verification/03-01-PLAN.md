---
phase: 03-pr-c-ci-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/cli.rs
autonomous: true
requirements:
  - CIVER-01

must_haves:
  truths:
    - "CLI_ABOUT on Windows describes OS-enforced isolation, not Windows-specific restricted execution"
    - "7 command help strings say 'not available on Windows' instead of 'preview surface'"
    - "Root help test asserts new CLI_ABOUT text and correct shell/wrap limitation strings"
  artifacts:
    - path: "crates/nono-cli/src/cli.rs"
      provides: "Updated CLI_ABOUT, 7 AFTER_HELP consts, test assertion"
      contains: "OS-enforced isolation"
  key_links:
    - from: "CLI_ABOUT const"
      to: "test_root_help_mentions_windows_restricted_execution_surface"
      via: "write_long_help assertion"
      pattern: "OS-enforced isolation"
---

<objective>
Update cli.rs Windows help strings to remove "preview surface" and "Windows restricted execution" language, replacing with the promoted contract wording. Update the root help test to assert the new text.

Purpose: Aligns CLI help output to the promoted Windows support contract (CIVER-01). After PR-A made the library contract real and PR-B cleaned CLI runtime messaging, the help strings are the last place the old "preview" language lives in cli.rs.

Output: cli.rs with updated CLI_ABOUT, 7 AFTER_HELP consts, and passing root help test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pr-c-ci-verification/03-CONTEXT.md

<interfaces>
<!-- Current cli.rs constants and test to modify. Executor uses these as the before-state. -->

From crates/nono-cli/src/cli.rs line 12-13 (Windows CLI_ABOUT):
```rust
#[cfg(target_os = "windows")]
const CLI_ABOUT: &str = "A capability-based shell for running untrusted AI agents and processes\nwith Windows restricted execution plus explicit command-surface limitations.\nUnsupported Windows flows fail closed instead of implying full sandbox parity.";
```

From crates/nono-cli/src/cli.rs lines 18-27 (SHELL_AFTER_HELP Windows):
```rust
const SHELL_AFTER_HELP: &str = "...\n  Live `nono shell` is intentionally unavailable on Windows.\n  ...";
```

From crates/nono-cli/src/cli.rs lines 37-45 (WRAP_AFTER_HELP Windows):
```rust
const WRAP_AFTER_HELP: &str = "...\n  Live `nono wrap` is intentionally unavailable on Windows.\n  ...";
```

Test at lines 2201-2222:
```rust
fn test_root_help_mentions_windows_restricted_execution_surface() {
    // Asserts: "Windows restricted execution plus explicit command-surface limitations"
    // Asserts: "live shell is unsupported on Windows"  <-- WRONG: actual text is "intentionally unavailable"
    // Asserts: "live wrap is unsupported on Windows"    <-- WRONG: actual text is "intentionally unavailable"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI_ABOUT and 7 AFTER_HELP preview-surface strings (D-01, D-03)</name>
  <read_first>crates/nono-cli/src/cli.rs</read_first>
  <files>crates/nono-cli/src/cli.rs</files>
  <action>
1. Replace the Windows `CLI_ABOUT` const (line 13) per D-01:
   OLD: `"A capability-based shell for running untrusted AI agents and processes\nwith Windows restricted execution plus explicit command-surface limitations.\nUnsupported Windows flows fail closed instead of implying full sandbox parity."`
   NEW: `"A capability-based shell for running untrusted AI agents and processes with OS-enforced isolation.\nUnsupported flows fail closed instead of implying full sandbox parity."`

2. Replace the "preview surface" phrase in all 7 Windows AFTER_HELP consts per D-03. Each replacement is within a multi-line string — change only the sentence containing "preview surface", leave surrounding text intact:

   a. PS_AFTER_HELP (~line 127): Replace `"not implemented for the current Windows preview surface."` with `"not available on Windows."` — full line becomes: `  that is not available on Windows.`

   b. STOP_AFTER_HELP (~line 149): Replace `"not implemented for the current Windows preview surface."` with `"not available on Windows."` — full line becomes: `  plumbing that is not available on Windows.`

   c. DETACH_AFTER_HELP (~line 167): Replace `"not implemented for the current\n  Windows preview surface, so help examples for in-band detach do not apply."` with `"not available on Windows, so help examples for in-band detach do not apply."` — the line wraps, so the replacement spans two lines becoming one: `  PTY-backed detachable runtime sessions are not available on Windows, so help examples for in-band detach do not apply.`

   d. ATTACH_AFTER_HELP (~line 191): Replace `"not implemented for the current Windows preview surface."` with `"not available on Windows."` — full line becomes: `  that is not available on Windows.`

   e. LOGS_AFTER_HELP (~line 215): Replace `"on the current Windows preview surface."` with `"on Windows."` — full line becomes: `  session infrastructure on Windows.`

   f. INSPECT_AFTER_HELP (~line 259): Replace `"not implemented for the current Windows preview surface."` with `"not available on Windows."` — full line becomes: `  that is not available on Windows.`

   g. PRUNE_AFTER_HELP (~line 300): Replace `"are not produced by the current Windows preview surface."` with `"are not available on Windows."` — full line becomes: `  are not available on Windows.`

Do NOT touch non-Windows variants. Do NOT touch SHELL_AFTER_HELP or WRAP_AFTER_HELP (they don't contain "preview surface").
  </action>
  <verify>
    <automated>cargo build -p nono-cli 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "preview surface" crates/nono-cli/src/cli.rs` returns 0
    - `grep -c "OS-enforced isolation" crates/nono-cli/src/cli.rs` returns at least 1
    - `grep -c "Windows restricted execution" crates/nono-cli/src/cli.rs` returns 0
    - `grep -c "not available on Windows" crates/nono-cli/src/cli.rs` returns at least 7
    - `cargo build -p nono-cli` succeeds
  </acceptance_criteria>
  <done>CLI_ABOUT says "OS-enforced isolation"; all 7 AFTER_HELP consts say "not available on Windows" instead of "preview surface"; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Update root help test assertions (D-02)</name>
  <read_first>crates/nono-cli/src/cli.rs</read_first>
  <files>crates/nono-cli/src/cli.rs</files>
  <action>
Update `test_root_help_mentions_windows_restricted_execution_surface` (line 2203) to match the new CLI_ABOUT and actual SHELL/WRAP_AFTER_HELP text. Per D-02, update the assertion body. Optionally rename the function.

1. Rename the function to `test_root_help_describes_os_enforced_isolation` (optional per D-02 but improves clarity).

2. Replace the first assertion:
   OLD: `help.contains("Windows restricted execution plus explicit command-surface limitations")`
   NEW: `help.contains("OS-enforced isolation")`
   Update message: `"root help should describe OS-enforced isolation"`

3. Replace the second assertion — the current text `"live shell is unsupported on Windows"` does NOT match the actual SHELL_AFTER_HELP which says `"Live \x60nono shell\x60 is intentionally unavailable on Windows"`. Fix to match reality:
   OLD: `help.contains("live shell is unsupported on Windows")`
   NEW: `help.contains("intentionally unavailable on Windows")`
   Update message: `"root help should mention shell/wrap Windows limitation"`
   Note: This single assertion covers both shell and wrap since both AFTER_HELP consts contain "intentionally unavailable on Windows".

4. Remove the third assertion entirely (was `"live wrap is unsupported on Windows"`) — it is now redundant with the updated second assertion that checks for "intentionally unavailable on Windows" which appears in both SHELL and WRAP AFTER_HELP.

IMPORTANT: If you renamed the function, the $smokeTests array in windows-test-harness.ps1 references the old name. Plan 02 Task 1 will update the harness. The test filter still works if the old name is a substring of the new name — but `test_root_help_mentions_windows_restricted_execution_surface` is NOT a substring of `test_root_help_describes_os_enforced_isolation`. So for now, keep the old function name OR add a `#[test]` alias. Simplest: keep the old name `test_root_help_mentions_windows_restricted_execution_surface` but update only the assertion body. The rename can happen when the harness is updated in Plan 02.

Revised approach: Do NOT rename the function. Only update the assertion body. This keeps the harness $smokeTests filter valid.
  </action>
  <verify>
    <automated>cargo test -p nono-cli test_root_help_mentions_windows_restricted_execution_surface -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nono-cli test_root_help_mentions_windows_restricted_execution_surface -- --nocapture` passes (exit 0, "1 passed")
    - `grep "OS-enforced isolation" crates/nono-cli/src/cli.rs` matches in the test function
    - `grep "intentionally unavailable on Windows" crates/nono-cli/src/cli.rs` matches in the test function
    - `grep "live shell is unsupported" crates/nono-cli/src/cli.rs` returns no matches (old assertion removed)
    - `grep "live wrap is unsupported" crates/nono-cli/src/cli.rs` returns no matches (old assertion removed)
    - `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` passes clean
  </acceptance_criteria>
  <done>Root help test passes with assertions matching actual CLI_ABOUT and SHELL/WRAP_AFTER_HELP text; no stale assertion strings remain; clippy clean</done>
</task>

</tasks>

<verification>
- `cargo test -p nono-cli test_root_help_mentions_windows_restricted_execution_surface -- --nocapture` passes
- `cargo clippy -p nono-cli -- -D warnings -D clippy::unwrap_used` passes clean
- `grep -c "preview surface" crates/nono-cli/src/cli.rs` returns 0
- `grep -c "Windows restricted execution" crates/nono-cli/src/cli.rs` returns 0
</verification>

<success_criteria>
- CLI_ABOUT on Windows says "OS-enforced isolation" — no Windows-specific qualifier
- All 7 AFTER_HELP consts use "not available on Windows" — no "preview surface" language
- Root help test passes with assertions matching actual help output text
- clippy and build both pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-pr-c-ci-verification/03-01-SUMMARY.md`
</output>
