---
phase: 24-parity-drift-prevention
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - .planning/templates/upstream-sync-quick.md
  - docs/cli/development/upstream-drift.mdx
  - .planning/PROJECT.md
  - tests/integration/test_upstream_drift.sh
autonomous: true
requirements:
  - DRIFT-02
threat_model_summary:
  - T-24-05 (V14 Configuration) — maintainer ships PLAN.md with raw `{name}` placeholders unfilled; smoke test asserts substitution renders cleanly

must_haves:
  truths:
    - "Maintainer can copy `.planning/templates/upstream-sync-quick.md` to `.planning/quick/YYMMDD-xxx-upstream-sync-vX.Y/PLAN.md`, fill the single-brace `{from_tag}` / `{to_tag}` / `{commit_count}` / `{date}` / `{quick_slug}` placeholders, and end up with a frontmatter-valid Markdown PLAN.md skeleton ready for cherry-pick execution."
    - "Template encodes the D-19 cherry-pick trailer block VERBATIM in 6 lines with the EXACT field order Upstream-commit → Upstream-tag → Upstream-author → Co-Authored-By → Signed-off-by (full name) → Signed-off-by (github handle), with `Upstream-author` in lowercase 'a', and TWO `Signed-off-by` lines (DCO + GitHub attribution)."
    - "Template includes a fork-divergence catalog section that explicitly names: `validate_path_within` retention pattern (Phase 22-03 PKG-04), async-runtime wrapping for `load_production_trusted_root`, deferred enum variants like `ArtifactType::Plugin`. These are the silent-drop hazards a maintainer would otherwise re-litigate from commit archaeology."
    - "Template includes a Windows-specific retrofit checklist with the per-feature gate question: 'Does the Windows path exist for this feature? If yes, port. If no, add behind `#[cfg(target_os = \"windows\")]` and document why the cross-platform code path doesn't apply.'"
    - "PROJECT.md has a new `## Upstream Parity Process` section (~10 lines) listing the 4-step workflow (inventory → scaffold → cherry-pick → retrofit) with cross-link to `docs/cli/development/upstream-drift.mdx` long-form runbook."
    - "`docs/cli/development/upstream-drift.mdx` exists with Mintlify frontmatter (`title:` + `description:`) and covers: running the drift check, output formats, categorization rules, using output with the template, regenerating fixtures."
    - "Placeholder smoke test in `tests/integration/test_upstream_drift.sh` (extended from Plan 24-01) substitutes all `{name}` placeholders with sample values and asserts (a) zero `{[a-z_]+}` markers remain in the rendered output, (b) frontmatter is valid Markdown YAML, (c) the D-19 trailer block has 6 lines in the correct field order with two `Signed-off-by` entries."
  artifacts:
    - path: ".planning/templates/upstream-sync-quick.md"
      provides: "fillable-blanks Markdown template scaffolding upstream-sync quick tasks; single-brace `{name}` placeholders; D-19 trailer block verbatim; conflict-file inventory; Windows retrofit checklist; fork-divergence catalog"
      contains: "Upstream-commit: {upstream_sha_abbrev}"
      contains: "Upstream-author: {upstream_author_name}"
      contains: "validate_path_within"
    - path: "docs/cli/development/upstream-drift.mdx"
      provides: "long-form runbook for the drift-check tool + template workflow; Mintlify frontmatter; sections for running, formats, categorization, template integration, fixture regeneration"
      contains: "title: Upstream Drift Check"
      contains: "make check-upstream-drift"
    - path: ".planning/PROJECT.md"
      provides: "new `## Upstream Parity Process` H2 section with 4-step workflow + cross-link to .mdx runbook"
      contains: "Upstream Parity Process"
      contains: "docs/cli/development/upstream-drift.mdx"
    - path: "tests/integration/test_upstream_drift.sh"
      provides: "extended with placeholder-smoke + cross-link assertions for DRIFT-02 deliverables (in addition to DRIFT-01 tests from Plan 24-01)"
      contains: "upstream-sync-quick.md"
  key_links:
    - from: ".planning/PROJECT.md § Upstream Parity Process"
      to: ".planning/templates/upstream-sync-quick.md"
      via: "step 2 of the workflow (`copy template to quick task dir`)"
      pattern: "\\.planning/templates/upstream-sync-quick\\.md"
    - from: ".planning/PROJECT.md § Upstream Parity Process"
      to: "docs/cli/development/upstream-drift.mdx"
      via: "cross-link to long-form runbook"
      pattern: "docs/cli/development/upstream-drift\\.mdx"
    - from: "docs/cli/development/upstream-drift.mdx"
      to: ".planning/templates/upstream-sync-quick.md"
      via: "section 'Using the output with the upstream-sync template'"
      pattern: "upstream-sync-quick\\.md"
    - from: ".planning/templates/upstream-sync-quick.md § Drift inventory"
      to: "make check-upstream-drift (Plan 24-01)"
      via: "manual instruction (D-14: maintainer runs and pastes; template does NOT auto-include output)"
      pattern: "make check-upstream-drift"
    - from: "tests/integration/test_upstream_drift.sh"
      to: ".planning/templates/upstream-sync-quick.md"
      via: "placeholder smoke test renders template via sed substitution and asserts output validity"
      pattern: "upstream-sync-quick\\.md"
---

<objective>
Deliver the GSD upstream-sync template (DRIFT-02) plus its PROJECT.md cross-link and `.mdx` long-form runbook, with a placeholder smoke test that proves the template renders to a frontmatter-valid PLAN.md skeleton when filled.

Purpose: Plan 24-01 ships the inventory tooling; without the template, the next maintainer absorbing v0.41+ would still have to invent the cherry-pick chain shape, conflict-file inventory, and Windows retrofit checklist from scratch. The template freezes the institutional knowledge from Phase 22-03 (PKG validate_path_within retention, deferred enum variants, async-runtime wrapping) into a reusable scaffold.

Output:
- `.planning/templates/upstream-sync-quick.md` — fillable-blanks Markdown template (single-brace `{name}` placeholders matching GSD convention)
- `docs/cli/development/upstream-drift.mdx` — long-form runbook (.mdx per D-16, NOT .md as REQ-DRIFT-01 acceptance #3 says)
- `.planning/PROJECT.md` — new `## Upstream Parity Process` H2 section
- `tests/integration/test_upstream_drift.sh` — extended (in place) with placeholder smoke test + cross-link grep assertions

**ACCEPTANCE LOCK:** D-19 trailer block in the template is BYTE-EXACT verbatim from commits `73e1e3b8` / `adf81aec` / `869349df`: `Upstream-author` has lowercase 'a' (NOT `Upstream-Author`); 6-line block ends with TWO `Signed-off-by` lines (full name + github handle) for DCO + GitHub attribution; field order is fixed and not negotiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-parity-drift-prevention/24-CONTEXT.md
@.planning/phases/24-parity-drift-prevention/24-RESEARCH.md
@.planning/phases/24-parity-drift-prevention/24-PATTERNS.md
@.planning/phases/24-parity-drift-prevention/24-VALIDATION.md
@.planning/phases/24-parity-drift-prevention/24-01-PLAN.md
@.planning/phases/22-upst2-upstream-v038-v040-parity-sync/22-03-PKG-PROGRESS.md
@.planning/quick/260424-upr-review-upstream-037-to-040/PLAN.md
@.planning/quick/260424-upr-review-upstream-037-to-040/SUMMARY.md
@docs/cli/development/testing.mdx

<interfaces>
<!-- D-19 cherry-pick trailer block — verbatim from commits 73e1e3b8 / adf81aec / 869349df. -->
<!-- The template MUST encode this EXACT shape with placeholders. Do not change field order. -->
<!-- Do not capitalize 'a' in 'Upstream-author'. Two Signed-off-by lines are mandatory (DCO + GitHub). -->

# Reference (verified verbatim from `git log -1 --format='%B' 73e1e3b8`)
Upstream-commit: 600ba4ec
Upstream-tag: v0.38.0
Upstream-author: Luke Hinds <lukehinds@gmail.com>
Co-Authored-By: Luke Hinds <lukehinds@gmail.com>
Signed-off-by: Oscar Mack <oscar.mack.jr@gmail.com>
Signed-off-by: oscarmackjr-twg <oscar.mack.jr@gmail.com>

# Template encoding (use these EXACT placeholders, single-brace, lowercase snake_case)
Upstream-commit: {upstream_sha_abbrev}
Upstream-tag: {upstream_tag}
Upstream-author: {upstream_author_name} <{upstream_author_email}>
Co-Authored-By: {upstream_author_name} <{upstream_author_email}>
Signed-off-by: {fork_author_name} <{fork_author_email}>
Signed-off-by: {fork_author_handle} <{fork_author_email}>

# Frontmatter shape (from .planning/quick/260424-upr-review-upstream-037-to-040/PLAN.md lines 1–4)
---
slug: {quick_slug}
created: {date}
type: upstream-sync
range: {from_tag}..{to_tag}
---

# Single-brace `{name}` placeholder convention (verified across 31 GSD templates).
# DO NOT use {{NAME}} or <NAME>.
# Inline maintainer guidance uses HTML comments: <!-- guidance -->
# Replace-all checklist at top of template:
# <!-- Before committing: replace all {placeholder} markers. Smoke check: grep -oE '\\{[a-z_]+\\}' should return 0 matches. -->

# .mdx Mintlify frontmatter (from docs/cli/development/testing.mdx lines 1–4)
---
title: {title}
description: {description}
---

# Fork-divergence catalog ENTRIES that MUST appear in the template (per CONTEXT.md Specifics §2 + 22-03-PKG-PROGRESS.md):
# - validate_path_within retention (defense-in-depth — kept even when upstream removed it)
# - async-runtime wrapping for load_production_trusted_root
# - deferred enum variants (e.g., ArtifactType::Plugin not in fork — explicit catch in pattern matches)
# - Windows hooks.rs retention vs upstream's claude-code package removal
</interfaces>

<source_audit_summary>
- D-12 (template at .planning/templates/upstream-sync-quick.md, no GSD skill wrapper) → Task 1
- D-13 (fillable-blanks Markdown with placeholders + frontmatter + headline + D-19 trailer + conflict-file inventory + Windows retrofit + fork-divergence catalog) → Task 1
- D-14 (template references make check-upstream-drift, does NOT auto-include) → Task 1 (manual-paste-step in template body)
- D-15 (PROJECT.md § Upstream Parity Process new section) → Task 2
- D-16 (.mdx not .md per existing convention) → Task 2
- D-19 (trailer block verbatim, 6 lines, lowercase 'a' in Upstream-author, two Signed-off-by) → Task 1
- T-24-05 (placeholder smoke test catches unfilled markers) → Task 3
</source_audit_summary>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the upstream-sync template (Wave 2 — DRIFT-02 scaffold)</name>
  <files>.planning/templates/upstream-sync-quick.md</files>
  <read_first>
    - .planning/phases/24-parity-drift-prevention/24-RESEARCH.md § Pattern 7 (placeholder convention lines 510–524), § Pattern 8 (D-19 trailer block lines 526–555)
    - .planning/phases/24-parity-drift-prevention/24-PATTERNS.md § .planning/templates/upstream-sync-quick.md (lines 166–254) — verbatim D-19 block + frontmatter shape + fork-divergence catalog requirements
    - .planning/phases/24-parity-drift-prevention/24-CONTEXT.md § Decisions D-12, D-13, D-14, D-19
    - .planning/quick/260424-upr-review-upstream-037-to-040/PLAN.md (full file — frontmatter + headline + scope shape to mirror)
    - .planning/quick/260424-upr-review-upstream-037-to-040/SUMMARY.md (lines 1–60 — headline + categorization narrative voice the template scaffolds for)
    - .planning/phases/22-upst2-upstream-v038-v040-parity-sync/22-03-PKG-PROGRESS.md (full file — fork-divergence patterns to seed the catalog: validate_path_within retention; deferred enum variants; cherry-pick chain ordering)
    - The 3 reference commits to validate the trailer-block shape verbatim (read the commit message bodies to confirm field order + lowercase 'a' + two Signed-off-by lines):
      `git log -1 --format='%B' 73e1e3b8`
      `git log -1 --format='%B' adf81aec`
      `git log -1 --format='%B' 869349df`
    - $HOME/.claude/get-shit-done/templates/AI-SPEC.md (line 1 onward — single-brace placeholder convention precedent; 31-template baseline)
  </read_first>
  <action>
Create `.planning/templates/upstream-sync-quick.md` as a fillable-blanks Markdown template. The directory `.planning/templates/` does not yet exist — `mkdir -p .planning/templates` before writing.

**Placeholder convention:** Single-brace `{name}` (lowercase snake_case). Verified across 31 GSD templates (RESEARCH § Pattern 7). NEVER `{{NAME}}` (zero precedent in this project). NEVER `<NAME>` (collides with HTML/JSX in any consumer that pre-renders Markdown). Inline guidance uses `<!-- HTML comments -->`.

**Required template structure (in order):**

```markdown
<!--
  Upstream-sync quick task template.

  Usage:
    1. Copy this file to .planning/quick/YYMMDD-xxx-upstream-sync-vX.Y/PLAN.md
    2. Replace all {placeholder} markers with values for the upstream range you're absorbing.
    3. Run `make check-upstream-drift > drift.json` to inventory commits, then curate
       the categorized commit list into the "Drift inventory" section below.
    4. Cherry-pick per commit, applying the trailer block at the bottom of THIS file
       to every commit message (D-19 convention; see .planning/phases/24-... or
       .planning/PROJECT.md § Upstream Parity Process for context).

  Smoke check before committing the filled-in PLAN.md:
    grep -oE '\{[a-z_]+\}' PLAN.md
  Should return zero matches. Any remaining {placeholder} is an unfilled blank.
-->

---
slug: {quick_slug}
created: {date}
type: upstream-sync
range: {from_tag}..{to_tag}
---

# Quick task: Sync upstream {from_tag} → {to_tag} into the fork

**Ask:** Absorb upstream's {from_tag} → {to_tag} cross-platform feature deltas into the fork
without breaking Windows parity. Per-commit cherry-picks preserve provenance via the D-19
trailer block.

**Scope:**
- Diff range: `{from_tag}..{to_tag}` ({commit_count} non-merge commits, ~{insertions} insertions / ~{deletions} deletions over cross-platform paths).
- Tags covered: <!-- list intermediate tags if range spans more than one release, e.g., v0.41.0, v0.42.0 -->
- Categories present (run `make check-upstream-drift ARGS="--from {from_tag} --to {to_tag} --format json"` to inventory): <!-- profile / policy / package / proxy / audit / other -->
- Deliverable: per-commit cherry-picks landing on `windows-squash` (or current parity branch), each carrying the D-19 trailer block.

**What I will NOT do:**
- No upstream merge (only per-commit cherry-pick — preserves windowed scope).
- No automated cherry-pick orchestration (each commit reviewed for Windows-retrofit hazards before landing).
- No changes to `.planning/ROADMAP.md` / `.planning/PROJECT.md` outside this quick task's scope.

**STATE.md update plan:** appended to "Quick Tasks Completed" table on close-out, with reference to this PLAN.md and the resulting commit chain.

---

## Drift inventory

<!--
  Run: make check-upstream-drift ARGS="--from {from_tag} --to {to_tag} --format json" > drift.json
  Then paste curated entries into the per-category sections below. Per D-14, this template
  does NOT auto-include the inventory — the maintainer reviews + categorizes manually.
-->

**Total unique commits in cross-platform path filter:** {commit_count}

**Per-category breakdown (from `make check-upstream-drift --format json`'s `by_category` field):**

| Category | Commits | Cherry-pick priority |
|----------|---------|----------------------|
| profile  | {n_profile} | <!-- 1=lowest risk, 5=highest risk; mirror Phase 22-01..05 ordering --> |
| policy   | {n_policy}  | |
| package  | {n_package} | |
| proxy    | {n_proxy}   | |
| audit    | {n_audit}   | |
| other    | {n_other}   | |

### Commits to absorb (per-category)

<!--
  For each category, list the commits the fork must absorb. Format per row:
    - {sha}  {subject}  ({adds}/{dels})
  Example:
    - 4f9552ec  feat(audit): add tamper-evident audit log integrity  (+1419/-226)
-->

#### profile
<!-- paste curated commit list -->

#### policy
<!-- paste curated commit list -->

#### package
<!-- paste curated commit list -->

#### proxy
<!-- paste curated commit list -->

#### audit
<!-- paste curated commit list -->

#### other
<!-- paste curated commit list (or note "no cross-platform impact, skipping" per commit) -->

---

## Conflict-file inventory

<!--
  Files in the fork that have diverged from upstream and will conflict on cherry-pick.
  Pre-populated from prior upstream syncs (Phase 22-03 PKG, Phase 20 UPST). Update for
  the current range based on `git diff --stat upstream/{from_tag}..upstream/{to_tag} -- <file>`.
-->

| File | Why it conflicts | Resolution pattern |
|------|------------------|---------------------|
| `crates/nono-cli/src/profile/mod.rs` | Fork added Windows-specific deserialization paths; upstream evolves the struct | Apply upstream's serde changes; keep fork's `#[cfg(target_os = "windows")]` arms |
| `crates/nono-cli/src/exec_strategy.rs` | Fork has 144+ lines of Windows-specific exec wiring | Apply upstream changes around the cfg-gated regions; do not touch the Windows arms unless audit explicitly requires |
| `crates/nono-cli/src/supervised_runtime.rs` | Heavy fork divergence (Phase 22-05 audit ledger + Plan 18.1-03 profile widening) | Hand-resolve; verify CLEAN-04 and AIPC invariants still hold post-resolution |
| `crates/nono-cli/src/rollback_runtime.rs` | +586 LOC upstream change; fork has Windows snapshot/rollback wiring | Resolve hunk-by-hunk; preserve fork's `prepare_live_windows_launch` callsite |
| `crates/nono-cli/src/package_cmd.rs` (and `crates/nono/src/package*`) | Fork ships Windows install_dir + long-path handling; upstream evolves the cross-platform shape | Apply upstream changes; verify Phase 22-03 PKG-02 acceptance still passes |
| `crates/nono-proxy/src/oauth2.rs` (Phase 22-04) | Fork's port may diverge if upstream evolves the credential-cache shape | Apply upstream changes; verify token cache + WSAStartup ordering preserved |

<!-- Add file rows specific to {from_tag}..{to_tag} as cherry-pick reveals new conflicts. -->

---

## Windows-specific retrofit checklist

<!--
  For each cross-platform feature absorbed, check the Windows path. If absent, add it
  behind `#[cfg(target_os = "windows")]` AND document why the cross-platform code path
  doesn't apply (e.g., uses Unix procfs that has no Windows analog, requires named
  semaphores not present in the WinAPI surface, etc.).
-->

For every cross-platform feature in the inventory above:

- [ ] **Profile struct change?** → Verify `crates/nono-cli/src/profile/mod.rs` deserialize works on Windows (smoke: `cargo test -p nono-cli --features windows-tests profile::tests`)
- [ ] **Policy fail-closed addition?** → Verify Windows fail-closed path matches macOS (smoke: `bash tests/integration/test_override_deny.sh`)
- [ ] **Package manager change?** → Verify `%LOCALAPPDATA%\nono\packages` resolution + long-path handling (smoke: Phase 22-03 PKG-02 regression test)
- [ ] **Proxy / OAuth2 change?** → Verify Windows nono-proxy stack + WSAStartup ordering (smoke: Phase 22-04 OAuth2 integration tests)
- [ ] **Audit / attestation change?** → Verify Windows ledger emission survives `AppliedLabelsGuard` Drop AND `exec_identity` resolves via `GetModuleFileNameW` (smoke: Phase 22-05 + Phase 23 AUD tests)
- [ ] **WFP / network change?** → Verify per-session-SID filter is still kernel-enforced; no AppID leak on detached path (smoke: `bash tests/integration/test_network_wfp.sh`)
- [ ] **For each new feature without a Windows code path:** explicit `#[cfg(not(target_os = "windows"))]` gate + comment documenting why no Windows analog is needed

---

## Fork-divergence catalog

<!--
  Decisions the fork has made that look "removable" on cherry-pick but MUST be preserved.
  Silently dropping these is the most common upstream-sync regression. Read this list
  in full before resolving any conflict.
-->

### `validate_path_within` defense-in-depth retention (Phase 22-03 PKG-04)

The fork retains `validate_path_within(child, parent)` calls even when upstream removed
them in `0cbb7e62 refactor(package): simplify artifact signer validation`. Reason: the
function is a structural defense-in-depth check against path-traversal in package
artifacts (`../`, UNC aliasing, symlink escapes). Upstream relies on `Path::canonicalize`
+ `starts_with`, which is the well-known footgun (string `starts_with("/home")` matches
`/homeevil`; CLAUDE.md § Common Footguns #1). The fork's `validate_path_within` uses
`Path::components()` iteration and is the correct primitive on Windows where paths can
take UNC, `\\?\`, and drive-letter forms.

**Action on cherry-pick:** When upstream commits remove `validate_path_within` calls,
KEEP them in the fork. Add a comment: `// Defense-in-depth (fork divergence: see Phase
22-03 PKG-04). Do not remove without security review.`

### Deferred enum variants (e.g., `ArtifactType::Plugin`)

Upstream's `ArtifactType` enum has more variants than the fork's port absorbed. When
cherry-picking commits that touch `match` arms over `ArtifactType`, the fork's narrower
enum will fail compile if the upstream commit adds a new variant.

**Action on cherry-pick:** Either add the variant to the fork's enum (preferred, tracks
upstream feature surface) OR add an explicit `_ => Err(NonoError::Unsupported(format!("variant not yet in fork: {:?}", v)))` arm with a comment pointing to a deferral ticket. Document the choice in the cherry-pick's commit message body.

### Async-runtime wrapping for `load_production_trusted_root`

Upstream evolved `load_production_trusted_root` to be async over time; the fork wraps
the synchronous call in a `tokio::task::spawn_blocking` at the call site. When upstream
introduces an async helper, the fork's wrapper becomes redundant.

**Action on cherry-pick:** Audit the call site. If upstream now provides an async
variant, switch the fork to it (delete the `spawn_blocking` wrapper). If upstream still
emits sync code under an async signature (anti-pattern), keep the fork's wrapping.

### Hooks subsystem ownership (`hooks.rs` retention)

Upstream removed the `claude-code integration package` in favor of bundling hooks into
the package manager. The fork keeps `crates/nono-cli/src/hooks.rs` as the sole
hook-installation surface (Phase 22-03 PKG-03 wiring routes through it).

**Action on cherry-pick:** Cherry-picks that delete `hooks.rs` or rewire it through the
package-manager path must be hand-resolved to preserve the fork's centralized hook
installer. Do NOT silently accept upstream's removal.

### Windows-only file globs (D-21 invariance)

Cherry-picks that touch cross-platform files MUST NOT also modify `*_windows.rs` /
`crates/nono-cli/src/exec_strategy_windows/` files unless the change is explicitly
Windows-targeting. When upstream's commit accidentally edits a Windows file (e.g., a
cross-platform refactor that leaks into a `#[cfg(windows)]` arm), revert the Windows
change in the cherry-pick.

**Verification:** `git diff --stat HEAD~1 HEAD -- crates/nono/src/ crates/nono-cli/src/ | grep -v _windows | grep -v exec_strategy_windows`. The output should match the upstream commit's intended scope.

---

## D-19 cherry-pick trailer block

<!--
  REQUIRED on EVERY cherry-pick commit. Verbatim shape (lowercase 'a' in 'Upstream-author';
  two Signed-off-by lines for DCO + GitHub attribution).

  Workflow:
    1. git cherry-pick {upstream_sha_full}
    2. git commit --amend  (to add the trailer block)
    3. Append the block below to the commit message body, separated by ONE blank line.

  Smoke check on the rebased branch before push:
    git log --format='%B' {fork_branch}~{commit_count}..{fork_branch} \
      | grep -c '^Upstream-commit: '
    Should equal {commit_count}.
-->

```
Upstream-commit: {upstream_sha_abbrev}
Upstream-tag: {upstream_tag}
Upstream-author: {upstream_author_name} <{upstream_author_email}>
Co-Authored-By: {upstream_author_name} <{upstream_author_email}>
Signed-off-by: {fork_author_name} <{fork_author_email}>
Signed-off-by: {fork_author_handle} <{fork_author_email}>
```

**Field rules (verified verbatim from commits 73e1e3b8, adf81aec, 869349df):**

1. Trailer block separated from body by EXACTLY ONE blank line.
2. Field order is FIXED: `Upstream-commit` → `Upstream-tag` → `Upstream-author` → `Co-Authored-By` → `Signed-off-by` (full name) → `Signed-off-by` (github handle).
3. `Upstream-author` and `Co-Authored-By` carry the SAME name + email.
4. TWO `Signed-off-by` lines: full name + github handle. Both required (DCO + GitHub attribution).
5. Field name is `Upstream-author` with LOWERCASE 'a' (NOT `Upstream-Author`).
6. Abbreviated 8-char SHA is the in-use convention for `Upstream-commit:` (the actual commit body may reference the full SHA, but the trailer uses 8-char).

---

## Acceptance

- [ ] All {commit_count} commits cherry-picked with D-19 trailer present
- [ ] Conflict-file inventory section reflects what actually conflicted
- [ ] Windows retrofit checklist all items either ✓ or explicitly N/A with reason
- [ ] Fork-divergence catalog reviewed; no entries silently dropped
- [ ] `cargo build --workspace` clean
- [ ] `cargo test --workspace` clean (or known-flake tests documented inline)
- [ ] `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` clean
- [ ] `cargo fmt --all -- --check` clean

## Out-of-scope deferrals

<!-- Track commits absorbed but with intentional deferrals (e.g., "ported deserialization
     but feature wiring deferred to Phase XX"). -->

---

*Quick task type: upstream-sync. Tooling: `make check-upstream-drift` (Plan 24-01). Long-form runbook: [`docs/cli/development/upstream-drift.mdx`](../../../docs/cli/development/upstream-drift.mdx).*
```

**Hard requirements (verified by acceptance criteria):**
1. The 6-line D-19 trailer block must appear VERBATIM in the template (with `{placeholder}` substitutions, lowercase 'a' in `Upstream-author`, TWO `Signed-off-by:` lines).
2. The fork-divergence catalog must explicitly NAME `validate_path_within`, `ArtifactType::Plugin` (or equivalent "deferred enum variants" wording), `load_production_trusted_root` (async-wrapping), and the hooks ownership pattern.
3. The Windows-specific retrofit checklist must use `#[cfg(target_os = "windows")]` literal text in at least one bullet (RESEARCH Pattern 8 lock).
4. The `## Drift inventory` section's instruction line must say `make check-upstream-drift > drift.json` (D-14: maintainer runs and pastes; template does NOT auto-include).
5. Frontmatter must use single-brace `{quick_slug}` / `{date}` / `{from_tag}` / `{to_tag}` placeholders (D-13 + Pattern 7 — match GSD convention).
6. The replace-all checklist comment must specify the smoke check `grep -oE '\\{[a-z_]+\\}' PLAN.md` returning zero matches.

**Encoding:** UTF-8 without BOM. LF line endings (match other repo Markdown files; verified via `file` command on `.planning/PROJECT.md`).

**Verification before commit:**
```bash
mkdir -p .planning/templates
# Write the file via the planner action above
[[ -f .planning/templates/upstream-sync-quick.md ]]
grep -c '^Signed-off-by: ' .planning/templates/upstream-sync-quick.md
# expected: 2

grep -F 'Upstream-commit: {upstream_sha_abbrev}' .planning/templates/upstream-sync-quick.md
grep -F 'Upstream-author: {upstream_author_name}' .planning/templates/upstream-sync-quick.md
grep -F 'Co-Authored-By: {upstream_author_name}' .planning/templates/upstream-sync-quick.md
grep -F 'validate_path_within' .planning/templates/upstream-sync-quick.md
grep -F '#[cfg(target_os = "windows")]' .planning/templates/upstream-sync-quick.md
grep -F 'make check-upstream-drift' .planning/templates/upstream-sync-quick.md
```
All grep invocations must exit 0.
  </action>
  <verify>
    <automated>[[ -f .planning/templates/upstream-sync-quick.md ]] && [[ "$(grep -c '^Signed-off-by: ' .planning/templates/upstream-sync-quick.md)" == "2" ]] && grep -qF 'Upstream-commit: {upstream_sha_abbrev}' .planning/templates/upstream-sync-quick.md && grep -qF 'Upstream-author: {upstream_author_name}' .planning/templates/upstream-sync-quick.md && grep -qF 'validate_path_within' .planning/templates/upstream-sync-quick.md && grep -qF 'make check-upstream-drift' .planning/templates/upstream-sync-quick.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `[[ -f .planning/templates/upstream-sync-quick.md ]]` exits 0
    - `[[ -d .planning/templates ]]` exits 0 (directory was created as part of this task)
    - `grep -c '^Signed-off-by: ' .planning/templates/upstream-sync-quick.md` outputs `2` (D-19 acceptance — TWO Signed-off-by lines)
    - `grep -E '^Upstream-commit: \{[a-z_]+\}' .planning/templates/upstream-sync-quick.md` exits 0 (placeholder is `{upstream_sha_abbrev}` or similar lowercase snake_case)
    - `grep -E '^Upstream-author: \{[a-z_]+\}' .planning/templates/upstream-sync-quick.md` exits 0 (note LOWERCASE 'a' in `Upstream-author`)
    - `grep -E '^Upstream-Author' .planning/templates/upstream-sync-quick.md` outputs nothing (UPPERCASE 'A' is FORBIDDEN — would be a regression on D-19 verification)
    - `grep -E '^Upstream-tag: \{[a-z_]+\}' .planning/templates/upstream-sync-quick.md` exits 0
    - `grep -E '^Co-Authored-By: \{[a-z_]+\}' .planning/templates/upstream-sync-quick.md` exits 0
    - `grep -F 'validate_path_within' .planning/templates/upstream-sync-quick.md` exits 0 (fork-divergence catalog explicit naming)
    - `grep -F 'load_production_trusted_root' .planning/templates/upstream-sync-quick.md` exits 0
    - `grep -E '(ArtifactType::Plugin|deferred enum variants?)' .planning/templates/upstream-sync-quick.md` exits 0
    - `grep -F 'hooks.rs' .planning/templates/upstream-sync-quick.md` exits 0 (hooks ownership pattern present)
    - `grep -F '#[cfg(target_os = "windows")]' .planning/templates/upstream-sync-quick.md` exits 0
    - `grep -F 'make check-upstream-drift' .planning/templates/upstream-sync-quick.md` exits 0 (cross-link to Plan 24-01 tooling)
    - Frontmatter uses single-brace placeholders: `grep -E '^slug: \{[a-z_]+\}$' .planning/templates/upstream-sync-quick.md` exits 0; `grep -E '^range: \{[a-z_]+\}\.\.\{[a-z_]+\}$' .planning/templates/upstream-sync-quick.md` exits 0
    - No double-brace placeholders: `grep -F '{{' .planning/templates/upstream-sync-quick.md` outputs nothing
    - No angle-bracket placeholders that look like JSX: `grep -E '<[A-Z_]+>' .planning/templates/upstream-sync-quick.md` outputs nothing (HTML comments `<!-- ... -->` are fine; bare `<NAME>` placeholders are forbidden)
    - File starts with the leading HTML guidance comment block (smoke check: `head -3 .planning/templates/upstream-sync-quick.md | grep -F '<!--'` exits 0)
    - File contains the smoke-check instruction text: `grep -F "grep -oE '\\\\{[a-z_]+\\\\}'" .planning/templates/upstream-sync-quick.md` exits 0
  </acceptance_criteria>
  <done>Template exists at `.planning/templates/upstream-sync-quick.md` with: (a) frontmatter using single-brace placeholders, (b) D-19 6-line trailer block verbatim with lowercase 'a' in `Upstream-author` and TWO `Signed-off-by` lines, (c) fork-divergence catalog explicitly naming `validate_path_within`, deferred enum variants, `load_production_trusted_root` async wrapping, hooks ownership pattern, (d) Windows retrofit checklist using `#[cfg(target_os = "windows")]` literal, (e) drift-inventory instruction referencing `make check-upstream-drift` (D-14 — manual paste, not auto-include), (f) replace-all smoke-check instruction in the leading comment.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add `## Upstream Parity Process` to PROJECT.md + create docs/cli/development/upstream-drift.mdx (Wave 2 — DRIFT-02 documentation)</name>
  <files>.planning/PROJECT.md, docs/cli/development/upstream-drift.mdx</files>
  <read_first>
    - .planning/phases/24-parity-drift-prevention/24-RESEARCH.md § Open Question 5 (PROJECT.md section length recommendation: ~10 lines)
    - .planning/phases/24-parity-drift-prevention/24-PATTERNS.md § PROJECT.md (lines 562–596 — insertion-point + section style + bold-anchored bullet pattern), § docs/cli/development/upstream-drift.mdx (lines 256–308 — analog: testing.mdx; Mintlify frontmatter)
    - .planning/phases/24-parity-drift-prevention/24-CONTEXT.md § Decisions D-15, D-16
    - .planning/PROJECT.md (full file — to identify the correct insertion point: between `## Key Decisions` table and `## Evolution`, OR between `## Constraints` and `## Context` — PATTERNS recommends the former)
    - docs/cli/development/testing.mdx (lines 1–60 — frontmatter + section convention; verb-noun H2; fenced bash code blocks)
    - .planning/templates/upstream-sync-quick.md (newly created; cross-link target)
    - scripts/check-upstream-drift.sh (newly created; the .mdx documents how to use it)
  </read_first>
  <action>
This task lands two file edits (one new file, one modification). Both must be done in this single task to keep the cross-link verifications passing — the .mdx references the template, PROJECT.md references both, the template references `make check-upstream-drift` (Plan 24-01).

### Part A — Modify `.planning/PROJECT.md`

**Insertion point:** Insert a new H2 section `## Upstream Parity Process` between the existing `## Key Decisions` table (which ends around line 165 — the row about CLEAN-04 / Phase 21 Low-IL ownership / etc.) and `## Evolution` (line 166). PATTERNS § PROJECT.md recommends this position because the new section is workflow-procedural like Evolution (not constraint-like).

**Use the Edit tool with the existing `## Evolution` line as anchor.** Insert ABOVE it. Read the file with `Read` first to get the exact bytes around line 165–166 so the Edit's old_string matches.

**Recommended block to insert (~10 lines per D-15 + Open Question 5; matches the existing PROJECT.md numbered-list + bold-anchored bullet style):**
```markdown
## Upstream Parity Process

To prevent the Windows-vs-macOS parity gap from re-opening as upstream ships v0.41+:

1. **Inventory drift** — `make check-upstream-drift` reports unabsorbed upstream commits grouped by file category. JSON output (`make check-upstream-drift ARGS="--from <tag> --to <tag> --format json"`) is suitable for templates and CI; default `--format table` for human review.
2. **Scaffold the sync** — copy `.planning/templates/upstream-sync-quick.md` to `.planning/quick/YYMMDD-xxx-upstream-sync-vX.Y/PLAN.md` and fill the single-brace `{placeholder}` markers (smoke check: `grep -oE '\{[a-z_]+\}' PLAN.md` returns zero).
3. **Cherry-pick per commit** — preserve the `Upstream-commit:` / `Upstream-tag:` / `Upstream-author:` / `Co-Authored-By:` / `Signed-off-by:` D-19 trailer block on every cherry-pick (template encodes the verbatim 6-line shape).
4. **Verify Windows retrofit** — for every cross-platform feature absorbed, confirm the Windows path either exists or is added behind `#[cfg(target_os = "windows")]`; the template's "Windows-specific retrofit checklist" enumerates the per-feature questions.

For the long-form runbook (output formats, categorization rules, fixture regeneration procedure, fork-divergence catalog rationale), see [`docs/cli/development/upstream-drift.mdx`](../docs/cli/development/upstream-drift.mdx).
```

**Style conformance (PATTERNS § PROJECT.md gotchas):**
- H2 (`##`) — matches Evolution / Constraints / Context
- Numbered list with bold-anchored bullets — matches the "Target features" pattern at lines 13–19
- Cross-link with relative path `../docs/cli/development/upstream-drift.mdx` (PROJECT.md is at `.planning/PROJECT.md`, .mdx is at `docs/cli/development/upstream-drift.mdx` — the relative path from `.planning/PROJECT.md` is `../docs/cli/development/upstream-drift.mdx`)
- DO NOT touch the existing `### Validated` / `### Active (v2.2)` / `### Deferred` lists
- DO NOT modify the `*Last updated:* footer (line 184) — that's milestone-end work

### Part B — Create `docs/cli/development/upstream-drift.mdx`

**Mintlify-style frontmatter (PATTERNS § docs/cli/development/upstream-drift.mdx; analog: docs/cli/development/testing.mdx lines 1–4):**
```mdx
---
title: Upstream Drift Check
description: How to inventory unabsorbed upstream commits and scaffold an upstream-sync quick task
---
```

**Required sections (verb-noun H2 headings, mirror testing.mdx rhythm):**

```mdx
The `make check-upstream-drift` target reports upstream commits the fork hasn't absorbed,
grouped by file category. Use it before starting an upstream-sync quick task to inventory
the work; combine it with the `.planning/templates/upstream-sync-quick.md` template to
scaffold the PLAN.md.

## Running the drift check

\`\`\`bash
make check-upstream-drift                                                # auto-detect range, table format
make check-upstream-drift ARGS="--from v0.40.1 --to v0.42.0"             # specific range
make check-upstream-drift ARGS="--from v0.40.1 --to v0.42.0 --format json"  # JSON for templates / CI
\`\`\`

The script auto-detects the last-synced upstream tag (highest `v0.*` reachable from `HEAD`)
and the latest upstream tag (`git describe --tags --abbrev=0 upstream/main`). Pass `--from`
and/or `--to` to override.

If the `upstream` remote is missing, the script exits 1 with an actionable hint:

\`\`\`bash
git remote add upstream https://github.com/always-further/nono.git
\`\`\`

The script never adds the remote for you, never fetches, and never modifies git config —
it is strictly read-only over the local `.git` directory. After invocation, `git status`
remains clean.

## Output formats

| Format | Default | Best for |
|--------|---------|----------|
| `table` | yes | Human review during initial drift assessment |
| `json`  | no  | Pasting into the upstream-sync template; CI consumption |

The JSON output shape:

\`\`\`json
{
  "range": "v0.40.1..v0.42.0",
  "from": "v0.40.1",
  "to": "v0.42.0",
  "total_unique_commits": 24,
  "by_category": {
    "profile": 5, "policy": 2, "package": 3, "proxy": 1, "audit": 6, "other": 7
  },
  "commits": [
    {
      "sha": "abc123...",
      "subject": "feat(audit): ...",
      "author": "...",
      "date": "2026-...",
      "additions": 152, "deletions": 31,
      "files_changed": ["..."],
      "categories": ["audit", "policy"]
    }
  ]
}
\`\`\`

A commit touching multiple categories appears under each one in `--format table` (so
sum-of-rows can exceed `total_unique_commits`); in JSON, each commit's `categories`
field lists the union.

## Categorization rules

The script categorizes by file path prefix (no subject-line keyword scanning). The lookup
table is the single source of truth at the top of `scripts/check-upstream-drift.sh`:

| Path prefix | Category |
|-------------|----------|
| `crates/nono-cli/src/profile/` or `profile.rs` | `profile` |
| `crates/nono-cli/src/policy.rs` or `data/policy.json` | `policy` |
| `crates/nono-cli/src/package*` or `crates/nono/src/package*` | `package` |
| `crates/nono-proxy/` | `proxy` |
| `crates/nono/src/audit/` or `audit_attestation*` or `crates/nono-cli/src/audit*` | `audit` |
| anything else under `crates/{nono,nono-cli,nono-proxy}/src/` or `crates/nono/Cargo.toml` | `other` |

**Excluded paths** (D-11 path filter): `*_windows.rs` (suffix glob, covers all Windows-only
files), `crates/nono-cli/src/exec_strategy_windows/` (directory). Commits that touch ONLY
excluded paths do not appear in the report; commits that touch BOTH excluded and
cross-platform paths appear with only their cross-platform files listed.

## Using the output with the upstream-sync template

After running `make check-upstream-drift > drift.json`:

1. Copy `.planning/templates/upstream-sync-quick.md` to `.planning/quick/YYMMDD-xxx-upstream-sync-vX.Y/PLAN.md`.
2. Replace single-brace `{placeholder}` markers with values from `drift.json` (e.g., `{from_tag}` ← `from`, `{commit_count}` ← `total_unique_commits`, per-category counts from `by_category`).
3. Curate the per-category commit lists from `drift.json["commits"]` into the template's `## Drift inventory > Commits to absorb` sub-sections.
4. Run the placeholder smoke check before committing the filled PLAN.md:

\`\`\`bash
grep -oE '\{[a-z_]+\}' .planning/quick/YYMMDD-xxx-upstream-sync-vX.Y/PLAN.md
\`\`\`

Output should be empty. Any remaining marker is an unfilled blank.

## D-19 cherry-pick trailer block

Every commit in an upstream-sync quick task carries a 6-line trailer block:

\`\`\`
Upstream-commit: <8-char abbrev sha>
Upstream-tag: <upstream tag containing it>
Upstream-author: <full name> <email>
Co-Authored-By: <upstream author full name> <email>
Signed-off-by: <fork author full name> <email>
Signed-off-by: <fork author github handle> <email>
\`\`\`

Field rules (verbatim from commits `73e1e3b8`, `adf81aec`, `869349df`):

- Field order is fixed; do not rearrange.
- `Upstream-author` is lowercase 'a' (NOT `Upstream-Author`).
- TWO `Signed-off-by` lines: full name + github handle (DCO + GitHub attribution).
- Trailer block separated from body by exactly one blank line.

The template at `.planning/templates/upstream-sync-quick.md` encodes this exact block
with `{placeholder}` markers ready for substitution.

## Regenerating the test fixtures

The drift check is regression-tested against frozen JSON fixtures under
`tests/integration/fixtures/upstream-drift/`. To regenerate (e.g., after upstream
publishes a new tag, or after the script's lookup table is updated):

\`\`\`bash
bash scripts/check-upstream-drift.sh --from v0.37.1 --to v0.40.1 --format json \
  > tests/integration/fixtures/upstream-drift/v0.37.1__v0.40.1.json
bash scripts/check-upstream-drift.sh --from v0.39.0 --to v0.40.0 --format json \
  > tests/integration/fixtures/upstream-drift/v0.39.0__v0.40.0.json
bash scripts/check-upstream-drift.sh --from v0.40.0 --to v0.40.1 --format json \
  > tests/integration/fixtures/upstream-drift/v0.40.0__v0.40.1.json

bash tests/integration/test_upstream_drift.sh
\`\`\`

The integration test diffs both `bash scripts/check-upstream-drift.sh` AND
`pwsh -File scripts/check-upstream-drift.ps1` (when PowerShell is available) against
the same fixture file, byte-for-byte. Twin-script parity is the structural enforcer:
update one twin, the other diff fails.

See `tests/integration/fixtures/upstream-drift/README.md` for the full regeneration
procedure and the documented 22-commit informational delta vs the
`.planning/quick/260424-upr-review-upstream-037-to-040/SUMMARY.md` headline.

## See also

- `scripts/check-upstream-drift.sh` and `scripts/check-upstream-drift.ps1` — the script source
- `.planning/templates/upstream-sync-quick.md` — the GSD quick-task template
- `tests/integration/test_upstream_drift.sh` — the integration test
- `.planning/PROJECT.md` § "Upstream Parity Process" — the 4-step workflow summary
```

**Mintlify .mdx gotchas (PATTERNS § docs/cli/development/upstream-drift.mdx):**
- File extension is `.mdx`, NOT `.md` (D-16). Verified all 18 files in `docs/cli/development/` are `.mdx`.
- Frontmatter uses `title:` and `description:` keys (Mintlify schema). Do NOT include `slug:`, `date:`, `created:`, or other GSD-flavored keys here.
- `<NAME>` syntax inside body is FORBIDDEN — collides with JSX. The template's placeholder examples in this `.mdx` use single-brace `{name}` form, but they're inside fenced code blocks (` ``` `) so MDX doesn't parse them.
- No emojis (CLAUDE.md notes block restricts emoji usage; PATTERNS line 308 reiterates).
- Pipe-alignment in tables is loose in existing files; do not over-tighten.

**Verification:**
```bash
[[ -f docs/cli/development/upstream-drift.mdx ]]
head -1 docs/cli/development/upstream-drift.mdx | grep -qF '---'
grep -F 'title: Upstream Drift Check' docs/cli/development/upstream-drift.mdx
grep -F 'make check-upstream-drift' docs/cli/development/upstream-drift.mdx
grep -F 'upstream-sync-quick.md' docs/cli/development/upstream-drift.mdx

grep -F 'Upstream Parity Process' .planning/PROJECT.md
grep -F '.planning/templates/upstream-sync-quick.md' .planning/PROJECT.md
grep -F 'docs/cli/development/upstream-drift' .planning/PROJECT.md
```
All grep invocations must exit 0.
  </action>
  <verify>
    <automated>[[ -f docs/cli/development/upstream-drift.mdx ]] && grep -qF 'title: Upstream Drift Check' docs/cli/development/upstream-drift.mdx && grep -qF 'make check-upstream-drift' docs/cli/development/upstream-drift.mdx && grep -qF 'upstream-sync-quick.md' docs/cli/development/upstream-drift.mdx && grep -qF 'Upstream Parity Process' .planning/PROJECT.md && grep -qF '.planning/templates/upstream-sync-quick.md' .planning/PROJECT.md && grep -qF 'docs/cli/development/upstream-drift' .planning/PROJECT.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `[[ -f docs/cli/development/upstream-drift.mdx ]]` exits 0
    - File extension is `.mdx` NOT `.md`: `[[ ! -f docs/cli/development/upstream-drift.md ]]` exits 0 (verifies the .md form was NOT created by mistake)
    - `head -1 docs/cli/development/upstream-drift.mdx` outputs `---`
    - `grep -E '^title: Upstream Drift Check$' docs/cli/development/upstream-drift.mdx` exits 0 (Mintlify frontmatter title)
    - `grep -E '^description: ' docs/cli/development/upstream-drift.mdx` exits 0 (Mintlify frontmatter description)
    - `grep -F 'make check-upstream-drift' docs/cli/development/upstream-drift.mdx` exits 0
    - `grep -F 'upstream-sync-quick.md' docs/cli/development/upstream-drift.mdx` exits 0 (cross-link to template)
    - `grep -F 'Upstream-author' docs/cli/development/upstream-drift.mdx` exits 0 AND `grep -F 'Upstream-Author' docs/cli/development/upstream-drift.mdx` outputs nothing (lowercase 'a' enforced in docs too)
    - `grep -E '^## (Running|Output|Categorization|Using|Regenerating|D-19)' docs/cli/development/upstream-drift.mdx | wc -l` outputs at least `5` (verb-noun H2 sections present)
    - .mdx contains no JSX-style placeholders: `grep -E '<[A-Z_]+>' docs/cli/development/upstream-drift.mdx` outputs nothing
    - `grep -F 'Upstream Parity Process' .planning/PROJECT.md` exits 0 (new H2 section header)
    - `grep -E '^## Upstream Parity Process$' .planning/PROJECT.md` exits 0 (specifically H2 level — not H3 or H1)
    - `grep -F '.planning/templates/upstream-sync-quick.md' .planning/PROJECT.md` exits 0 (template cross-link)
    - `grep -F 'docs/cli/development/upstream-drift' .planning/PROJECT.md` exits 0 (.mdx cross-link)
    - `grep -F 'make check-upstream-drift' .planning/PROJECT.md` exits 0 (tooling cross-link)
    - PROJECT.md still has `## Evolution` AFTER the new section (insertion order preserved): `awk '/^## Upstream Parity Process$/{p=NR} /^## Evolution$/{e=NR} END{exit !(p && e && p < e)}' .planning/PROJECT.md` exits 0
    - PROJECT.md `### Validated`, `### Active (v2.2)`, `### Deferred` lists are unchanged (compare line counts before/after — task should not have edited those):
      - `grep -c '^### Validated$' .planning/PROJECT.md` outputs `1`
      - `grep -c '^### Active (v2.2)$' .planning/PROJECT.md` outputs `1`
      - `grep -c '^### Deferred (v2.3' .planning/PROJECT.md` outputs `1`
  </acceptance_criteria>
  <done>PROJECT.md has the new `## Upstream Parity Process` H2 section between `## Key Decisions` and `## Evolution`, with the 4-step workflow + cross-link to the .mdx runbook. The .mdx runbook exists at `docs/cli/development/upstream-drift.mdx` (NOT `.md`) with Mintlify frontmatter (`title:` + `description:`), 5+ verb-noun H2 sections, fenced code blocks for commands, and cross-links back to the script + template + PROJECT.md. Lowercase 'a' in `Upstream-author` is preserved in the .mdx documentation prose. PROJECT.md `### Validated`/`### Active`/`### Deferred` lists are untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend test_upstream_drift.sh with placeholder smoke test + cross-link assertions (Wave 2 — DRIFT-02 verification)</name>
  <files>tests/integration/test_upstream_drift.sh</files>
  <read_first>
    - .planning/phases/24-parity-drift-prevention/24-RESEARCH.md § Validation Architecture "Template-placeholder smoke test" (lines 901–912), § "Documentation completeness check" (lines 914–926)
    - .planning/phases/24-parity-drift-prevention/24-VALIDATION.md § Per-Task Verification Map (DRIFT-02 rows: template file exists, D-19 trailer block grep, placeholder smoke test, PROJECT.md cross-link, .mdx file exists)
    - tests/integration/test_upstream_drift.sh (current state from Plan 24-01 Task 3 — extend, do not rewrite from scratch)
    - tests/lib/test_helpers.sh (helper API — `pass`, `fail`, `print_summary`)
    - .planning/templates/upstream-sync-quick.md (newly created in Task 1 — the file under test)
    - docs/cli/development/upstream-drift.mdx (newly created in Task 2)
  </read_first>
  <action>
**Extend the existing `tests/integration/test_upstream_drift.sh` IN PLACE.** Plan 24-01 Task 3 created this file with 6 test groups (golden-fixture, twin-parity, tag auto-detect, missing-remote, table format, read-only invariant, ref-injection). Task 3 of Plan 24-02 adds 4 more test groups for DRIFT-02 deliverables. The file remains a single integration test runner.

**Insertion point:** Add the new test groups just BEFORE the final `print_summary` call.

### Test 8: Template file exists with D-19 trailer block

```bash
echo ""
echo "Template file presence + D-19 trailer block:"
TEMPLATE=".planning/templates/upstream-sync-quick.md"
TEMPLATE_PATH="$REPO_ROOT/$TEMPLATE"

if [[ -f "$TEMPLATE_PATH" ]]; then
  pass "template file exists at $TEMPLATE"
else
  fail "template file missing at $TEMPLATE"
fi

# 6-line D-19 trailer block, verified per-line.
if grep -qE '^Upstream-commit: \{[a-z_]+\}' "$TEMPLATE_PATH"; then
  pass "trailer line 1 (Upstream-commit) present"
else
  fail "trailer line 1 (Upstream-commit) missing or wrong placeholder shape"
fi

if grep -qE '^Upstream-tag: \{[a-z_]+\}' "$TEMPLATE_PATH"; then
  pass "trailer line 2 (Upstream-tag) present"
else
  fail "trailer line 2 (Upstream-tag) missing"
fi

# Lowercase 'a' in Upstream-author — D-19 LOCKED
if grep -qE '^Upstream-author: \{[a-z_]+\}' "$TEMPLATE_PATH"; then
  pass "trailer line 3 (Upstream-author, lowercase 'a') present"
else
  fail "trailer line 3 (Upstream-author) missing or capitalized 'Upstream-Author' (D-19 violation)"
fi

if grep -qE '^Upstream-Author' "$TEMPLATE_PATH"; then
  fail "FORBIDDEN capitalized 'Upstream-Author' present (D-19: must be lowercase 'a')"
else
  pass "no FORBIDDEN 'Upstream-Author' (capital A) present"
fi

if grep -qE '^Co-Authored-By: \{[a-z_]+\}' "$TEMPLATE_PATH"; then
  pass "trailer line 4 (Co-Authored-By) present"
else
  fail "trailer line 4 (Co-Authored-By) missing"
fi

# TWO Signed-off-by lines (DCO + GitHub attribution)
sob_count=$(grep -c '^Signed-off-by: ' "$TEMPLATE_PATH" || true)
if [[ "$sob_count" -eq 2 ]]; then
  pass "exactly 2 Signed-off-by lines (DCO + GitHub attribution)"
else
  fail "expected 2 Signed-off-by lines, got $sob_count (D-19 violation)"
fi
```

### Test 9: Fork-divergence catalog explicit naming

```bash
echo ""
echo "Fork-divergence catalog explicit entries:"
for entry in 'validate_path_within' 'load_production_trusted_root' 'hooks.rs' '#[cfg(target_os = "windows")]'; do
  if grep -qF "$entry" "$TEMPLATE_PATH"; then
    pass "fork-divergence catalog mentions: $entry"
  else
    fail "fork-divergence catalog missing: $entry"
  fi
done

# Deferred enum variants — accept either ArtifactType::Plugin OR generic phrase
if grep -qE '(ArtifactType::Plugin|deferred enum variants?)' "$TEMPLATE_PATH"; then
  pass "fork-divergence catalog mentions deferred enum variants"
else
  fail "fork-divergence catalog missing deferred-enum-variants entry"
fi
```

### Test 10: Placeholder smoke test (T-24-05 mitigation)

```bash
echo ""
echo "Placeholder smoke test (T-24-05):"
TMPDIR=$(mktemp -d)
trap "rm -rf '$TMPDIR'" EXIT
cp "$TEMPLATE_PATH" "$TMPDIR/PLAN.md"

# Substitute every {name} placeholder with sample values for a hypothetical v0.41.0 sync.
# Use BSD-and-GNU-portable sed (no -i flag interpretation differences). Use a stable file
# rewrite via temp file rather than `sed -i` to avoid macOS BSD vs GNU divergence.
sed \
  -e 's|{quick_slug}|260501-upr-sync-v041|g' \
  -e 's|{date}|2026-05-01|g' \
  -e 's|{from_tag}|v0.40.1|g' \
  -e 's|{to_tag}|v0.41.0|g' \
  -e 's|{commit_count}|18|g' \
  -e 's|{insertions}|3|g' \
  -e 's|{deletions}|0|g' \
  -e 's|{n_profile}|3|g' \
  -e 's|{n_policy}|2|g' \
  -e 's|{n_package}|4|g' \
  -e 's|{n_proxy}|1|g' \
  -e 's|{n_audit}|6|g' \
  -e 's|{n_other}|2|g' \
  -e 's|{upstream_sha_abbrev}|abc12345|g' \
  -e 's|{upstream_tag}|v0.41.0|g' \
  -e 's|{upstream_author_name}|Upstream Author|g' \
  -e 's|{upstream_author_email}|upstream@example.com|g' \
  -e 's|{fork_author_name}|Fork Author|g' \
  -e 's|{fork_author_email}|fork@example.com|g' \
  -e 's|{fork_author_handle}|fork-handle|g' \
  -e 's|{fork_branch}|windows-squash|g' \
  "$TEMPLATE_PATH" > "$TMPDIR/PLAN.md"

# Assert no remaining {placeholder} markers.
remaining=$(grep -oE '\{[a-z_]+\}' "$TMPDIR/PLAN.md" || true)
remaining_count=$(echo -n "$remaining" | grep -c '^' || true)
if [[ "$remaining_count" -eq 0 ]]; then
  pass "all {placeholder} markers substituted cleanly"
else
  fail "unfilled placeholders remain in rendered template: $remaining"
fi

# Assert frontmatter is valid YAML-shaped Markdown (basic structural check).
if head -1 "$TMPDIR/PLAN.md" | grep -qF '---'; then
  pass "rendered PLAN.md starts with frontmatter delimiter"
else
  fail "rendered PLAN.md missing frontmatter ---"
fi

if grep -qE '^slug: 260501-upr-sync-v041$' "$TMPDIR/PLAN.md"; then
  pass "rendered slug field substituted correctly"
else
  fail "rendered slug field missing or incorrect"
fi

if grep -qE '^range: v0\.40\.1\.\.v0\.41\.0$' "$TMPDIR/PLAN.md"; then
  pass "rendered range field substituted correctly"
else
  fail "rendered range field missing or incorrect"
fi

# Assert D-19 trailer block has expected structure post-substitution
if grep -qE '^Upstream-commit: abc12345$' "$TMPDIR/PLAN.md"; then
  pass "rendered D-19 trailer Upstream-commit substituted"
else
  fail "rendered D-19 trailer Upstream-commit missing"
fi

if [[ "$(grep -c '^Signed-off-by: ' "$TMPDIR/PLAN.md")" -eq 2 ]]; then
  pass "rendered D-19 trailer has 2 Signed-off-by lines"
else
  fail "rendered D-19 trailer Signed-off-by count wrong"
fi
```

### Test 11: Documentation cross-links

```bash
echo ""
echo "Documentation cross-links:"
PROJECT_MD="$REPO_ROOT/.planning/PROJECT.md"
DOCS_MDX="$REPO_ROOT/docs/cli/development/upstream-drift.mdx"

# .mdx file exists (D-16: NOT .md)
if [[ -f "$DOCS_MDX" ]]; then
  pass "docs file exists at docs/cli/development/upstream-drift.mdx (D-16 .mdx convention)"
else
  fail "docs file missing at docs/cli/development/upstream-drift.mdx"
fi

# .md form does NOT exist (D-16 acceptance: enforces .mdx convention)
if [[ ! -f "$REPO_ROOT/docs/cli/development/upstream-drift.md" ]]; then
  pass "no stray .md form (D-16 .mdx convention upheld)"
else
  fail "FORBIDDEN docs/cli/development/upstream-drift.md exists (must be .mdx per D-16)"
fi

# .mdx Mintlify frontmatter present
if grep -qE '^title: Upstream Drift Check$' "$DOCS_MDX"; then
  pass "docs has Mintlify title frontmatter"
else
  fail "docs missing Mintlify title frontmatter"
fi

if grep -qE '^description: ' "$DOCS_MDX"; then
  pass "docs has Mintlify description frontmatter"
else
  fail "docs missing Mintlify description frontmatter"
fi

# .mdx cross-references to script + template + PROJECT.md
for ref in 'make check-upstream-drift' 'upstream-sync-quick.md' 'Upstream-author'; do
  if grep -qF "$ref" "$DOCS_MDX"; then
    pass "docs references: $ref"
  else
    fail "docs missing reference to: $ref"
  fi
done

# PROJECT.md has new section + cross-links
if grep -qE '^## Upstream Parity Process$' "$PROJECT_MD"; then
  pass "PROJECT.md has '## Upstream Parity Process' H2 section"
else
  fail "PROJECT.md missing '## Upstream Parity Process' H2 section"
fi

for ref in '.planning/templates/upstream-sync-quick.md' 'docs/cli/development/upstream-drift' 'make check-upstream-drift'; do
  if grep -qF "$ref" "$PROJECT_MD"; then
    pass "PROJECT.md references: $ref"
  else
    fail "PROJECT.md missing reference to: $ref"
  fi
done

# PROJECT.md ordering: Upstream Parity Process must come BEFORE Evolution
if awk '/^## Upstream Parity Process$/{p=NR} /^## Evolution$/{e=NR} END{exit !(p && e && p < e)}' "$PROJECT_MD"; then
  pass "PROJECT.md section ordering: Upstream Parity Process precedes Evolution"
else
  fail "PROJECT.md section ordering wrong"
fi
```

### Final invariant check

The `print_summary` call (already at the end of the file from Plan 24-01) tallies all `pass`/`fail` invocations. With the 4 new test groups (Tests 8–11), the suite goes from ~10 assertions (Plan 24-01) to ~30 assertions total. All must pass for the integration test to exit 0.
  </action>
  <verify>
    <automated>bash tests/integration/test_upstream_drift.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/integration/test_upstream_drift.sh` exits 0 (full suite passes; PS portions of original 24-01 tests skipped if no PS available — accepted)
    - The integration test contains 4 new test group headers added by this task: `grep -cF "Template file presence + D-19 trailer block:" tests/integration/test_upstream_drift.sh` outputs `1`; `grep -cF "Fork-divergence catalog explicit entries:" tests/integration/test_upstream_drift.sh` outputs `1`; `grep -cF "Placeholder smoke test (T-24-05):" tests/integration/test_upstream_drift.sh` outputs `1`; `grep -cF "Documentation cross-links:" tests/integration/test_upstream_drift.sh` outputs `1`
    - The placeholder smoke test substitutes ≥17 distinct placeholders (verify by `grep -c "^  -e 's|" tests/integration/test_upstream_drift.sh` outputs ≥17)
    - The placeholder smoke test asserts zero remaining `{[a-z_]+}` markers post-substitution: `grep -F "remaining_count" tests/integration/test_upstream_drift.sh` exits 0
    - The cross-link test asserts BOTH files exist AND .md form does NOT: `grep -F "no stray .md form" tests/integration/test_upstream_drift.sh` exits 0
    - The cross-link test asserts D-16 mdx-not-md form: `grep -F 'docs/cli/development/upstream-drift.mdx' tests/integration/test_upstream_drift.sh` exits 0; `grep -F 'docs/cli/development/upstream-drift.md' tests/integration/test_upstream_drift.sh | grep -v '\.mdx'` outputs only the path used in the negative-existence check (`! -f`)
    - The cross-link test asserts PROJECT.md ordering invariant via awk: `grep -F 'awk' tests/integration/test_upstream_drift.sh` exits 0
    - `git status --porcelain` is empty after running the test (read-only invariant preserved by Test 6 from Plan 24-01 still holds)
  </acceptance_criteria>
  <done>The integration test at `tests/integration/test_upstream_drift.sh` is extended (in place) with 4 new test groups (Tests 8–11) covering DRIFT-02 deliverables: template existence + D-19 trailer block + lowercase 'a' enforcement + 2-Signed-off-by-lines invariant; fork-divergence catalog explicit-naming invariants; placeholder smoke test (T-24-05 mitigation) substituting all `{name}` markers and verifying zero remain post-substitution; documentation cross-links across PROJECT.md ↔ template ↔ .mdx with H2 ordering check. Full suite (Tests 1–11) exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer-author → template `{placeholder}` substitution | Maintainer copies template to PLAN.md and fills placeholders manually. Smoke test (T-24-05 mitigation) catches "shipped with raw placeholders" defects. |
| Mintlify renderer (downstream) → `.mdx` body | `.mdx` files are rendered by Mintlify; bare `<NAME>` JSX-style markers would be parsed as components and break rendering. Mitigation: docs uses single-brace `{name}` only inside fenced code blocks (where MDX doesn't parse). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Configuration (V14) | `.planning/templates/upstream-sync-quick.md` placeholder substitution | mitigate | Smoke test in `tests/integration/test_upstream_drift.sh` Test 10 substitutes all `{name}` markers with sample values, asserts (a) `grep -oE '\\{[a-z_]+\\}'` returns zero matches post-substitution, (b) frontmatter renders correctly (`---` delimiter, `slug:` and `range:` fields filled), (c) D-19 trailer block has 6 lines including 2 Signed-off-by entries. Defect mode caught: maintainer ships PLAN.md with raw `{from_tag}` markers. The leading HTML comment in the template instructs the maintainer to run the smoke check before commit; the integration test enforces it via CI-runnable assertion. |
| T-24-01..T-24-04 (cross-plan) | n/a | scripts/check-upstream-drift.{sh,ps1} | inherited from Plan 24-01 | All four bash/PS-script threats (ref injection, read-only invariant, PS console encoding, JSON escape) are mitigated in Plan 24-01. This plan does not modify the scripts; it only modifies the test suite (extending Plan 24-01's runner) and creates the template + docs files (which have no shell execution surface). |

**Threat scope conclusion:** Low. The template + docs are static text. The only attack surface is the maintainer's substitution discipline, which is enforced by the smoke test.
</threat_model>

<verification>
- `bash tests/integration/test_upstream_drift.sh` exits 0 (all 11 test groups: 7 from Plan 24-01 + 4 from this plan)
- `[[ -f .planning/templates/upstream-sync-quick.md ]]` exits 0
- `[[ -f docs/cli/development/upstream-drift.mdx ]]` exits 0
- `[[ ! -f docs/cli/development/upstream-drift.md ]]` exits 0 (D-16: .mdx form, no stray .md)
- `grep -c '^Signed-off-by: ' .planning/templates/upstream-sync-quick.md` outputs `2`
- `grep -E '^Upstream-author: \{' .planning/templates/upstream-sync-quick.md` exits 0 (lowercase 'a')
- `grep -E '^Upstream-Author' .planning/templates/upstream-sync-quick.md` outputs nothing
- `grep -F '## Upstream Parity Process' .planning/PROJECT.md` exits 0
- All cross-links resolve (3 from PROJECT.md → template/.mdx/script; 2 from .mdx → template/script; 1 from template → make target)
- `git status --porcelain` is empty after running integration test (read-only invariant preserved)
</verification>

<success_criteria>
- All 3 acceptance criteria from REQ-DRIFT-02 satisfied:
  1. ✓ Template file committed at `.planning/templates/upstream-sync-quick.md` (D-12)
  2. ✓ PROJECT.md references the template via the new `## Upstream Parity Process` section (D-15)
  3. ✓ Dry-run substitution for hypothetical v0.41.0 sync produces a sensible PLAN.md skeleton — verified by Test 10's placeholder smoke test rendering the template and asserting structural validity
- D-19 trailer block encoded BYTE-EXACT verbatim (lowercase 'a' in `Upstream-author`, two `Signed-off-by` lines, fixed field order)
- Fork-divergence catalog explicitly names `validate_path_within`, deferred enum variants, `load_production_trusted_root` async wrapping, hooks ownership pattern
- Windows retrofit checklist uses `#[cfg(target_os = "windows")]` literal text
- T-24-05 mitigated: integration test catches unfilled-placeholder regressions
- D-16 enforced: docs file is `.mdx` not `.md`
- All cross-links bidirectional and asserted by the integration test
</success_criteria>

<output>
After completion, create `.planning/phases/24-parity-drift-prevention/24-02-SUMMARY.md`. Reference: (a) the byte-exact D-19 trailer block as it appears in the committed template, (b) the substituted-template sample (rendered for v0.41.0 with the smoke test's sample values) so future maintainers can see the "before" and "after" of substitution at a glance, (c) the cross-link map (PROJECT.md ↔ template ↔ .mdx ↔ scripts) for traceability.
</output>
