---
phase: 04-state-integrity-deployment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - scripts/sign-windows-artifacts.ps1
  - docs/cli/development/windows-signing-guide.mdx
autonomous: true
requirements:
  - DEPL-01
must_haves:
  truths:
    - "Windows release artifacts are signed only for tag/release workflows, not normal local or branch builds."
    - "Both Windows MSIs and the shipped `.exe` are signed with SHA-256 and verified before upload."
    - "The release workflow fails closed if secrets, timestamping, signing, or verification steps do not succeed."
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Release workflow gates for Windows signing and verification"
    - path: "scripts/sign-windows-artifacts.ps1"
      provides: "Centralized Authenticode signing and verification contract"
    - path: "docs/cli/development/windows-signing-guide.mdx"
      provides: "Operator-facing contract for CI-only Windows signing secrets"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/sign-windows-artifacts.ps1"
      via: "Windows signing step"
      pattern: "sign-windows-artifacts"
    - from: ".github/workflows/release.yml"
      to: "GitHub Secrets"
      via: "WINDOWS_SIGNING_CERT and WINDOWS_SIGNING_CERT_PASSWORD"
      pattern: "WINDOWS_SIGNING_CERT"
    - from: "scripts/sign-windows-artifacts.ps1"
      to: "signtool.exe"
      via: "sign + verify commands"
      pattern: "signtool"
---

<objective>
Harden the Windows release-signing path so CI publishes only verified signed artifacts.

Purpose: satisfy the release half of Phase 04 with a tag-only, fail-closed signing workflow that matches the user decisions and current research.
Output: updated signing script, updated release workflow, and a documented CI secret contract.
</objective>

<execution_context>
@C:/Users/omack/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-state-integrity-deployment/04-CONTEXT.md
@.planning/phases/04-state-integrity-deployment/04-RESEARCH.md
@.github/workflows/release.yml
@scripts/sign-windows-artifacts.ps1
@scripts/build-windows-msi.ps1
@scripts/validate-windows-msi-contract.ps1
@docs/cli/development/windows-signing-guide.mdx

<decisions>
D-09: Signing uses GitHub secrets with a base64-encoded `.pfx`.
D-10: Signing is tag/release only; local development skips signing.
D-11: Maintain separate machine and user MSIs.
D-12: Use DigiCert timestamping and SHA-256 only.
D-13: Fail the CI run if signing or verification fails; never upload unsigned release artifacts.
D-14: Use `signtool.exe`, not custom signing logic.
</decisions>

<interfaces>
From `scripts/sign-windows-artifacts.ps1`:
```powershell
param(
    [string]$CertBase64,
    [string]$CertPassword,
    [string[]]$ArtifactPaths,
    [string]$TimestampUrl = "http://timestamp.digicert.com"
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the shared Windows signing script</name>
  <files>scripts/sign-windows-artifacts.ps1, docs/cli/development/windows-signing-guide.mdx</files>
  <action>
    Make the script the single source of truth for the Windows signing contract.
    - In `scripts/sign-windows-artifacts.ps1`, keep `signtool.exe` as the only signing primitive per D-14.
    - Update the signing invocation to explicit SHA-256 digest flags and RFC 3161-style timestamp verification if the chosen DigiCert endpoint supports it; otherwise document the compatibility fallback clearly as a locked decision. In either case, verification must include a timestamp-aware failure mode per D-12 and D-13.
    - Keep certificate import scoped to the current user store for the job and remove it afterward.
    - Update `docs/cli/development/windows-signing-guide.mdx` so the secret names, CI-only usage, and dual-MSI artifact expectations match the script and workflow.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "Get-Command pwsh | Out-Null; [void][System.Management.Automation.Language.Parser]::ParseFile('scripts/sign-windows-artifacts.ps1',[ref]$null,[ref]$null)"</automated>
  </verify>
  <done>
    The signing script uses explicit SHA-256 settings, verifies signatures before returning success, and the signing guide matches the real CI contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate release uploads on verified signed Windows artifacts</name>
  <files>.github/workflows/release.yml</files>
  <action>
    Tighten the GitHub Actions release workflow around the signing contract.
    - Update `.github/workflows/release.yml` so Windows signing runs only for tag/release builds per D-10, including workflow-dispatch runs that target an actual tag.
    - Keep building both machine and user MSIs per D-11, then sign the raw `.exe`, machine MSI, and user MSI before any upload step.
    - Fail closed if required secrets are missing, if any artifact is absent, or if `signtool`/PowerShell verification fails per D-13.
    - Ensure artifact upload and GitHub release creation remain downstream of successful signing and verification only.
  </action>
  <verify>
    <automated>cargo run -p nono-cli -- --help</automated>
  </verify>
  <done>
    The release workflow signs only tag-targeted Windows releases, verifies every shipped Windows artifact before upload, and blocks publication on any signing failure.
  </done>
</task>

</tasks>

<verification>
- `<automated>` `pwsh -NoProfile -Command "Get-Command pwsh | Out-Null; [void][System.Management.Automation.Language.Parser]::ParseFile('scripts/sign-windows-artifacts.ps1',[ref]$null,[ref]$null)"`
- `<automated>` `cargo run -p nono-cli -- --help`
</verification>

<success_criteria>
- The Windows release path signs only tagged releases and never uploads unsigned `.exe` or MSI artifacts.
- Both MSI variants remain in the workflow and are verified after signing.
- The signing guide matches the release workflow and secret names.
</success_criteria>

<output>
After completion, create `.planning/phases/04-state-integrity-deployment/04-03-SUMMARY.md`
</output>
