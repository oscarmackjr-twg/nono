---
phase: 04-state-integrity-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/rollback_runtime.rs
  - crates/nono/src/undo/types.rs
  - crates/nono-cli/src/rollback_commands.rs
  - crates/nono/src/undo/snapshot.rs
  - scripts/windows-test-harness.ps1
autonomous: true
requirements:
  - STAT-01
  - STAT-02
must_haves:
  truths:
    - "A Windows run started with `--rollback` still launches when snapshot capture hits a rollback-specific failure, and the user is warned that rollback is unavailable for that session."
    - "A Windows rollback session records whether baseline snapshot capture succeeded so `nono rollback` can distinguish a valid snapshot from an audit-only session."
    - "Restoring a Windows snapshot surfaces locked-file failures explicitly instead of claiming full rollback success."
  artifacts:
    - path: "crates/nono-cli/src/rollback_runtime.rs"
      provides: "Warning-only rollback initialization and session metadata persistence"
    - path: "crates/nono/src/undo/types.rs"
      provides: "Session metadata contract for rollback availability/status"
    - path: "crates/nono/src/undo/snapshot.rs"
      provides: "Windows-aware restore failure reporting"
    - path: "scripts/windows-test-harness.ps1"
      provides: "Automated Windows regression coverage for rollback capture and restore"
  key_links:
    - from: "crates/nono-cli/src/rollback_runtime.rs"
      to: "crates/nono/src/undo/types.rs"
      via: "SessionMetadata serialization"
      pattern: "SessionMetadata"
    - from: "crates/nono-cli/src/rollback_commands.rs"
      to: "crates/nono-cli/src/rollback_runtime.rs"
      via: "session.json status reporting"
      pattern: "snapshot_count|rollback"
    - from: "crates/nono/src/undo/snapshot.rs"
      to: "scripts/windows-test-harness.ps1"
      via: "Windows rollback regression cases"
      pattern: "rollback"
---

<objective>
Make Windows rollback behavior match the phase decisions and the actual codebase state.

Purpose: close the remaining Windows-specific correctness gap in snapshot capture and restore handling rather than re-planning rollback plumbing that already exists.
Output: warning-only snapshot initialization, durable rollback-status metadata, and Windows rollback regression coverage.
</objective>

<execution_context>
@C:/Users/omack/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-state-integrity-deployment/04-CONTEXT.md
@.planning/phases/04-state-integrity-deployment/04-RESEARCH.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-03-SUMMARY.md
@crates/nono-cli/src/supervised_runtime.rs
@crates/nono-cli/src/rollback_runtime.rs
@crates/nono-cli/src/rollback_commands.rs
@crates/nono/src/undo/snapshot.rs
@crates/nono/src/undo/types.rs
@scripts/windows-test-harness.ps1

<decisions>
D-01: Use the existing Merkle-tree rollback engine, not VSS/TxF.
D-02: Snapshot all granted write-capable paths for the session scope.
D-03: Rollback soft limits warn; they do not abort execution.
D-04: Snapshot initialization failures warn and execution continues without rollback capability.
</decisions>

<interfaces>
From `crates/nono-cli/src/rollback_runtime.rs`:
```rust
pub(crate) type RollbackRuntimeState = (
    nono::undo::SnapshotManager,
    nono::undo::SnapshotManifest,
    Vec<PathBuf>,
    HashSet<PathBuf>,
);

pub(crate) fn initialize_rollback_state(
    rollback: &RollbackLaunchOptions,
    caps: &CapabilitySet,
    audit_state: Option<&AuditState>,
    silent: bool,
) -> Result<Option<RollbackRuntimeState>>;

pub(crate) fn finalize_supervised_exit(ctx: RollbackExitContext<'_>) -> Result<()>;
```

From `crates/nono/src/undo/types.rs`:
```rust
pub struct SessionMetadata {
    pub session_id: String,
    pub started: String,
    pub ended: Option<String>,
    pub command: Vec<String>,
    pub tracked_paths: Vec<PathBuf>,
    pub snapshot_count: u32,
    pub exit_code: Option<i32>,
    pub merkle_roots: Vec<ContentHash>,
    pub network_events: Vec<NetworkAuditEvent>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make rollback initialization warning-only and record status</name>
  <files>crates/nono-cli/src/rollback_runtime.rs, crates/nono/src/undo/types.rs, crates/nono-cli/src/rollback_commands.rs</files>
  <behavior>
    - Test 1: when baseline snapshot capture fails during `--rollback`, supervised execution still reaches child launch and session metadata marks rollback as unavailable.
    - Test 2: `nono rollback list/show` distinguishes an audit-only session from a session with valid snapshots instead of implying rollback is available.
    - Test 3: soft-limit warnings remain visible but do not turn into fatal initialization errors.
  </behavior>
  <action>
    Update rollback initialization and metadata flow to match the locked filesystem decisions.
    - In `crates/nono-cli/src/rollback_runtime.rs`, keep the current tracked-path discovery and Merkle snapshot pipeline per D-01 and D-02, but change baseline snapshot failure handling to warning-only per D-04: create the audit/session directory, emit a warning, and return `Ok(None)` for rollback state instead of aborting the run.
    - Extend `crates/nono/src/undo/types.rs` with an explicit rollback status field or equivalent structured metadata that records whether baseline capture succeeded, was skipped, or failed-warning-only; preserve backward compatibility for older `session.json` payloads.
    - Update `crates/nono-cli/src/rollback_commands.rs` so `list`, `show`, and restore-oriented messaging surface the new status cleanly and never present an audit-only session as rollback-capable.
    - Keep library policy-free: the status metadata belongs to the undo/session contract, while warning presentation stays in the CLI.
  </action>
  <verify>
    <automated>cargo test -p nono-cli rollback -- --nocapture</automated>
  </verify>
  <done>
    Snapshot capture failures no longer abort Windows supervised runs, `session.json` explicitly records rollback availability, and rollback commands expose that state without ambiguity.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden Windows restore semantics and add harness coverage</name>
  <files>crates/nono/src/undo/snapshot.rs, scripts/windows-test-harness.ps1</files>
  <behavior>
    - Test 1: restoring a Windows session with a locked target file returns a partial-failure error that names the locked path.
    - Test 2: Windows regression suite exercises rollback capture plus `nono rollback restore` behavior instead of only checking command execution.
  </behavior>
  <action>
    Focus the remaining Windows filesystem work on restore correctness and verification.
    - In `crates/nono/src/undo/snapshot.rs`, keep same-directory temp replacement and explicit per-file error aggregation, but tighten Windows-specific restore diagnostics so locked/in-use files are clearly surfaced as partial rollback failures per the research guidance for STAT-02.
    - Preserve current manifest path validation and fail-secure behavior; do not weaken tracked-path checks or path canonicalization.
    - Expand `scripts/windows-test-harness.ps1` with targeted rollback regression entries that prove STAT-01 and STAT-02 on Windows, including a restore-path test instead of capture-only smoke coverage.
  </action>
  <verify>
    <automated>pwsh scripts/windows-test-harness.ps1 -Suite integration</automated>
  </verify>
  <done>
    Windows restore failures are explicit and test-covered, and the Windows harness now proves both snapshot capture and rollback restore behavior.
  </done>
</task>

</tasks>

<verification>
Run the focused rollback tests first, then the Windows integration harness:

- `<automated>` `cargo test -p nono-cli rollback -- --nocapture`
- `<automated>` `pwsh scripts/windows-test-harness.ps1 -Suite integration`
</verification>

<success_criteria>
- A failed Windows baseline snapshot no longer blocks `nono run --rollback`; execution proceeds with a warning and rollback is marked unavailable for that session.
- Rollback session metadata explicitly states whether snapshots exist.
- Windows rollback restore failures identify locked files and are covered by automated Windows-harness tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-state-integrity-deployment/04-01-SUMMARY.md`
</output>
