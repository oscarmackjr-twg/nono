---
phase: 04-state-integrity-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/bin/nono-wfp-service.rs
  - scripts/build-windows-msi.ps1
  - scripts/validate-windows-msi-contract.ps1
  - Cargo.toml
  - CLAUDE.md
  - .planning/codebase/STACK.md
autonomous: true
requirements:
  - DEPL-01
must_haves:
  truths:
    - "The Windows WFP service starts by cleaning up provider-owned stale filters instead of relying only on best-effort per-process teardown."
    - "WFP cleanup actions are written to Windows Event Log for machine installs."
    - "The machine MSI remains the install boundary for Windows service and event-log registration while the user MSI stays service-free."
  artifacts:
    - path: "crates/nono-cli/src/bin/nono-wfp-service.rs"
      provides: "Dynamic-session cleanup sweep and Event Log reporting"
    - path: "scripts/build-windows-msi.ps1"
      provides: "Machine MSI registration for WFP service and Event Log source"
    - path: "scripts/validate-windows-msi-contract.ps1"
      provides: "Contract checks that machine/user MSI scopes stay separated"
  key_links:
    - from: "crates/nono-cli/src/bin/nono-wfp-service.rs"
      to: "Windows Filtering Platform"
      via: "dynamic session + provider-guided startup sweep"
      pattern: "FWPM_SESSION_FLAG_DYNAMIC|Fwpm"
    - from: "scripts/build-windows-msi.ps1"
      to: "crates/nono-cli/src/bin/nono-wfp-service.rs"
      via: "machine MSI service/event-log registration"
      pattern: "ServiceInstall|EventLog"
    - from: "Cargo.toml"
      to: "crates/nono-cli/src/bin/nono-wfp-service.rs"
      via: "MSRV compatible Windows bindings"
      pattern: "rust-version|windows"
---

<objective>
Finish the Windows service-side lifecycle hardening that Phase 03 set up and bind it to the machine installer contract.

Purpose: make WFP cleanup observable and deterministic on Windows while keeping installation ownership inside the machine MSI.
Output: startup orphan sweep, Windows Event Log reporting, machine-MSI registration updates, and an MSRV bump aligned with the planned Windows bindings work.
</objective>

<execution_context>
@C:/Users/omack/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-state-integrity-deployment/04-CONTEXT.md
@.planning/phases/04-state-integrity-deployment/04-RESEARCH.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-01-SUMMARY.md
@.planning/phases/03-network-sandboxing-wfp-integration/03-02-SUMMARY.md
@crates/nono-cli/src/bin/nono-wfp-service.rs
@scripts/build-windows-msi.ps1
@scripts/validate-windows-msi-contract.ps1
@Cargo.toml
@CLAUDE.md
@.planning/codebase/STACK.md

<decisions>
D-05: WFP filters stay tied to a dynamic session for normal cleanup.
D-06: Service startup performs an orphan sweep keyed to the nono provider GUID and dead owners.
D-07: WFP leakage and cleanup events go to Windows Event Log.
D-08: Bump MSRV to support safer Windows bindings for this service work.
</decisions>

<interfaces>
From `crates/nono-cli/src/bin/nono-wfp-service.rs`:
```rust
const SERVICE_NAME: &str = "nono-wfp-service";
const SERVICE_MODE_ARG: &str = "--service-mode";

fn run_service(arguments: Vec<OsString>) -> windows_service::Result<()>;
async fn run_named_pipe_server() -> Result<(), String>;
fn activate_policy_mode(request: &WfpRuntimeActivationRequest) -> WfpRuntimeActivationResponse;
fn deactivate_policy_mode(request: &WfpRuntimeActivationRequest) -> WfpRuntimeActivationResponse;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WFP startup sweep and Event Log reporting</name>
  <files>crates/nono-cli/src/bin/nono-wfp-service.rs</files>
  <behavior>
    - Test 1: service startup enumerates provider-owned WFP objects and attempts cleanup before accepting new activation requests.
    - Test 2: cleanup/logging helpers produce deterministic messages for removed, skipped, and failed stale-object cases.
  </behavior>
  <action>
    Build on the Phase 03 dynamic-session work instead of replacing it.
    - Keep `FWPM_SESSION_FLAG_DYNAMIC` as the normal cleanup path per D-05.
    - Add a startup sweep in `crates/nono-cli/src/bin/nono-wfp-service.rs` that enumerates the nono provider/sublayer/filter surface, identifies dead owners, and deletes stale objects per D-06 before the named-pipe server starts accepting new runtime activation requests.
    - Add classic Windows Event Log writes for cleanup outcomes per D-07. Keep the implementation low-volume and admin-oriented; do not add ETW manifest complexity in this phase.
    - Preserve deterministic filter keys and fail-secure behavior: if cleanup cannot prove ownership or liveness, prefer logging and refusing unsafe assumptions over deleting unrelated WFP objects.
  </action>
  <verify>
    <automated>cargo test -p nono-cli --bin nono-wfp-service -- --nocapture</automated>
  </verify>
  <done>
    The service performs a provider-guided orphan sweep at startup, writes cleanup outcomes to Windows Event Log, and retains dynamic-session cleanup for the normal lifecycle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the machine MSI own service and Event Log registration</name>
  <files>scripts/build-windows-msi.ps1, scripts/validate-windows-msi-contract.ps1</files>
  <action>
    Move the Windows install boundary fully into the machine MSI.
    - Update `scripts/build-windows-msi.ps1` so the machine MSI registers the `nono-wfp-service` install surface and the classic Application Event Log source used by Task 1, while the user MSI remains service-free.
    - Extend `scripts/validate-windows-msi-contract.ps1` to assert the machine/user split: service and Event Log registration must exist only in the machine MSI, and the user MSI must continue to exclude that surface.
    - Keep the existing dual-MSI contract intact: same artifact names, separate scopes, separate upgrade codes, and no service components in the user installer.
  </action>
  <verify>
    <automated>pwsh scripts/validate-windows-msi-contract.ps1 -BinaryPath target\debug\nono.exe</automated>
  </verify>
  <done>
    The machine MSI carries the Windows service/Event Log registration contract, and the validator enforces that the user MSI does not.
  </done>
</task>

<task type="auto">
  <name>Task 3: Raise documented MSRV for the Windows service stack</name>
  <files>Cargo.toml, CLAUDE.md, .planning/codebase/STACK.md</files>
  <action>
    Apply the locked MSRV bump consistently.
    - Update `Cargo.toml` `workspace.package.rust-version` to the minimum version required by the Windows bindings approach chosen for Task 1 per D-08.
    - Reflect the same minimum version in `CLAUDE.md` and `.planning/codebase/STACK.md`.
    - Keep the rationale explicit: this bump is for safer Windows service/WFP API work, not a broad tooling refresh.
  </action>
  <verify>
    <automated>cargo check -p nono-cli --bin nono-wfp-service</automated>
  </verify>
  <done>
    The repo MSRV and project docs agree on the Windows-service-compatible Rust minimum.
  </done>
</task>

</tasks>

<verification>
- `<automated>` `cargo test -p nono-cli --bin nono-wfp-service -- --nocapture`
- `<automated>` `pwsh scripts/validate-windows-msi-contract.ps1 -BinaryPath target\debug\nono.exe`
- `<automated>` `cargo check -p nono-cli --bin nono-wfp-service`
</verification>

<success_criteria>
- The Windows WFP service cleans stale provider-owned objects at startup and records outcomes in Event Log.
- Machine MSI, not ad hoc runtime code, owns service/Event Log registration.
- MSRV and docs are aligned with the Windows service implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-state-integrity-deployment/04-02-SUMMARY.md`
</output>
