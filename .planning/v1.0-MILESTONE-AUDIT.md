---
milestone: v1.0
audited: 2026-04-06T00:00:00Z
status: gaps_found
scores:
  requirements: 9/12
  phases: 5/6 verified
  integration: 10/12 wired
  flows: 2/3 complete
gaps:
  requirements:
    - id: "STAT-01"
      status: "partial"
      phase: "04-state-integrity-deployment"
      claimed_by_plans: ["04-01-PLAN.md"]
      completed_by_plans: ["04-01-SUMMARY.md"]
      verification_status: "missing"
      evidence: "Phase 04 has no VERIFICATION.md. UAT.md shows 4/13 tests blocked (rollback list, show, restore with audit-only sessions — blocked by nono-wfp-service not running during Phase 4). Those blockers are now resolved by Phases 5+6. Code is structurally implemented and SUMMARY exists."
    - id: "STAT-02"
      status: "partial"
      phase: "04-state-integrity-deployment"
      claimed_by_plans: ["04-01-PLAN.md"]
      completed_by_plans: ["04-01-SUMMARY.md"]
      verification_status: "missing"
      evidence: "Same as STAT-01. Additionally, integration checker found MISSING-01 and MISSING-02: uncommitted changes to exec_strategy.rs break Unix execute_supervised signature and ExecConfig struct, causing compile errors on Linux/macOS for the rollback code path."
    - id: "DEPL-01"
      status: "partial"
      phase: "04-state-integrity-deployment"
      claimed_by_plans: ["04-03-PLAN.md"]
      completed_by_plans: ["04-03-SUMMARY.md"]
      verification_status: "missing"
      evidence: "Phase 04 VERIFICATION.md absent. UAT tests for DEPL-01 (MSI EventLog, MSRV, signing script — tests 9-13) all passed in 04-UAT.md. Code and CI scripts are present. Lower risk than STAT-01/02 but still unverified formally."
  integration:
    - id: "MISSING-01"
      description: "Unix execute_supervised signature missing rollback_status parameter"
      from: "supervised_runtime.rs (non-Windows cfg block)"
      to: "exec_strategy.rs execute_supervised (Unix, 13-param signature)"
      affected_requirements: ["STAT-01", "STAT-02"]
      severity: "compile error on Linux/macOS"
      evidence: "Working-tree diff shows rollback_status removed from Unix execute_supervised but supervised_runtime.rs non-Windows call site still passes it. Masked by Windows-only build env."
    - id: "MISSING-02"
      description: "Unix ExecConfig struct missing startup_timeout field while execution_runtime.rs still sets it"
      from: "execution_runtime.rs (non-Windows cfg block, line ~269)"
      to: "exec_strategy.rs ExecConfig struct (Unix version)"
      affected_requirements: ["STAT-01", "STAT-02"]
      severity: "compile error on Linux/macOS"
      evidence: "Working-tree diff shows startup_timeout removed from ExecConfig but execution_runtime.rs Unix ExecConfig construction still references it. error[E0560] on Linux/macOS."
  flows:
    - id: "Flow-C-Unix"
      description: "nono run -> snapshot baseline -> rollback restore — Unix build is broken"
      broken_at: "Compile time on Linux/macOS due to MISSING-01 and MISSING-02"
      affected_requirements: ["STAT-01", "STAT-02"]
      note: "Windows path fully wired and verified. Unix break is from uncommitted in-progress changes to exec_strategy.rs."
tech_debt:
  - phase: "04-state-integrity-deployment"
    items:
      - "04-UAT.md status marked 'complete' with 4 tests in 'blocked' state (not 'passed'). Tests 1-4 (rollback CLI with audit-only sessions) were blocked by nono-wfp-service not running. Phases 5+6 now unblock this — re-test pending."
      - "04-VALIDATION.md is a template stub (nyquist_compliant: false, wave_0_complete: false — unfilled placeholders)."
      - "exec_strategy.rs has two commented-out call sites (cosmetic, not compile errors)."
  - phase: "05-windows-detach-readiness-fix"
    items:
      - "Two human verification items remain pending: (1) live Windows detach/attach E2E; (2) fail-closed WaitNamedPipeW error injection. Both require live Windows env with running supervisor."
nyquist:
  overall: "SKIP — workflow.nyquist_validation is false"
---

# Milestone v1.0 — Audit Report: Windows Parity

**Audited:** 2026-04-06
**Status:** gaps_found
**Score:** 9/12 requirements | 5/6 phases verified | 10/12 integration paths wired | 2/3 E2E flows complete

---

## Summary

The Windows Parity milestone is substantially complete. 9 of 12 v1 requirements have full VERIFICATION.md backing and pass integration checks. Both key E2E flows for Windows users (detach/attach and WFP network blocking) are fully wired and verified end-to-end.

Two issues block milestone completion:

1. **Phase 04 is missing a VERIFICATION.md** — STAT-01, STAT-02, and DEPL-01 are implemented and largely UAT-tested, but lack a formal verification pass.
2. **Uncommitted `exec_strategy.rs` changes break the Unix build** — two signature/struct mismatches in the non-Windows `#[cfg]` branches cause compile errors on Linux/macOS for the rollback code path (MISSING-01, MISSING-02). Currently masked by the Windows-only build environment.

---

## Phase Verification Status

| Phase | VERIFICATION.md | Status | Requirements |
|-------|----------------|--------|-------------|
| 01 — Windows Control Foundation | ✓ Present | passed | SUPV-03, SUPV-04, SUPV-05, DEPL-02 |
| 02 — Persistent Sessions | ✓ Present | passed | ConPTY, IPC infrastructure |
| 03 — Network Sandboxing (WFP) | ✓ Present | passed | NETW-02 |
| **04 — State Integrity & Deployment** | **✗ MISSING** | **unverified** | **STAT-01, STAT-02, DEPL-01** |
| 05 — Windows Detach Readiness Fix | ✓ Present | passed (5/5) | SUPV-01, SUPV-02 |
| 06 — WFP Enforcement Activation | ✓ Present | passed (8/8) | NETW-01, NETW-03 |

---

## Requirements Coverage (3-Source Cross-Reference)

| Requirement | Description | VERIFICATION | SUMMARY | REQUIREMENTS.md | Final Status |
|-------------|-------------|-------------|---------|-----------------|--------------|
| SUPV-01 | Detach with `nono run --detach` | Phase 05: satisfied | ✓ | [x] | **satisfied** |
| SUPV-02 | Re-attach via Named Pipe IPC | Phase 05: satisfied | ✓ | [x] | **satisfied** |
| SUPV-03 | List agents via `nono ps` | Phase 01: passed | ✓ | [x] | **satisfied** |
| SUPV-04 | Stop agent process tree | Phase 01: passed | ✓ | [x] | **satisfied** |
| SUPV-05 | Named Job Objects with session IDs | Phase 01: passed | ✓ | [x] | **satisfied** |
| NETW-01 | Block network access via WFP | Phase 06: satisfied | ✓ | [x] | **satisfied** |
| NETW-02 | WFP rules persist during detach | Phase 03: passed | ✓ | [x] | **satisfied** |
| NETW-03 | Allow specific local ports via WFP | Phase 06: satisfied | ✓ | [x] | **satisfied** |
| **STAT-01** | **Filesystem snapshot before execution** | **MISSING (Phase 04)** | ✓ | [x] | **partial** |
| **STAT-02** | **Rollback filesystem changes** | **MISSING (Phase 04)** | ✓ | [x] | **partial** |
| **DEPL-01** | **Signed MSI packages in CI** | **MISSING (Phase 04)** | ✓ | [x] | **partial** |
| DEPL-02 | Support status via `nono setup --check-only` | Phase 01: passed | ✓ | [x] | **satisfied** |

**Coverage:** 9/12 satisfied | 3/12 partial | 0/12 unsatisfied | 0 orphaned

---

## Integration Gaps

### MISSING-01: Unix `execute_supervised` rollback_status signature break

| Field | Value |
|-------|-------|
| **From** | `supervised_runtime.rs` (non-Windows `#[cfg]` block) |
| **To** | `exec_strategy.rs` `execute_supervised` (Unix, 13-param signature) |
| **Status** | Compile error on Linux/macOS |
| **Impact** | STAT-01, STAT-02 — rollback code path broken on Unix |
| **Root cause** | Uncommitted `exec_strategy.rs` diff removed `rollback_status` from Unix `execute_supervised` but caller was not updated for the non-Windows branch |

### MISSING-02: Unix `ExecConfig` missing `startup_timeout` field

| Field | Value |
|-------|-------|
| **From** | `execution_runtime.rs` (non-Windows `#[cfg]` block, line ~269) |
| **To** | `exec_strategy.rs` `ExecConfig` struct (Unix version) |
| **Status** | Compile error on Linux/macOS |
| **Impact** | All E2E flows on Unix |
| **Root cause** | Same uncommitted `exec_strategy.rs` change — `startup_timeout` removed from struct but `execution_runtime.rs` Unix branch still sets it |

Both gaps are currently masked by the Windows-only build environment. They will surface as CI failures on Linux/macOS build lanes.

---

## E2E Flow Status

| Flow | Description | Status | Detail |
|------|-------------|--------|--------|
| **A** | `nono run --detach` → Named Pipe ready → `nono attach` | ✓ **Complete** | Pipe name format verified at both ends (`startup_runtime.rs:83` ↔ `supervisor.rs:312`) |
| **B** | `nono run --block-net` → SID → WFP filters installed → agent network blocked | ✓ **Complete** | Full chain verified: `execution_runtime.rs:290` → `ExecConfig.session_sid` → `network.rs:1426` → WFP service IPC |
| **C** | `nono run` → snapshot baseline → agent modifies files → `nono rollback` | ⚠ **Partial** | Windows path fully wired; Unix compile-broken by MISSING-01 and MISSING-02 |

---

## Cross-Phase Wiring Verified

| Integration Point | Status |
|-------------------|--------|
| Detach pipe name: `startup_runtime.rs:83` ↔ `supervisor.rs:312` — exact format match | ✓ Wired |
| WFP SID: `execution_runtime.rs:290` → `ExecConfig.session_sid` → `network.rs:1426` → IPC payload → service | ✓ Wired |
| WFP persistence during detach: `FWPM_SESSION_FLAG_DYNAMIC` — service owns rules, not CLI process | ✓ Wired |
| Rollback status in `session.json`: `rollback_runtime.rs:finalize_supervised_exit` → `SnapshotManager::write_session_metadata` | ✓ Windows wired; Unix broken (MISSING-01) |

---

## Tech Debt (Non-Blocking)

**Phase 04 — State Integrity & Deployment**
- `04-UAT.md` shows `status: complete` but 4/13 tests in `blocked` state. Tests 1–4 (rollback list/show/restore with audit-only sessions) were blocked by `nono-wfp-service` not running at test time. Phases 5+6 resolved that blocker — re-testing pending.
- `04-VALIDATION.md` is an unfilled template stub.
- Two commented-out call sites in `exec_strategy.rs` (cosmetic, not errors).

**Phase 05 — Windows Detach Readiness Fix**
- Two human-verification items pending in `05-VERIFICATION.md`: live detach/attach E2E test; fail-closed `WaitNamedPipeW` error injection. Both require a live Windows environment.

---

## Nyquist Compliance

Skipped — `workflow.nyquist_validation` is `false`.

---

## Path to Completion

### 1. Fix uncommitted `exec_strategy.rs` changes *(unblocks MISSING-01, MISSING-02)*

The working tree has in-progress changes breaking the non-Windows build. Options:
- **Complete the migration:** update the non-Windows `#[cfg]` call sites in `supervised_runtime.rs` and `execution_runtime.rs` to match the new Unix signatures
- **Revert if accidental:** `git checkout -- crates/nono-cli/src/exec_strategy.rs`

### 2. Generate Phase 04 VERIFICATION.md *(satisfies STAT-01, STAT-02, DEPL-01)*

Run `/gsd:validate-phase 4`. The UAT is 9/13 done; the 4 blocked items are now testable with `nono-wfp-service` available from Phases 5+6.

### 3. Re-run Phase 04 UAT blocked tests *(completes the UAT record)*

Re-test UAT items 1–4 (rollback list/show/restore with audit-only sessions). Update `04-UAT.md` blocked entries to pass/fail.

---

_Audited: 2026-04-06_
_Auditor: Claude (gsd orchestrator + gsd-integration-checker)_
